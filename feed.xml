<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://lambdageo.github.io//feed.xml" rel="self" type="application/atom+xml"/><link href="https://lambdageo.github.io//" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-11T22:31:26+00:00</updated><id>https://lambdageo.github.io//feed.xml</id><title type="html">LambdaGEO — Research Lab in Computing and Geotechnologies</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Elixir e Phoenix LiveView: Construindo uma Aplicação Todo List do Zero</title><link href="https://lambdageo.github.io//blog/tutorial-elixir-phoenix-liveview-todo-list/" rel="alternate" type="text/html" title="Elixir e Phoenix LiveView: Construindo uma Aplicação Todo List do Zero"/><published>2026-07-11T13:01:00+00:00</published><updated>2026-07-11T13:01:00+00:00</updated><id>https://lambdageo.github.io//blog/tutorial-elixir-phoenix-liveview</id><content type="html" xml:base="https://lambdageo.github.io//blog/tutorial-elixir-phoenix-liveview-todo-list/"><![CDATA[<p>Este é um tutorial completo e guiado para construir uma aplicação de <strong>Lista de Tarefas (Todo List)</strong> do zero, usando a stack moderna de <strong>Elixir com Phoenix LiveView</strong> — um framework funcional e reativo para o desenvolvimento web full-stack.</p> <p>Mas há um diferencial: este guia é o <strong>segundo lado de uma mesma jornada</strong>.</p> <p>No outro tutorial — <a href="/blog/tutorial-clojure-clojurescript-todo-list/">Clojure/ClojureScript: Construindo uma Aplicação Persistente e Reativa</a> — resolvemos o mesmo problema usando a stack <strong>Clojure + Reagent + next.jdbc</strong>, explorando o modelo de atualização reativa no navegador e a comunicação via API REST.</p> <p>Aqui, faremos o mesmo <strong>conceitualmente</strong>, mas com <strong>Elixir e LiveView</strong>, onde <strong>frontend e backend se fundem</strong> em um único processo funcional e altamente performático.</p> <hr/> <h3 id="-objetivo-pedagógico">🎯 Objetivo Pedagógico</h3> <p>O propósito deste tutorial não é apenas “fazer funcionar”, mas <strong>entender o porquê</strong>. Cada comando, cada função e cada módulo será explicado com contexto e analogia. Você aprenderá:</p> <ul> <li>Como o Phoenix organiza um projeto web;</li> <li>O que são <strong>schemas</strong>, <strong>migrations</strong> e <strong>changesets</strong> (e como se relacionam com os <em>models</em> dos ORMs tradicionais);</li> <li>Como o <strong>LiveView</strong> elimina a separação rígida entre frontend e backend, permitindo <strong>interações em tempo real</strong> sem escrever uma linha de JavaScript;</li> <li>E, claro, como criar, listar, marcar e excluir tarefas com atualização instantânea na interface.</li> </ul> <hr/> <h3 id="️-a-stack-que-vamos-usar">⚙️ A Stack que Vamos Usar</h3> <ul> <li><strong>Linguagem:</strong> Elixir (baseada em Erlang, funcional e concorrente);</li> <li><strong>Framework Web:</strong> Phoenix 1.8 + LiveView 1.1;</li> <li><strong>Banco de Dados:</strong> SQLite (via Ecto);</li> <li><strong>Estilo:</strong> Tailwind CSS v4 + daisyUI (já integrados ao Phoenix).</li> </ul> <h3 id="-versões-utilizadas-importante">📌 Versões Utilizadas (Importante!)</h3> <p>Para que o tutorial seja reprodutível, estas são as versões de referência:</p> <table> <thead> <tr> <th>Ferramenta / Biblioteca</th> <th>Versão</th> </tr> </thead> <tbody> <tr> <td>Erlang/OTP</td> <td>26+</td> </tr> <tr> <td>Elixir</td> <td>1.17+</td> </tr> <tr> <td>Node.js</td> <td>18+ (LTS)</td> </tr> <tr> <td>Phoenix (<code class="language-plaintext highlighter-rouge">phx_new</code>)</td> <td>1.8.x</td> </tr> <tr> <td>Phoenix LiveView</td> <td>1.1.x</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ecto_sql</code></td> <td>~&gt; 3.10</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ecto_sqlite3</code></td> <td>~&gt; 0.12</td> </tr> </tbody> </table> <aside> <p>⚠️ <strong>Atenção especial ao Phoenix 1.8.</strong> Muitos tutoriais e respostas antigas na internet (e em IAs!) referem-se ao Phoenix <strong>1.7</strong>, que usava Tailwind v3 com <code class="language-plaintext highlighter-rouge">tailwind.config.js</code> e componentes ligeiramente diferentes. O Phoenix <strong>1.8</strong> mudou várias dessas coisas. Este tutorial está inteiramente alinhado ao 1.8 — se algo que você encontrar por aí divergir daqui, desconfie da versão.</p> </aside> <hr/> <h3 id="-um-mesmo-problema-dois-caminhos-funcionais">🔁 Um Mesmo Problema, Dois Caminhos Funcionais</h3> <table> <thead> <tr> <th>Aspecto</th> <th>Clojure</th> <th>Elixir</th> </tr> </thead> <tbody> <tr> <td>Paradigma</td> <td>Funcional puro (imutabilidade explícita)</td> <td>Funcional concorrente (processos leves)</td> </tr> <tr> <td>Renderização</td> <td>Frontend reativo com Reagent (React)</td> <td>LiveView (renderização no servidor em tempo real)</td> </tr> <tr> <td>Comunicação</td> <td>API REST + JSON</td> <td>Canal WebSocket interno (phx)</td> </tr> <tr> <td>Persistência</td> <td>next.jdbc + SQLite</td> <td>Ecto + SQLite</td> </tr> <tr> <td>Reatividade</td> <td><code class="language-plaintext highlighter-rouge">r/atom</code> (frontend)</td> <td>Estado do socket (<code class="language-plaintext highlighter-rouge">assigns</code>)</td> </tr> </tbody> </table> <p>Ambos ensinam o mesmo princípio:</p> <blockquote> <p><strong>como o estado flui em uma aplicação funcional e reativa.</strong></p> </blockquote> <hr/> <h3 id="-os-marcos-do-git-seu-histórico-final">🧾 Os Marcos do Git (Seu Histórico Final)</h3> <p>Este tutorial também é um exercício de <strong>desenvolvimento incremental com Git</strong>. Cada fase termina em um commit; ao final, seu <code class="language-plaintext highlighter-rouge">git log --oneline</code> deve contar esta história (do mais recente para o mais antigo):</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Fase 8: Atualiza README com instruções de execução
Fase 7: Ajusta o tema e personaliza o visual (Tailwind/daisyUI)
Fase 6: Implementa conclusão de tarefas (toggle_complete)
Fase 5: Implementa exclusão de tarefas (delete)
Fase 4: Refatora TodoLive para usar Ecto, Repo e to_form()
Fase 3: Persistência - Configura Ecto, Repo, Migrations e Task Schema
Fase 2: Lógica em Memória - Implementa adição de tarefas
Fase 1: Prova de Vida - Substitui a rota raiz por TodoLive
Fase 0: Gera o esqueleto do Phoenix com LiveView (sem Ecto)
Fase 0: Inicializa o repositório e .gitignore
</code></pre></div></div> <hr/> <h3 id="-índice-das-fases">📚 Índice das Fases</h3> <ul> <li><strong>⚙️ Fase 0:</strong> Setup (Ambiente, Git e o Esqueleto do Projeto)</li> <li><strong>🏃 Fase 1:</strong> “Hello World” — Prova de Vida</li> <li><strong>🧠 Fase 2:</strong> Lógica em Memória</li> <li><strong>🧱 Fase 3:</strong> Persistência — A Camada de Dados (Ecto, Repo, Migration e Schema)</li> <li><strong>🫀 Fase 4:</strong> O “Transplante” — Conectando o LiveView ao Banco</li> <li><strong>🗑️ Fase 5:</strong> Refinamento — Excluindo Tarefas</li> <li><strong>✅ Fase 6:</strong> Refinamento — Concluindo Tarefas (Toggle)</li> <li><strong>🎨 Fase 7:</strong> Personalizando o Design (Tailwind CSS v4 e daisyUI)</li> <li><strong>📄 Fase 8:</strong> README e Entrega</li> </ul> <hr/> <h3 id="-um-tutorial-a-duas-mãos">💡 Um Tutorial a Duas Mãos</h3> <p>Assim como o tutorial em Clojure, este também é fruto de uma construção colaborativa — unindo clareza didática com profundidade técnica.</p> <p>Prepare seu ambiente, abra o terminal e venha ver como <strong>Elixir + LiveView</strong> transforma o jeito de pensar aplicações web. 🚀</p> <hr/> <h1 id="️-fase-0-setup-ambiente-git-e-o-esqueleto-do-projeto">⚙️ Fase 0: Setup (Ambiente, Git e o Esqueleto do Projeto)</h1> <p><strong>Objetivo:</strong> Preparar o ambiente, instalar as ferramentas e gerar o esqueleto do projeto. Mas antes, vamos entender o que é essa stack.</p> <h3 id="a-stack-phoenix-liveview">A Stack: Phoenix LiveView</h3> <p>Antes de começar, é crucial entender por que o <strong>LiveView</strong> é uma abordagem diferente — e por que ela vem ganhando tanto destaque entre desenvolvedores acostumados a React, Vue ou Next.js.</p> <h3 id="o-modo-tradicional-react--nextjs">O Modo Tradicional (React / Next.js)</h3> <p>Em um modelo tradicional baseado em frameworks JavaScript, a aplicação é dividida em <strong>duas camadas independentes</strong>:</p> <ol> <li><strong>Backend (API)</strong> — armazena e serve dados, geralmente em <strong>JSON</strong>.</li> <li><strong>Frontend (SPA ou SSR)</strong> — construído em JavaScript, responsável pela interface, pelo estado e pela renderização dos componentes.</li> </ol> <p>No <strong>React puro</strong>, o navegador recebe um HTML básico e depois baixa e executa o JavaScript que monta toda a interface (renderização no cliente). O <strong>Next.js</strong> otimiza isso com <strong>SSR/SSG</strong>, renderizando o HTML inicial no servidor — mas, após o carregamento, o React ainda assume o controle no cliente, “reidratando” a página.</p> <p>Apesar de eficiente, esse modelo <strong>mantém o estado do aplicativo no navegador</strong>, exigindo sincronização constante com o backend via AJAX ou GraphQL. Isso significa <strong>duas fontes de verdade</strong> (cliente e servidor), <strong>gerenciamento de estado complexo</strong> e <strong>múltiplas camadas de código</strong> para manter tudo sincronizado.</p> <h3 id="o-modo-liveview-estado-no-servidor">O Modo LiveView (Estado no Servidor)</h3> <p>O <strong>Phoenix LiveView</strong> propõe algo radicalmente diferente:</p> <p>👉 <strong>toda a lógica de estado e renderização vive no servidor.</strong></p> <p>O navegador abre uma <strong>conexão WebSocket</strong> — um “túnel” bidirecional e contínuo — com o servidor Phoenix. A partir daí, toda interação do usuário (como clicar em “Adicionar Tarefa”) envia apenas uma <strong>mensagem leve</strong> para o servidor: <code class="language-plaintext highlighter-rouge">"o evento 'save_task' aconteceu"</code>.</p> <p>O <strong>servidor Elixir</strong> processa o evento, atualiza o estado (a lista de tarefas) e re-renderiza o HTML <strong>no próprio servidor</strong>. Em seguida, calcula o que mudou (o <em>diff</em>) e envia apenas os fragmentos atualizados de volta. Um <strong>JavaScript minúsculo</strong>, incluído automaticamente pelo LiveView, faz o “remendo” na página — sem recarregar, sem sincronizar estados, sem React Hooks, sem Redux.</p> <h3 id="a-vantagem">A Vantagem</h3> <ul> <li>O <strong>estado vive em um só lugar</strong> — no servidor.</li> <li>Você escreve <strong>quase zero JavaScript</strong>.</li> <li>A interface é <strong>reativa em tempo real</strong> por padrão.</li> <li>A performance surpreende: o Elixir lida com <strong>milhares de conexões simultâneas</strong> graças ao modelo de concorrência leve da <strong>BEAM VM</strong>.</li> </ul> <p>O LiveView traz a <strong>simplicidade do desenvolvimento tradicional (HTML + servidor)</strong> com a <strong>interatividade do front moderno (SPA)</strong> — sem manter duas aplicações separadas.</p> <hr/> <h3 id="-passo-01-instalar-as-ferramentas">🧰 Passo 0.1: Instalar as Ferramentas</h3> <p>Precisamos de três coisas:</p> <ol> <li><strong>Node.js</strong> — o Phoenix o usa para compilar assets (CSS/JS);</li> <li><strong>Erlang</strong> — a máquina virtual (BEAM) sobre a qual o Elixir roda;</li> <li><strong>Elixir</strong> — a linguagem de programação.</li> </ol> <p><strong>1. Node.js:</strong> baixe e instale a versão <strong>LTS</strong> no <a href="https://nodejs.org/">site oficial</a>.</p> <p><strong>2. Elixir e Erlang (via script oficial):</strong> a maneira mais confiável, com controle exato das versões.</p> <ul> <li> <p><strong>Linux/macOS (Bash):</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Baixa e executa o script, fixando as versões</span>
curl <span class="nt">-fsSO</span> https://elixir-lang.org/install.sh
sh install.sh elixir@1.17.2 otp@26.2.2 <span class="nv">installs_dir</span><span class="o">=</span><span class="nv">$HOME</span>/.elixir-install/installs

<span class="c"># Adicione ao seu PATH (ex: no ~/.bashrc ou ~/.zshrc)</span>
<span class="c"># export PATH=$HOME/.elixir-install/installs/otp/26.2.2/bin:$PATH</span>
<span class="c"># export PATH=$HOME/.elixir-install/installs/elixir/1.17.2-otp-26/bin:$PATH</span>
</code></pre></div> </div> </li> <li> <p><strong>Windows (PowerShell):</strong></p> <div class="language-powershell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Baixa e executa o script</span><span class="w">
</span><span class="n">curl.exe</span><span class="w"> </span><span class="nt">-fsSO</span><span class="w"> </span><span class="nx">https://elixir-lang.org/install.bat</span><span class="w">
</span><span class="o">.</span><span class="n">\install.bat</span><span class="w"> </span><span class="nx">elixir</span><span class="err">@</span><span class="nx">1.17.2</span><span class="w"> </span><span class="nx">otp</span><span class="err">@</span><span class="nx">26.2.2</span><span class="w">

</span><span class="c"># Adicione os diretórios ao seu PATH de Ambiente de Usuário</span><span class="w">
</span><span class="c"># (ex: %USERPROFILE%\.elixir-install\installs\otp\26.2.2\bin)</span><span class="w">
</span><span class="c"># (ex: %USERPROFILE%\.elixir-install\installs\elixir\1.17.2-otp-26\bin)</span><span class="w">
</span></code></pre></div> </div> </li> </ul> <p><strong>Verificação:</strong> feche e reabra o terminal, e confirme que tudo respondeu com uma versão:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>elixir <span class="nt">--version</span>   <span class="c"># Elixir 1.17.x (compiled with Erlang/OTP 26)</span>
node <span class="nt">-v</span>            <span class="c"># v18 ou superior</span>
git <span class="nt">--version</span>
</code></pre></div></div> <p><em>(Se <code class="language-plaintext highlighter-rouge">elixir</code> não for encontrado, o <code class="language-plaintext highlighter-rouge">PATH</code> não foi configurado permanentemente — revise o passo acima antes de prosseguir.)</em></p> <h3 id="-passo-02-instalar-o-hex-e-o-gerador-do-phoenix">🧱 Passo 0.2: Instalar o Hex e o Gerador do Phoenix</h3> <p>O <strong>Mix</strong> é a ferramenta de build do Elixir — algo como o <strong>npm</strong> (Node.js), o <strong>pip</strong> (Python) ou o <strong>Maven</strong> (Java). Com ele, gerenciamos dependências, rodamos tarefas, executamos testes e criamos projetos. Ele já vem instalado junto com o Elixir.</p> <p><strong>O que é o Hex?</strong> O <strong>Hex</strong> é o <strong>gerenciador de pacotes oficial do Elixir</strong> — o papel que o npm faz para o JavaScript. Quando usarmos bibliotecas externas (como o Ecto), é o Hex quem as baixa. Instale-o com:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mix local.hex
</code></pre></div></div> <p><strong>O que é o Phoenix?</strong> O <strong>Phoenix</strong> é o principal <strong>framework web</strong> do ecossistema Elixir — comparável ao <strong>Django</strong> (Python), <strong>Rails</strong> (Ruby) ou <strong>Express</strong> (Node.js), mas construído para aproveitar ao máximo a <strong>concorrência</strong> e o <strong>tempo real</strong> da BEAM VM. Ele vem com o <strong>LiveView</strong>, que permite construir interfaces reativas <strong>sem JavaScript manual</strong>.</p> <p>Instale o gerador de projetos:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mix archive.install hex phx_new
</code></pre></div></div> <p>✅ <strong>Resumo:</strong></p> <ul> <li><strong>Mix</strong> → ferramenta de build e tarefas (como <code class="language-plaintext highlighter-rouge">npm</code> ou <code class="language-plaintext highlighter-rouge">pip</code>);</li> <li><strong>Hex</strong> → gerenciador de pacotes (como o registro do <code class="language-plaintext highlighter-rouge">npm</code>);</li> <li><strong>Phoenix</strong> → framework web completo, com foco em performance e tempo real.</li> </ul> <h3 id="-passo-03-o-diretório-e-o-git">📁 Passo 0.3: O Diretório e o Git</h3> <p>O <code class="language-plaintext highlighter-rouge">Git</code> é nosso sistema de controle de versão. Vamos usá-lo desde o início para salvar o progresso em “checkpoints” (commits).</p> <p><strong>1. Crie o diretório do projeto:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>elixir_todo_list
<span class="nb">cd </span>elixir_todo_list
</code></pre></div></div> <aside> <p>⚠️ <strong>O nome da pasta importa!</strong> O Phoenix vai derivar o nome da aplicação (<code class="language-plaintext highlighter-rouge">:elixir_todo_list</code>) e o prefixo de <strong>todos os módulos</strong> (<code class="language-plaintext highlighter-rouge">ElixirTodoList...</code>) do nome desta pasta. Se você usar outro nome, terá que adaptar todos os nomes de módulo do tutorial. Recomendamos usar exatamente <code class="language-plaintext highlighter-rouge">elixir_todo_list</code>.</p> </aside> <p><strong>2. Inicialize o Git:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git init
git branch <span class="nt">-m</span> main
</code></pre></div></div> <p><strong>3. Crie um <code class="language-plaintext highlighter-rouge">.gitignore</code> inicial.</strong></p> <p>Antes de gerar qualquer código, vamos garantir que o repositório só contenha o que é realmente necessário. O <code class="language-plaintext highlighter-rouge">.gitignore</code> diz ao Git o que <strong>não</strong> versionar — dependências, arquivos compilados, configurações locais e <strong>o arquivo do banco de dados</strong> (que criaremos na Fase 3: ele é <em>resultado</em> da aplicação, não código-fonte).</p> <p>Crie o arquivo <code class="language-plaintext highlighter-rouge">.gitignore</code> (atenção à grafia: <strong>ponto + gitignore</strong>, sem letras faltando!) com o conteúdo:</p> <pre><code class="language-gitignore">/_build
/deps
/priv/static/assets
/node_modules
/assets/node_modules
*.log
/config/dev.secret.exs
.DS_Store
.vscode/

# --- Banco de Dados (SQLite, usado a partir da Fase 3) ---
*.db
*.db-shm
*.db-wal
</code></pre> <h3 id="-passo-04-o-commit-inicial">💾 Passo 0.4: O Commit Inicial</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Fase 0: Inicializa o repositório e .gitignore"</span>
</code></pre></div></div> <aside> <p>💡 <strong>Se o Git reclamar de identidade</strong> (<code class="language-plaintext highlighter-rouge">Please tell me who you are</code>), configure uma vez:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config <span class="nt">--global</span> user.name <span class="s2">"Seu Nome"</span>
git config <span class="nt">--global</span> user.email <span class="s2">"seu@email.com"</span>
</code></pre></div> </div> <p>e repita o commit.</p> </aside> <h3 id="-passo-05-gerar-o-esqueleto-do-projeto">🧩 Passo 0.5: Gerar o Esqueleto do Projeto</h3> <p>Com o Mix e o gerador Phoenix instalados, vamos criar a estrutura da aplicação <strong>dentro do diretório atual</strong>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mix phx.new <span class="nb">.</span> <span class="nt">--no-ecto</span>
</code></pre></div></div> <p><strong>🔍 Analisando o comando:</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">.</code> → o projeto será criado <strong>no diretório atual</strong>. (Se quiséssemos uma pasta nova, seria <code class="language-plaintext highlighter-rouge">mix phx.new minha_app</code>.)</li> <li><code class="language-plaintext highlighter-rouge">--no-ecto</code> → evita instalar o <strong>Ecto</strong> (a camada de banco de dados) por enquanto. Vamos <strong>adiar essa parte</strong> para a Fase 3, pois queremos primeiro entender o funcionamento <strong>“em memória”</strong> do LiveView.</li> </ul> <aside> <p>💡 E o LiveView? Desde o Phoenix 1.7, o <strong>LiveView já vem incluído por padrão</strong> — não é preciso nenhuma flag para ativá-lo (existe apenas <code class="language-plaintext highlighter-rouge">--no-live</code> para quem quiser removê-lo, o que não é o nosso caso).</p> </aside> <p><strong>💡 Comparando com outras stacks:</strong></p> <ul> <li>No <strong>Django</strong>: <code class="language-plaintext highlighter-rouge">django-admin startproject nome_do_projeto</code>.</li> <li>No <strong>Rails</strong>: <code class="language-plaintext highlighter-rouge">rails new nome_do_projeto</code>.</li> <li>No <strong>Node.js</strong>: <code class="language-plaintext highlighter-rouge">npx create-next-app</code> ou <code class="language-plaintext highlighter-rouge">express-generator</code>.</li> </ul> <p>Assim como nesses casos, o Phoenix gera <strong>um esqueleto completo</strong> de aplicação web, com pastas organizadas para templates, rotas, assets e (opcionalmente) banco de dados.</p> <p><strong>⚠️ Atenção às perguntas do gerador!</strong> Como a pasta não está vazia, o Phoenix fará algumas perguntas:</p> <ol> <li><code class="language-plaintext highlighter-rouge">The directory ... already exists. Are you sure you want to continue?</code> → responda <strong>Y</strong>.</li> <li><code class="language-plaintext highlighter-rouge">.gitignore already exists, overwrite?</code> → responda <strong>Y</strong> (sim!). O <code class="language-plaintext highlighter-rouge">.gitignore</code> do Phoenix é mais completo que o nosso — vamos aceitá-lo e <strong>recolocar nossas regras do banco em seguida</strong>.</li> <li><code class="language-plaintext highlighter-rouge">Fetch and install dependencies? [Yn]</code> → responda <strong>Y</strong>.</li> </ol> <p>O Phoenix irá baixar todas as <strong>dependências Elixir</strong> (pelo Hex) e configurar os <strong>assets</strong> (Tailwind/esbuild), deixando o projeto pronto para rodar.</p> <h3 id="️-passo-06-reaplicar-as-regras-do-banco-no-gitignore">✂️ Passo 0.6: Reaplicar as Regras do Banco no <code class="language-plaintext highlighter-rouge">.gitignore</code></h3> <p>Como aceitamos o <code class="language-plaintext highlighter-rouge">.gitignore</code> do Phoenix (que não conhece nosso futuro banco SQLite), abra o <code class="language-plaintext highlighter-rouge">.gitignore</code> e <strong>acrescente ao final</strong>:</p> <pre><code class="language-gitignore"># --- Banco de Dados (SQLite, usado a partir da Fase 3) ---
*.db
*.db-shm
*.db-wal
</code></pre> <aside> <p>⚠️ <strong>Por que os três padrões?</strong> O SQLite, no modo padrão do Ecto, cria três arquivos: o banco em si (<code class="language-plaintext highlighter-rouge">.db</code>) e dois auxiliares de escrita (<code class="language-plaintext highlighter-rouge">.db-shm</code> e <code class="language-plaintext highlighter-rouge">.db-wal</code> — este último pode crescer para centenas de KB!). Nenhum deles deve ir para o GitHub. Esquecer isso é um dos erros mais comuns — e mais feios — em repositórios de alunos.</p> </aside> <h3 id="-passo-07-o-commit-do-esqueleto">💾 Passo 0.7: O Commit do Esqueleto</h3> <p>Agora que temos a estrutura gerada (e o <code class="language-plaintext highlighter-rouge">.gitignore</code> completo), vamos versionar o ponto de partida:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Fase 0: Gera o esqueleto do Phoenix com LiveView (sem Ecto)"</span>
</code></pre></div></div> <p>Esse commit marca o início oficial do projeto: um esqueleto Phoenix totalmente funcional, com LiveView configurado, mas sem banco de dados — perfeito para explorar a lógica do LiveView em tempo real.</p> <hr/> <p><strong>Fim da Fase 0!</strong> 🏁</p> <hr/> <h1 id="-fase-1-hello-world--prova-de-vida">🏃 Fase 1: “Hello World” — Prova de Vida</h1> <p><strong>Objetivo:</strong> ligar o servidor Phoenix e verificar se tudo está funcionando — o primeiro “sinal de vida” do projeto. Em seguida, substituir a página padrão pelo <strong>nosso</strong> LiveView.</p> <h3 id="-passo-11-ligar-o-servidor">🔌 Passo 1.1: Ligar o Servidor</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mix phx.server
</code></pre></div></div> <p>O servidor será iniciado e exibirá mensagens no terminal. Na primeira execução, o projeto será compilado — pode demorar um pouco.</p> <p>Abra o navegador e visite: 👉 <strong>http://localhost:4000</strong></p> <p>Se tudo deu certo, você verá a <strong>página de boas-vindas do Phoenix</strong> (com o logo e links para a documentação).</p> <aside> <p>⚠️ <strong>Aviso comum no Linux:</strong> se aparecer uma mensagem mencionando <strong>inotify-tools</strong>, o <em>live reload</em> (atualização automática do navegador quando os arquivos mudam) não está funcionando. Pare o servidor (<code class="language-plaintext highlighter-rouge">Ctrl+C</code> duas vezes) e instale:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt-get <span class="nb">install </span>inotify-tools
</code></pre></div> </div> <p>Depois rode <code class="language-plaintext highlighter-rouge">mix phx.server</code> novamente.</p> </aside> <aside> <p>💡 <strong>Deixe este terminal rodando.</strong> Diferente do tutorial de Clojure (que precisava de dois servidores), aqui <strong>um único processo</strong> cuida de tudo: backend, frontend e a compilação dos assets. Abra um <strong>segundo terminal</strong> para os comandos de Git e Mix daqui em diante.</p> </aside> <h3 id="-passo-12-alterar-o-roteador">🧭 Passo 1.2: Alterar o Roteador</h3> <p>O Phoenix está exibindo a página padrão (controlada pelo <code class="language-plaintext highlighter-rouge">PageController</code>). Vamos trocá-la pelo nosso próprio <strong>LiveView</strong>, que será o coração da aplicação.</p> <p>Abra o arquivo:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lib/elixir_todo_list_web/router.ex
</code></pre></div></div> <p>Encontre o escopo principal e substitua a rota raiz <code class="language-plaintext highlighter-rouge">/</code>.</p> <p><em>De:</em></p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">scope</span> <span class="s2">"/"</span><span class="p">,</span> <span class="no">ElixirTodoListWeb</span> <span class="k">do</span>
  <span class="n">pipe_through</span> <span class="ss">:browser</span>

  <span class="n">get</span> <span class="s2">"/"</span><span class="p">,</span> <span class="no">PageController</span><span class="p">,</span> <span class="ss">:home</span>
<span class="k">end</span>
</code></pre></div></div> <p><em>Para:</em></p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">scope</span> <span class="s2">"/"</span><span class="p">,</span> <span class="no">ElixirTodoListWeb</span> <span class="k">do</span>
  <span class="n">pipe_through</span> <span class="ss">:browser</span>

  <span class="n">live</span> <span class="s2">"/"</span><span class="p">,</span> <span class="no">TodoLive</span><span class="p">,</span> <span class="ss">:index</span>
<span class="k">end</span>
</code></pre></div></div> <p>A diferença está na palavra-chave <strong><code class="language-plaintext highlighter-rouge">live</code></strong>:</p> <ul> <li><code class="language-plaintext highlighter-rouge">get</code> → responde com uma página renderizada <strong>uma única vez</strong> por um controller tradicional.</li> <li><code class="language-plaintext highlighter-rouge">live</code> → mantém uma <strong>conexão em tempo real</strong> (WebSocket), capaz de atualizar o conteúdo dinamicamente.</li> </ul> <h3 id="-passo-13-o-primeiro-erro-e-por-que-ele-é-bom">💥 Passo 1.3: O Primeiro Erro (e por que ele é bom)</h3> <p>Salve o arquivo e recarregue a página no navegador. Você verá um erro como:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>** (UndefinedFunctionError) ... ElixirTodoListWeb.TodoLive ... is undefined
   (module ElixirTodoListWeb.TodoLive is not available)
</code></pre></div></div> <p>Excelente! 🎉 Isso significa que o Phoenix <strong>entendeu a nova rota</strong>, mas não encontrou o módulo <code class="language-plaintext highlighter-rouge">TodoLive</code>. Ou seja: a configuração está correta — só falta criarmos o módulo. Esse tipo de erro é um ótimo sinal no Phoenix: o compilador está te guiando sobre o que precisa existir.</p> <h3 id="-passo-14-criar-o-liveview">🧱 Passo 1.4: Criar o LiveView</h3> <aside> <p>⚠️ <strong>Atenção — nomes de módulos no Elixir</strong></p> <p>O Elixir segue uma convenção rígida: o <strong>nome dos módulos</strong> deve corresponder à <strong>estrutura de diretórios</strong> do projeto. Como nosso projeto foi criado na pasta <code class="language-plaintext highlighter-rouge">elixir_todo_list</code>, o prefixo dos módulos é <code class="language-plaintext highlighter-rouge">ElixirTodoList</code> (e, para a camada web, <code class="language-plaintext highlighter-rouge">ElixirTodoListWeb</code>).</p> <table> <thead> <tr> <th>Diretório</th> <th>Arquivo</th> <th>Módulo</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">lib/elixir_todo_list_web/live/</code></td> <td><code class="language-plaintext highlighter-rouge">todo_live.ex</code></td> <td><code class="language-plaintext highlighter-rouge">ElixirTodoListWeb.TodoLive</code></td> </tr> </tbody> </table> <p>Se o nome for diferente (por exemplo, <code class="language-plaintext highlighter-rouge">TodoListWeb.TodoLive</code>), o compilador <strong>não encontrará o módulo</strong> e você verá exatamente o erro do passo anterior — só que dessa vez sem solução à vista. Em resumo: <strong>use o nome do diretório raiz do projeto como base</strong> para os módulos.</p> </aside> <p>Crie o arquivo:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lib/elixir_todo_list_web/live/todo_live.ex
</code></pre></div></div> <p>E adicione o seguinte código:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">ElixirTodoListWeb</span><span class="o">.</span><span class="no">TodoLive</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">ElixirTodoListWeb</span><span class="p">,</span> <span class="ss">:live_view</span>

  <span class="c1"># mount/3 é o "construtor" do LiveView — chamado quando a página é carregada</span>
  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">mount</span><span class="p">(</span><span class="n">_params</span><span class="p">,</span> <span class="n">_session</span><span class="p">,</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
  <span class="k">end</span>

  <span class="c1"># render/1 define o HTML que será exibido</span>
  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">render</span><span class="p">(</span><span class="n">assigns</span><span class="p">)</span> <span class="k">do</span>
    <span class="sx">~H""</span><span class="s2">"
    &lt;div class="</span><span class="n">p</span><span class="o">-</span><span class="mi">12</span><span class="s2">"&gt;
      &lt;h1 class="</span><span class="n">text</span><span class="o">-</span><span class="err">3</span><span class="n">xl</span> <span class="n">font</span><span class="o">-</span><span class="n">bold</span><span class="s2">"&gt;Meu Todo List (Hello LiveView!)&lt;/h1&gt;
    &lt;/div&gt;
    """</span>
  <span class="k">end</span>
<span class="k">end</span>
</code></pre></div></div> <p>O que está acontecendo aqui:</p> <ul> <li><code class="language-plaintext highlighter-rouge">use ElixirTodoListWeb, :live_view</code> → diz ao Phoenix que este módulo é um <strong>LiveView</strong> (e já traz, de brinde, os componentes de UI e helpers que usaremos nas próximas fases).</li> <li><code class="language-plaintext highlighter-rouge">mount/3</code> → chamado quando a página é carregada; é o ponto de inicialização do “estado”.</li> <li><code class="language-plaintext highlighter-rouge">render/1</code> → retorna o HTML a ser exibido.</li> <li><code class="language-plaintext highlighter-rouge">~H""" ... """</code> → é o <strong>HEEx</strong> (HTML + Embedded Elixir), uma versão do HTML com suporte a expressões Elixir — similar ao JSX do React, mas processado <strong>no servidor</strong> e <strong>verificado em tempo de compilação</strong>.</li> </ul> <p>Salve o arquivo e observe: o servidor recompila automaticamente, o erro desaparece e o navegador atualiza sozinho mostrando:</p> <blockquote> <p><strong>Meu Todo List (Hello LiveView!)</strong></p> </blockquote> <p>Isso confirma que o LiveView está funcionando: você acabou de renderizar sua primeira página dinâmica em tempo real, <strong>sem escrever uma linha de JavaScript</strong>.</p> <h3 id="-passo-15-commit">💾 Passo 1.5: Commit</h3> <p>No <strong>segundo terminal</strong> (deixe o servidor rodando no primeiro):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Fase 1: Prova de Vida - Substitui a rota raiz por TodoLive"</span>
</code></pre></div></div> <p>Pronto! 🎯 Nosso projeto Phoenix com LiveView está oficialmente “vivo”.</p> <hr/> <h2 id="-pausa-didática--entendendo-use-impl-true-mount-e-render">🧠 Pausa Didática — Entendendo <code class="language-plaintext highlighter-rouge">use</code>, <code class="language-plaintext highlighter-rouge">@impl true</code>, <code class="language-plaintext highlighter-rouge">mount</code> e <code class="language-plaintext highlighter-rouge">render</code></h2> <h3 id="1-o-que-faz-o-use">1. O que faz o <code class="language-plaintext highlighter-rouge">use</code></h3> <p>A linha:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">use</span> <span class="no">ElixirTodoListWeb</span><span class="p">,</span> <span class="ss">:live_view</span>
</code></pre></div></div> <p>é uma forma especial de <strong>trazer comportamentos e configurações</strong> para o módulo atual — uma <strong>injeção de código</strong> feita durante a compilação.</p> <p>💡 <strong>Em termos simples:</strong> o <code class="language-plaintext highlighter-rouge">use</code> importa automaticamente todas as funções, macros e configurações necessárias para que o módulo se comporte como um LiveView.</p> <table> <thead> <tr> <th>Linguagem</th> <th>Equivalente aproximado</th> <th>O que faz</th> </tr> </thead> <tbody> <tr> <td><strong>Java</strong></td> <td><code class="language-plaintext highlighter-rouge">extends BaseView</code></td> <td>Herda métodos e atributos de uma classe base</td> </tr> <tr> <td><strong>Python</strong></td> <td><code class="language-plaintext highlighter-rouge">class MyView(BaseView):</code></td> <td>Cria uma subclasse com comportamento herdado</td> </tr> <tr> <td><strong>Elixir</strong></td> <td><code class="language-plaintext highlighter-rouge">use ElixirTodoListWeb, :live_view</code></td> <td>Injeta código e comportamentos no módulo atual</td> </tr> </tbody> </table> <p>➡️ O <code class="language-plaintext highlighter-rouge">use</code> <strong>não é herança</strong>, mas <strong>geração de código</strong>: ele deixa o módulo pronto para o ecossistema Phoenix. No nosso projeto, ele também já <strong>importa os componentes de UI</strong> (<code class="language-plaintext highlighter-rouge">&lt;.form&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;.input&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;.button&gt;</code>) e disponibiliza o alias <code class="language-plaintext highlighter-rouge">Layouts</code> — vamos usá-los a partir da Fase 4.</p> <h3 id="2-o-papel-do-impl-true">2. O papel do <code class="language-plaintext highlighter-rouge">@impl true</code></h3> <p>O marcador <code class="language-plaintext highlighter-rouge">@impl true</code> vem antes de funções que <strong>implementam callbacks</strong> de um <strong>comportamento</strong> (<em>behaviour</em>). Um <em>behaviour</em> em Elixir é parecido com uma <strong>interface</strong> em linguagens orientadas a objetos: ele define <strong>quais funções um módulo deve implementar</strong>.</p> <p>O LiveView espera que cada módulo tenha, no mínimo, <code class="language-plaintext highlighter-rouge">mount/3</code> e <code class="language-plaintext highlighter-rouge">render/1</code>. Quando você escreve <code class="language-plaintext highlighter-rouge">@impl true</code>, está dizendo ao compilador: <em>“esta função é a implementação esperada pelo comportamento do LiveView”</em>.</p> <table> <thead> <tr> <th>Linguagem</th> <th>Anotação equivalente</th> <th>Finalidade</th> </tr> </thead> <tbody> <tr> <td><strong>Java</strong></td> <td><code class="language-plaintext highlighter-rouge">@Override</code></td> <td>Indica que o método sobrescreve outro da interface/classe pai</td> </tr> <tr> <td><strong>Elixir</strong></td> <td><code class="language-plaintext highlighter-rouge">@impl true</code></td> <td>Indica que a função implementa um callback de um comportamento</td> </tr> </tbody> </table> <p>Isso ajuda o compilador a verificar se o nome da função, a quantidade de parâmetros e o contrato do framework estão corretos.</p> <h3 id="3-entendendo-mount3-e-render1">3. Entendendo <code class="language-plaintext highlighter-rouge">mount/3</code> e <code class="language-plaintext highlighter-rouge">render/1</code></h3> <p><strong><code class="language-plaintext highlighter-rouge">mount/3</code></strong> — chamado quando o LiveView é carregado pela primeira vez. É como o <strong>construtor</strong> de uma classe: inicializa o estado (socket) e prepara dados.</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">@impl</span> <span class="no">true</span>
<span class="k">def</span> <span class="n">mount</span><span class="p">(</span><span class="n">_params</span><span class="p">,</span> <span class="n">_session</span><span class="p">,</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
  <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">assign</span><span class="p">(</span><span class="n">socket</span><span class="p">,</span> <span class="ss">tarefas:</span> <span class="p">[])}</span>
<span class="k">end</span>
</code></pre></div></div> <p><strong><code class="language-plaintext highlighter-rouge">render/1</code></strong> — gera o HTML da página a partir das variáveis (<code class="language-plaintext highlighter-rouge">assigns</code>):</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">@impl</span> <span class="no">true</span>
<span class="k">def</span> <span class="n">render</span><span class="p">(</span><span class="n">assigns</span><span class="p">)</span> <span class="k">do</span>
  <span class="sx">~H""</span><span class="s2">"
  &lt;div&gt;
    &lt;h1&gt;Minhas Tarefas&lt;/h1&gt;
    &lt;ul&gt;
      &lt;li :for={tarefa &lt;- @tarefas}&gt;{tarefa}&lt;/li&gt;
    &lt;/ul&gt;
  &lt;/div&gt;
  """</span>
<span class="k">end</span>
</code></pre></div></div> <h3 id="4-o-paralelo-completo">4. O paralelo completo</h3> <table> <thead> <tr> <th>Conceito</th> <th>Em Elixir (Phoenix)</th> <th>Analogia em OO</th> <th>Finalidade</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">use</code></td> <td>Injeta código e macros</td> <td><code class="language-plaintext highlighter-rouge">extends</code> + <code class="language-plaintext highlighter-rouge">import</code></td> <td>Reutilização de lógica base</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">@impl true</code></td> <td>Declara implementação de callback</td> <td><code class="language-plaintext highlighter-rouge">@Override</code></td> <td>Validação e clareza</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">mount/3</code></td> <td>Inicializa o LiveView</td> <td>Construtor da classe</td> <td>Configura o estado inicial</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">render/1</code></td> <td>Retorna o HTML dinâmico</td> <td><code class="language-plaintext highlighter-rouge">render()</code> / <code class="language-plaintext highlighter-rouge">toString()</code></td> <td>Define a interface visual</td> </tr> </tbody> </table> <hr/> <p><strong>Fim da Fase 1!</strong> 🏁</p> <hr/> <h1 id="-fase-2-lógica-em-memória">🧠 Fase 2: Lógica em Memória</h1> <p><strong>Objetivo:</strong> construir a lógica que permite adicionar tarefas, <strong>sem ainda usar banco de dados</strong>.</p> <p>Nesta fase, o foco é compreender <strong>como o Phoenix LiveView mantém o estado da interface em tempo real</strong>, usando apenas o <strong>socket</strong> — o elo entre cliente e servidor.</p> <h3 id="-o-que-é-o-estado-no-liveview">🧩 O que é o “estado” no LiveView?</h3> <p>No modelo LiveView, <strong>o estado da aplicação vive no servidor</strong>. Sempre que o usuário interage com a interface (digita, clica, envia um formulário), o navegador envia um pequeno evento via WebSocket. O servidor processa o evento, <strong>atualiza o estado</strong> e devolve o HTML atualizado.</p> <p>Esse “estado” é armazenado no <strong>socket</strong>, uma estrutura que representa a sessão daquele usuário. Tudo o que a tela precisa saber — tarefas, campos de formulário, filtros — é guardado ali.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Socket = "memória viva" do LiveView
assign(socket, chave: valor)  ➜  grava ou atualiza o estado
@chave                        ➜  acessa o valor no render
</code></pre></div></div> <blockquote> <p>💡 <strong>Conexão com o tutorial de Clojure:</strong> o socket cumpre aqui o papel que o <code class="language-plaintext highlighter-rouge">r/atom</code> cumpria no Reagent — a “caixa” observada cuja mudança redesenha a interface. A diferença: lá a caixa vivia <strong>no navegador</strong>; aqui, ela vive <strong>no servidor</strong>.</p> </blockquote> <h2 id="️-passo-21-o-estado-inicial-mount3">⚙️ Passo 2.1: O Estado Inicial (<code class="language-plaintext highlighter-rouge">mount/3</code>)</h2> <p>Quando o usuário abre a página, o LiveView chama <code class="language-plaintext highlighter-rouge">mount/3</code>. É aqui que criamos o estado inicial — uma mini “memória RAM” para aquele cliente.</p> <p>Abra o arquivo:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lib/elixir_todo_list_web/live/todo_live.ex
</code></pre></div></div> <p>e substitua o <code class="language-plaintext highlighter-rouge">mount/3</code> atual por:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="c1"># @impl true → indicamos que esta função faz parte do comportamento LiveView</span>
  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">mount</span><span class="p">(</span><span class="n">_params</span><span class="p">,</span> <span class="n">_session</span><span class="p">,</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="c1"># Nossa "memória" inicial: duas tarefas de exemplo</span>
    <span class="n">tasks</span> <span class="o">=</span> <span class="p">[</span>
      <span class="p">%{</span><span class="ss">id:</span> <span class="mi">1</span><span class="p">,</span> <span class="ss">title:</span> <span class="s2">"Comprar leite"</span><span class="p">,</span> <span class="ss">completed:</span> <span class="no">false</span><span class="p">},</span>
      <span class="p">%{</span><span class="ss">id:</span> <span class="mi">2</span><span class="p">,</span> <span class="ss">title:</span> <span class="s2">"Aprender LiveView"</span><span class="p">,</span> <span class="ss">completed:</span> <span class="no">true</span><span class="p">}</span>
    <span class="p">]</span>

    <span class="c1"># assign/2 coloca dados no socket (nosso estado de interface)</span>
    <span class="n">socket</span> <span class="o">=</span>
      <span class="n">assign</span><span class="p">(</span><span class="n">socket</span><span class="p">,</span>
        <span class="ss">tasks:</span> <span class="n">tasks</span><span class="p">,</span>
        <span class="ss">new_task_title:</span> <span class="s2">""</span> <span class="c1"># o campo de texto começa vazio</span>
      <span class="p">)</span>

    <span class="c1"># {:ok, socket} → retorna o estado inicial ao LiveView</span>
    <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
  <span class="k">end</span>
</code></pre></div></div> <h2 id="-passo-22-mostrando-o-estado-render1">🎨 Passo 2.2: Mostrando o Estado (<code class="language-plaintext highlighter-rouge">render/1</code>)</h2> <p>A função <code class="language-plaintext highlighter-rouge">render/1</code> é o “desenhista” da interface: recebe os dados do socket (via <code class="language-plaintext highlighter-rouge">assigns</code>) e devolve o HTML dinâmico.</p> <p>Substitua o <code class="language-plaintext highlighter-rouge">render/1</code> por:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">render</span><span class="p">(</span><span class="n">assigns</span><span class="p">)</span> <span class="k">do</span>
    <span class="sx">~H""</span><span class="s2">"
    &lt;div class="</span><span class="n">w</span><span class="o">-</span><span class="n">full</span> <span class="n">max</span><span class="o">-</span><span class="n">w</span><span class="o">-</span><span class="n">lg</span> <span class="n">mx</span><span class="o">-</span><span class="n">auto</span> <span class="n">mt</span><span class="o">-</span><span class="mi">12</span> <span class="n">p</span><span class="o">-</span><span class="mi">6</span> <span class="n">bg</span><span class="o">-</span><span class="n">white</span> <span class="n">rounded</span><span class="o">-</span><span class="n">lg</span> <span class="n">shadow</span><span class="o">-</span><span class="n">md</span><span class="s2">"&gt;
      &lt;h1 class="</span><span class="n">text</span><span class="o">-</span><span class="err">3</span><span class="n">xl</span> <span class="n">font</span><span class="o">-</span><span class="n">bold</span> <span class="n">mb</span><span class="o">-</span><span class="mi">6</span> <span class="n">text</span><span class="o">-</span><span class="n">center</span> <span class="n">text</span><span class="o">-</span><span class="n">gray</span><span class="o">-</span><span class="mi">800</span><span class="s2">"&gt;
        Minha Lista de Tarefas
      &lt;/h1&gt;

      &lt;%!-- FORMULÁRIO DE ENTRADA --%&gt;
      &lt;form phx-submit="</span><span class="n">save_task</span><span class="s2">" phx-change="</span><span class="n">update_form</span><span class="s2">" class="</span><span class="n">flex</span> <span class="n">gap</span><span class="o">-</span><span class="mi">2</span> <span class="n">mb</span><span class="o">-</span><span class="mi">6</span><span class="s2">"&gt;
        &lt;input
          type="</span><span class="n">text</span><span class="s2">"
          name="</span><span class="n">title</span><span class="s2">"
          value={@new_task_title}
          placeholder="</span><span class="no">O</span> <span class="n">que</span> <span class="n">precisa</span> <span class="n">ser</span> <span class="n">feito?</span><span class="s2">"
          class="</span><span class="n">flex</span><span class="o">-</span><span class="n">grow</span> <span class="n">p</span><span class="o">-</span><span class="mi">2</span> <span class="n">border</span> <span class="n">rounded</span><span class="s2">"
          autofocus
        /&gt;
        &lt;button type="</span><span class="n">submit</span><span class="s2">" class="</span><span class="n">px</span><span class="o">-</span><span class="mi">4</span> <span class="n">py</span><span class="o">-</span><span class="mi">2</span> <span class="n">bg</span><span class="o">-</span><span class="n">blue</span><span class="o">-</span><span class="mi">500</span> <span class="n">text</span><span class="o">-</span><span class="n">white</span> <span class="n">rounded</span> <span class="ss">hover:</span><span class="n">bg</span><span class="o">-</span><span class="n">blue</span><span class="o">-</span><span class="mi">600</span><span class="s2">"&gt;
          Adicionar
        &lt;/button&gt;
      &lt;/form&gt;

      &lt;%!-- LISTA DE TAREFAS --%&gt;
      &lt;div class="</span><span class="n">mt</span><span class="o">-</span><span class="mi">8</span><span class="s2">"&gt;
        &lt;ul id="</span><span class="n">task</span><span class="o">-</span><span class="n">list</span><span class="s2">"&gt;
          &lt;li :for={task &lt;- @tasks} class="</span><span class="n">flex</span> <span class="n">justify</span><span class="o">-</span><span class="n">between</span> <span class="n">items</span><span class="o">-</span><span class="n">center</span> <span class="n">p</span><span class="o">-</span><span class="mi">3</span> <span class="n">border</span><span class="o">-</span><span class="n">b</span><span class="s2">"&gt;
            &lt;span class={if task.completed, do: "</span><span class="n">line</span><span class="o">-</span><span class="n">through</span> <span class="n">text</span><span class="o">-</span><span class="n">gray</span><span class="o">-</span><span class="mi">500</span><span class="s2">", else: "</span><span class="n">text</span><span class="o">-</span><span class="n">gray</span><span class="o">-</span><span class="mi">900</span><span class="s2">"}&gt;
              {task.title}
            &lt;/span&gt;
          &lt;/li&gt;
        &lt;/ul&gt;
      &lt;/div&gt;
    &lt;/div&gt;
    """</span>
  <span class="k">end</span>
</code></pre></div></div> <h3 id="-entendendo-os-atributos-mágicos">🧭 Entendendo os atributos “mágicos”</h3> <table> <thead> <tr> <th>Atributo</th> <th>Função</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">phx-submit="save_task"</code></td> <td>Dispara o evento <code class="language-plaintext highlighter-rouge">"save_task"</code> quando o formulário é enviado.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">phx-change="update_form"</code></td> <td>Dispara o evento <code class="language-plaintext highlighter-rouge">"update_form"</code> a cada tecla digitada.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">value={@new_task_title}</code></td> <td>Liga o campo de texto ao estado do socket.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">:for={task &lt;- @tasks}</code></td> <td>Repete o <code class="language-plaintext highlighter-rouge">&lt;li&gt;</code> para cada tarefa (o “for” do HEEx).</td> </tr> </tbody> </table> <h2 id="️-passo-23-o-aha--o-texto-que-desaparece">⚠️ Passo 2.3: O “Aha!” — o texto que desaparece</h2> <p>Salve o arquivo e recarregue a página. Você verá o formulário e as duas tarefas de exemplo.</p> <p>Agora <strong>digite algo no campo</strong>. Percebeu? <strong>O texto desaparece a cada letra!</strong></p> <p>Isso acontece porque o evento <code class="language-plaintext highlighter-rouge">"update_form"</code> é disparado, mas <strong>ainda não existe uma função para tratá-lo</strong> (<code class="language-plaintext highlighter-rouge">handle_event</code>). O processo LiveView falha, o supervisor o <strong>reinicia automaticamente</strong> (é assim que a BEAM lida com erros!), e o <code class="language-plaintext highlighter-rouge">mount/3</code> roda de novo — limpando o campo. Dê uma olhada no terminal do servidor: o erro está registrado lá.</p> <p>Essa é uma lição dupla: (1) todo evento <code class="language-plaintext highlighter-rouge">phx-*</code> precisa de um <code class="language-plaintext highlighter-rouge">handle_event</code> correspondente; (2) quando um processo LiveView “morre”, ele renasce do estado inicial.</p> <h2 id="-passo-24-tratando-a-digitação-update_form">🧩 Passo 2.4: Tratando a Digitação (<code class="language-plaintext highlighter-rouge">update_form</code>)</h2> <p>Vamos criar a função que captura o evento <code class="language-plaintext highlighter-rouge">"update_form"</code>. Adicione <strong>entre o <code class="language-plaintext highlighter-rouge">mount/3</code> e o <code class="language-plaintext highlighter-rouge">render/1</code></strong>:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="c1"># Captura o evento de digitação no campo</span>
  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="s2">"update_form"</span><span class="p">,</span> <span class="p">%{</span><span class="s2">"title"</span> <span class="o">=&gt;</span> <span class="n">new_title</span><span class="p">},</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="c1"># Atualiza o valor do campo no estado</span>
    <span class="n">socket</span> <span class="o">=</span> <span class="n">assign</span><span class="p">(</span><span class="n">socket</span><span class="p">,</span> <span class="ss">new_task_title:</span> <span class="n">new_title</span><span class="p">)</span>
    <span class="p">{</span><span class="ss">:noreply</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span> <span class="c1"># retorna o socket atualizado, sem recarregar a página</span>
  <span class="k">end</span>
</code></pre></div></div> <p>Salve e teste: agora o texto digitado <strong>permanece</strong>. O estado está sendo atualizado <strong>a cada tecla</strong> — e o LiveView reflete isso no HTML.</p> <h2 id="-passo-25-tratando-o-envio-save_task">🧱 Passo 2.5: Tratando o Envio (<code class="language-plaintext highlighter-rouge">save_task</code>)</h2> <p>O próximo passo é realmente <strong>adicionar a nova tarefa</strong> à lista quando o usuário clicar em “Adicionar”. Adicione esta função <strong>logo abaixo da anterior</strong>:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="c1"># Captura o evento de envio do formulário</span>
  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="s2">"save_task"</span><span class="p">,</span> <span class="p">%{</span><span class="s2">"title"</span> <span class="o">=&gt;</span> <span class="n">title</span><span class="p">},</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="k">if</span> <span class="no">String</span><span class="o">.</span><span class="n">trim</span><span class="p">(</span><span class="n">title</span><span class="p">)</span> <span class="o">!=</span> <span class="s2">""</span> <span class="k">do</span>
      <span class="c1"># Cria uma nova tarefa "em memória"</span>
      <span class="n">new_task</span> <span class="o">=</span> <span class="p">%{</span>
        <span class="ss">id:</span> <span class="no">System</span><span class="o">.</span><span class="n">unique_integer</span><span class="p">([</span><span class="ss">:positive</span><span class="p">]),</span>
        <span class="ss">title:</span> <span class="n">title</span><span class="p">,</span>
        <span class="ss">completed:</span> <span class="no">false</span>
      <span class="p">}</span>

      <span class="c1"># Atualiza a lista de tarefas e limpa o campo</span>
      <span class="n">socket</span> <span class="o">=</span>
        <span class="n">socket</span>
        <span class="o">|&gt;</span> <span class="n">update</span><span class="p">(</span><span class="ss">:tasks</span><span class="p">,</span> <span class="k">fn</span> <span class="n">tasks</span> <span class="o">-&gt;</span> <span class="n">tasks</span> <span class="o">++</span> <span class="p">[</span><span class="n">new_task</span><span class="p">]</span> <span class="k">end</span><span class="p">)</span>
        <span class="o">|&gt;</span> <span class="n">assign</span><span class="p">(</span><span class="ss">:new_task_title</span><span class="p">,</span> <span class="s2">""</span><span class="p">)</span>

      <span class="p">{</span><span class="ss">:noreply</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
    <span class="k">else</span>
      <span class="c1"># Ignora caso o campo esteja vazio</span>
      <span class="p">{</span><span class="ss">:noreply</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
    <span class="k">end</span>
  <span class="k">end</span>
</code></pre></div></div> <table> <thead> <tr> <th>Função</th> <th>Explicação</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">System.unique_integer/1</code></td> <td>Gera um ID único (suficiente para uso “em memória”).</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">update/3</code></td> <td>Atualiza um valor existente no socket aplicando uma função (a lista <code class="language-plaintext highlighter-rouge">:tasks</code>).</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">assign/3</code></td> <td>Redefine <code class="language-plaintext highlighter-rouge">new_task_title</code> para limpar o input.</td> </tr> </tbody> </table> <h2 id="-resultado">🎉 Resultado</h2> <p>Você tem um <strong>Todo List funcional</strong>, mesmo sem banco de dados! Adicione algumas tarefas e veja a lista crescer instantaneamente.</p> <p><strong>O teste da verdade:</strong> agora <strong>recarregue a página (F5)</strong>. As tarefas que você adicionou sumiram — só as duas de exemplo voltaram. E se você reiniciar o servidor, o mesmo acontece.</p> <p>Isso é esperado: as tarefas viviam <strong>apenas na memória daquele socket</strong>. Cada F5 abre uma conexão nova, e o <code class="language-plaintext highlighter-rouge">mount/3</code> recomeça do zero. Você acabou de ganhar a <strong>motivação</strong> para a Fase 3: persistência de verdade.</p> <p>O que aprendemos — o <strong>coração do LiveView</strong>:</p> <ul> <li>O <strong>estado</strong> mora no <strong>socket</strong>;</li> <li>Cada <strong>evento</strong> é tratado por um <code class="language-plaintext highlighter-rouge">handle_event/3</code>;</li> <li>O <strong><code class="language-plaintext highlighter-rouge">render/1</code></strong> reflete o estado atual em HTML, sem recarregar a página.</li> </ul> <h3 id="-passo-26-commit">💾 Passo 2.6: Commit</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Fase 2: Lógica em Memória - Implementa adição de tarefas"</span>
</code></pre></div></div> <hr/> <h2 id="-pausa-didática--entendendo-o-estilo-elixir">🧠 Pausa Didática — Entendendo o Estilo Elixir</h2> <p>Antes de continuar, vamos fazer uma pausa estratégica para compreender <strong>a estrutura e o estilo da linguagem</strong> que aparecem no nosso código. Mesmo com poucas linhas, o <code class="language-plaintext highlighter-rouge">todo_live.ex</code> já demonstra vários conceitos centrais: módulos, funções, imutabilidade, pattern matching e o pipe.</p> <p>Este é o estado atual do nosso arquivo (confira se o seu está igual):</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">ElixirTodoListWeb</span><span class="o">.</span><span class="no">TodoLive</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">ElixirTodoListWeb</span><span class="p">,</span> <span class="ss">:live_view</span>

  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">mount</span><span class="p">(</span><span class="n">_params</span><span class="p">,</span> <span class="n">_session</span><span class="p">,</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="n">tasks</span> <span class="o">=</span> <span class="p">[</span>
      <span class="p">%{</span><span class="ss">id:</span> <span class="mi">1</span><span class="p">,</span> <span class="ss">title:</span> <span class="s2">"Comprar leite"</span><span class="p">,</span> <span class="ss">completed:</span> <span class="no">false</span><span class="p">},</span>
      <span class="p">%{</span><span class="ss">id:</span> <span class="mi">2</span><span class="p">,</span> <span class="ss">title:</span> <span class="s2">"Aprender LiveView"</span><span class="p">,</span> <span class="ss">completed:</span> <span class="no">true</span><span class="p">}</span>
    <span class="p">]</span>

    <span class="n">socket</span> <span class="o">=</span>
      <span class="n">assign</span><span class="p">(</span><span class="n">socket</span><span class="p">,</span>
        <span class="ss">tasks:</span> <span class="n">tasks</span><span class="p">,</span>
        <span class="ss">new_task_title:</span> <span class="s2">""</span>
      <span class="p">)</span>

    <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
  <span class="k">end</span>

  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="s2">"update_form"</span><span class="p">,</span> <span class="p">%{</span><span class="s2">"title"</span> <span class="o">=&gt;</span> <span class="n">new_title</span><span class="p">},</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="n">socket</span> <span class="o">=</span> <span class="n">assign</span><span class="p">(</span><span class="n">socket</span><span class="p">,</span> <span class="ss">new_task_title:</span> <span class="n">new_title</span><span class="p">)</span>
    <span class="p">{</span><span class="ss">:noreply</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
  <span class="k">end</span>

  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="s2">"save_task"</span><span class="p">,</span> <span class="p">%{</span><span class="s2">"title"</span> <span class="o">=&gt;</span> <span class="n">title</span><span class="p">},</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="k">if</span> <span class="no">String</span><span class="o">.</span><span class="n">trim</span><span class="p">(</span><span class="n">title</span><span class="p">)</span> <span class="o">!=</span> <span class="s2">""</span> <span class="k">do</span>
      <span class="n">new_task</span> <span class="o">=</span> <span class="p">%{</span>
        <span class="ss">id:</span> <span class="no">System</span><span class="o">.</span><span class="n">unique_integer</span><span class="p">([</span><span class="ss">:positive</span><span class="p">]),</span>
        <span class="ss">title:</span> <span class="n">title</span><span class="p">,</span>
        <span class="ss">completed:</span> <span class="no">false</span>
      <span class="p">}</span>

      <span class="n">socket</span> <span class="o">=</span>
        <span class="n">socket</span>
        <span class="o">|&gt;</span> <span class="n">update</span><span class="p">(</span><span class="ss">:tasks</span><span class="p">,</span> <span class="k">fn</span> <span class="n">tasks</span> <span class="o">-&gt;</span> <span class="n">tasks</span> <span class="o">++</span> <span class="p">[</span><span class="n">new_task</span><span class="p">]</span> <span class="k">end</span><span class="p">)</span>
        <span class="o">|&gt;</span> <span class="n">assign</span><span class="p">(</span><span class="ss">:new_task_title</span><span class="p">,</span> <span class="s2">""</span><span class="p">)</span>

      <span class="p">{</span><span class="ss">:noreply</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
    <span class="k">else</span>
      <span class="p">{</span><span class="ss">:noreply</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
    <span class="k">end</span>
  <span class="k">end</span>

  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">render</span><span class="p">(</span><span class="n">assigns</span><span class="p">)</span> <span class="k">do</span>
    <span class="sx">~H""</span><span class="s2">"
    ... (o HTML completo do Passo 2.2) ...
    """</span>
  <span class="k">end</span>
<span class="k">end</span>
</code></pre></div></div> <h3 id="1️⃣-módulos--a-caixa-do-código">1️⃣ Módulos — a “caixa” do código</h3> <p>Em Elixir, tudo vive dentro de um <strong>módulo</strong> (<code class="language-plaintext highlighter-rouge">defmodule</code>). Um módulo é como uma caixa que organiza funções relacionadas — parecido com uma <em>classe</em>, mas com uma diferença fundamental: 👉 <strong>módulos não possuem estado interno nem herança</strong>. Eles apenas <strong>agrupam funções</strong>. O estado vive em estruturas como o <strong>socket</strong> ou em <strong>processos</strong> — nunca no módulo em si.</p> <h3 id="2️⃣-funções-e-aridade">2️⃣ Funções e Aridade</h3> <p>Funções são definidas com <code class="language-plaintext highlighter-rouge">def</code> (públicas) ou <code class="language-plaintext highlighter-rouge">defp</code> (privadas). O número após a barra é a <strong>aridade</strong> — quantos parâmetros a função recebe:</p> <ul> <li><code class="language-plaintext highlighter-rouge">mount/3</code> → 3 parâmetros;</li> <li><code class="language-plaintext highlighter-rouge">render/1</code> → 1 parâmetro;</li> <li><code class="language-plaintext highlighter-rouge">handle_event/3</code> → 3 parâmetros.</li> </ul> <p>Em Elixir, a aridade <strong>faz parte do nome da função</strong>: <code class="language-plaintext highlighter-rouge">handle_event/2</code> e <code class="language-plaintext highlighter-rouge">handle_event/3</code> são funções <em>diferentes</em>, e não sobrecargas como em Java ou C#.</p> <h3 id="3️⃣-pattern-matching--a-escolha-automática">3️⃣ Pattern Matching — a escolha automática</h3> <p>Observe:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="s2">"update_form"</span><span class="p">,</span> <span class="p">%{</span><span class="s2">"title"</span> <span class="o">=&gt;</span> <span class="n">new_title</span><span class="p">},</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span> <span class="o">...</span> <span class="k">end</span>
<span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="s2">"save_task"</span><span class="p">,</span>   <span class="p">%{</span><span class="s2">"title"</span> <span class="o">=&gt;</span> <span class="n">title</span><span class="p">},</span>     <span class="n">socket</span><span class="p">)</span> <span class="k">do</span> <span class="o">...</span> <span class="k">end</span>
</code></pre></div></div> <p>Temos <strong>duas funções com o mesmo nome e aridade</strong>, e o Elixir <strong>decide qual chamar</strong> pelo <strong>padrão dos argumentos</strong>: se o primeiro argumento for <code class="language-plaintext highlighter-rouge">"update_form"</code>, executa a primeira; se for <code class="language-plaintext highlighter-rouge">"save_task"</code>, a segunda. Isso é o <strong>pattern matching</strong> — um dos pilares do paradigma funcional.</p> <p>Em Python, escreveríamos:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">handle_event</span><span class="p">(</span><span class="n">event_name</span><span class="p">,</span> <span class="n">payload</span><span class="p">,</span> <span class="n">socket</span><span class="p">):</span>
    <span class="k">if</span> <span class="n">event_name</span> <span class="o">==</span> <span class="sh">"</span><span class="s">update_form</span><span class="sh">"</span><span class="p">:</span>
        <span class="bp">...</span>
    <span class="k">elif</span> <span class="n">event_name</span> <span class="o">==</span> <span class="sh">"</span><span class="s">save_task</span><span class="sh">"</span><span class="p">:</span>
        <span class="bp">...</span>
</code></pre></div></div> <p>Em Elixir, <strong>a própria definição da função já é o “if”</strong>.</p> <h3 id="4️⃣-tuplas--a-maneira-elixir-de-retornar">4️⃣ Tuplas — a maneira Elixir de retornar</h3> <p>Em vez de retornar valores “crus”, o Elixir usa <strong>tuplas etiquetadas</strong>:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
<span class="p">{</span><span class="ss">:noreply</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
<span class="p">{</span><span class="ss">:error</span><span class="p">,</span> <span class="s2">"algo deu errado"</span><span class="p">}</span>
</code></pre></div></div> <p>A primeira posição é sempre um <strong>átomo</strong> (<code class="language-plaintext highlighter-rouge">:ok</code>, <code class="language-plaintext highlighter-rouge">:noreply</code>, <code class="language-plaintext highlighter-rouge">:error</code>) descrevendo o status. Essa convenção vem do Erlang e facilita o controle de fluxo entre processos.</p> <h3 id="5️⃣-o-socket--nosso-estado-vivo">5️⃣ O Socket — nosso “estado vivo”</h3> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">socket</span> <span class="o">=</span> <span class="n">assign</span><span class="p">(</span><span class="n">socket</span><span class="p">,</span> <span class="ss">tasks:</span> <span class="n">tasks</span><span class="p">,</span> <span class="ss">new_task_title:</span> <span class="s2">""</span><span class="p">)</span>
</code></pre></div></div> <ul> <li>O <code class="language-plaintext highlighter-rouge">socket</code> é a “caixa de estado” daquela sessão;</li> <li><code class="language-plaintext highlighter-rouge">assign</code> <strong>não muta</strong> a caixa: cria uma <strong>nova versão</strong> dela (imutabilidade!);</li> <li>Cada atualização do socket dispara uma <strong>nova renderização</strong> automática.</li> </ul> <h3 id="6️⃣-o-operador-pipe---elegância-funcional">6️⃣ O Operador Pipe (<code class="language-plaintext highlighter-rouge">|&gt;</code>) — elegância funcional</h3> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">socket</span>
<span class="o">|&gt;</span> <span class="n">update</span><span class="p">(</span><span class="ss">:tasks</span><span class="p">,</span> <span class="k">fn</span> <span class="n">tasks</span> <span class="o">-&gt;</span> <span class="n">tasks</span> <span class="o">++</span> <span class="p">[</span><span class="n">new_task</span><span class="p">]</span> <span class="k">end</span><span class="p">)</span>
<span class="o">|&gt;</span> <span class="n">assign</span><span class="p">(</span><span class="ss">:new_task_title</span><span class="p">,</span> <span class="s2">""</span><span class="p">)</span>
</code></pre></div></div> <p>Lê-se: <em>“pegue o socket, atualize as tarefas e depois limpe o campo de texto”</em>. O pipe passa o resultado de cada linha como <strong>primeiro argumento</strong> da próxima, eliminando variáveis intermediárias — como uma receita passo a passo.</p> <h3 id="-recapitulando">📘 Recapitulando</h3> <table> <thead> <tr> <th>Conceito</th> <th>Significado</th> <th>Analogia</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">defmodule</code></td> <td>Agrupa funções relacionadas</td> <td>Classe sem estado</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">def</code> / <code class="language-plaintext highlighter-rouge">defp</code></td> <td>Funções públicas / privadas</td> <td>Métodos</td> </tr> <tr> <td>Aridade</td> <td>Nº de parâmetros (parte do nome)</td> <td>—</td> </tr> <tr> <td>Pattern Matching</td> <td>Escolhe a função pelo padrão dos argumentos</td> <td>Substitui <code class="language-plaintext highlighter-rouge">if</code>/<code class="language-plaintext highlighter-rouge">switch</code></td> </tr> <tr> <td>Tuplas <code class="language-plaintext highlighter-rouge">{:ok, ...}</code></td> <td>Retornos estruturados</td> <td><code class="language-plaintext highlighter-rouge">return (status, valor)</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">\|&gt;</code> (pipe)</td> <td>Encadeia transformações</td> <td>Fluent interface</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">socket</code></td> <td>Estado imutável da interface</td> <td>Store/State no React</td> </tr> </tbody> </table> <hr/> <p><strong>Fim da Fase 2!</strong> 🏁</p> <p>Agora estamos prontos para conectar nossa lógica em memória a um <strong>banco de dados real com Ecto</strong>.</p> <hr/> <h1 id="-fase-3-persistência--a-camada-de-dados-ecto-repo-migration-e-schema">🧱 Fase 3: Persistência — A Camada de Dados (Ecto, Repo, Migration e Schema)</h1> <p>Na fase anterior, nossa aplicação mantinha as tarefas apenas <strong>em memória</strong>. Agora daremos um passo importante: <strong>armazenar os dados de forma permanente</strong> usando o <strong>Ecto</strong>, a biblioteca de persistência do Elixir.</p> <p>Optamos por <strong>não incluir o Ecto desde o início</strong> (<code class="language-plaintext highlighter-rouge">--no-ecto</code>) para que você entendesse primeiro a lógica interna do LiveView. Agora veremos <strong>como adicionar novas dependências</strong> a um projeto existente e configurar o banco de dados real.</p> <p>Nesta fase, montamos <strong>apenas a camada de dados</strong> (dependências, Repo, migration e schema) e a testamos isoladamente. O “transplante” — trocar o coração em memória do <code class="language-plaintext highlighter-rouge">TodoLive</code> pelo banco — fica para a <strong>Fase 4</strong>.</p> <h3 id="️-passo-31-adicionar-as-dependências-do-ecto">⚙️ Passo 3.1: Adicionar as Dependências do Ecto</h3> <p>Pare o servidor (<code class="language-plaintext highlighter-rouge">Ctrl+C</code> duas vezes) e abra o arquivo <code class="language-plaintext highlighter-rouge">mix.exs</code>, o equivalente ao <code class="language-plaintext highlighter-rouge">package.json</code> (JavaScript) ou <code class="language-plaintext highlighter-rouge">requirements.txt</code> (Python). É nele que declaramos as <strong>dependências do projeto</strong>, dentro da função <code class="language-plaintext highlighter-rouge">defp deps do</code>.</p> <aside> <p>⚠️ <strong>NÃO substitua a lista de dependências!</strong> O <code class="language-plaintext highlighter-rouge">phx.new</code> já gerou uma lista grande (phoenix, phoenix_html, live_view, tailwind, esbuild, bandit e mais uma dúzia) — <strong>todas são necessárias</strong>. Nossa tarefa é apenas <strong>acrescentar quatro linhas</strong> a essa lista. Substituir a lista inteira por uma menor quebraria o projeto por completo.</p> </aside> <p><strong>Ação:</strong> localize <code class="language-plaintext highlighter-rouge">defp deps do</code> no <code class="language-plaintext highlighter-rouge">mix.exs</code> e <strong>adicione</strong> estas quatro linhas em qualquer ponto da lista (por exemplo, logo após a linha do <code class="language-plaintext highlighter-rouge">{:phoenix_live_view, ...}</code>), mantendo tudo o que já existe:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="k">defp</span> <span class="n">deps</span> <span class="k">do</span>
    <span class="p">[</span>
      <span class="p">{</span><span class="ss">:phoenix</span><span class="p">,</span> <span class="s2">"~&gt; 1.8.1"</span><span class="p">},</span>
      <span class="c1"># ... (todas as dependências que o phx.new gerou — NÃO apague nada!) ...</span>

      <span class="c1"># --- ADICIONE ESTAS 4 LINHAS (suporte ao Ecto) ---</span>
      <span class="p">{</span><span class="ss">:ecto</span><span class="p">,</span> <span class="s2">"~&gt; 3.11"</span><span class="p">},</span>
      <span class="p">{</span><span class="ss">:phoenix_ecto</span><span class="p">,</span> <span class="s2">"~&gt; 4.4"</span><span class="p">},</span>
      <span class="p">{</span><span class="ss">:ecto_sql</span><span class="p">,</span> <span class="s2">"~&gt; 3.10"</span><span class="p">},</span>
      <span class="p">{</span><span class="ss">:ecto_sqlite3</span><span class="p">,</span> <span class="s2">"~&gt; 0.12"</span><span class="p">},</span> <span class="c1"># SQLite: banco leve, baseado em arquivo</span>
      <span class="c1"># --------------------------------------------------</span>

      <span class="c1"># ... (o restante das dependências geradas) ...</span>
    <span class="p">]</span>
  <span class="k">end</span>
</code></pre></div></div> <p>O que cada uma faz:</p> <table> <thead> <tr> <th>Dependência</th> <th>Papel</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">ecto</code></td> <td>O núcleo: schemas, changesets e queries.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ecto_sql</code></td> <td>A camada SQL do Ecto (migrations, adaptadores).</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ecto_sqlite3</code></td> <td>O “driver” específico do SQLite.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">phoenix_ecto</code></td> <td>A cola entre o Phoenix e o Ecto (ex: integração de changesets com formulários).</td> </tr> </tbody> </table> <p>Em seguida, baixe as dependências:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mix deps.get
</code></pre></div></div> <h3 id="-passo-32-configurar-o-repo--o-agente-do-banco-de-dados">🧩 Passo 3.2: Configurar o “Repo” — o Agente do Banco de Dados</h3> <p>O <strong>Repo</strong> (de <em>repository</em>) é o módulo que representa a <strong>conexão com o banco</strong>. É como o <code class="language-plaintext highlighter-rouge">settings.py</code> do Django ou o <code class="language-plaintext highlighter-rouge">database.yml</code> do Rails: ele sabe <strong>onde está o banco e como acessá-lo</strong>.</p> <p><strong>Ação 1:</strong> crie o arquivo <code class="language-plaintext highlighter-rouge">lib/elixir_todo_list/repo.ex</code>:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">ElixirTodoList</span><span class="o">.</span><span class="no">Repo</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">Ecto</span><span class="o">.</span><span class="no">Repo</span><span class="p">,</span>
    <span class="ss">otp_app:</span> <span class="ss">:elixir_todo_list</span><span class="p">,</span>
    <span class="ss">adapter:</span> <span class="no">Ecto</span><span class="o">.</span><span class="no">Adapters</span><span class="o">.</span><span class="no">SQLite3</span>
<span class="k">end</span>
</code></pre></div></div> <p><strong>Ação 2:</strong> agora precisamos <strong>supervisionar</strong> o Repo — garantir que ele seja iniciado (e reiniciado, se cair) junto com a aplicação. Abra <code class="language-plaintext highlighter-rouge">lib/elixir_todo_list/application.ex</code> e adicione o Repo <strong>no início</strong> da lista de processos supervisionados:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code>    <span class="n">children</span> <span class="o">=</span> <span class="p">[</span>
      <span class="no">ElixirTodoList</span><span class="o">.</span><span class="no">Repo</span><span class="p">,</span> <span class="c1"># 👈 ADICIONE ESTA LINHA (antes dos demais)</span>
      <span class="no">ElixirTodoListWeb</span><span class="o">.</span><span class="no">Telemetry</span><span class="p">,</span>
      <span class="p">{</span><span class="no">DNSCluster</span><span class="p">,</span> <span class="ss">query:</span> <span class="no">Application</span><span class="o">.</span><span class="n">get_env</span><span class="p">(</span><span class="ss">:elixir_todo_list</span><span class="p">,</span> <span class="ss">:dns_cluster_query</span><span class="p">)</span> <span class="o">||</span> <span class="ss">:ignore</span><span class="p">},</span>
      <span class="p">{</span><span class="no">Phoenix</span><span class="o">.</span><span class="no">PubSub</span><span class="p">,</span> <span class="ss">name:</span> <span class="no">ElixirTodoList</span><span class="o">.</span><span class="no">PubSub</span><span class="p">},</span>
      <span class="c1"># ...</span>
      <span class="no">ElixirTodoListWeb</span><span class="o">.</span><span class="no">Endpoint</span>
    <span class="p">]</span>
</code></pre></div></div> <aside> <p>💡 Essa lista <code class="language-plaintext highlighter-rouge">children</code> é a <strong>árvore de supervisão</strong> — um dos superpoderes do Elixir. Cada item é um processo que a aplicação inicia e vigia. Se o Repo travar, o supervisor o reinicia automaticamente. É o mesmo mecanismo que “ressuscitou” nosso LiveView na Fase 2!</p> </aside> <p><strong>Ação 3:</strong> por fim, configure o Repo no arquivo <code class="language-plaintext highlighter-rouge">config/config.exs</code>. Adicione (por exemplo, logo após a linha <code class="language-plaintext highlighter-rouge">import Config</code> no topo):</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">config</span> <span class="ss">:elixir_todo_list</span><span class="p">,</span> <span class="no">ElixirTodoList</span><span class="o">.</span><span class="no">Repo</span><span class="p">,</span>
  <span class="ss">database:</span> <span class="s2">"elixir_todo_list.db"</span><span class="p">,</span>
  <span class="ss">priv:</span> <span class="s2">"priv/repo"</span>

<span class="n">config</span> <span class="ss">:elixir_todo_list</span><span class="p">,</span> <span class="ss">ecto_repos:</span> <span class="p">[</span><span class="no">ElixirTodoList</span><span class="o">.</span><span class="no">Repo</span><span class="p">]</span>
</code></pre></div></div> <p>Entendendo as opções:</p> <ul> <li><code class="language-plaintext highlighter-rouge">database:</code> → o <strong>arquivo</strong> onde o SQLite salvará tudo. Com esse valor, ele será criado na <strong>raiz do projeto</strong> (<code class="language-plaintext highlighter-rouge">elixir_todo_list.db</code>).</li> <li><code class="language-plaintext highlighter-rouge">priv:</code> → onde ficam os arquivos de apoio do Repo — em especial, as <strong>migrations</strong> (<code class="language-plaintext highlighter-rouge">priv/repo/migrations/</code>). <em>Atenção: isso não muda o local do banco!</em></li> <li><code class="language-plaintext highlighter-rouge">ecto_repos:</code> → informa às tarefas do Mix (<code class="language-plaintext highlighter-rouge">mix ecto.create</code>, <code class="language-plaintext highlighter-rouge">mix ecto.migrate</code>) quais Repos existem.</li> </ul> <aside> <p>💡 Lembra do <code class="language-plaintext highlighter-rouge">.gitignore</code> da Fase 0? As regras <code class="language-plaintext highlighter-rouge">*.db</code>, <code class="language-plaintext highlighter-rouge">*.db-shm</code> e <code class="language-plaintext highlighter-rouge">*.db-wal</code> foram escritas exatamente para este momento: o banco (e seus arquivos auxiliares de escrita) vão aparecer na raiz do projeto e <strong>não devem</strong> ser versionados.</p> </aside> <h3 id="️-passo-33-criar-o-banco-de-dados">🏗️ Passo 3.3: Criar o Banco de Dados</h3> <p>O banco ainda não existe. Crie-o com:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mix ecto.create
</code></pre></div></div> <p><strong>Resultado Esperado:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>The database for ElixirTodoList.Repo has been created
</code></pre></div></div> <p>Olhe a raiz do projeto: o arquivo <code class="language-plaintext highlighter-rouge">elixir_todo_list.db</code> apareceu. (E rode <code class="language-plaintext highlighter-rouge">git status</code> para confirmar que ele <strong>não</strong> aparece para o Git — obrigado, <code class="language-plaintext highlighter-rouge">.gitignore</code>!)</p> <h3 id="-passo-34-migrations--a-planta-baixa-do-banco">🧬 Passo 3.4: Migrations — a “Planta Baixa” do Banco</h3> <p>Em praticamente todos os frameworks modernos (Django, Rails, Laravel), usamos <strong>migrations</strong> para versionar e aplicar mudanças no banco. Cada migration é um pequeno “passo evolutivo”: criar uma tabela, adicionar uma coluna, etc.</p> <p><strong>Ação 1:</strong> gere a migration da nossa tabela de tarefas:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mix ecto.gen.migration create_tasks_table
</code></pre></div></div> <p>Isso cria um arquivo dentro de <code class="language-plaintext highlighter-rouge">priv/repo/migrations/</code> (o nome começa com um <em>timestamp</em> — é assim que o Ecto sabe a ordem de aplicação).</p> <p><strong>Ação 2:</strong> abra o arquivo gerado e complete a função <code class="language-plaintext highlighter-rouge">change</code>:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">ElixirTodoList</span><span class="o">.</span><span class="no">Repo</span><span class="o">.</span><span class="no">Migrations</span><span class="o">.</span><span class="no">CreateTasksTable</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">Ecto</span><span class="o">.</span><span class="no">Migration</span>

  <span class="k">def</span> <span class="n">change</span> <span class="k">do</span>
    <span class="n">create</span> <span class="n">table</span><span class="p">(</span><span class="ss">:tasks</span><span class="p">)</span> <span class="k">do</span>
      <span class="n">add</span> <span class="ss">:title</span><span class="p">,</span> <span class="ss">:string</span>
      <span class="n">add</span> <span class="ss">:completed</span><span class="p">,</span> <span class="ss">:boolean</span><span class="p">,</span> <span class="ss">default:</span> <span class="no">false</span>

      <span class="n">timestamps</span><span class="p">()</span> <span class="c1"># Adiciona as colunas inserted_at e updated_at</span>
    <span class="k">end</span>
  <span class="k">end</span>
<span class="k">end</span>
</code></pre></div></div> <p><strong>Ação 3:</strong> aplique a migration:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mix ecto.migrate
</code></pre></div></div> <p><strong>Saída esperada:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[info] == Running ... ElixirTodoList.Repo.Migrations.CreateTasksTable.change/0 forward
[info] create table tasks
[info] == Migrated ... in 0.0s
</code></pre></div></div> <h3 id="-passo-35-conceitos-chave--schema-changeset-e-form">📘 Passo 3.5: Conceitos-Chave — Schema, Changeset e Form</h3> <p>Para conectar o código à tabela recém-criada, precisamos de três peças:</p> <table> <thead> <tr> <th>Conceito</th> <th>O que é</th> <th>Analogia</th> </tr> </thead> <tbody> <tr> <td><strong>Schema</strong></td> <td>Define a estrutura da tabela e cria uma struct <code class="language-plaintext highlighter-rouge">%Task{}</code> correspondente.</td> <td>O “model” do Django ou o ActiveRecord do Rails.</td> </tr> <tr> <td><strong>Changeset</strong></td> <td>Um conjunto de regras para validar e transformar dados antes de salvar.</td> <td>O <code class="language-plaintext highlighter-rouge">ModelForm</code> do Django.</td> </tr> <tr> <td><strong><code class="language-plaintext highlighter-rouge">to_form/1</code></strong></td> <td>Converte o changeset para o formato que o LiveView usa no <code class="language-plaintext highlighter-rouge">&lt;.form&gt;</code>.</td> <td>Um <em>form object</em> no MVC.</td> </tr> </tbody> </table> <h3 id="-passo-36-criar-o-schema-task">🧩 Passo 3.6: Criar o Schema <code class="language-plaintext highlighter-rouge">Task</code></h3> <p>Crie o arquivo <code class="language-plaintext highlighter-rouge">lib/elixir_todo_list/task.ex</code>:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">ElixirTodoList</span><span class="o">.</span><span class="no">Task</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">Ecto</span><span class="o">.</span><span class="no">Schema</span>
  <span class="kn">import</span> <span class="no">Ecto</span><span class="o">.</span><span class="no">Changeset</span>

  <span class="c1"># "schema" espelha a tabela "tasks" no banco</span>
  <span class="n">schema</span> <span class="s2">"tasks"</span> <span class="k">do</span>
    <span class="n">field</span> <span class="ss">:title</span><span class="p">,</span> <span class="ss">:string</span>
    <span class="n">field</span> <span class="ss">:completed</span><span class="p">,</span> <span class="ss">:boolean</span><span class="p">,</span> <span class="ss">default:</span> <span class="no">false</span>
    <span class="n">timestamps</span><span class="p">(</span><span class="ss">type:</span> <span class="ss">:utc_datetime</span><span class="p">)</span>
  <span class="k">end</span>

  <span class="c1"># Define como validar os dados antes de salvar</span>
  <span class="k">def</span> <span class="n">changeset</span><span class="p">(</span><span class="n">task_struct</span><span class="p">,</span> <span class="n">attrs</span><span class="p">)</span> <span class="k">do</span>
    <span class="n">task_struct</span>
    <span class="o">|&gt;</span> <span class="n">cast</span><span class="p">(</span><span class="n">attrs</span><span class="p">,</span> <span class="p">[</span><span class="ss">:title</span><span class="p">,</span> <span class="ss">:completed</span><span class="p">])</span>
    <span class="o">|&gt;</span> <span class="n">validate_required</span><span class="p">([</span><span class="ss">:title</span><span class="p">])</span>
  <span class="k">end</span>
<span class="k">end</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">cast/3</code> → filtra os dados de entrada, aceitando <strong>apenas</strong> os campos listados (<code class="language-plaintext highlighter-rouge">:title</code>, <code class="language-plaintext highlighter-rouge">:completed</code>) — proteção contra dados indesejados;</li> <li><code class="language-plaintext highlighter-rouge">validate_required/2</code> → garante que <code class="language-plaintext highlighter-rouge">:title</code> não está vazio. É esta linha que fará o formulário exibir <em>“can’t be blank”</em> na Fase 4.</li> </ul> <h3 id="-passo-37-testando-a-camada-de-dados-no-iex-sem-o-liveview">🧪 Passo 3.7: Testando a Camada de Dados no IEx (sem o LiveView!)</h3> <p>Assim como fizemos no REPL do Clojure, vamos <strong>provar</strong> que a camada de dados funciona <strong>antes</strong> de conectá-la à interface. A versão Elixir do REPL é o <strong>IEx</strong> (Interactive Elixir), e podemos iniciá-lo <strong>com o projeto carregado</strong>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>iex <span class="nt">-S</span> mix
</code></pre></div></div> <p>No prompt <code class="language-plaintext highlighter-rouge">iex(1)&gt;</code>, experimente:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Atalhos para digitar menos</span>
<span class="n">iex</span><span class="o">&gt;</span> <span class="n">alias</span> <span class="no">ElixirTodoList</span><span class="o">.</span><span class="p">{</span><span class="no">Repo</span><span class="p">,</span> <span class="no">Task</span><span class="p">}</span>

<span class="c1"># 1. O banco está vazio?</span>
<span class="n">iex</span><span class="o">&gt;</span> <span class="no">Repo</span><span class="o">.</span><span class="n">all</span><span class="p">(</span><span class="no">Task</span><span class="p">)</span>
<span class="p">[]</span>

<span class="c1"># 2. Crie uma tarefa (validando com o changeset)</span>
<span class="n">iex</span><span class="o">&gt;</span> <span class="p">%</span><span class="no">Task</span><span class="p">{}</span> <span class="o">|&gt;</span> <span class="no">Task</span><span class="o">.</span><span class="n">changeset</span><span class="p">(%{</span><span class="ss">title:</span> <span class="s2">"Testar o IEx"</span><span class="p">})</span> <span class="o">|&gt;</span> <span class="no">Repo</span><span class="o">.</span><span class="n">insert</span><span class="p">()</span>
<span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="p">%</span><span class="no">ElixirTodoList</span><span class="o">.</span><span class="no">Task</span><span class="p">{</span><span class="ss">id:</span> <span class="mi">1</span><span class="p">,</span> <span class="ss">title:</span> <span class="s2">"Testar o IEx"</span><span class="p">,</span> <span class="ss">completed:</span> <span class="no">false</span><span class="p">,</span> <span class="o">...</span><span class="p">}}</span>

<span class="c1"># 3. E uma inválida? (sem título)</span>
<span class="n">iex</span><span class="o">&gt;</span> <span class="p">%</span><span class="no">Task</span><span class="p">{}</span> <span class="o">|&gt;</span> <span class="no">Task</span><span class="o">.</span><span class="n">changeset</span><span class="p">(%{})</span> <span class="o">|&gt;</span> <span class="no">Repo</span><span class="o">.</span><span class="n">insert</span><span class="p">()</span>
<span class="p">{</span><span class="ss">:error</span><span class="p">,</span> <span class="c1">#Ecto.Changeset&lt;..., errors: [title: {"can't be blank", ...}], valid?: false&gt;}</span>

<span class="c1"># 4. Confira a lista</span>
<span class="n">iex</span><span class="o">&gt;</span> <span class="no">Repo</span><span class="o">.</span><span class="n">all</span><span class="p">(</span><span class="no">Task</span><span class="p">)</span>
<span class="p">[%</span><span class="no">ElixirTodoList</span><span class="o">.</span><span class="no">Task</span><span class="p">{</span><span class="ss">id:</span> <span class="mi">1</span><span class="p">,</span> <span class="ss">title:</span> <span class="s2">"Testar o IEx"</span><span class="p">,</span> <span class="o">...</span><span class="p">}]</span>
</code></pre></div></div> <p><strong>Momento “Aha!”:</strong> repare nas tuplas <code class="language-plaintext highlighter-rouge">{:ok, ...}</code> e <code class="language-plaintext highlighter-rouge">{:error, changeset}</code> — são exatamente os dois casos que trataremos com <code class="language-plaintext highlighter-rouge">case</code> no LiveView, na próxima fase. E a tarefa inválida <strong>não foi salva</strong>: o changeset barrou antes de chegar ao banco.</p> <p>Saia do IEx com <code class="language-plaintext highlighter-rouge">Ctrl+C</code> duas vezes.</p> <h3 id="-passo-38-commit-camada-de-dados">💾 Passo 3.8: Commit (Camada de Dados)</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Fase 3: Persistência - Configura Ecto, Repo, Migrations e Task Schema"</span>
</code></pre></div></div> <hr/> <p><strong>Fim da Fase 3!</strong> 🏁</p> <p>Agora temos a estrutura completa:</p> <ul> <li><strong>O banco de dados</strong> criado e versionado por migrations;</li> <li><strong>O <code class="language-plaintext highlighter-rouge">Repo</code></strong> gerenciando conexões e consultas (supervisionado!);</li> <li><strong>O schema <code class="language-plaintext highlighter-rouge">Task</code></strong> refletindo nossa tabela, com validação via changeset — tudo testado no IEx.</li> </ul> <p>Falta o passo mais gratificante: trocar o “coração” em memória do <code class="language-plaintext highlighter-rouge">TodoLive</code> por esse novo, persistente.</p> <hr/> <h1 id="-fase-4-o-transplante--conectando-o-liveview-ao-banco">🫀 Fase 4: O “Transplante” — Conectando o LiveView ao Banco</h1> <p>Já temos o banco criado, o schema configurado e o <code class="language-plaintext highlighter-rouge">Repo</code> operacional (Fase 3). Chegou o momento de <strong>conectar tudo ao nosso LiveView</strong>.</p> <p>Chamamos esta etapa de “transplante” porque substituímos o coração antigo (estado em memória) por um novo (persistência real) — <strong>sem mudar o corpo</strong> (a estrutura mount/eventos/render permanece a mesma).</p> <h3 id="-visão-geral-o-que-vai-mudar">🔍 Visão Geral: o que vai mudar?</h3> <p><strong>Antes</strong>, nossa aplicação:</p> <ul> <li>Mantinha as tarefas apenas no <strong>socket</strong> (memória);</li> <li>Cada F5 apagava tudo;</li> <li>O formulário era um <code class="language-plaintext highlighter-rouge">&lt;form&gt;</code> “cru”, com o estado do campo gerenciado manualmente (<code class="language-plaintext highlighter-rouge">update_form</code>).</li> </ul> <p><strong>Agora</strong>:</p> <ul> <li>O <strong><code class="language-plaintext highlighter-rouge">Repo</code></strong> vira a ponte entre o LiveView e o banco;</li> <li>O <strong>schema <code class="language-plaintext highlighter-rouge">Task</code></strong> substitui os mapas manuais;</li> <li>O <strong>changeset + <code class="language-plaintext highlighter-rouge">to_form/1</code></strong> cuidam da validação e da integração com o formulário;</li> <li>E as mensagens de sucesso aparecerão via <strong>flash</strong>.</li> </ul> <h3 id="1️⃣-passo-41-o-setup-do-módulo-aliases">1️⃣ Passo 4.1: O Setup do Módulo (aliases)</h3> <p>Abra <code class="language-plaintext highlighter-rouge">lib/elixir_todo_list_web/live/todo_live.ex</code> e ajuste o <strong>topo</strong> do módulo:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">ElixirTodoListWeb</span><span class="o">.</span><span class="no">TodoLive</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">ElixirTodoListWeb</span><span class="p">,</span> <span class="ss">:live_view</span>

  <span class="c1"># Atalhos (aliases) para não digitar o nome completo toda hora</span>
  <span class="n">alias</span> <span class="no">ElixirTodoList</span><span class="o">.</span><span class="no">Repo</span>
  <span class="n">alias</span> <span class="no">ElixirTodoList</span><span class="o">.</span><span class="no">Task</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">alias</code> permite escrever <code class="language-plaintext highlighter-rouge">Repo.all(...)</code> em vez de <code class="language-plaintext highlighter-rouge">ElixirTodoList.Repo.all(...)</code>.</li> </ul> <aside> <p>💡 <strong>E os componentes de UI (<code class="language-plaintext highlighter-rouge">&lt;.form&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;.input&gt;</code>, <code class="language-plaintext highlighter-rouge">&lt;.button&gt;</code>)?</strong> Você <strong>não</strong> precisa importar nada: o <code class="language-plaintext highlighter-rouge">use ElixirTodoListWeb, :live_view</code> (lá do topo) já importa o <code class="language-plaintext highlighter-rouge">CoreComponents</code> e disponibiliza o alias <code class="language-plaintext highlighter-rouge">Layouts</code> para todos os LiveViews. É uma das “injeções” que a Pausa Didática da Fase 1 explicou.</p> </aside> <h3 id="2️⃣-passo-42-o-construtor--carregamento-inicial-mount3">2️⃣ Passo 4.2: O “Construtor” — Carregamento Inicial (<code class="language-plaintext highlighter-rouge">mount/3</code>)</h3> <p>Substitua o <code class="language-plaintext highlighter-rouge">mount/3</code> por:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">mount</span><span class="p">(</span><span class="n">_params</span><span class="p">,</span> <span class="n">_session</span><span class="p">,</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="n">tasks</span> <span class="o">=</span> <span class="no">Repo</span><span class="o">.</span><span class="n">all</span><span class="p">(</span><span class="no">Task</span><span class="p">)</span>                     <span class="c1"># Lê as tarefas do banco de dados</span>
    <span class="n">changeset</span> <span class="o">=</span> <span class="no">Task</span><span class="o">.</span><span class="n">changeset</span><span class="p">(%</span><span class="no">Task</span><span class="p">{},</span> <span class="p">%{})</span>   <span class="c1"># Cria um "molde" vazio</span>
    <span class="n">form</span> <span class="o">=</span> <span class="n">to_form</span><span class="p">(</span><span class="n">changeset</span><span class="p">)</span>                  <span class="c1"># Converte para o formulário de UI</span>

    <span class="n">socket</span> <span class="o">=</span>
      <span class="n">assign</span><span class="p">(</span><span class="n">socket</span><span class="p">,</span>
        <span class="ss">tasks:</span> <span class="n">tasks</span><span class="p">,</span>
        <span class="ss">form:</span> <span class="n">form</span>
      <span class="p">)</span>

    <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
  <span class="k">end</span>
</code></pre></div></div> <p><strong>O que mudou?</strong></p> <table> <thead> <tr> <th>Antes</th> <th>Agora</th> </tr> </thead> <tbody> <tr> <td>Lista fixa de tarefas escrita no código.</td> <td>Tarefas reais do banco, via <code class="language-plaintext highlighter-rouge">Repo.all(Task)</code>.</td> </tr> <tr> <td>Um simples <code class="language-plaintext highlighter-rouge">new_task_title</code> no estado.</td> <td>Um <code class="language-plaintext highlighter-rouge">changeset</code> convertido em <code class="language-plaintext highlighter-rouge">form</code> — validável e integrado.</td> </tr> </tbody> </table> <blockquote> <p>💡 O <code class="language-plaintext highlighter-rouge">mount/3</code> é como o <code class="language-plaintext highlighter-rouge">get()</code> de uma view do Django ou o <code class="language-plaintext highlighter-rouge">index()</code> de um controller Rails: define o estado inicial da página.</p> </blockquote> <h3 id="3️⃣-passo-43-o-coração--salvando-no-banco-handle_event3">3️⃣ Passo 4.3: O “Coração” — Salvando no Banco (<code class="language-plaintext highlighter-rouge">handle_event/3</code>)</h3> <p>Agora, <strong>remova as duas funções <code class="language-plaintext highlighter-rouge">handle_event</code></strong> da Fase 2 (<code class="language-plaintext highlighter-rouge">"update_form"</code> e <code class="language-plaintext highlighter-rouge">"save_task"</code>) e coloque no lugar esta única função:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="s2">"save_task"</span><span class="p">,</span> <span class="p">%{</span><span class="s2">"task"</span> <span class="o">=&gt;</span> <span class="n">task_params</span><span class="p">},</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="c1"># 1. Cria um changeset com os dados do formulário</span>
    <span class="n">changeset</span> <span class="o">=</span> <span class="no">Task</span><span class="o">.</span><span class="n">changeset</span><span class="p">(%</span><span class="no">Task</span><span class="p">{},</span> <span class="n">task_params</span><span class="p">)</span>

    <span class="c1"># 2. Tenta inserir no banco — e trata os DOIS resultados possíveis</span>
    <span class="n">socket_atualizado</span> <span class="o">=</span>
      <span class="k">case</span> <span class="no">Repo</span><span class="o">.</span><span class="n">insert</span><span class="p">(</span><span class="n">changeset</span><span class="p">)</span> <span class="k">do</span>
        <span class="c1"># 2A. SUCESSO!</span>
        <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">_new_task</span><span class="p">}</span> <span class="o">-&gt;</span>
          <span class="n">novo_changeset_vazio</span> <span class="o">=</span> <span class="no">Task</span><span class="o">.</span><span class="n">changeset</span><span class="p">(%</span><span class="no">Task</span><span class="p">{},</span> <span class="p">%{})</span>

          <span class="n">socket</span>
          <span class="o">|&gt;</span> <span class="n">assign</span><span class="p">(</span><span class="ss">:tasks</span><span class="p">,</span> <span class="no">Repo</span><span class="o">.</span><span class="n">all</span><span class="p">(</span><span class="no">Task</span><span class="p">))</span>              <span class="c1"># Recarrega as tarefas</span>
          <span class="o">|&gt;</span> <span class="n">assign</span><span class="p">(</span><span class="ss">:form</span><span class="p">,</span> <span class="n">to_form</span><span class="p">(</span><span class="n">novo_changeset_vazio</span><span class="p">))</span> <span class="c1"># Reseta o formulário</span>
          <span class="o">|&gt;</span> <span class="n">put_flash</span><span class="p">(</span><span class="ss">:info</span><span class="p">,</span> <span class="s2">"Tarefa salva com sucesso!"</span><span class="p">)</span>

        <span class="c1"># 2B. FALHA! (validação — ex: título em branco)</span>
        <span class="p">{</span><span class="ss">:error</span><span class="p">,</span> <span class="n">failed_changeset</span><span class="p">}</span> <span class="o">-&gt;</span>
          <span class="c1"># Re-atribui o formulário *com os erros* para exibi-los</span>
          <span class="n">assign</span><span class="p">(</span><span class="n">socket</span><span class="p">,</span> <span class="ss">form:</span> <span class="n">to_form</span><span class="p">(</span><span class="n">failed_changeset</span><span class="p">))</span>
      <span class="k">end</span>

    <span class="c1"># 3. Retorna o socket atualizado</span>
    <span class="p">{</span><span class="ss">:noreply</span><span class="p">,</span> <span class="n">socket_atualizado</span><span class="p">}</span>
  <span class="k">end</span>
</code></pre></div></div> <p><strong>Mudanças-chave:</strong></p> <ul> <li>Antes, adicionávamos a tarefa direto na lista do socket → agora, <code class="language-plaintext highlighter-rouge">Repo.insert(changeset)</code> <strong>salva no banco</strong>.</li> <li>As tuplas <code class="language-plaintext highlighter-rouge">{:ok, ...}</code> / <code class="language-plaintext highlighter-rouge">{:error, ...}</code> que vimos no IEx (Fase 3) reaparecem aqui, tratadas pelo <code class="language-plaintext highlighter-rouge">case</code>.</li> <li><code class="language-plaintext highlighter-rouge">put_flash/3</code> prepara uma <strong>mensagem amigável</strong> (como o <code class="language-plaintext highlighter-rouge">messages.success()</code> do Django).</li> </ul> <blockquote> <p>💡 Repare que <strong>não existe mais</strong> o <code class="language-plaintext highlighter-rouge">handle_event("update_form", ...)</code>. Por quê? Ao trocar o <code class="language-plaintext highlighter-rouge">&lt;form&gt;</code> cru pelo componente <code class="language-plaintext highlighter-rouge">&lt;.form&gt;</code> com <code class="language-plaintext highlighter-rouge">to_form</code> (próximo passo), quem passa a controlar o valor do campo é o próprio mecanismo de formulários do Phoenix — não precisamos mais rastrear cada tecla.</p> </blockquote> <aside> <p>⚠️ <strong>Por que o <code class="language-plaintext highlighter-rouge">case</code> precisa “devolver” o socket?</strong> Note que atribuímos o resultado do <code class="language-plaintext highlighter-rouge">case</code> a <code class="language-plaintext highlighter-rouge">socket_atualizado</code> e retornamos <strong>ele</strong>. Um erro clássico é chamar <code class="language-plaintext highlighter-rouge">Repo.insert</code> e esquecer de usar o socket que saiu do <code class="language-plaintext highlighter-rouge">case</code> — aí a UI nunca reflete a mudança. Em Elixir, dados são imutáveis: o socket “novo” é um <strong>valor retornado</strong>, nunca um efeito colateral.</p> </aside> <h3 id="4️⃣-passo-44-o-desenhista--renderização-com-form-e-layoutsapp">4️⃣ Passo 4.4: O “Desenhista” — Renderização com <code class="language-plaintext highlighter-rouge">&lt;.form&gt;</code> e <code class="language-plaintext highlighter-rouge">Layouts.app</code></h3> <p>Substitua o <code class="language-plaintext highlighter-rouge">render/1</code> por:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">render</span><span class="p">(</span><span class="n">assigns</span><span class="p">)</span> <span class="k">do</span>
    <span class="sx">~H""</span><span class="s2">"
    &lt;Layouts.app flash={@flash}&gt;
      &lt;div class="</span><span class="n">w</span><span class="o">-</span><span class="n">full</span> <span class="n">max</span><span class="o">-</span><span class="n">w</span><span class="o">-</span><span class="n">lg</span> <span class="n">mx</span><span class="o">-</span><span class="n">auto</span> <span class="n">mt</span><span class="o">-</span><span class="mi">12</span> <span class="n">p</span><span class="o">-</span><span class="mi">6</span> <span class="n">bg</span><span class="o">-</span><span class="n">white</span> <span class="n">rounded</span><span class="o">-</span><span class="n">lg</span> <span class="n">shadow</span><span class="o">-</span><span class="n">md</span><span class="s2">"&gt;
        &lt;h1 class="</span><span class="n">text</span><span class="o">-</span><span class="err">3</span><span class="n">xl</span> <span class="n">font</span><span class="o">-</span><span class="n">bold</span> <span class="n">mb</span><span class="o">-</span><span class="mi">6</span> <span class="n">text</span><span class="o">-</span><span class="n">center</span> <span class="n">text</span><span class="o">-</span><span class="n">gray</span><span class="o">-</span><span class="mi">800</span><span class="s2">"&gt;
          Minha Lista de Tarefas (com DB!)
        &lt;/h1&gt;

        &lt;%!-- O formulário agora usa @form --%&gt;
        &lt;.form for={@form} id="</span><span class="n">task</span><span class="o">-</span><span class="n">form</span><span class="s2">" phx-submit="</span><span class="n">save_task</span><span class="s2">"&gt;
          &lt;.input
            field={@form[:title]}
            type="</span><span class="n">text</span><span class="s2">"
            label="</span><span class="no">Nova</span> <span class="no">Tarefa</span><span class="s2">"
            placeholder="</span><span class="no">O</span> <span class="n">que</span> <span class="n">precisa</span> <span class="n">ser</span> <span class="n">feito?</span><span class="s2">"
          /&gt;
          &lt;.button variant="</span><span class="n">primary</span><span class="s2">" phx-disable-with="</span><span class="no">Salvando</span><span class="o">...</span><span class="s2">"&gt;Adicionar Tarefa&lt;/.button&gt;
        &lt;/.form&gt;

        &lt;%!-- A LISTA DE TAREFAS --%&gt;
        &lt;div class="</span><span class="n">mt</span><span class="o">-</span><span class="mi">8</span><span class="s2">"&gt;
          &lt;ul id="</span><span class="n">task</span><span class="o">-</span><span class="n">list</span><span class="s2">"&gt;
            &lt;li :for={task &lt;- @tasks} class="</span><span class="n">flex</span> <span class="n">justify</span><span class="o">-</span><span class="n">between</span> <span class="n">items</span><span class="o">-</span><span class="n">center</span> <span class="n">p</span><span class="o">-</span><span class="mi">3</span> <span class="n">border</span><span class="o">-</span><span class="n">b</span><span class="s2">"&gt;
              &lt;span class={if task.completed, do: "</span><span class="n">line</span><span class="o">-</span><span class="n">through</span> <span class="n">text</span><span class="o">-</span><span class="n">gray</span><span class="o">-</span><span class="mi">500</span><span class="s2">", else: "</span><span class="n">text</span><span class="o">-</span><span class="n">gray</span><span class="o">-</span><span class="mi">900</span><span class="s2">"}&gt;
                {task.title}
              &lt;/span&gt;
            &lt;/li&gt;
          &lt;/ul&gt;
        &lt;/div&gt;
      &lt;/div&gt;
    &lt;/Layouts.app&gt;
    """</span>
  <span class="k">end</span>
</code></pre></div></div> <p><strong>Destaques:</strong></p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">&lt;Layouts.app flash={@flash}&gt;</code></strong> — este envelope é <strong>essencial</strong>: no Phoenix 1.8, é dentro dele que vivem o cabeçalho padrão da aplicação e o <strong><code class="language-plaintext highlighter-rouge">flash_group</code></strong> — o componente que <strong>exibe</strong> as mensagens do <code class="language-plaintext highlighter-rouge">put_flash</code>. Sem esse envelope, o <code class="language-plaintext highlighter-rouge">put_flash(:info, "Tarefa salva...")</code> do passo anterior rodaria… e a mensagem <strong>nunca apareceria na tela</strong>.</li> <li><strong><code class="language-plaintext highlighter-rouge">&lt;.form for={@form}&gt;</code></strong> — o componente de formulário integrado ao changeset. Repare que os campos agora chegam ao servidor “embrulhados”: <code class="language-plaintext highlighter-rouge">%{"task" =&gt; %{"title" =&gt; "..."}}</code> — por isso o pattern do <code class="language-plaintext highlighter-rouge">handle_event</code> mudou para <code class="language-plaintext highlighter-rouge">%{"task" =&gt; task_params}</code>.</li> <li><strong><code class="language-plaintext highlighter-rouge">&lt;.input field={@form[:title]}&gt;</code></strong> — o campo integrado: exibe o valor, o label <strong>e as mensagens de erro de validação</strong>, tudo automaticamente.</li> <li><strong><code class="language-plaintext highlighter-rouge">&lt;.button variant="primary"&gt;</code></strong> — o botão estilizado do daisyUI; <code class="language-plaintext highlighter-rouge">phx-disable-with</code> troca o texto enquanto o envio está em andamento.</li> </ul> <h3 id="-passo-45-testando-tudo">🧪 Passo 4.5: Testando Tudo</h3> <p>Suba o servidor (<code class="language-plaintext highlighter-rouge">mix phx.server</code>) e acesse <code class="language-plaintext highlighter-rouge">http://localhost:4000</code>:</p> <ol> <li><strong>Sucesso:</strong> adicione “Tarefa 1” → ela aparece na lista, o formulário limpa e a mensagem <em>“Tarefa salva com sucesso!”</em> surge no topo. ✅</li> <li><strong>Falha (validação):</strong> clique em “Adicionar Tarefa” com o campo em branco → aparece o erro <strong>“can’t be blank”</strong> junto ao campo. ✅</li> <li><strong>O teste que falhava na Fase 2:</strong> adicione “Tarefa 2” e <strong>recarregue a página (F5)</strong> → as tarefas <strong>continuam lá</strong>. ✅</li> <li><strong>A prova final:</strong> <strong>pare o servidor</strong> (<code class="language-plaintext highlighter-rouge">Ctrl+C</code> duas vezes) e suba de novo (<code class="language-plaintext highlighter-rouge">mix phx.server</code>) → F5 → <strong>tudo continua lá.</strong> Persistência real! 🎉</li> </ol> <h3 id="-passo-46-commit">💾 Passo 4.6: Commit</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Fase 4: Refatora TodoLive para usar Ecto, Repo e to_form()"</span>
</code></pre></div></div> <hr/> <p><strong>Fim da Fase 4!</strong> 🏁</p> <p>Com essa refatoração, nosso app deixou de ser um protótipo volátil e virou uma aplicação completa, com:</p> <ul> <li><strong>Banco de dados real</strong> (Ecto + SQLite);</li> <li><strong>Validação automática</strong> via changeset;</li> <li><strong>Formulário dinâmico</strong> com <code class="language-plaintext highlighter-rouge">to_form()</code> e mensagens de flash funcionando;</li> <li><strong>Interface reativa</strong> com Phoenix LiveView.</li> </ul> <h3 id="-próximos-desafios">🔮 Próximos desafios</h3> <ol> <li>Adicionar o botão de <strong>excluir</strong> (<code class="language-plaintext highlighter-rouge">phx-click="delete"</code>) → <strong>Fase 5</strong>;</li> <li>Adicionar o <strong>checkbox de conclusão</strong> (<code class="language-plaintext highlighter-rouge">phx-change="toggle_complete"</code>) → <strong>Fase 6</strong>.</li> </ol> <hr/> <h1 id="️-fase-5-refinamento--excluindo-tarefas">🗑️ Fase 5: Refinamento — Excluindo Tarefas</h1> <h3 id="-objetivo">🎯 Objetivo</h3> <p>Adicionar um <strong>botão “X”</strong> ao lado de cada tarefa para que, ao ser clicado, ela seja <strong>removida permanentemente do banco de dados e da interface</strong>, sem recarregar a página.</p> <h3 id="-o-que-você-aprenderá-aqui">💡 O que você aprenderá aqui</h3> <ul> <li>Como <strong>disparar eventos de clique</strong> com <code class="language-plaintext highlighter-rouge">phx-click</code> (sem formulários);</li> <li>Como <strong>enviar dados específicos</strong> para o servidor (o ID da tarefa) com <code class="language-plaintext highlighter-rouge">phx-value-*</code>;</li> <li>Como <strong>interagir com o banco</strong> via <code class="language-plaintext highlighter-rouge">Repo.get</code> e <code class="language-plaintext highlighter-rouge">Repo.delete</code>;</li> <li>E, de quebra, um pouco mais da linguagem de template do LiveView — o <strong>HEEx</strong>.</li> </ul> <h2 id="-passo-51-atualizando-a-interface-o-botão-x">🧱 Passo 5.1: Atualizando a Interface (o botão “X”)</h2> <p>Abra o arquivo:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lib/elixir_todo_list_web/live/todo_live.ex
</code></pre></div></div> <p>Na função <code class="language-plaintext highlighter-rouge">render/1</code>, localize o bloco <code class="language-plaintext highlighter-rouge">&lt;ul&gt;...&lt;/ul&gt;</code> que exibe as tarefas. Dentro do <code class="language-plaintext highlighter-rouge">&lt;li&gt;</code>, adicione um botão ao lado do título.</p> <p><em>Antes:</em></p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&lt;</span><span class="n">li</span> <span class="ss">:for=</span><span class="p">{</span><span class="n">task</span> <span class="o">&lt;-</span> <span class="nv">@tasks</span><span class="p">}</span> <span class="n">class</span><span class="o">=</span><span class="s2">"flex justify-between items-center p-3 border-b"</span><span class="o">&gt;</span>
  <span class="o">&lt;</span><span class="n">span</span> <span class="n">class</span><span class="o">=</span><span class="p">{</span><span class="k">if</span> <span class="n">task</span><span class="o">.</span><span class="n">completed</span><span class="p">,</span> <span class="k">do</span><span class="p">:</span> <span class="s2">"line-through text-gray-500"</span><span class="p">,</span> <span class="k">else</span><span class="p">:</span> <span class="s2">"text-gray-900"</span><span class="p">}</span><span class="o">&gt;</span>
    <span class="p">{</span><span class="n">task</span><span class="o">.</span><span class="n">title</span><span class="p">}</span>
  <span class="o">&lt;/</span><span class="n">span</span><span class="o">&gt;</span>
<span class="o">&lt;/</span><span class="n">li</span><span class="o">&gt;</span>
</code></pre></div></div> <p><em>Depois (com o botão de exclusão):</em></p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&lt;</span><span class="n">li</span> <span class="ss">:for=</span><span class="p">{</span><span class="n">task</span> <span class="o">&lt;-</span> <span class="nv">@tasks</span><span class="p">}</span> <span class="n">class</span><span class="o">=</span><span class="s2">"flex justify-between items-center p-3 border-b"</span><span class="o">&gt;</span>
  <span class="o">&lt;</span><span class="n">span</span> <span class="n">class</span><span class="o">=</span><span class="p">{</span><span class="k">if</span> <span class="n">task</span><span class="o">.</span><span class="n">completed</span><span class="p">,</span> <span class="k">do</span><span class="p">:</span> <span class="s2">"line-through text-gray-500"</span><span class="p">,</span> <span class="k">else</span><span class="p">:</span> <span class="s2">"text-gray-900"</span><span class="p">}</span><span class="o">&gt;</span>
    <span class="p">{</span><span class="n">task</span><span class="o">.</span><span class="n">title</span><span class="p">}</span>
  <span class="o">&lt;/</span><span class="n">span</span><span class="o">&gt;</span>

  <span class="o">&lt;</span><span class="p">%</span><span class="n">!</span><span class="o">--</span> <span class="no">NOVO</span> <span class="n">BOT</span><span class="err">Ã</span><span class="no">O</span> <span class="no">EXCLUIR</span> <span class="o">--</span><span class="p">%</span><span class="o">&gt;</span>
  <span class="o">&lt;.</span><span class="n">button</span>
    <span class="n">type</span><span class="o">=</span><span class="s2">"button"</span>
    <span class="n">phx</span><span class="o">-</span><span class="n">click</span><span class="o">=</span><span class="s2">"delete"</span>
    <span class="n">phx</span><span class="o">-</span><span class="n">value</span><span class="o">-</span><span class="n">id</span><span class="o">=</span><span class="p">{</span><span class="n">task</span><span class="o">.</span><span class="n">id</span><span class="p">}</span>
    <span class="n">class</span><span class="o">=</span><span class="s2">"!p-2 !bg-red-600 hover:!bg-red-700 text-white font-bold rounded-full"</span>
  <span class="o">&gt;</span>
    <span class="o">&amp;</span><span class="n">times</span><span class="p">;</span> <span class="o">&lt;</span><span class="p">%</span><span class="n">!</span><span class="o">--</span> <span class="no">Renderiza</span> <span class="n">um</span> <span class="s2">"X"</span> <span class="o">--</span><span class="p">%</span><span class="o">&gt;</span>
  <span class="o">&lt;/.</span><span class="n">button</span><span class="o">&gt;</span>
<span class="o">&lt;/</span><span class="n">li</span><span class="o">&gt;</span>
</code></pre></div></div> <h3 id="-entendendo-o-template-heex">🧠 Entendendo o Template (HEEx)</h3> <p>O Phoenix usa o <strong>HEEx</strong> (<em>HTML + Embedded Elixir Extended</em>) — parecido com os templates do Django (Jinja), o ERB do Rails —, mas com uma diferença marcante: <strong>tudo é verificado em tempo de compilação</strong>, inclusive atributos e componentes.</p> <table> <thead> <tr> <th>Sintaxe</th> <th>Função</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">{task.title}</code></td> <td>Interpola um valor Elixir no HTML, com escape seguro.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">&lt;%!-- ... --%&gt;</code></td> <td>Comentário do HEEx (não vai para o navegador).</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">&lt;.button&gt;</code></td> <td>Um <strong>componente</strong> do Phoenix — uma função Elixir que gera HTML.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">:for={task &lt;- @tasks}</code></td> <td>Estrutura de repetição do HEEx.</td> </tr> </tbody> </table> <h3 id="-entendendo-os-atributos-do-botão">⚡ Entendendo os Atributos do Botão</h3> <table> <thead> <tr> <th>Atributo</th> <th>Significado</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">phx-click="delete"</code></td> <td>Ao clicar, envia o evento <strong><code class="language-plaintext highlighter-rouge">"delete"</code></strong> para o servidor (via WebSocket).</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">phx-value-id={task.id}</code></td> <td>Envia o <strong>ID da tarefa</strong> junto — o servidor o recebe como <code class="language-plaintext highlighter-rouge">%{"id" =&gt; "1"}</code> (repare: <strong>string!</strong>).</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">class="!p-2 !bg-red-600 ..."</code></td> <td>Classes Tailwind; o <code class="language-plaintext highlighter-rouge">!</code> força prioridade sobre o estilo padrão do componente.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">&amp;times;</code></td> <td>Entidade HTML do símbolo “×”.</td> </tr> </tbody> </table> <h3 id="-comparando-com-outros-frameworks">🧩 Comparando com outros frameworks</h3> <table> <thead> <tr> <th>Framework</th> <th>Como enviaria o ID no evento?</th> </tr> </thead> <tbody> <tr> <td><strong>Django</strong></td> <td>Formulário <code class="language-plaintext highlighter-rouge">&lt;form&gt;</code> ou JavaScript + <code class="language-plaintext highlighter-rouge">fetch()</code></td> </tr> <tr> <td><strong>Rails</strong></td> <td><code class="language-plaintext highlighter-rouge">link_to "X", task_path(task), method: :delete, remote: true</code></td> </tr> <tr> <td><strong>Flask</strong></td> <td>Formulário POST + rota <code class="language-plaintext highlighter-rouge">/delete/&lt;id&gt;</code></td> </tr> <tr> <td><strong>Phoenix LiveView</strong></td> <td>Apenas: <code class="language-plaintext highlighter-rouge">phx-click="delete" phx-value-id={task.id}</code> — o resto viaja pelo WebSocket.</td> </tr> </tbody> </table> <h2 id="-passo-52-hora-do-erro-e-da-descoberta">💥 Passo 5.2: Hora do Erro (e da Descoberta)</h2> <p>Salve o arquivo. Os botões “X” aparecem. Agora <strong>clique em um deles</strong> e olhe o terminal:</p> <blockquote> <p>❌ <code class="language-plaintext highlighter-rouge">** (UndefinedFunctionError) ... no function clause matching in ElixirTodoListWeb.TodoLive.handle_event/3</code> (ou erro similar)</p> </blockquote> <p>O Phoenix está certo: enviamos o evento <code class="language-plaintext highlighter-rouge">"delete"</code>, mas <strong>nenhuma cláusula de <code class="language-plaintext highlighter-rouge">handle_event</code> combina com ele</strong> — o pattern matching não encontrou destino. Igualzinho ao “texto que desaparece” da Fase 2: evento sem tratador derruba (e reinicia) o processo.</p> <h2 id="️-passo-53-implementando-o-handle_eventdelete-">⚙️ Passo 5.3: Implementando o <code class="language-plaintext highlighter-rouge">handle_event("delete", ...)</code></h2> <p>Adicione esta função <strong>logo abaixo</strong> do <code class="language-plaintext highlighter-rouge">handle_event("save_task", ...)</code>:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="s2">"delete"</span><span class="p">,</span> <span class="p">%{</span><span class="s2">"id"</span> <span class="o">=&gt;</span> <span class="n">id</span><span class="p">},</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="c1"># 1. Busca a tarefa correspondente no banco</span>
    <span class="c1">#    (se o id não existir, Repo.get retorna nil)</span>
    <span class="n">task</span> <span class="o">=</span> <span class="no">Repo</span><span class="o">.</span><span class="n">get</span><span class="p">(</span><span class="no">Task</span><span class="p">,</span> <span class="n">id</span><span class="p">)</span>

    <span class="c1"># 2. Remove a tarefa (apenas se ela existir)</span>
    <span class="k">if</span> <span class="n">task</span> <span class="k">do</span>
      <span class="no">Repo</span><span class="o">.</span><span class="n">delete</span><span class="p">(</span><span class="n">task</span><span class="p">)</span>
    <span class="k">end</span>

    <span class="c1"># 3. Atualiza a lista de tarefas na tela e avisa o usuário</span>
    <span class="n">socket</span> <span class="o">=</span>
      <span class="n">socket</span>
      <span class="o">|&gt;</span> <span class="n">assign</span><span class="p">(</span><span class="ss">:tasks</span><span class="p">,</span> <span class="no">Repo</span><span class="o">.</span><span class="n">all</span><span class="p">(</span><span class="no">Task</span><span class="p">))</span>
      <span class="o">|&gt;</span> <span class="n">put_flash</span><span class="p">(</span><span class="ss">:info</span><span class="p">,</span> <span class="s2">"Tarefa removida com sucesso!"</span><span class="p">)</span>

    <span class="c1"># 4. Retorna o novo estado</span>
    <span class="p">{</span><span class="ss">:noreply</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
  <span class="k">end</span>
</code></pre></div></div> <h3 id="-explicando-em-detalhes">🧠 Explicando em detalhes</h3> <table> <thead> <tr> <th>Etapa</th> <th>Descrição</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">def handle_event("delete", %{"id" =&gt; id}, socket)</code></td> <td>O pattern matching “escuta” o evento <code class="language-plaintext highlighter-rouge">"delete"</code>; o segundo parâmetro traz o mapa com o <code class="language-plaintext highlighter-rouge">phx-value-id</code>.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Repo.get(Task, id)</code></td> <td>Busca a tarefa pelo ID (o Ecto converte a string para o tipo da chave). Retorna <code class="language-plaintext highlighter-rouge">nil</code> se não existir.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">if task do ... end</code></td> <td>Defesa contra cliques em botões “velhos” (ex: a tarefa já foi removida em outra aba).</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Repo.delete(task)</code></td> <td>Exclui o registro do banco.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">assign(socket, :tasks, Repo.all(Task))</code></td> <td>Recarrega a lista — a UI re-renderiza sem a tarefa excluída.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">put_flash(:info, ...)</code></td> <td>A mensagem de confirmação — exibida pelo <code class="language-plaintext highlighter-rouge">Layouts.app</code> que adicionamos na Fase 4.</td> </tr> </tbody> </table> <aside> <p>💡 <strong>Alternativa mais “estrita”:</strong> <code class="language-plaintext highlighter-rouge">Repo.get!(Task, id)</code> (com <code class="language-plaintext highlighter-rouge">!</code>) <strong>lança uma exceção</strong> se o ID não existir, em vez de retornar <code class="language-plaintext highlighter-rouge">nil</code>. É uma convenção do Ecto: funções com <code class="language-plaintext highlighter-rouge">!</code> falham “alto”. Aqui preferimos a versão defensiva, mas você encontrará o <code class="language-plaintext highlighter-rouge">get!</code> com frequência por aí.</p> </aside> <h3 id="-o-ciclo-completo-do-evento">🧩 O ciclo completo do evento</h3> <ol> <li>Usuário clica no botão “X” →</li> <li>O navegador envia <code class="language-plaintext highlighter-rouge">"delete"</code> com <code class="language-plaintext highlighter-rouge">%{"id" =&gt; "3"}</code> pelo WebSocket →</li> <li>O servidor executa <code class="language-plaintext highlighter-rouge">handle_event("delete", ...)</code> →</li> <li>O LiveView atualiza o estado (<code class="language-plaintext highlighter-rouge">assign/3</code>) →</li> <li>O template HEEx é <strong>re-renderizado automaticamente</strong> →</li> <li>A lista aparece na tela <strong>já sem a tarefa</strong> — em tempo real!</li> </ol> <h2 id="-passo-54-testando">🔍 Passo 5.4: Testando</h2> <ol> <li>Salve o arquivo (o servidor recompila sozinho).</li> <li>Acesse <code class="language-plaintext highlighter-rouge">http://localhost:4000</code>.</li> <li>Crie algumas tarefas e clique no “X” vermelho de uma delas.</li> </ol> <p>✅ A lista se atualiza imediatamente, com o aviso: <em>“Tarefa removida com sucesso!”</em></p> <p>🔁 Recarregue a página (F5): a tarefa continua excluída — o <code class="language-plaintext highlighter-rouge">DELETE</code> persistiu no banco.</p> <h2 id="-passo-55-commit">💾 Passo 5.5: Commit</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Fase 5: Implementa exclusão de tarefas (delete)"</span>
</code></pre></div></div> <hr/> <p><strong>Fim da Fase 5!</strong> 🏁</p> <p>Seu <code class="language-plaintext highlighter-rouge">TodoLive</code> agora <strong>cria, valida, lista e exclui</strong> tarefas em tempo real — usando apenas Elixir + LiveView, sem JavaScript.</p> <p>Falta o “U” do CRUD: marcar tarefas como concluídas.</p> <hr/> <h1 id="-fase-6-refinamento--concluindo-tarefas-toggle">✅ Fase 6: Refinamento — Concluindo Tarefas (Toggle)</h1> <h3 id="-objetivo-1">🎯 Objetivo</h3> <p>Adicionar um <strong>checkbox</strong> ao lado de cada tarefa. Ao clicar, ele deve marcar a tarefa como <strong>concluída</strong> (ou não concluída) <strong>no banco de dados</strong>, atualizando a interface em tempo real — aplicando ou removendo o estilo “riscado”.</p> <h3 id="-por-que-isso-é-importante">💡 Por que isso é importante?</h3> <p>Este passo ensina a:</p> <ul> <li>Criar formulários dinâmicos <strong>dentro de loops</strong> (<code class="language-plaintext highlighter-rouge">:for</code>);</li> <li>Usar <code class="language-plaintext highlighter-rouge">phx-change</code> para reagir instantaneamente a um checkbox;</li> <li><strong>Atualizar</strong> registros existentes com <code class="language-plaintext highlighter-rouge">Repo.get</code> + <code class="language-plaintext highlighter-rouge">Repo.update</code>;</li> <li>Reutilizar o <code class="language-plaintext highlighter-rouge">Task.changeset/2</code> para validação e atualização;</li> <li>E a decifrar uma pegadinha clássica dos checkboxes HTML. 🕵️</li> </ul> <h2 id="-passo-61-a-interface-adicionando-o-checkbox">🧩 Passo 6.1: A Interface (adicionando o checkbox)</h2> <p>A maneira mais robusta de lidar com checkboxes no LiveView é criar <strong>um pequeno <code class="language-plaintext highlighter-rouge">&lt;.form&gt;</code> por tarefa</strong> — assim cada item da lista tem seu próprio estado independente.</p> <p>Abra <code class="language-plaintext highlighter-rouge">lib/elixir_todo_list_web/live/todo_live.ex</code>, encontre o loop <code class="language-plaintext highlighter-rouge">&lt;li :for={task &lt;- @tasks}&gt;</code> no <code class="language-plaintext highlighter-rouge">render/1</code> e substitua o <code class="language-plaintext highlighter-rouge">&lt;span&gt;</code> do título por um formulário com checkbox. O bloco da lista <strong>completo</strong> fica assim:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code>        <span class="o">&lt;</span><span class="p">%</span><span class="n">!</span><span class="o">--</span> <span class="no">A</span> <span class="no">LISTA</span> <span class="no">DE</span> <span class="no">TAREFAS</span> <span class="o">--</span><span class="p">%</span><span class="o">&gt;</span>
        <span class="o">&lt;</span><span class="n">div</span> <span class="n">class</span><span class="o">=</span><span class="s2">"mt-8"</span><span class="o">&gt;</span>
          <span class="o">&lt;</span><span class="n">ul</span> <span class="n">id</span><span class="o">=</span><span class="s2">"task-list"</span><span class="o">&gt;</span>
            <span class="o">&lt;</span><span class="n">li</span>
              <span class="ss">:for=</span><span class="p">{</span><span class="n">task</span> <span class="o">&lt;-</span> <span class="nv">@tasks</span><span class="p">}</span>
              <span class="n">id</span><span class="o">=</span><span class="p">{</span><span class="s2">"task-</span><span class="si">#{</span><span class="n">task</span><span class="o">.</span><span class="n">id</span><span class="si">}</span><span class="s2">"</span><span class="p">}</span>
              <span class="n">class</span><span class="o">=</span><span class="s2">"flex justify-between items-center p-3 border-b"</span>
            <span class="o">&gt;</span>
              <span class="o">&lt;</span><span class="p">%</span> <span class="n">task_form</span> <span class="o">=</span> <span class="no">Task</span><span class="o">.</span><span class="n">changeset</span><span class="p">(</span><span class="n">task</span><span class="p">,</span> <span class="p">%{})</span> <span class="o">|&gt;</span> <span class="n">to_form</span><span class="p">()</span> <span class="p">%</span><span class="o">&gt;</span>

              <span class="o">&lt;.</span><span class="n">form</span>
                <span class="n">for</span><span class="o">=</span><span class="p">{</span><span class="n">task_form</span><span class="p">}</span>
                <span class="n">phx</span><span class="o">-</span><span class="n">change</span><span class="o">=</span><span class="s2">"toggle_complete"</span>
                <span class="n">phx</span><span class="o">-</span><span class="n">value</span><span class="o">-</span><span class="n">id</span><span class="o">=</span><span class="p">{</span><span class="n">task</span><span class="o">.</span><span class="n">id</span><span class="p">}</span>
                <span class="n">class</span><span class="o">=</span><span class="s2">"flex-grow"</span>
              <span class="o">&gt;</span>
                <span class="o">&lt;</span><span class="n">div</span> <span class="n">class</span><span class="o">=</span><span class="s2">"flex items-center space-x-4"</span><span class="o">&gt;</span>
                  <span class="o">&lt;.</span><span class="n">input</span>
                    <span class="n">type</span><span class="o">=</span><span class="s2">"checkbox"</span>
                    <span class="n">field</span><span class="o">=</span><span class="p">{</span><span class="n">task_form</span><span class="p">[</span><span class="ss">:completed</span><span class="p">]}</span>
                    <span class="n">class</span><span class="o">=</span><span class="s2">"flex-shrink-0"</span>
                  <span class="o">/&gt;</span>

                  <span class="o">&lt;</span><span class="p">%</span><span class="n">!</span><span class="o">--</span> <span class="no">Um</span> <span class="o">&lt;</span><span class="n">label</span><span class="o">&gt;</span> <span class="n">separado</span> <span class="n">para</span> <span class="n">controle</span> <span class="n">total</span> <span class="k">do</span> <span class="n">estilo</span> <span class="o">--</span><span class="p">%</span><span class="o">&gt;</span>
                  <span class="o">&lt;</span><span class="n">label</span> <span class="n">class</span><span class="o">=</span><span class="p">{</span><span class="k">if</span> <span class="n">task</span><span class="o">.</span><span class="n">completed</span><span class="p">,</span> <span class="k">do</span><span class="p">:</span> <span class="s2">"line-through text-gray-500"</span><span class="p">,</span> <span class="k">else</span><span class="p">:</span> <span class="s2">"text-gray-900"</span><span class="p">}</span><span class="o">&gt;</span>
                    <span class="p">{</span><span class="n">task</span><span class="o">.</span><span class="n">title</span><span class="p">}</span>
                  <span class="o">&lt;/</span><span class="n">label</span><span class="o">&gt;</span>
                <span class="o">&lt;/</span><span class="n">div</span><span class="o">&gt;</span>
              <span class="o">&lt;/.</span><span class="n">form</span><span class="o">&gt;</span>

              <span class="o">&lt;.</span><span class="n">button</span>
                <span class="n">type</span><span class="o">=</span><span class="s2">"button"</span>
                <span class="n">phx</span><span class="o">-</span><span class="n">click</span><span class="o">=</span><span class="s2">"delete"</span>
                <span class="n">phx</span><span class="o">-</span><span class="n">value</span><span class="o">-</span><span class="n">id</span><span class="o">=</span><span class="p">{</span><span class="n">task</span><span class="o">.</span><span class="n">id</span><span class="p">}</span>
                <span class="n">class</span><span class="o">=</span><span class="s2">"!p-2 !bg-red-600 hover:!bg-red-700 text-white font-bold rounded-full"</span>
              <span class="o">&gt;</span>
                <span class="o">&amp;</span><span class="n">times</span><span class="p">;</span>
              <span class="o">&lt;/.</span><span class="n">button</span><span class="o">&gt;</span>
            <span class="o">&lt;/</span><span class="n">li</span><span class="o">&gt;</span>
          <span class="o">&lt;/</span><span class="n">ul</span><span class="o">&gt;</span>
        <span class="o">&lt;/</span><span class="n">div</span><span class="o">&gt;</span>
</code></pre></div></div> <h3 id="-explicação-didática">🧠 Explicação didática</h3> <table> <thead> <tr> <th>Elemento</th> <th>Função</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">&lt;% task_form = ... %&gt;</code></td> <td>Cria uma variável temporária: o formulário individual <strong>daquela</strong> tarefa (um changeset “vazio” sobre a tarefa existente, convertido com <code class="language-plaintext highlighter-rouge">to_form</code>).</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">id={"task-#{task.id}"}</code></td> <td>Um <code class="language-plaintext highlighter-rouge">id</code> único por <code class="language-plaintext highlighter-rouge">&lt;li&gt;</code> — ajuda o LiveView a rastrear cada item.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">phx-change="toggle_complete"</code></td> <td>Evento disparado automaticamente ao marcar/desmarcar o checkbox.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">phx-value-id={task.id}</code></td> <td>Envia o <strong>ID da tarefa</strong> junto ao evento.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">&lt;.input type="checkbox" field={task_form[:completed]}&gt;</code></td> <td>O checkbox integrado ao campo <code class="language-plaintext highlighter-rouge">completed</code> do schema (já renderiza marcado/desmarcado conforme o banco).</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">&lt;label class={...}&gt;</code></td> <td>Aplica o “riscado” quando a tarefa está concluída.</td> </tr> </tbody> </table> <p>💥 <strong>Erro esperado:</strong> salve o arquivo e clique em um checkbox. Como na Fase 5, o terminal mostrará um erro — o evento <code class="language-plaintext highlighter-rouge">"toggle_complete"</code> ainda não tem tratador. Já sabemos o remédio.</p> <h2 id="️-passo-62-a-pegadinha-do-checkbox-leia-antes-de-codar">🕵️ Passo 6.2: A Pegadinha do Checkbox (leia antes de codar!)</h2> <p>Antes de escrever o <code class="language-plaintext highlighter-rouge">handle_event</code>, precisamos entender <strong>o que exatamente chega ao servidor</strong> quando o checkbox muda. E aqui mora uma pegadinha dupla:</p> <p><strong>1. HTML puro:</strong> um checkbox <strong>desmarcado não é enviado</strong> em formulários HTML. Se dependêssemos disso, “desmarcar” não geraria dado nenhum.</p> <p><strong>2. A solução do Phoenix:</strong> para contornar isso, o componente <code class="language-plaintext highlighter-rouge">&lt;.input type="checkbox"&gt;</code> renderiza, escondido, um segundo campo:</p> <div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;input</span> <span class="na">type=</span><span class="s">"hidden"</span> <span class="na">name=</span><span class="s">"task[completed]"</span> <span class="na">value=</span><span class="s">"false"</span> <span class="nt">/&gt;</span> <span class="nt">&lt;input</span> <span class="na">type=</span><span class="s">"checkbox"</span> <span class="na">name=</span><span class="s">"task[completed]"</span> <span class="na">value=</span><span class="s">"true"</span> <span class="err">...</span> <span class="nt">/&gt;</span>
</code></pre></div></div> <p>Graças ao campo oculto, o servidor <strong>sempre</strong> recebe a chave <code class="language-plaintext highlighter-rouge">"completed"</code>:</p> <ul> <li>Checkbox <strong>marcado</strong> → <code class="language-plaintext highlighter-rouge">%{"task" =&gt; %{"completed" =&gt; "true"}}</code></li> <li>Checkbox <strong>desmarcado</strong> → <code class="language-plaintext highlighter-rouge">%{"task" =&gt; %{"completed" =&gt; "false"}}</code></li> </ul> <aside> <p>⚠️ <strong>A consequência prática:</strong> como a chave <code class="language-plaintext highlighter-rouge">"completed"</code> <strong>sempre existe</strong>, testar sua <em>presença</em> (por exemplo, com <code class="language-plaintext highlighter-rouge">Map.has_key?</code>) <strong>não funciona</strong> — daria <code class="language-plaintext highlighter-rouge">true</code> nos dois casos, e a tarefa, uma vez marcada, <strong>nunca mais desmarcaria</strong>! O teste correto é sobre o <strong>valor</strong>: <code class="language-plaintext highlighter-rouge">task_params["completed"] == "true"</code>. Guarde essa: é um dos bugs mais sorrateiros do LiveView.</p> </aside> <h2 id="️-passo-63-implementando-o-handle_eventtoggle_complete-">⚙️ Passo 6.3: Implementando o <code class="language-plaintext highlighter-rouge">handle_event("toggle_complete", ...)</code></h2> <p>Adicione a função <strong>logo abaixo</strong> do <code class="language-plaintext highlighter-rouge">handle_event("delete", ...)</code>:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="s2">"toggle_complete"</span><span class="p">,</span> <span class="p">%{</span><span class="s2">"id"</span> <span class="o">=&gt;</span> <span class="n">id</span><span class="p">,</span> <span class="s2">"task"</span> <span class="o">=&gt;</span> <span class="n">task_params</span><span class="p">},</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="c1"># 1. Busca a tarefa correspondente no banco</span>
    <span class="n">task</span> <span class="o">=</span> <span class="no">Repo</span><span class="o">.</span><span class="n">get!</span><span class="p">(</span><span class="no">Task</span><span class="p">,</span> <span class="n">id</span><span class="p">)</span>

    <span class="c1"># 2. Lê o novo estado do checkbox</span>
    <span class="c1">#    (graças ao campo oculto, "completed" é sempre "true" ou "false")</span>
    <span class="n">completed_status</span> <span class="o">=</span> <span class="n">task_params</span><span class="p">[</span><span class="s2">"completed"</span><span class="p">]</span> <span class="o">==</span> <span class="s2">"true"</span>

    <span class="c1"># 3. Cria um changeset de ATUALIZAÇÃO (repare: partimos de 'task',</span>
    <span class="c1">#    a struct vinda do banco — não de %Task{} vazio!)</span>
    <span class="n">changeset</span> <span class="o">=</span> <span class="no">Task</span><span class="o">.</span><span class="n">changeset</span><span class="p">(</span><span class="n">task</span><span class="p">,</span> <span class="p">%{</span><span class="ss">completed:</span> <span class="n">completed_status</span><span class="p">})</span>

    <span class="c1"># 4. Atualiza o registro no banco de dados</span>
    <span class="no">Repo</span><span class="o">.</span><span class="n">update</span><span class="p">(</span><span class="n">changeset</span><span class="p">)</span>

    <span class="c1"># 5. Recarrega a lista para atualizar a UI</span>
    <span class="n">socket</span> <span class="o">=</span> <span class="n">assign</span><span class="p">(</span><span class="n">socket</span><span class="p">,</span> <span class="ss">tasks:</span> <span class="no">Repo</span><span class="o">.</span><span class="n">all</span><span class="p">(</span><span class="no">Task</span><span class="p">))</span>
    <span class="p">{</span><span class="ss">:noreply</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
  <span class="k">end</span>
</code></pre></div></div> <h3 id="-o-fluxo-passo-a-passo">🧩 O fluxo, passo a passo</h3> <ol> <li> <p><strong>Evento recebido</strong> — ao clicar no checkbox, o navegador envia:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">%{</span><span class="s2">"id"</span> <span class="o">=&gt;</span> <span class="s2">"1"</span><span class="p">,</span> <span class="s2">"task"</span> <span class="o">=&gt;</span> <span class="p">%{</span><span class="s2">"completed"</span> <span class="o">=&gt;</span> <span class="s2">"true"</span><span class="p">}}</span>  <span class="c1"># marcou</span>
<span class="p">%{</span><span class="s2">"id"</span> <span class="o">=&gt;</span> <span class="s2">"1"</span><span class="p">,</span> <span class="s2">"task"</span> <span class="o">=&gt;</span> <span class="p">%{</span><span class="s2">"completed"</span> <span class="o">=&gt;</span> <span class="s2">"false"</span><span class="p">}}</span> <span class="c1"># desmarcou</span>
</code></pre></div> </div> </li> <li><strong>Busca</strong> — <code class="language-plaintext highlighter-rouge">Repo.get!</code> localiza a tarefa (aqui usamos a versão com <code class="language-plaintext highlighter-rouge">!</code>: se o ID sumiu, algo está muito errado e preferimos o erro explícito).</li> <li><strong>Conversão</strong> — <code class="language-plaintext highlighter-rouge">task_params["completed"] == "true"</code> transforma a <em>string</em> do formulário em um <em>booleano</em> de verdade. (Ecoa a lição do tutorial de Clojure, onde convertíamos o 0/1 do SQLite — formulários e bancos adoram fingir que booleanos são outra coisa!)</li> <li><strong>Atualização</strong> — <code class="language-plaintext highlighter-rouge">Task.changeset(task, ...)</code> + <code class="language-plaintext highlighter-rouge">Repo.update</code> aplicam a mudança. É o mesmo <code class="language-plaintext highlighter-rouge">changeset/2</code> da criação: como ele parte da struct recebida, serve tanto para <code class="language-plaintext highlighter-rouge">insert</code> quanto para <code class="language-plaintext highlighter-rouge">update</code>.</li> <li><strong>Re-render</strong> — o <code class="language-plaintext highlighter-rouge">assign</code> atualiza <code class="language-plaintext highlighter-rouge">@tasks</code>, e o LiveView redesenha o template.</li> </ol> <h2 id="-passo-64-teste-final-o-crud-completo">🧪 Passo 6.4: Teste Final (o CRUD completo)</h2> <p>Com o servidor rodando, em <code class="language-plaintext highlighter-rouge">http://localhost:4000</code>:</p> <ol> <li><strong>Create:</strong> adicione duas ou três tarefas. ✅</li> <li><strong>Read:</strong> F5 — continuam lá. ✅</li> <li><strong>Update:</strong> marque o checkbox de uma → ela risca imediatamente. <strong>Desmarque</strong> → o risco some (se não sumir, revise o Passo 6.2!). F5 → o estado persiste. ✅</li> <li><strong>Delete:</strong> clique no “X” de outra → some. F5 → continua fora. ✅</li> <li><strong>A prova final:</strong> pare o servidor, suba de novo, F5 → <strong>tudo exatamente como você deixou.</strong> ✅</li> </ol> <h2 id="-passo-65-commit">💾 Passo 6.5: Commit</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Fase 6: Implementa conclusão de tarefas (toggle_complete)"</span>
</code></pre></div></div> <hr/> <h2 id="-o-código-completo-para-conferência">📄 O Código Completo (para conferência)</h2> <p>Se algo não funcionou, compare seu <code class="language-plaintext highlighter-rouge">lib/elixir_todo_list_web/live/todo_live.ex</code> com o estado final:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">defmodule</span> <span class="no">ElixirTodoListWeb</span><span class="o">.</span><span class="no">TodoLive</span> <span class="k">do</span>
  <span class="kn">use</span> <span class="no">ElixirTodoListWeb</span><span class="p">,</span> <span class="ss">:live_view</span>

  <span class="n">alias</span> <span class="no">ElixirTodoList</span><span class="o">.</span><span class="no">Repo</span>
  <span class="n">alias</span> <span class="no">ElixirTodoList</span><span class="o">.</span><span class="no">Task</span>

  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">mount</span><span class="p">(</span><span class="n">_params</span><span class="p">,</span> <span class="n">_session</span><span class="p">,</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="n">tasks</span> <span class="o">=</span> <span class="no">Repo</span><span class="o">.</span><span class="n">all</span><span class="p">(</span><span class="no">Task</span><span class="p">)</span>
    <span class="n">changeset</span> <span class="o">=</span> <span class="no">Task</span><span class="o">.</span><span class="n">changeset</span><span class="p">(%</span><span class="no">Task</span><span class="p">{},</span> <span class="p">%{})</span>
    <span class="n">form</span> <span class="o">=</span> <span class="n">to_form</span><span class="p">(</span><span class="n">changeset</span><span class="p">)</span>

    <span class="n">socket</span> <span class="o">=</span>
      <span class="n">assign</span><span class="p">(</span><span class="n">socket</span><span class="p">,</span>
        <span class="ss">tasks:</span> <span class="n">tasks</span><span class="p">,</span>
        <span class="ss">form:</span> <span class="n">form</span>
      <span class="p">)</span>

    <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
  <span class="k">end</span>

  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="s2">"save_task"</span><span class="p">,</span> <span class="p">%{</span><span class="s2">"task"</span> <span class="o">=&gt;</span> <span class="n">task_params</span><span class="p">},</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="n">changeset</span> <span class="o">=</span> <span class="no">Task</span><span class="o">.</span><span class="n">changeset</span><span class="p">(%</span><span class="no">Task</span><span class="p">{},</span> <span class="n">task_params</span><span class="p">)</span>

    <span class="n">socket_atualizado</span> <span class="o">=</span>
      <span class="k">case</span> <span class="no">Repo</span><span class="o">.</span><span class="n">insert</span><span class="p">(</span><span class="n">changeset</span><span class="p">)</span> <span class="k">do</span>
        <span class="p">{</span><span class="ss">:ok</span><span class="p">,</span> <span class="n">_new_task</span><span class="p">}</span> <span class="o">-&gt;</span>
          <span class="n">novo_changeset_vazio</span> <span class="o">=</span> <span class="no">Task</span><span class="o">.</span><span class="n">changeset</span><span class="p">(%</span><span class="no">Task</span><span class="p">{},</span> <span class="p">%{})</span>

          <span class="n">socket</span>
          <span class="o">|&gt;</span> <span class="n">assign</span><span class="p">(</span><span class="ss">:tasks</span><span class="p">,</span> <span class="no">Repo</span><span class="o">.</span><span class="n">all</span><span class="p">(</span><span class="no">Task</span><span class="p">))</span>
          <span class="o">|&gt;</span> <span class="n">assign</span><span class="p">(</span><span class="ss">:form</span><span class="p">,</span> <span class="n">to_form</span><span class="p">(</span><span class="n">novo_changeset_vazio</span><span class="p">))</span>
          <span class="o">|&gt;</span> <span class="n">put_flash</span><span class="p">(</span><span class="ss">:info</span><span class="p">,</span> <span class="s2">"Tarefa salva com sucesso!"</span><span class="p">)</span>

        <span class="p">{</span><span class="ss">:error</span><span class="p">,</span> <span class="n">failed_changeset</span><span class="p">}</span> <span class="o">-&gt;</span>
          <span class="n">assign</span><span class="p">(</span><span class="n">socket</span><span class="p">,</span> <span class="ss">form:</span> <span class="n">to_form</span><span class="p">(</span><span class="n">failed_changeset</span><span class="p">))</span>
      <span class="k">end</span>

    <span class="p">{</span><span class="ss">:noreply</span><span class="p">,</span> <span class="n">socket_atualizado</span><span class="p">}</span>
  <span class="k">end</span>

  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="s2">"delete"</span><span class="p">,</span> <span class="p">%{</span><span class="s2">"id"</span> <span class="o">=&gt;</span> <span class="n">id</span><span class="p">},</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="n">task</span> <span class="o">=</span> <span class="no">Repo</span><span class="o">.</span><span class="n">get</span><span class="p">(</span><span class="no">Task</span><span class="p">,</span> <span class="n">id</span><span class="p">)</span>

    <span class="k">if</span> <span class="n">task</span> <span class="k">do</span>
      <span class="no">Repo</span><span class="o">.</span><span class="n">delete</span><span class="p">(</span><span class="n">task</span><span class="p">)</span>
    <span class="k">end</span>

    <span class="n">socket</span> <span class="o">=</span>
      <span class="n">socket</span>
      <span class="o">|&gt;</span> <span class="n">assign</span><span class="p">(</span><span class="ss">:tasks</span><span class="p">,</span> <span class="no">Repo</span><span class="o">.</span><span class="n">all</span><span class="p">(</span><span class="no">Task</span><span class="p">))</span>
      <span class="o">|&gt;</span> <span class="n">put_flash</span><span class="p">(</span><span class="ss">:info</span><span class="p">,</span> <span class="s2">"Tarefa removida com sucesso!"</span><span class="p">)</span>

    <span class="p">{</span><span class="ss">:noreply</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
  <span class="k">end</span>

  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">handle_event</span><span class="p">(</span><span class="s2">"toggle_complete"</span><span class="p">,</span> <span class="p">%{</span><span class="s2">"id"</span> <span class="o">=&gt;</span> <span class="n">id</span><span class="p">,</span> <span class="s2">"task"</span> <span class="o">=&gt;</span> <span class="n">task_params</span><span class="p">},</span> <span class="n">socket</span><span class="p">)</span> <span class="k">do</span>
    <span class="n">task</span> <span class="o">=</span> <span class="no">Repo</span><span class="o">.</span><span class="n">get!</span><span class="p">(</span><span class="no">Task</span><span class="p">,</span> <span class="n">id</span><span class="p">)</span>
    <span class="n">completed_status</span> <span class="o">=</span> <span class="n">task_params</span><span class="p">[</span><span class="s2">"completed"</span><span class="p">]</span> <span class="o">==</span> <span class="s2">"true"</span>
    <span class="n">changeset</span> <span class="o">=</span> <span class="no">Task</span><span class="o">.</span><span class="n">changeset</span><span class="p">(</span><span class="n">task</span><span class="p">,</span> <span class="p">%{</span><span class="ss">completed:</span> <span class="n">completed_status</span><span class="p">})</span>
    <span class="no">Repo</span><span class="o">.</span><span class="n">update</span><span class="p">(</span><span class="n">changeset</span><span class="p">)</span>

    <span class="n">socket</span> <span class="o">=</span> <span class="n">assign</span><span class="p">(</span><span class="n">socket</span><span class="p">,</span> <span class="ss">tasks:</span> <span class="no">Repo</span><span class="o">.</span><span class="n">all</span><span class="p">(</span><span class="no">Task</span><span class="p">))</span>
    <span class="p">{</span><span class="ss">:noreply</span><span class="p">,</span> <span class="n">socket</span><span class="p">}</span>
  <span class="k">end</span>

  <span class="nv">@impl</span> <span class="no">true</span>
  <span class="k">def</span> <span class="n">render</span><span class="p">(</span><span class="n">assigns</span><span class="p">)</span> <span class="k">do</span>
    <span class="sx">~H""</span><span class="s2">"
    &lt;Layouts.app flash={@flash}&gt;
      &lt;div class="</span><span class="n">w</span><span class="o">-</span><span class="n">full</span> <span class="n">max</span><span class="o">-</span><span class="n">w</span><span class="o">-</span><span class="n">lg</span> <span class="n">mx</span><span class="o">-</span><span class="n">auto</span> <span class="n">mt</span><span class="o">-</span><span class="mi">12</span> <span class="n">p</span><span class="o">-</span><span class="mi">6</span> <span class="n">bg</span><span class="o">-</span><span class="n">white</span> <span class="n">rounded</span><span class="o">-</span><span class="n">lg</span> <span class="n">shadow</span><span class="o">-</span><span class="n">md</span><span class="s2">"&gt;
        &lt;h1 class="</span><span class="n">text</span><span class="o">-</span><span class="err">3</span><span class="n">xl</span> <span class="n">font</span><span class="o">-</span><span class="n">bold</span> <span class="n">mb</span><span class="o">-</span><span class="mi">6</span> <span class="n">text</span><span class="o">-</span><span class="n">center</span> <span class="n">text</span><span class="o">-</span><span class="n">gray</span><span class="o">-</span><span class="mi">800</span><span class="s2">"&gt;
          Minha Lista de Tarefas (com DB!)
        &lt;/h1&gt;

        &lt;.form for={@form} id="</span><span class="n">task</span><span class="o">-</span><span class="n">form</span><span class="s2">" phx-submit="</span><span class="n">save_task</span><span class="s2">"&gt;
          &lt;.input
            field={@form[:title]}
            type="</span><span class="n">text</span><span class="s2">"
            label="</span><span class="no">Nova</span> <span class="no">Tarefa</span><span class="s2">"
            placeholder="</span><span class="no">O</span> <span class="n">que</span> <span class="n">precisa</span> <span class="n">ser</span> <span class="n">feito?</span><span class="s2">"
          /&gt;
          &lt;.button variant="</span><span class="n">primary</span><span class="s2">" phx-disable-with="</span><span class="no">Salvando</span><span class="o">...</span><span class="s2">"&gt;Adicionar Tarefa&lt;/.button&gt;
        &lt;/.form&gt;

        &lt;div class="</span><span class="n">mt</span><span class="o">-</span><span class="mi">8</span><span class="s2">"&gt;
          &lt;ul id="</span><span class="n">task</span><span class="o">-</span><span class="n">list</span><span class="s2">"&gt;
            &lt;li
              :for={task &lt;- @tasks}
              id={"</span><span class="n">task</span><span class="o">-</span><span class="c1">#{task.id}"}</span>
              <span class="n">class</span><span class="o">=</span><span class="s2">"flex justify-between items-center p-3 border-b"</span>
            <span class="o">&gt;</span>
              <span class="o">&lt;</span><span class="p">%</span> <span class="n">task_form</span> <span class="o">=</span> <span class="no">Task</span><span class="o">.</span><span class="n">changeset</span><span class="p">(</span><span class="n">task</span><span class="p">,</span> <span class="p">%{})</span> <span class="o">|&gt;</span> <span class="n">to_form</span><span class="p">()</span> <span class="p">%</span><span class="o">&gt;</span>

              <span class="o">&lt;.</span><span class="n">form</span>
                <span class="n">for</span><span class="o">=</span><span class="p">{</span><span class="n">task_form</span><span class="p">}</span>
                <span class="n">phx</span><span class="o">-</span><span class="n">change</span><span class="o">=</span><span class="s2">"toggle_complete"</span>
                <span class="n">phx</span><span class="o">-</span><span class="n">value</span><span class="o">-</span><span class="n">id</span><span class="o">=</span><span class="p">{</span><span class="n">task</span><span class="o">.</span><span class="n">id</span><span class="p">}</span>
                <span class="n">class</span><span class="o">=</span><span class="s2">"flex-grow"</span>
              <span class="o">&gt;</span>
                <span class="o">&lt;</span><span class="n">div</span> <span class="n">class</span><span class="o">=</span><span class="s2">"flex items-center space-x-4"</span><span class="o">&gt;</span>
                  <span class="o">&lt;.</span><span class="n">input</span>
                    <span class="n">type</span><span class="o">=</span><span class="s2">"checkbox"</span>
                    <span class="n">field</span><span class="o">=</span><span class="p">{</span><span class="n">task_form</span><span class="p">[</span><span class="ss">:completed</span><span class="p">]}</span>
                    <span class="n">class</span><span class="o">=</span><span class="s2">"flex-shrink-0"</span>
                  <span class="o">/&gt;</span>

                  <span class="o">&lt;</span><span class="n">label</span> <span class="n">class</span><span class="o">=</span><span class="p">{</span><span class="k">if</span> <span class="n">task</span><span class="o">.</span><span class="n">completed</span><span class="p">,</span> <span class="k">do</span><span class="p">:</span> <span class="s2">"line-through text-gray-500"</span><span class="p">,</span> <span class="k">else</span><span class="p">:</span> <span class="s2">"text-gray-900"</span><span class="p">}</span><span class="o">&gt;</span>
                    <span class="p">{</span><span class="n">task</span><span class="o">.</span><span class="n">title</span><span class="p">}</span>
                  <span class="o">&lt;/</span><span class="n">label</span><span class="o">&gt;</span>
                <span class="o">&lt;/</span><span class="n">div</span><span class="o">&gt;</span>
              <span class="o">&lt;/.</span><span class="n">form</span><span class="o">&gt;</span>

              <span class="o">&lt;.</span><span class="n">button</span>
                <span class="n">type</span><span class="o">=</span><span class="s2">"button"</span>
                <span class="n">phx</span><span class="o">-</span><span class="n">click</span><span class="o">=</span><span class="s2">"delete"</span>
                <span class="n">phx</span><span class="o">-</span><span class="n">value</span><span class="o">-</span><span class="n">id</span><span class="o">=</span><span class="p">{</span><span class="n">task</span><span class="o">.</span><span class="n">id</span><span class="p">}</span>
                <span class="n">class</span><span class="o">=</span><span class="s2">"!p-2 !bg-red-600 hover:!bg-red-700 text-white font-bold rounded-full"</span>
              <span class="o">&gt;</span>
                <span class="o">&amp;</span><span class="n">times</span><span class="p">;</span>
              <span class="o">&lt;/.</span><span class="n">button</span><span class="o">&gt;</span>
            <span class="o">&lt;/</span><span class="n">li</span><span class="o">&gt;</span>
          <span class="o">&lt;/</span><span class="n">ul</span><span class="o">&gt;</span>
        <span class="o">&lt;/</span><span class="n">div</span><span class="o">&gt;</span>
      <span class="o">&lt;/</span><span class="n">div</span><span class="o">&gt;</span>
    <span class="o">&lt;/</span><span class="no">Layouts</span><span class="o">.</span><span class="n">app</span><span class="o">&gt;</span>
    <span class="s2">"""
  end
end
</span></code></pre></div></div> <hr/> <p><strong>Fim da Fase 6!</strong> 🏁</p> <p>Você agora tem um aplicativo <strong>CRUD completo</strong> em Elixir + Phoenix LiveView:</p> <ul> <li><strong>Criar</strong> novas tarefas (com validação);</li> <li><strong>Listar</strong> as existentes;</li> <li><strong>Atualizar</strong> (marcar/desmarcar como concluída);</li> <li><strong>Excluir</strong> tarefas.</li> </ul> <p>Tudo com <strong>atualização em tempo real</strong> e sem escrever uma linha de JavaScript. 💪</p> <hr/> <h1 id="-fase-7-personalizando-o-design-tailwind-css-v4-e-daisyui">🎨 Fase 7: Personalizando o Design (Tailwind CSS v4 e daisyUI)</h1> <p>Até aqui, construímos uma aplicação funcional e interativa. Agora vamos entender <strong>como o visual é definido</strong> e <strong>como personalizá-lo</strong>.</p> <p>O Phoenix 1.8 vem configurado com o <strong>Tailwind CSS v4</strong>, um framework CSS baseado em <strong>classes utilitárias</strong>, e com o <strong>daisyUI</strong>, um plugin que fornece componentes prontos (botões, alertas, inputs) e <strong>temas</strong> pré-configurados.</p> <aside> <p>⚠️ <strong>Atenção à versão!</strong> Se você pesquisar por “configurar tema daisyUI Phoenix”, encontrará muitos guias mandando editar o arquivo <code class="language-plaintext highlighter-rouge">assets/tailwind.config.js</code>. <strong>Esse arquivo não existe mais</strong>: o Tailwind <strong>v4</strong> (usado pelo Phoenix 1.8) abandonou a configuração em JavaScript — agora tudo é configurado <strong>dentro do próprio CSS</strong>, no arquivo <code class="language-plaintext highlighter-rouge">assets/css/app.css</code>. Guias baseados no Phoenix ≤ 1.7 não se aplicam aqui.</p> </aside> <h3 id="-passo-71-como-o-tailwind-funciona">🔧 Passo 7.1: Como o Tailwind funciona</h3> <p>O Tailwind é diferente de frameworks tradicionais como o Bootstrap. Em vez de classes de componentes prontos, ele oferece pequenas classes <strong>de propósito único</strong>:</p> <table> <thead> <tr> <th>Propósito</th> <th>Classe Tailwind</th> <th>Efeito</th> </tr> </thead> <tbody> <tr> <td>Cor de fundo</td> <td><code class="language-plaintext highlighter-rouge">bg-blue-500</code></td> <td>Fundo azul médio</td> </tr> <tr> <td>Cor do texto</td> <td><code class="language-plaintext highlighter-rouge">text-gray-800</code></td> <td>Texto cinza escuro</td> </tr> <tr> <td>Tamanho do texto</td> <td><code class="language-plaintext highlighter-rouge">text-3xl</code></td> <td>Texto grande</td> </tr> <tr> <td>Espaçamento interno</td> <td><code class="language-plaintext highlighter-rouge">p-4</code></td> <td>Padding de 1rem</td> </tr> <tr> <td>Bordas arredondadas</td> <td><code class="language-plaintext highlighter-rouge">rounded-lg</code></td> <td>Cantos suavizados</td> </tr> <tr> <td>Sombra</td> <td><code class="language-plaintext highlighter-rouge">shadow-md</code></td> <td>Sombra média</td> </tr> </tbody> </table> <p>Essas classes se combinam livremente — é o padrão usado em todo o nosso <code class="language-plaintext highlighter-rouge">TodoLive</code>:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&lt;</span><span class="n">div</span> <span class="n">class</span><span class="o">=</span><span class="s2">"w-full max-w-lg mx-auto mt-12 p-6 bg-white rounded-lg shadow-md"</span><span class="o">&gt;</span>
  <span class="o">&lt;</span><span class="n">h1</span> <span class="n">class</span><span class="o">=</span><span class="s2">"text-3xl font-bold mb-6 text-center text-gray-800"</span><span class="o">&gt;</span>
    <span class="no">Minha</span> <span class="no">Lista</span> <span class="n">de</span> <span class="no">Tarefas</span> <span class="p">(</span><span class="n">com</span> <span class="no">DB</span><span class="n">!</span><span class="p">)</span>
  <span class="o">&lt;/</span><span class="n">h1</span><span class="o">&gt;</span>
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">w-full max-w-lg mx-auto</code> centraliza o bloco com largura máxima;</li> <li><code class="language-plaintext highlighter-rouge">bg-white rounded-lg shadow-md</code> cria um “cartão” branco com sombra;</li> <li><code class="language-plaintext highlighter-rouge">text-3xl font-bold text-gray-800</code> formata o título.</li> </ul> <h3 id="-passo-72-daisyui--o-tema-visual-do-tailwind">🌈 Passo 7.2: daisyUI — o “tema visual” do Tailwind</h3> <p>O <strong>daisyUI</strong> adiciona classes mais semânticas para componentes comuns. Observe o componente <code class="language-plaintext highlighter-rouge">&lt;.button&gt;</code> no arquivo <code class="language-plaintext highlighter-rouge">lib/elixir_todo_list_web/components/core_components.ex</code>:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">variants</span> <span class="o">=</span> <span class="p">%{</span><span class="s2">"primary"</span> <span class="o">=&gt;</span> <span class="s2">"btn-primary"</span><span class="p">,</span> <span class="no">nil</span> <span class="o">=&gt;</span> <span class="s2">"btn-primary btn-soft"</span><span class="p">}</span>
</code></pre></div></div> <p>As classes <code class="language-plaintext highlighter-rouge">btn</code>, <code class="language-plaintext highlighter-rouge">btn-primary</code> e <code class="language-plaintext highlighter-rouge">btn-soft</code> vêm do <strong>daisyUI</strong> — é por isso que escrevemos <code class="language-plaintext highlighter-rouge">&lt;.button variant="primary"&gt;</code> na Fase 4. Outros exemplos no mesmo arquivo: <code class="language-plaintext highlighter-rouge">alert-info</code>/<code class="language-plaintext highlighter-rouge">alert-error</code> (mensagens flash), <code class="language-plaintext highlighter-rouge">checkbox</code> e <code class="language-plaintext highlighter-rouge">input</code> (formulários).</p> <p>A grande vantagem: esses componentes <strong>obedecem ao tema ativo</strong> (claro, escuro, etc.) sem que você redefina cores manualmente.</p> <h3 id="-passo-73-os-temas-e-um-mistério-resolvido">🌗 Passo 7.3: Os Temas (e um mistério resolvido)</h3> <p>Faça um teste: se o seu sistema operacional estiver no <strong>modo escuro</strong>, abra a aplicação. Notou que partes da página (o fundo, o cabeçalho do <code class="language-plaintext highlighter-rouge">Layouts.app</code>, os inputs) ficam <strong>escuras</strong>, enquanto o nosso “cartão” continua branco (<code class="language-plaintext highlighter-rouge">bg-white</code>)? O contraste fica estranho.</p> <p><strong>O que está acontecendo?</strong> O Phoenix 1.8 já vem com <strong>dois temas daisyUI</strong> (um claro e um escuro) definidos no <code class="language-plaintext highlighter-rouge">assets/css/app.css</code>:</p> <div class="language-css highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">@plugin</span> <span class="s1">"../vendor/daisyui"</span> <span class="p">{</span>
  <span class="py">themes</span><span class="p">:</span> <span class="no">false</span><span class="p">;</span>
<span class="p">}</span>

<span class="k">@plugin</span> <span class="s1">"../vendor/daisyui-theme"</span> <span class="p">{</span>
  <span class="py">name</span><span class="p">:</span> <span class="s1">"dark"</span><span class="p">;</span>
  <span class="py">default</span><span class="p">:</span> <span class="no">false</span><span class="p">;</span>
  <span class="py">prefersdark</span><span class="p">:</span> <span class="no">true</span><span class="p">;</span>   <span class="c">/* 👈 segue a preferência do sistema! */</span>
  <span class="err">...</span>
<span class="p">}</span>

<span class="k">@plugin</span> <span class="s1">"../vendor/daisyui-theme"</span> <span class="p">{</span>
  <span class="py">name</span><span class="p">:</span> <span class="s1">"light"</span><span class="p">;</span>
  <span class="py">default</span><span class="p">:</span> <span class="no">true</span><span class="p">;</span>
  <span class="err">...</span>
<span class="p">}</span>
</code></pre></div></div> <p>E o <code class="language-plaintext highlighter-rouge">root.html.heex</code> inclui um pequeno script que aplica o tema conforme o atributo <code class="language-plaintext highlighter-rouge">data-theme</code> (ou a preferência do sistema, por causa do <code class="language-plaintext highlighter-rouge">prefersdark: true</code>).</p> <p><strong>Como forçar o tema claro</strong> (a opção mais simples para o nosso visual): abra</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>lib/elixir_todo_list_web/components/layouts/root.html.heex
</code></pre></div></div> <p>e adicione <code class="language-plaintext highlighter-rouge">data-theme="light"</code> ao <code class="language-plaintext highlighter-rouge">&lt;body&gt;</code>:</p> <p><em>Mude:</em></p> <div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;body&gt;</span>
  {@inner_content}
<span class="nt">&lt;/body&gt;</span>
</code></pre></div></div> <p><em>Para:</em></p> <div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nt">&lt;body</span> <span class="na">data-theme=</span><span class="s">"light"</span><span class="nt">&gt;</span>
  {@inner_content}
<span class="nt">&lt;/body&gt;</span>
</code></pre></div></div> <p>Pronto: a aplicação fica no tema claro para todos, independentemente da preferência do sistema.</p> <aside> <p>💡 <strong>Alternativa avançada:</strong> em vez de forçar o claro, você pode abraçar o modo escuro trocando as classes “fixas” do nosso card (<code class="language-plaintext highlighter-rouge">bg-white</code>, <code class="language-plaintext highlighter-rouge">text-gray-800</code>) por classes <strong>semânticas</strong> do daisyUI, que mudam com o tema: <code class="language-plaintext highlighter-rouge">bg-base-100</code>, <code class="language-plaintext highlighter-rouge">text-base-content</code>, etc. Fica como exercício!</p> </aside> <h3 id="-passo-74-exemplos-práticos-de-personalização">💡 Passo 7.4: Exemplos práticos de personalização</h3> <p><strong>Quer destacar mais as tarefas concluídas?</strong> Ajuste a classe condicional no <code class="language-plaintext highlighter-rouge">TodoLive</code>:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&lt;</span><span class="n">label</span> <span class="n">class</span><span class="o">=</span><span class="p">{</span>
  <span class="k">if</span> <span class="n">task</span><span class="o">.</span><span class="n">completed</span><span class="p">,</span>
    <span class="k">do</span><span class="p">:</span> <span class="s2">"line-through text-gray-400 italic"</span><span class="p">,</span>
    <span class="k">else</span><span class="p">:</span> <span class="s2">"text-gray-900 font-medium"</span>
<span class="p">}</span><span class="o">&gt;</span>
  <span class="p">{</span><span class="n">task</span><span class="o">.</span><span class="n">title</span><span class="p">}</span>
<span class="o">&lt;/</span><span class="n">label</span><span class="o">&gt;</span>
</code></pre></div></div> <p><strong>Quer um botão de exclusão mais discreto?</strong> É só trocar as classes:</p> <div class="language-elixir highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">&lt;.</span><span class="n">button</span>
  <span class="n">type</span><span class="o">=</span><span class="s2">"button"</span>
  <span class="n">phx</span><span class="o">-</span><span class="n">click</span><span class="o">=</span><span class="s2">"delete"</span>
  <span class="n">phx</span><span class="o">-</span><span class="n">value</span><span class="o">-</span><span class="n">id</span><span class="o">=</span><span class="p">{</span><span class="n">task</span><span class="o">.</span><span class="n">id</span><span class="p">}</span>
  <span class="n">class</span><span class="o">=</span><span class="s2">"!p-1 !bg-red-500 hover:!bg-red-700"</span>
<span class="o">&gt;</span>
  <span class="o">&amp;</span><span class="n">times</span><span class="p">;</span>
<span class="o">&lt;/.</span><span class="n">button</span><span class="o">&gt;</span>
</code></pre></div></div> <p>(O <code class="language-plaintext highlighter-rouge">!</code> prefixando a classe força a prioridade sobre o estilo padrão do componente <code class="language-plaintext highlighter-rouge">&lt;.button&gt;</code>.)</p> <p>Sem tocar em arquivos CSS, você molda toda a interface.</p> <h3 id="-passo-75-commit">💾 Passo 7.5: Commit</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"Fase 7: Ajusta o tema e personaliza o visual (Tailwind/daisyUI)"</span>
</code></pre></div></div> <hr/> <p><strong>Fim da Fase 7!</strong> 🏁</p> <p>O Tailwind (com daisyUI) é o que dá ao Phoenix sua <strong>agilidade visual</strong>: elimina o CSS manual, mantém o template declarativo e ainda permite personalização total. A partir daqui, você pode experimentar temas, criar componentes próprios e dar identidade à sua aplicação.</p> <p>Falta um último passo para o projeto ficar apresentável ao mundo: a <strong>📄 Fase 8: README e Entrega</strong>.</p> <hr/> <h1 id="-fase-8-readme-e-entrega">📄 Fase 8: README e Entrega</h1> <p><strong>Objetivo:</strong> transformar o <code class="language-plaintext highlighter-rouge">README.md</code> padrão do Phoenix em uma documentação de verdade e fazer o checklist final do repositório.</p> <p><strong>Por que fazemos isso?</strong> O README é a porta de entrada de qualquer repositório — o primeiro (às vezes o único) arquivo que um colega, recrutador ou avaliador vai ler. O critério de qualidade é simples: <em>alguém conseguiria clonar e rodar seu projeto lendo apenas o README?</em></p> <h3 id="-passo-81-atualizar-o-readmemd">📝 Passo 8.1: Atualizar o <code class="language-plaintext highlighter-rouge">README.md</code></h3> <p>O <code class="language-plaintext highlighter-rouge">phx.new</code> já gerou um <code class="language-plaintext highlighter-rouge">README.md</code> genérico na raiz do projeto. Vamos substituí-lo por um personalizado. Adapte o modelo (nome, links, o que quiser):</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gh"># Todo List — Elixir + Phoenix LiveView</span>

<span class="gs">**Aluno(a):**</span> Seu Nome Completo Aqui

<span class="gs">**Tutorial original:**</span> <span class="p">[</span><span class="nv">Como Criar um App "Todo List" com Elixir e LiveView
do Zero</span><span class="p">](</span><span class="sx">https://profsergiocosta.notion.site/Como-Criar-um-App-Todo-List-com-Elixir-e-LiveView-do-Zero-2a8cce97509380eba53fc82bbeb08435</span><span class="p">)</span>

<span class="gu">## Descrição</span>

Aplicação de lista de tarefas (Todo List) construída de forma incremental
para estudar a arquitetura <span class="gs">**full-stack funcional reativa**</span> do
<span class="p">[</span><span class="nv">Phoenix LiveView</span><span class="p">](</span><span class="sx">https://hexdocs.pm/phoenix_live_view</span><span class="p">)</span>:
<span class="p">
-</span> <span class="gs">**Framework:**</span> Phoenix 1.8 + LiveView 1.1 — o estado vive no servidor e a
  interface atualiza em tempo real via WebSocket, sem JavaScript manual;
<span class="p">-</span> <span class="gs">**Persistência:**</span> <span class="p">[</span><span class="nv">Ecto</span><span class="p">](</span><span class="sx">https://hexdocs.pm/ecto</span><span class="p">)</span> + SQLite (schema,
  migrations e validação com changesets);
<span class="p">-</span> <span class="gs">**Visual:**</span> Tailwind CSS v4 + daisyUI.

Funcionalidades: criar tarefas (com validação), listar, marcar como
concluída (toggle) e excluir — tudo com atualização instantânea.

<span class="gu">## Pré-requisitos</span>

| Ferramenta | Versão    |
| ---------- | --------- |
| Erlang/OTP | 26+       |
| Elixir     | 1.17+     |
| Node.js    | 18+ (LTS) |

<span class="gu">## Como Rodar</span>

<span class="p">```</span><span class="nl">bash
</span>git clone https://github.com/SEU-USUARIO/SEU-REPO.git
<span class="nb">cd </span>SEU-REPO

<span class="c"># Instala dependências e prepara os assets</span>
mix setup

<span class="c"># Cria o banco SQLite e aplica as migrations</span>
mix ecto.create
mix ecto.migrate

<span class="c"># Sobe o servidor</span>
mix phx.server
<span class="p">```</span>

Depois, abra <span class="p">[</span><span class="nv">http://localhost:4000</span><span class="p">](</span><span class="sx">http://localhost:4000</span><span class="p">)</span> no navegador.

<span class="gu">## Estrutura Principal</span>

| Arquivo                                      | Papel                               |
| -------------------------------------------- | ----------------------------------- |
| <span class="sb">`lib/elixir_todo_list_web/live/todo_live.ex`</span> | O LiveView (estado, eventos e HTML) |
| <span class="sb">`lib/elixir_todo_list/task.ex`</span>               | O schema <span class="sb">`Task`</span> e seu changeset     |
| <span class="sb">`lib/elixir_todo_list/repo.ex`</span>               | A conexão com o banco (Ecto.Repo)   |
| <span class="sb">`priv/repo/migrations/`</span>                      | As migrations do banco              |
</code></pre></div></div> <h3 id="-passo-82-o-teste-do-colega">🧪 Passo 8.2: O Teste do “Colega”</h3> <p>Faça de conta que você é outra pessoa: clone o seu repositório em uma <strong>pasta nova</strong> (ex: <code class="language-plaintext highlighter-rouge">/tmp/teste-todo</code>) e siga o seu próprio README, literalmente. A aplicação subiu? O CRUD funciona? Os dados persistem após reiniciar o servidor?</p> <p>Esse teste revela os dois vacilos clássicos:</p> <ol> <li> <p><strong>Banco commitado</strong>: se <code class="language-plaintext highlighter-rouge">git status</code>/a página do GitHub mostrar <code class="language-plaintext highlighter-rouge">elixir_todo_list.db</code> (ou <code class="language-plaintext highlighter-rouge">.db-shm</code>/<code class="language-plaintext highlighter-rouge">.db-wal</code>), o <code class="language-plaintext highlighter-rouge">.gitignore</code> da Fase 0 não foi aplicado antes do commit. Corrija com:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git <span class="nb">rm</span> <span class="nt">--cached</span> elixir_todo_list.db elixir_todo_list.db-shm elixir_todo_list.db-wal
git commit <span class="nt">-m</span> <span class="s2">"Remove arquivos de banco do versionamento"</span>
</code></pre></div> </div> </li> <li> <p><strong>Passo de setup faltando</strong> no README (ex: esquecer o <code class="language-plaintext highlighter-rouge">mix ecto.migrate</code> — sem ele, a aplicação sobe mas quebra ao carregar as tarefas).</p> </li> </ol> <h3 id="-passo-83-commit-final">💾 Passo 8.3: Commit Final</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add README.md
git commit <span class="nt">-m</span> <span class="s2">"Fase 8: Atualiza README com instruções de execução"</span>
</code></pre></div></div> <h3 id="-passo-84-checklist-final">✅ Passo 8.4: Checklist Final</h3> <p>Confira o histórico:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span>
</code></pre></div></div> <p>Esperado (de cima para baixo):</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Fase 8: Atualiza README com instruções de execução
Fase 7: Ajusta o tema e personaliza o visual (Tailwind/daisyUI)
Fase 6: Implementa conclusão de tarefas (toggle_complete)
Fase 5: Implementa exclusão de tarefas (delete)
Fase 4: Refatora TodoLive para usar Ecto, Repo e to_form()
Fase 3: Persistência - Configura Ecto, Repo, Migrations e Task Schema
Fase 2: Lógica em Memória - Implementa adição de tarefas
Fase 1: Prova de Vida - Substitui a rota raiz por TodoLive
Fase 0: Gera o esqueleto do Phoenix com LiveView (sem Ecto)
Fase 0: Inicializa o repositório e .gitignore
</code></pre></div></div> <p><strong>Checklist:</strong></p> <ul class="task-list"> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>O repositório no GitHub é público e o <code class="language-plaintext highlighter-rouge">git push</code> foi feito?</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Nenhum arquivo <code class="language-plaintext highlighter-rouge">*.db</code>, <code class="language-plaintext highlighter-rouge">*.db-shm</code> ou <code class="language-plaintext highlighter-rouge">*.db-wal</code> aparece no GitHub?</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>As pastas <code class="language-plaintext highlighter-rouge">_build/</code> e <code class="language-plaintext highlighter-rouge">deps/</code> também não aparecem?</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>O README tem nome, link do tutorial, descrição e instruções que funcionam?</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>O CRUD completo funciona e os dados sobrevivem ao restart do servidor?</li> </ul> <hr/> <h2 id="-tutorial-concluído">🚀 Tutorial Concluído!</h2> <p><strong>Parabéns!</strong> 🥳 Você construiu uma aplicação web <strong>full-stack funcional e reativa</strong> — e, se fez também o tutorial de Clojure, agora viu <strong>o mesmo problema resolvido por duas filosofias</strong>:</p> <table> <thead> <tr> <th> </th> <th>Clojure</th> <th>Elixir</th> </tr> </thead> <tbody> <tr> <td>Onde vive o estado reativo</td> <td>No navegador (<code class="language-plaintext highlighter-rouge">r/atom</code>)</td> <td>No servidor (socket/<code class="language-plaintext highlighter-rouge">assigns</code>)</td> </tr> <tr> <td>Como a UI conversa com os dados</td> <td>API REST + JSON + CORS</td> <td>WebSocket interno (<code class="language-plaintext highlighter-rouge">phx-*</code>)</td> </tr> <tr> <td>Onde a validação acontece</td> <td>No handler, “à mão”</td> <td>No changeset, declarativa</td> </tr> <tr> <td>Quantos servidores em dev</td> <td>Dois (API + shadow-cljs)</td> <td>Um (<code class="language-plaintext highlighter-rouge">mix phx.server</code>)</td> </tr> </tbody> </table> <p><strong>Quer ir além?</strong> Algumas extensões (opcionais):</p> <ul> <li>Um filtro “Todas / Ativas / Concluídas” (um <code class="language-plaintext highlighter-rouge">assign</code> + cláusulas de <code class="language-plaintext highlighter-rouge">handle_event</code>);</li> <li>Edição do título de uma tarefa (outro pequeno <code class="language-plaintext highlighter-rouge">&lt;.form&gt;</code> por item);</li> <li>Um contador “X de Y concluídas” no topo;</li> <li>Trocar o SQLite por PostgreSQL (<code class="language-plaintext highlighter-rouge">ecto_sqlite3</code> → <code class="language-plaintext highlighter-rouge">postgrex</code>, e o adapter no Repo);</li> <li>Explorar o <strong>Phoenix PubSub</strong> para que duas abas do navegador vejam as mudanças uma da outra em tempo real — a “mágica” concorrente da BEAM em ação.</li> </ul>]]></content><author><name></name></author><category term="tutorial"/><category term="elixir"/><category term="phoenix"/><category term="liveview"/><category term="ecto"/><category term="sqlite"/><category term="functional-programming"/><summary type="html"><![CDATA[Um guia prático e completo do ecossistema Phoenix, explorando schemas, migrations, changesets e a reatividade em tempo real com LiveView.]]></summary></entry><entry><title type="html">Clojure e ClojureScript: Construindo uma Aplicação Todo List do Zero</title><link href="https://lambdageo.github.io//blog/tutorial-clojure-clojurescript-todo-list/" rel="alternate" type="text/html" title="Clojure e ClojureScript: Construindo uma Aplicação Todo List do Zero"/><published>2026-07-11T13:00:00+00:00</published><updated>2026-07-11T13:00:00+00:00</updated><id>https://lambdageo.github.io//blog/tutorial-clojure-todo-list</id><content type="html" xml:base="https://lambdageo.github.io//blog/tutorial-clojure-clojurescript-todo-list/"><![CDATA[<p>Olá e bem-vindo(a) a este guia prático.</p> <p>O objetivo aqui é construir juntos uma aplicação <strong>Todo List completa</strong>, indo de um repositório Git vazio até um <strong>projeto full-stack funcional</strong>, usando o ecossistema <strong>Clojure</strong> moderno.</p> <p>Mais do que um simples tutorial de “copiar e colar”, este guia foi pensado para <strong>ensinar arquitetura</strong> — passo a passo, com atenção ao raciocínio funcional e à depuração de problemas reais.</p> <p>Não vamos apenas construir uma aplicação: vamos entender <strong>por que ela funciona</strong> e <strong>por que ela quebra</strong>, explorando erros típicos (como CORS, formatos de dados incompatíveis e sincronização de estado) e aprendendo a corrigi-los com clareza.</p> <p>Usaremos o clássico aplicativo <strong>Todo List</strong> como exemplo, pois sua simplicidade nos permite concentrar no que realmente importa: <strong>a arquitetura e a interação entre as partes de um sistema reativo</strong>.</p> <hr/> <h3 id="-o-que-vamos-construir">🧱 O que Vamos Construir</h3> <ul> <li><strong>Backend:</strong> Clojure, com <strong>Ring</strong>, <strong>Reitit</strong> e <strong>next.jdbc</strong>;</li> <li><strong>Frontend:</strong> ClojureScript, com <strong>Reagent 2.0 (React 19)</strong> e <strong>Shadow-CLJS</strong>;</li> <li><strong>Banco de Dados:</strong> <strong>SQLite</strong>, para persistência real.</li> </ul> <p>Seguiremos uma jornada incremental:</p> <ol> <li><strong>Fundação:</strong> verificação do ambiente, Git e <code class="language-plaintext highlighter-rouge">.gitignore</code>.</li> <li><strong>Backend mínimo:</strong> um servidor “Hello World”.</li> <li><strong>Banco em memória:</strong> criando e lendo tarefas com um <code class="language-plaintext highlighter-rouge">atom</code>.</li> <li><strong>Frontend reativo isolado:</strong> interface dinâmica com Reagent.</li> <li><strong>Integração full-stack:</strong> comunicação via API REST, lidando com CORS e formato de dados.</li> <li><strong>Banco real:</strong> migrando para SQLite com <code class="language-plaintext highlighter-rouge">next.jdbc</code>.</li> <li><strong>CRUD completo:</strong> marcar tarefas como concluídas (<strong>Update</strong>) e removê-las (<strong>Delete</strong>), com um visual melhorado.</li> <li><strong>Documentação:</strong> um <code class="language-plaintext highlighter-rouge">README.md</code> profissional e a preparação para a entrega.</li> </ol> <p>Ao final, você compreenderá <strong>como os componentes de um sistema Clojure moderno se encaixam</strong>, dominando o fluxo entre estado, renderização e persistência.</p> <hr/> <h3 id="️-a-arquitetura-mapa-mental">🗺️ A Arquitetura (Mapa Mental)</h3> <p>Durante quase todo o tutorial, você trabalhará com <strong>dois terminais abertos ao mesmo tempo</strong>:</p> <table> <thead> <tr> <th>Terminal</th> <th>Comando</th> <th>O que roda</th> <th>Porta</th> </tr> </thead> <tbody> <tr> <td><strong>Terminal 1 (Backend)</strong></td> <td><code class="language-plaintext highlighter-rouge">clj -M:run</code></td> <td>A API REST (Jetty + Reitit)</td> <td><code class="language-plaintext highlighter-rouge">3000</code></td> </tr> <tr> <td><strong>Terminal 2 (Frontend)</strong></td> <td><code class="language-plaintext highlighter-rouge">npx shadow-cljs watch app</code></td> <td>O compilador CLJS + servidor de desenvolvimento</td> <td><code class="language-plaintext highlighter-rouge">8000</code></td> </tr> </tbody> </table> <p>O navegador acessa sempre o <strong>frontend</strong> (<code class="language-plaintext highlighter-rouge">http://localhost:8000</code>), e o frontend conversa com o <strong>backend</strong> (<code class="language-plaintext highlighter-rouge">http://localhost:3000/api/...</code>) via <code class="language-plaintext highlighter-rouge">fetch</code>.</p> <aside> <p>💡 Guarde esta tabela. A confusão mais comum do tutorial é acessar a porta errada ou esquecer que um dos dois servidores precisa estar rodando (ou precisa ser <strong>reiniciado</strong> após mudar o <code class="language-plaintext highlighter-rouge">deps.edn</code>).</p> </aside> <hr/> <h3 id="-versões-utilizadas-importante">📌 Versões Utilizadas (Importante!)</h3> <p>Para garantir que o tutorial seja reprodutível, <strong>fixamos todas as versões</strong>. Use exatamente estas — misturar versões (principalmente do <code class="language-plaintext highlighter-rouge">shadow-cljs</code>) é a causa nº 1 de erros difíceis de diagnosticar.</p> <table> <thead> <tr> <th>Ferramenta / Biblioteca</th> <th>Versão</th> </tr> </thead> <tbody> <tr> <td>Java (JDK)</td> <td>11 ou superior (17 ou 21 recomendado)</td> </tr> <tr> <td>Clojure CLI (<code class="language-plaintext highlighter-rouge">clj</code> / <code class="language-plaintext highlighter-rouge">clojure</code>)</td> <td>1.11+</td> </tr> <tr> <td>Node.js</td> <td>18 ou superior</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">shadow-cljs</code> (npm <strong>e</strong> <code class="language-plaintext highlighter-rouge">deps.edn</code>)</td> <td><code class="language-plaintext highlighter-rouge">2.28.23</code> — <strong>a mesma nos dois lugares!</strong></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">reagent/reagent</code></td> <td><code class="language-plaintext highlighter-rouge">2.0.0</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">react</code> / <code class="language-plaintext highlighter-rouge">react-dom</code> (npm)</td> <td><code class="language-plaintext highlighter-rouge">19.2.0</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ring</code> / <code class="language-plaintext highlighter-rouge">ring-jetty-adapter</code></td> <td><code class="language-plaintext highlighter-rouge">1.12.2</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">metosin/reitit-ring</code></td> <td><code class="language-plaintext highlighter-rouge">0.7.0</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ring/ring-json</code></td> <td><code class="language-plaintext highlighter-rouge">0.5.1</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ring-cors/ring-cors</code></td> <td><code class="language-plaintext highlighter-rouge">0.1.13</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">seancorfield/next.jdbc</code></td> <td><code class="language-plaintext highlighter-rouge">1.2.659</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">org.xerial/sqlite-jdbc</code></td> <td><code class="language-plaintext highlighter-rouge">3.45.3.0</code></td> </tr> </tbody> </table> <blockquote> <p>[!NOTE] Em sistemas operacionais onde a ferramenta <code class="language-plaintext highlighter-rouge">rlwrap</code> não estiver instalada, o comando de atalho <code class="language-plaintext highlighter-rouge">clj</code> pode falhar com o erro: <code class="language-plaintext highlighter-rouge">Please install rlwrap for command editing or use "clojure" instead.</code> Se isso acontecer no seu ambiente, basta substituir todas as chamadas a <code class="language-plaintext highlighter-rouge">clj</code> do tutorial pela palavra <code class="language-plaintext highlighter-rouge">clojure</code> (ex: use <code class="language-plaintext highlighter-rouge">clojure -M:run</code> em vez de <code class="language-plaintext highlighter-rouge">clj -M:run</code>, e <code class="language-plaintext highlighter-rouge">clojure</code> para abrir o REPL). Ambos executam exatamente a mesma engine Clojure CLI, mas o comando <code class="language-plaintext highlighter-rouge">clojure</code> funciona sem depender do editor de linha de comando <code class="language-plaintext highlighter-rouge">rlwrap</code>.</p> </blockquote> <hr/> <h3 id="-os-marcos-do-git-seu-histórico-final">🧾 Os Marcos do Git (Seu Histórico Final)</h3> <p>Este tutorial é também um exercício de <strong>desenvolvimento incremental com Git</strong>. Ao final, seu <code class="language-plaintext highlighter-rouge">git log --oneline</code> deve contar esta história (do mais recente para o mais antigo):</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docs: adiciona README com instruções de execução
feat(crud): implementa funcionalidades de toggle e delete
refactor(db): substitui banco em memória por persistência SQLite
feat: conecta frontend com API do backend (CORS corrigido)
feat: implementa UI do frontend com estado local (sem API)
feat: implementa API REST de 'todos' com banco em memória
feat: implementa servidor 'Hello World' com Jetty e Reitit
feat: setup inicial do projeto com .gitignore
</code></pre></div></div> <p>Cada fase termina com um <strong>Git Checkpoint</strong> que gera exatamente um desses commits.</p> <hr/> <h3 id="-índice-das-fases">📚 Índice das Fases</h3> <ul> <li><strong>Fase 0:</strong> A Fundação (Ambiente, Setup e Git)</li> <li><strong>Fase 1:</strong> O Backend Mínimo (Servidor “Hello World”)</li> <li><strong>Fase 2:</strong> O Backend Funcional (API com Banco em Memória)</li> <li><strong>Fase 3:</strong> Introdução ao Frontend (Reagent e Shadow-CLJS)</li> <li><strong>Fase 4:</strong> Conectando o Frontend ao Backend (CORS e <code class="language-plaintext highlighter-rouge">fetch</code>)</li> <li><strong>Fase 5:</strong> Persistência Real (SQLite com <code class="language-plaintext highlighter-rouge">next.jdbc</code>)</li> <li><strong>Fase 6:</strong> 🏆 CRUD Completo — “Marcar como Feito”, “Deletar” e o Visual Final</li> <li><strong>Fase 7:</strong> README e Entrega</li> </ul> <p>Boa jornada! 🚀</p> <hr/> <h1 id="fase-0-a-fundação-ambiente-setup-e-git">Fase 0: A Fundação (Ambiente, Setup e Git)</h1> <p><strong>Objetivo:</strong> Garantir que sua máquina tem todas as ferramentas necessárias, criar a pasta do projeto e iniciar o controle de versão com um <code class="language-plaintext highlighter-rouge">.gitignore</code> correto.</p> <h3 id="passo-01-verificar-o-ambiente-pré-requisitos">Passo 0.1: Verificar o Ambiente (Pré-requisitos)</h3> <p><strong>Por que fazemos isso?</strong> Nada é mais frustrante do que descobrir, no meio da Fase 3, que o Node.js não está instalado. Vamos verificar tudo <strong>agora</strong>, em 30 segundos.</p> <p><strong>Ação:</strong> Abra seu terminal e execute os comandos abaixo, um por um:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>java <span class="nt">-version</span>         <span class="c"># Precisa ser 11 ou superior (17 ou 21 recomendado)</span>
clj <span class="nt">--version</span>         <span class="c"># O Clojure CLI (qualquer versão 1.11+).</span>
                      <span class="c"># Se falhar reclamando de rlwrap, use: clojure --version</span>
node <span class="nt">-v</span>               <span class="c"># Precisa ser 18 ou superior</span>
git <span class="nt">--version</span>         <span class="c"># Qualquer versão recente</span>
</code></pre></div></div> <blockquote> <p>[!NOTE] <strong>Sobre o clj vs. clojure:</strong> O utilitário <code class="language-plaintext highlighter-rouge">clj</code> é apenas um script wrapper que tenta executar o Clojure em conjunto com a ferramenta de edição de linha de comando <code class="language-plaintext highlighter-rouge">rlwrap</code>. Em alguns sistemas, se o <code class="language-plaintext highlighter-rouge">rlwrap</code> não estiver instalado, rodar <code class="language-plaintext highlighter-rouge">clj</code> resultará em um erro como: <code class="language-plaintext highlighter-rouge">Please install rlwrap for command editing or use "clojure" instead.</code> Se for o seu caso, não se preocupe! Você não precisa instalar nada a mais. Basta usar o comando <code class="language-plaintext highlighter-rouge">clojure</code> no lugar de <code class="language-plaintext highlighter-rouge">clj</code> em todo este tutorial (ex: <code class="language-plaintext highlighter-rouge">clojure --version</code> para testar a versão, <code class="language-plaintext highlighter-rouge">clojure -M:run</code> para rodar o backend e <code class="language-plaintext highlighter-rouge">clojure</code> para REPL). Ambos executam exatamente o mesmo compilador.</p> </blockquote> <p><strong>Resultado Esperado:</strong> Cada comando deve imprimir uma versão. Se algum deles der <code class="language-plaintext highlighter-rouge">command not found</code>:</p> <ul> <li><strong>Java:</strong> instale um JDK (ex: <a href="https://adoptium.net/">Adoptium/Temurin</a>).</li> <li><strong>Clojure CLI:</strong> siga o guia oficial em <a href="https://clojure.org/guides/install_clojure">clojure.org/guides/install_clojure</a>.</li> <li><strong>Node.js:</strong> instale a versão LTS em <a href="https://nodejs.org/">nodejs.org</a>.</li> <li><strong>Git:</strong> <a href="https://git-scm.com/">git-scm.com</a>.</li> </ul> <aside> <p>💡 O <code class="language-plaintext highlighter-rouge">npm</code> (gerenciador de pacotes do Node) vem junto com o Node.js. Você pode confirmar com <code class="language-plaintext highlighter-rouge">npm -v</code>.</p> </aside> <h3 id="passo-02-criar-a-estrutura-de-pastas">Passo 0.2: Criar a Estrutura de Pastas</h3> <p>Primeiro, vamos criar um diretório principal para o projeto e navegar para dentro dele.</p> <p><strong>Ação:</strong> No seu terminal, execute:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir </span>todo-app
<span class="nb">cd </span>todo-app
</code></pre></div></div> <p>Agora, você deve estar dentro da pasta <code class="language-plaintext highlighter-rouge">todo-app/</code>. Esta será a “raiz” (root) de todo o nosso projeto, onde colocaremos o <code class="language-plaintext highlighter-rouge">deps.edn</code>, o <code class="language-plaintext highlighter-rouge">.gitignore</code> e tudo mais.</p> <aside> <p>⚠️ <strong>Todos os comandos deste tutorial são executados a partir desta pasta raiz</strong> (<code class="language-plaintext highlighter-rouge">todo-app/</code>), a menos que digamos o contrário. Se um comando falhar com “arquivo não encontrado”, a primeira coisa a verificar é: <em>estou na pasta certa?</em> (use <code class="language-plaintext highlighter-rouge">pwd</code> para conferir).</p> </aside> <h3 id="passo-03-iniciar-o-git">Passo 0.3: Iniciar o Git</h3> <p><strong>Por que fazemos isso?</strong> O Git é nosso sistema de controle de versão. Pense nele como uma “máquina do tempo” para o nosso código. Ele nos permite salvar “fotos” (chamadas <em>commits</em>) do nosso projeto à medida que avançamos. Se algo quebrar, podemos facilmente voltar para uma versão que funcionava.</p> <p><strong>Ação:</strong> Dentro da pasta <code class="language-plaintext highlighter-rouge">todo-app/</code>, execute:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git init
git branch <span class="nt">-m</span> main
</code></pre></div></div> <p><strong>O que vai acontecer?</strong> Você verá uma resposta parecida com: <code class="language-plaintext highlighter-rouge">Initialized empty Git repository in /path/to/your/todo-app/.git/</code></p> <p>O Git criou um subdiretório oculto chamado <code class="language-plaintext highlighter-rouge">.git</code>. É ali que ele armazena todo o histórico. Você não precisa (e geralmente não deve) mexer nesse diretório diretamente. O segundo comando apenas renomeia a branch principal para <code class="language-plaintext highlighter-rouge">main</code> (o padrão moderno).</p> <h3 id="passo-04-criar-o-gitignore">Passo 0.4: Criar o <code class="language-plaintext highlighter-rouge">.gitignore</code></h3> <p><strong>Por que fazemos isso?</strong> O Git agora está observando <em>tudo</em> em sua pasta. Mas não queremos salvar tudo. Existem arquivos que <strong>não</strong> devem ir para o controle de versão:</p> <ol> <li><strong>Dependências:</strong> pastas como <code class="language-plaintext highlighter-rouge">node_modules/</code> podem ter milhares de arquivos. Elas podem ser reinstaladas a qualquer momento.</li> <li><strong>Arquivos compilados:</strong> o Clojure e o shadow-cljs criam pastas de “saída” (como <code class="language-plaintext highlighter-rouge">target/</code> ou <code class="language-plaintext highlighter-rouge">resources/public/js/</code>). Nosso código-fonte é o que importa; o código compilado é apenas um resultado.</li> <li><strong>Arquivos de sistema/IDE:</strong> seu sistema operacional ou sua IDE podem criar “lixo” (como <code class="language-plaintext highlighter-rouge">.DS_Store</code> ou <code class="language-plaintext highlighter-rouge">.calva/</code>).</li> <li><strong>Dados gerados pela aplicação:</strong> na Fase 5, nossa aplicação criará um arquivo de banco de dados (<code class="language-plaintext highlighter-rouge">prod.db</code>). Ele é <em>resultado</em> da aplicação rodando, não código-fonte — já vamos deixá-lo ignorado desde agora.</li> <li><strong>Segredos:</strong> se um dia você tiver uma chave de API ou senha, ela também iria para o <code class="language-plaintext highlighter-rouge">.gitignore</code> para <em>nunca</em> ser enviada ao GitHub.</li> </ol> <p><strong>Ação:</strong> Crie um novo arquivo na raiz do projeto chamado <code class="language-plaintext highlighter-rouge">.gitignore</code> (começando com um ponto) e cole o seguinte conteúdo:</p> <pre><code class="language-gitignore"># --- Geral ---
# Arquivos de sistema operacional
.DS_Store
Thumbs.db
*.log

# --- Dependências ---
# Dependências do Node.js (para shadow-cljs)
/node_modules/

# --- Clojure &amp; Java ---
# Pasta de build padrão
/target/

# Cache de dependências do Clojure CLI
.cpcache/
.clj-kondo/
.cider-repl-history

# --- shadow-cljs (Frontend) ---
# Saída do build do frontend
/resources/public/js/

# Cache do shadow-cljs
.shadow-cljs/

# --- IDEs ---
# VS Code
.vscode/

# Emacs
*~
\#*\#

# Calva (VS Code Clojure)
.calva/

# --- Banco de Dados (usado a partir da Fase 5) ---
*.db
*.sqlite
*.sqlite3
</code></pre> <h3 id="o-que-fizemos">O que fizemos?</h3> <p>Instruímos o Git a ignorar as pastas e arquivos mais comuns de um projeto Clojure/ClojureScript — <strong>incluindo, desde já, o arquivo do banco de dados</strong> que só aparecerá na Fase 5. Assim ninguém commita o <code class="language-plaintext highlighter-rouge">prod.db</code> por acidente.</p> <p>Agora, quando você executar <code class="language-plaintext highlighter-rouge">git status</code>, verá seu novo arquivo <code class="language-plaintext highlighter-rouge">.gitignore</code>, mas não verá nenhuma das pastas listadas (mesmo que elas existam).</p> <h3 id="passo-05-o-primeiro-commit">Passo 0.5: O Primeiro Commit</h3> <p><strong>Por que fazemos isso?</strong> Até agora, criamos um arquivo (<code class="language-plaintext highlighter-rouge">.gitignore</code>) e o Git sabe que ele existe, mas ele não foi “salvo” na nossa linha do tempo.</p> <p>O processo no Git é sempre em duas etapas:</p> <ol> <li><strong>Stage (Preparar):</strong> você diz ao Git quais arquivos quer incluir na próxima “foto”. O comando é <code class="language-plaintext highlighter-rouge">git add</code>.</li> <li><strong>Commit (Salvar):</strong> você tira a “foto” de todos os arquivos preparados e anexa uma mensagem. O comando é <code class="language-plaintext highlighter-rouge">git commit</code>.</li> </ol> <p><strong>Ação:</strong> Execute estes dois comandos, um após o outro:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. Adiciona TODOS os arquivos novos ou modificados na área de "Stage"</span>
<span class="c">#    (neste caso, apenas o .gitignore)</span>
git add <span class="nb">.</span>

<span class="c"># 2. Salva (faz o commit) os arquivos que estão em "Stage"</span>
<span class="c">#    -m "..." é a mensagem que descreve o que fizemos</span>
git commit <span class="nt">-m</span> <span class="s2">"feat: setup inicial do projeto com .gitignore"</span>
</code></pre></div></div> <p><strong>Resultado Esperado:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[main (root-commit) a1b2c3d] feat: setup inicial do projeto com .gitignore
 1 file changed, 40 insertions(+)
 create mode 100644 .gitignore
</code></pre></div></div> <aside> <p>💡 <strong>Se o Git reclamar de identidade</strong> (<code class="language-plaintext highlighter-rouge">Please tell me who you are</code>), configure uma vez:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git config <span class="nt">--global</span> user.name <span class="s2">"Seu Nome"</span>
git config <span class="nt">--global</span> user.email <span class="s2">"seu@email.com"</span>
</code></pre></div> </div> <p>e repita o <code class="language-plaintext highlighter-rouge">git commit</code>.</p> </aside> <h3 id="o-que-fizemos-1">O que fizemos?</h3> <p>Salvamos a “Versão Zero” do nosso projeto. Você pode executar <code class="language-plaintext highlighter-rouge">git log</code> a qualquer momento para ver o histórico.</p> <p><strong>Sobre a Mensagem de Commit (<code class="language-plaintext highlighter-rouge">feat: ...</code>):</strong> A mensagem <code class="language-plaintext highlighter-rouge">feat: setup inicial...</code> segue uma convenção chamada <strong>Conventional Commits</strong>:</p> <ul> <li><code class="language-plaintext highlighter-rouge">feat:</code> significa <em>feature</em> (uma nova funcionalidade — neste caso, o próprio setup).</li> <li>Outros prefixos comuns: <code class="language-plaintext highlighter-rouge">fix:</code> (corrige um bug), <code class="language-plaintext highlighter-rouge">docs:</code> (documentação), <code class="language-plaintext highlighter-rouge">refactor:</code> (muda o código sem mudar o comportamento) e <code class="language-plaintext highlighter-rouge">style:</code> (formatação/visual).</li> <li>Usar isso torna seu histórico Git muito fácil de ler — e é <strong>exatamente o que será avaliado</strong> no seu repositório.</li> </ul> <hr/> <p><strong>Fim da Fase 0!</strong> 🏁</p> <p>Temos uma fundação sólida: ambiente verificado, uma pasta de projeto limpa, um repositório Git rastreando nossas mudanças e um <code class="language-plaintext highlighter-rouge">.gitignore</code> para manter o “lixo” do lado de fora.</p> <hr/> <h1 id="fase-1-o-backend-mínimo-servidor-hello-world">Fase 1: O Backend Mínimo (Servidor “Hello World”)</h1> <p><strong>Objetivo:</strong> Fazer um servidor web subir, rodar na sua máquina e responder “Hello, World!” quando acessado por uma URL. Isso prova que nossa configuração base está correta.</p> <h3 id="passo-11-o-depsedn-a-lista-de-compras-do-backend">Passo 1.1: O <code class="language-plaintext highlighter-rouge">deps.edn</code> (A “Lista de Compras” do Backend)</h3> <p><strong>O que é o <code class="language-plaintext highlighter-rouge">deps.edn</code>?</strong> Pense neste arquivo como a “lista de compras” do seu projeto. Ele diz ao Clojure (<code class="language-plaintext highlighter-rouge">clj</code>) quais bibliotecas (dependências) ele precisa baixar da internet para o projeto funcionar.</p> <p>Ele também define “apelidos” (<em>aliases</em>), que são atalhos para comandos que usamos com frequência, como “rodar o servidor”.</p> <p><strong>Ação:</strong> Crie o arquivo <code class="language-plaintext highlighter-rouge">deps.edn</code> na raiz do projeto (<code class="language-plaintext highlighter-rouge">todo-app/</code>) e cole o seguinte conteúdo:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="no">:paths</span><span class="w"> </span><span class="p">[</span><span class="s">"src"</span><span class="w"> </span><span class="s">"resources"</span><span class="p">]</span><span class="w"> </span><span class="c1">;; 1. Onde nosso código-fonte vai ficar</span><span class="w">

 </span><span class="no">:deps</span><span class="w"> </span><span class="c1">;; 2. Nossa "lista de compras" de bibliotecas</span><span class="w">
 </span><span class="p">{</span><span class="c1">;; O próprio Clojure</span><span class="w">
  </span><span class="n">org.clojure/clojure</span><span class="w"> </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"1.11.1"</span><span class="p">}</span><span class="w">

  </span><span class="c1">;; --- Dependências do Backend (API REST) ---</span><span class="w">
  </span><span class="c1">;; O "motor" do servidor web (Jetty) e as bibliotecas base do Ring</span><span class="w">
  </span><span class="n">ring/ring-core</span><span class="w">          </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"1.12.2"</span><span class="p">}</span><span class="w">
  </span><span class="n">ring/ring-jetty-adapter</span><span class="w"> </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"1.12.2"</span><span class="p">}</span><span class="w">

  </span><span class="c1">;; A biblioteca de roteamento (para definir as URLs)</span><span class="w">
  </span><span class="n">metosin/reitit-ring</span><span class="w">     </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"0.7.0"</span><span class="p">}}</span><span class="w">

 </span><span class="no">:aliases</span><span class="w"> </span><span class="c1">;; 3. Nossos "atalhos" de comando</span><span class="w">
 </span><span class="p">{</span><span class="c1">;; O alias que usaremos para iniciar o servidor</span><span class="w">
  </span><span class="no">:run</span><span class="w">
  </span><span class="p">{</span><span class="no">:main-opts</span><span class="w"> </span><span class="p">[</span><span class="s">"-m"</span><span class="w"> </span><span class="s">"todo.backend.core"</span><span class="p">]}}}</span><span class="w">
</span></code></pre></div></div> <h3 id="o-que-fizemos-2">O que fizemos?</h3> <ol> <li><strong><code class="language-plaintext highlighter-rouge">:paths</code></strong>: dissemos ao Clojure para procurar nosso código nas pastas <code class="language-plaintext highlighter-rouge">src</code> e <code class="language-plaintext highlighter-rouge">resources</code> (que ainda vamos criar).</li> <li><strong><code class="language-plaintext highlighter-rouge">:deps</code></strong>: pedimos <em>apenas</em> as bibliotecas essenciais do backend: <ul> <li><code class="language-plaintext highlighter-rouge">ring/ring-jetty-adapter</code>: o servidor web que vai “ouvir” na <code class="language-plaintext highlighter-rouge">localhost:3000</code>.</li> <li><code class="language-plaintext highlighter-rouge">metosin/reitit-ring</code>: o “roteador” que olha a URL (ex: <code class="language-plaintext highlighter-rouge">/api/hello</code>) e decide qual função Clojure deve ser chamada.</li> <li><strong>Nota:</strong> ainda <em>não</em> adicionamos <code class="language-plaintext highlighter-rouge">shadow-cljs</code> ou <code class="language-plaintext highlighter-rouge">reagent</code>. Faremos isso só na Fase 3, para manter o backend limpo.</li> </ul> </li> <li><strong><code class="language-plaintext highlighter-rouge">:aliases</code></strong>: criamos o atalho <code class="language-plaintext highlighter-rouge">:run</code>. Quando rodarmos <code class="language-plaintext highlighter-rouge">clj -M:run</code>, ele executará a função principal (<code class="language-plaintext highlighter-rouge">-main</code>) do namespace <code class="language-plaintext highlighter-rouge">todo.backend.core</code> (que vamos criar a seguir).</li> </ol> <h3 id="passo-12-o-handler-mínimo-handlerclj">Passo 1.2: O Handler Mínimo (<code class="language-plaintext highlighter-rouge">handler.clj</code>)</h3> <p><strong>O que é um “Handler”?</strong> No mundo do <strong>Ring</strong> (a biblioteca base da web em Clojure), um <em>handler</em> é simplesmente uma <strong>função</strong> que segue um contrato:</p> <ol> <li>Ela recebe <strong>um</strong> argumento: um mapa <code class="language-plaintext highlighter-rouge">request</code> (com todos os dados da requisição HTTP que chegou).</li> <li>Ela retorna <strong>um</strong> valor: um mapa <code class="language-plaintext highlighter-rouge">response</code> (que descreve a resposta que queremos enviar de volta).</li> </ol> <p>Nosso objetivo é criar a <code class="language-plaintext highlighter-rouge">hello-handler</code> mais simples possível.</p> <p><strong>Ação 1: Criar os diretórios</strong></p> <p>O <code class="language-plaintext highlighter-rouge">deps.edn</code> diz ao Clojure para procurar código na pasta <code class="language-plaintext highlighter-rouge">src/</code>. Em Clojure, os namespaces são mapeados diretamente para a estrutura de pastas: <code class="language-plaintext highlighter-rouge">todo.backend.handler</code> deve viver no arquivo <code class="language-plaintext highlighter-rouge">src/todo/backend/handler.clj</code>.</p> <p>No seu terminal (dentro de <code class="language-plaintext highlighter-rouge">todo-app/</code>), execute:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> src/todo/backend
</code></pre></div></div> <ul> <li><code class="language-plaintext highlighter-rouge">mkdir</code> cria diretórios; a flag <code class="language-plaintext highlighter-rouge">-p</code> cria todos os “diretórios pais” necessários no caminho, sem dar erro.</li> </ul> <aside> <p>💡 <strong>O que é um Namespace (<code class="language-plaintext highlighter-rouge">ns</code>)?</strong></p> <p>Em Clojure, não “importamos arquivos”, nós “requeremos namespaces”. Um namespace é um nome para um grupo de códigos, diretamente ligado à estrutura de pastas e ao nome do arquivo:</p> <table> <thead> <tr> <th><strong>Caminho do Arquivo</strong></th> <th><strong>Declaração de Namespace (no topo do arquivo)</strong></th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">src/todo/backend/db.clj</code></td> <td><code class="language-plaintext highlighter-rouge">(ns todo.backend.db ...)</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">src/todo/backend/handler.clj</code></td> <td><code class="language-plaintext highlighter-rouge">(ns todo.backend.handler ...)</code></td> </tr> </tbody> </table> <p>Quando, em outro arquivo, quisermos usar as funções do <code class="language-plaintext highlighter-rouge">db.clj</code>, vamos “requerer” o namespace <code class="language-plaintext highlighter-rouge">todo.backend.db</code>, geralmente com um apelido (<em>alias</em>):</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.backend.handler</span><span class="w">
  </span><span class="p">(</span><span class="no">:require</span><span class="w"> </span><span class="p">[</span><span class="n">todo.backend.db</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">db</span><span class="p">]))</span><span class="w"> </span><span class="c1">;; "db" agora é o apelido</span><span class="w">
</span></code></pre></div> </div> <p>⚠️ <strong>Atenção a um detalhe que pega muita gente:</strong> se o namespace tem um hífen no nome (ex: <code class="language-plaintext highlighter-rouge">db-config</code>), o <strong>arquivo</strong> usa <em>underscore</em> (<code class="language-plaintext highlighter-rouge">db_config.clj</code>). Hífen no <code class="language-plaintext highlighter-rouge">ns</code>, underscore no nome do arquivo.</p> </aside> <p><strong>Ação 2: Criar o arquivo do handler</strong></p> <p>Crie o arquivo <code class="language-plaintext highlighter-rouge">src/todo/backend/handler.clj</code> e cole o seguinte código:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.backend.handler</span><span class="w">
  </span><span class="s">"Este namespace define nossas 'funções de resposta' (Handlers)."</span><span class="p">)</span><span class="w">

</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">hello-handler</span><span class="w">
  </span><span class="s">"Nosso primeiro handler. Ele apenas diz 'Olá, Mundo!'"</span><span class="w">

  </span><span class="p">[</span><span class="n">_request</span><span class="p">]</span><span class="w"> </span><span class="c1">;; 1. O handler recebe a 'request' como argumento.</span><span class="w">
             </span><span class="c1">;;    Usamos '_' para sinalizar que, nesta função,</span><span class="w">
             </span><span class="c1">;;    vamos ignorar esse argumento.</span><span class="w">

  </span><span class="c1">;; 2. O handler retorna um mapa de 'response'.</span><span class="w">
  </span><span class="p">{</span><span class="no">:status</span><span class="w"> </span><span class="mi">200</span><span class="w">            </span><span class="c1">;; :status 200 é o código HTTP para "OK"</span><span class="w">
   </span><span class="no">:body</span><span class="w"> </span><span class="s">"Hello, World!"</span><span class="p">})</span><span class="w"> </span><span class="c1">;; :body é o conteúdo enviado ao navegador</span><span class="w">
</span></code></pre></div></div> <h3 id="o-que-fizemos-3">O que fizemos?</h3> <p>Criamos nossa primeira peça de lógica: uma função pura e simples que atende ao contrato do Ring — ignora a entrada e retorna um mapa de resposta com status <code class="language-plaintext highlighter-rouge">200</code> e o texto “Hello, World!”.</p> <p>No entanto, essa função não faz nada sozinha. Precisamos de duas coisas:</p> <ol> <li>Um <strong>Servidor</strong> (Jetty) para “ouvir” na <code class="language-plaintext highlighter-rouge">localhost:3000</code>.</li> <li>Um <strong>Roteador</strong> (Reitit) para dizer: “quando chegar um <code class="language-plaintext highlighter-rouge">GET</code> em <code class="language-plaintext highlighter-rouge">/api/hello</code>, execute a <code class="language-plaintext highlighter-rouge">hello-handler</code>”.</li> </ol> <h3 id="passo-13-o-servidor-e-o-roteador-coreclj">Passo 1.3: O Servidor e o Roteador (<code class="language-plaintext highlighter-rouge">core.clj</code>)</h3> <p>O <code class="language-plaintext highlighter-rouge">core.clj</code> é o “cérebro” que junta todas as peças:</p> <ol> <li><strong>Inicia o servidor</strong> (Jetty), que escuta na porta <code class="language-plaintext highlighter-rouge">3000</code>.</li> <li><strong>Define o roteador</strong> (Reitit), que mapeia URLs para handlers.</li> <li>É o <strong>ponto de entrada</strong> que o comando <code class="language-plaintext highlighter-rouge">clj -M:run</code> (definido no <code class="language-plaintext highlighter-rouge">deps.edn</code>) executa.</li> </ol> <p><strong>Ação:</strong> Crie o arquivo <code class="language-plaintext highlighter-rouge">src/todo/backend/core.clj</code> (na mesma pasta do <code class="language-plaintext highlighter-rouge">handler.clj</code>) e cole:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.backend.core</span><span class="w">
  </span><span class="p">(</span><span class="no">:require</span><span class="w"> </span><span class="p">[</span><span class="n">ring.adapter.jetty</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">jetty</span><span class="p">]</span><span class="w">       </span><span class="c1">;; 1. O software do Servidor (Jetty)</span><span class="w">
            </span><span class="p">[</span><span class="n">reitit.ring</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">ring</span><span class="p">]</span><span class="w">               </span><span class="c1">;; 2. O Roteador (Reitit)</span><span class="w">
            </span><span class="p">[</span><span class="n">todo.backend.handler</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">handler</span><span class="p">])</span><span class="w">  </span><span class="c1">;; 3. Nossas funções (handler.clj)</span><span class="w">
  </span><span class="p">(</span><span class="no">:gen-class</span><span class="p">))</span><span class="w">

</span><span class="c1">;; --- 1. Definição das Rotas ---</span><span class="w">
</span><span class="c1">;; A URL "/api/hello", quando acessada com o método :get,</span><span class="w">
</span><span class="c1">;; deve executar nossa função handler/hello-handler.</span><span class="w">
</span><span class="p">(</span><span class="k">def</span><span class="w"> </span><span class="n">app-routes</span><span class="w">
  </span><span class="p">(</span><span class="nf">ring/router</span><span class="w">
   </span><span class="p">[[</span><span class="s">"/api/hello"</span><span class="w"> </span><span class="p">{</span><span class="no">:get</span><span class="w"> </span><span class="p">{</span><span class="no">:handler</span><span class="w"> </span><span class="n">handler/hello-handler</span><span class="p">}}]]))</span><span class="w">

</span><span class="c1">;; --- 2. Definição da Aplicação (App) ---</span><span class="w">
</span><span class="c1">;; O 'app' final é a função Ring principal.</span><span class="w">
</span><span class="p">(</span><span class="k">def</span><span class="w"> </span><span class="n">app</span><span class="w">
  </span><span class="p">(</span><span class="nf">ring/ring-handler</span><span class="w">
   </span><span class="n">app-routes</span><span class="w">                     </span><span class="c1">;; Nossas rotas</span><span class="w">
   </span><span class="p">(</span><span class="nf">ring/create-default-handler</span><span class="p">)))</span><span class="w"> </span><span class="c1">;; Um handler padrão para 404 (Not Found)</span><span class="w">

</span><span class="c1">;; --- 3. Função para Iniciar o Servidor ---</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">start-server</span><span class="w"> </span><span class="p">[</span><span class="n">port</span><span class="p">]</span><span class="w">
  </span><span class="p">(</span><span class="nb">println</span><span class="w"> </span><span class="p">(</span><span class="nb">str</span><span class="w"> </span><span class="s">"Servidor iniciado na porta "</span><span class="w"> </span><span class="n">port</span><span class="p">))</span><span class="w">
  </span><span class="c1">;; #'app passa a "var" da nossa app para o Jetty (útil no desenvolvimento)</span><span class="w">
  </span><span class="c1">;; :join? false evita que o servidor bloqueie a thread principal.</span><span class="w">
  </span><span class="p">(</span><span class="nf">jetty/run-jetty</span><span class="w"> </span><span class="o">#</span><span class="ss">'app</span><span class="w"> </span><span class="p">{</span><span class="no">:port</span><span class="w"> </span><span class="n">port</span><span class="w"> </span><span class="no">:join?</span><span class="w"> </span><span class="n">false</span><span class="p">}))</span><span class="w">

</span><span class="c1">;; --- 4. Ponto de Entrada Principal (-main) ---</span><span class="w">
</span><span class="c1">;; Esta é a função que o alias :run (do deps.edn) procura.</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">-main</span><span class="w"> </span><span class="p">[</span><span class="o">&amp;</span><span class="w"> </span><span class="n">args</span><span class="p">]</span><span class="w">
  </span><span class="c1">;; Permite passar a porta como argumento (ex: clj -M:run 8080)</span><span class="w">
  </span><span class="c1">;; ou usa "3000" como padrão.</span><span class="w">
  </span><span class="p">(</span><span class="k">let</span><span class="w"> </span><span class="p">[</span><span class="n">port</span><span class="w"> </span><span class="p">(</span><span class="nf">Integer/parseInt</span><span class="w"> </span><span class="p">(</span><span class="nb">or</span><span class="w"> </span><span class="p">(</span><span class="nb">first</span><span class="w"> </span><span class="n">args</span><span class="p">)</span><span class="w"> </span><span class="s">"3000"</span><span class="p">))]</span><span class="w">
    </span><span class="p">(</span><span class="nf">start-server</span><span class="w"> </span><span class="n">port</span><span class="p">)))</span><span class="w">
</span></code></pre></div></div> <h3 id="o-que-fizemos-4">O que fizemos?</h3> <ol> <li><strong><code class="language-plaintext highlighter-rouge">(:require ...)</code></strong>: importamos nossas “ferramentas”: Jetty, Reitit e nosso próprio <code class="language-plaintext highlighter-rouge">handler.clj</code>.</li> <li><strong><code class="language-plaintext highlighter-rouge">(:gen-class)</code></strong>: prepara este namespace para ser compilado como uma classe Java. Não é estritamente obrigatório para rodar com <code class="language-plaintext highlighter-rouge">clj -M:run</code>, mas é a convenção para namespaces com <code class="language-plaintext highlighter-rouge">-main</code> e será necessário se um dia você quiser empacotar a aplicação em um <code class="language-plaintext highlighter-rouge">.jar</code> executável. Vamos mantê-lo como boa prática.</li> <li><strong><code class="language-plaintext highlighter-rouge">app-routes</code></strong>: nosso “mapa do site”. Por enquanto, com uma única rota.</li> <li><strong><code class="language-plaintext highlighter-rouge">app</code></strong>: a aplicação Ring principal, que “entrega” nossas rotas ao Jetty.</li> <li><strong><code class="language-plaintext highlighter-rouge">-main</code></strong>: a função que o <code class="language-plaintext highlighter-rouge">deps.edn</code> chama; pega a porta (ou usa <code class="language-plaintext highlighter-rouge">3000</code>) e chama <code class="language-plaintext highlighter-rouge">start-server</code>.</li> </ol> <p>Neste ponto, temos as três peças: <code class="language-plaintext highlighter-rouge">deps.edn</code> (1.1), <code class="language-plaintext highlighter-rouge">handler.clj</code> (1.2) e <code class="language-plaintext highlighter-rouge">core.clj</code> (1.3). Vamos ver a mágica acontecer.</p> <h3 id="passo-14-teste-navegador-e-terminal">Passo 1.4: Teste (Navegador e Terminal)</h3> <p><strong>Ação 1: Inicie o servidor</strong></p> <p>No terminal, na raiz do projeto (onde está o <code class="language-plaintext highlighter-rouge">deps.edn</code>), execute:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>clj <span class="nt">-M</span>:run
</code></pre></div></div> <p><strong>Resultado Esperado:</strong> Na primeira vez, o Clojure vai <strong>baixar todas as dependências</strong> (pode demorar um pouco — várias linhas de download aparecerão). Em seguida:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Servidor iniciado na porta 3000
</code></pre></div></div> <p><strong>Importante:</strong> este terminal agora está “ocupado” rodando o servidor. Deixe-o rodando.</p> <p><strong>Ação 2: Teste no navegador</strong></p> <ol> <li>Abra o navegador.</li> <li>Digite a URL exata da nossa rota: <code class="language-plaintext highlighter-rouge">http://localhost:3000/api/hello</code></li> <li>Pressione Enter.</li> </ol> <p><strong>Resultado Esperado:</strong> a página deve mostrar apenas o texto do <code class="language-plaintext highlighter-rouge">:body</code> do nosso handler:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Hello, World!
</code></pre></div></div> <p><strong>Ação 3: Teste no terminal com <code class="language-plaintext highlighter-rouge">curl</code></strong></p> <p>Para o restante do tutorial, usaremos bastante o <code class="language-plaintext highlighter-rouge">curl</code>, pois ele nos permite testar <em>todos</em> os métodos HTTP (<code class="language-plaintext highlighter-rouge">GET</code>, <code class="language-plaintext highlighter-rouge">POST</code>, <code class="language-plaintext highlighter-rouge">DELETE</code>, etc.).</p> <ol> <li>Abra um <strong>novo</strong> terminal (deixe o servidor rodando no primeiro).</li> <li>Execute:</li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:3000/api/hello
</code></pre></div></div> <p><strong>Resultado Esperado:</strong> o <code class="language-plaintext highlighter-rouge">curl</code> imprime o <code class="language-plaintext highlighter-rouge">:body</code> diretamente no terminal:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Hello, World!
</code></pre></div></div> <h3 id="se-algo-deu-errado">Se algo deu errado…</h3> <table> <thead> <tr> <th>Sintoma</th> <th>Causa provável</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">Connection refused</code></td> <td>O servidor não está rodando no Terminal 1 (ou caiu com erro).</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">404 Not Found</code></td> <td>Erro de digitação na URL ou na rota do <code class="language-plaintext highlighter-rouge">core.clj</code> (<code class="language-plaintext highlighter-rouge">/api/hello</code>).</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Could not locate todo/backend/core...</code></td> <td>O caminho do arquivo não bate com o namespace (confira <code class="language-plaintext highlighter-rouge">src/todo/backend/core.clj</code>) ou você não está na raiz do projeto.</td> </tr> <tr> <td>Erro de sintaxe ao iniciar</td> <td>Algum parêntese a mais/menos ao colar. Compare com o código acima com calma.</td> </tr> </tbody> </table> <h3 id="passo-15-git-checkpoint-hello-world">Passo 1.5: Git Checkpoint (“Hello World”)</h3> <p><strong>Por que fazemos isso?</strong> Se, na próxima fase, ao adicionar a lógica do banco, quebrarmos tudo acidentalmente, teremos um “ponto seguro” para o qual podemos voltar.</p> <p><strong>Ação 1:</strong> No terminal do servidor, pare-o (<code class="language-plaintext highlighter-rouge">Ctrl+C</code>). Agora, veja o que o Git enxerga:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
</code></pre></div></div> <p><strong>Resultado Esperado:</strong> o Git mostrará os arquivos novos (“Untracked files”): <code class="language-plaintext highlighter-rouge">deps.edn</code> e <code class="language-plaintext highlighter-rouge">src/</code>.</p> <p><strong>Ação 2:</strong> Prepare e salve:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"feat: implementa servidor 'Hello World' com Jetty e Reitit"</span>
</code></pre></div></div> <p><strong>Resultado Esperado:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[main 1a2b3c4] feat: implementa servidor 'Hello World' com Jetty e Reitit
 3 files changed, ...
 create mode 100644 deps.edn
 create mode 100644 src/todo/backend/core.clj
 create mode 100644 src/todo/backend/handler.clj
</code></pre></div></div> <hr/> <p><strong>Fim da Fase 1!</strong> 🏁</p> <p>Temos um projeto Git limpo, com um servidor web “Hello World” totalmente funcional e testado. Agora estamos prontos para construir a lógica de negócios real: a API, começando pelo “banco de dados em memória” (<code class="language-plaintext highlighter-rouge">atom</code>).</p> <hr/> <h1 id="fase-2-o-backend-funcional-api-com-banco-em-memória">Fase 2: O Backend Funcional (API com Banco em Memória)</h1> <p><strong>Objetivo:</strong> Construir a lógica de negócios da nossa API. Antes de nos preocuparmos com um banco de dados real, vamos criar um “banco de dados” que vive <em>apenas na memória</em>.</p> <h3 id="passo-21-o-conceito-imutabilidade-e-por-que-precisamos-de-atoms">Passo 2.1: O Conceito (Imutabilidade e por que precisamos de <code class="language-plaintext highlighter-rouge">atom</code>s)</h3> <p>Em muitas linguagens, se você tem uma lista de “todos”, você simplesmente “adiciona” (push/append) um novo item, e a lista original é <em>modificada</em>.</p> <p>Em Clojure, as estruturas de dados (como mapas <code class="language-plaintext highlighter-rouge">{}</code> e vetores <code class="language-plaintext highlighter-rouge">[]</code>) são <strong>imutáveis</strong>: uma vez criado, um valor <strong>nunca</strong> pode ser alterado.</p> <p>Vamos ver um exemplo rápido. <code class="language-plaintext highlighter-rouge">conj</code> é a função para “adicionar” a um vetor:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">;; 1. Criamos um vetor chamado 'meu-vetor'</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="k">def</span><span class="w"> </span><span class="n">meu-vetor</span><span class="w"> </span><span class="p">[</span><span class="mi">1</span><span class="w"> </span><span class="mi">2</span><span class="p">])</span><span class="w">
</span><span class="o">#</span><span class="ss">'user/meu-vetor</span><span class="w">

</span><span class="c1">;; 2. "Adicionamos" o número 3 a ele</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="nb">conj</span><span class="w"> </span><span class="n">meu-vetor</span><span class="w"> </span><span class="mi">3</span><span class="p">)</span><span class="w">
</span><span class="p">[</span><span class="mi">1</span><span class="w"> </span><span class="mi">2</span><span class="w"> </span><span class="mi">3</span><span class="p">]</span><span class="w">

</span><span class="c1">;; 3. Agora, vamos checar o 'meu-vetor' original</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="n">meu-vetor</span><span class="w">
</span><span class="p">[</span><span class="mi">1</span><span class="w"> </span><span class="mi">2</span><span class="p">]</span><span class="w">
</span></code></pre></div></div> <p>Veja! O <code class="language-plaintext highlighter-rouge">meu-vetor</code> original <strong>não mudou</strong>. A função <code class="language-plaintext highlighter-rouge">(conj meu-vetor 3)</code> não <em>modificou</em> o vetor; ela <em>retornou um novo vetor</em> com o item adicionado.</p> <p><strong>Isso cria um problema para nós:</strong> se nosso banco fosse <code class="language-plaintext highlighter-rouge">(def todos-db {})</code>, ele seria um mapa vazio <em>para sempre</em>. Como “salvar” um novo todo?</p> <p><strong>A Solução: o <code class="language-plaintext highlighter-rouge">atom</code> (a “caixa” segura)</strong></p> <p>Nós colocamos nosso valor imutável (um mapa <code class="language-plaintext highlighter-rouge">{}</code>) dentro de uma “caixa” de referência: um <strong><code class="language-plaintext highlighter-rouge">atom</code></strong>.</p> <ul> <li>O <strong>valor dentro da caixa</strong> (o mapa) é imutável.</li> <li>O <strong><code class="language-plaintext highlighter-rouge">atom</code> é a caixa</strong> em si.</li> <li>Nós não <em>mudamos</em> o valor: nós <strong>trocamos</strong> (<em>swap</em>) o valor antigo por um valor <em>totalmente novo</em> dentro da caixa.</li> </ul> <p>Para trabalhar com <code class="language-plaintext highlighter-rouge">atom</code>s, usamos três coisas:</p> <ol> <li><strong><code class="language-plaintext highlighter-rouge">(atom {...})</code></strong>: o <strong>construtor</strong> — cria a caixa com um valor inicial dentro.</li> <li><strong><code class="language-plaintext highlighter-rouge">@</code></strong> (lê-se “deref”): o <strong>leitor</strong> — “olha dentro” da caixa e vê o valor atual.</li> <li><strong><code class="language-plaintext highlighter-rouge">(swap! ...)</code></strong>: o <strong>escritor</strong> — troca o conteúdo da caixa aplicando uma função ao valor antigo. Ele é “atômico” (daí o nome): mesmo que 1000 requisições tentem criar um todo ao mesmo tempo, nenhuma escrita é perdida.</li> </ol> <h3 id="passo-22-o-banco-dbclj">Passo 2.2: O Banco (<code class="language-plaintext highlighter-rouge">db.clj</code>)</h3> <p><strong>Ação:</strong> Crie o arquivo <code class="language-plaintext highlighter-rouge">src/todo/backend/db.clj</code> (ao lado de <code class="language-plaintext highlighter-rouge">core.clj</code> e <code class="language-plaintext highlighter-rouge">handler.clj</code>) e cole:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.backend.db</span><span class="w">
  </span><span class="s">"Este namespace gerencia os dados dos 'todos' em memória."</span><span class="p">)</span><span class="w">

</span><span class="c1">;; (def) cria uma 'var' global.</span><span class="w">
</span><span class="c1">;; (atom {}) cria nossa "caixa" (atom) e coloca</span><span class="w">
</span><span class="c1">;; um mapa imutável vazio {} dentro dela.</span><span class="w">
</span><span class="p">(</span><span class="k">def</span><span class="w"> </span><span class="n">todos-db</span><span class="w"> </span><span class="p">(</span><span class="nf">atom</span><span class="w"> </span><span class="p">{}))</span><span class="w">
</span><span class="c1">;; Nosso banco terá a forma: {1 {:id 1, :title "..."}, 2 {:id 2, ...}}</span><span class="w">

</span><span class="c1">;; Uma "caixa" separada para o contador de IDs.</span><span class="w">
</span><span class="p">(</span><span class="k">def</span><span class="w"> </span><span class="n">next-id</span><span class="w"> </span><span class="p">(</span><span class="nf">atom</span><span class="w"> </span><span class="mi">1</span><span class="p">))</span><span class="w">

</span><span class="c1">;; --- Nossas Funções de Acesso ao Banco ---</span><span class="w">

</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">get-all-todos</span><span class="w">
  </span><span class="s">"Retorna uma lista com todos os 'todos' no banco."</span><span class="w">
  </span><span class="p">[]</span><span class="w">
  </span><span class="c1">;; @todos-db: "olha dentro" da caixa (lê o valor).</span><span class="w">
  </span><span class="c1">;; (vals): pega apenas os valores do mapa (ignora as chaves/IDs).</span><span class="w">
  </span><span class="p">(</span><span class="nf">vec</span><span class="w"> </span><span class="p">(</span><span class="nb">vals</span><span class="w"> </span><span class="o">@</span><span class="n">todos-db</span><span class="p">)))</span><span class="w">

</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">create-todo</span><span class="w">
  </span><span class="s">"Cria um novo 'todo', salva no banco e o retorna."</span><span class="w">
  </span><span class="p">[</span><span class="n">todo-data</span><span class="p">]</span><span class="w"> </span><span class="c1">;; ex: {:title "Minha tarefa"}</span><span class="w">
  </span><span class="p">(</span><span class="k">let</span><span class="w"> </span><span class="p">[</span><span class="c1">;; 1. Lê o ID atual (ex: 1) usando '@'</span><span class="w">
        </span><span class="n">id</span><span class="w"> </span><span class="o">@</span><span class="n">next-id</span><span class="w">

        </span><span class="c1">;; 2. Cria um NOVO mapa imutável com os dados completos</span><span class="w">
        </span><span class="n">new-todo</span><span class="w"> </span><span class="p">(</span><span class="nb">assoc</span><span class="w"> </span><span class="n">todo-data</span><span class="w">
                        </span><span class="no">:id</span><span class="w"> </span><span class="n">id</span><span class="w">
                        </span><span class="no">:completed</span><span class="w"> </span><span class="n">false</span><span class="w">
                        </span><span class="no">:created-at</span><span class="w"> </span><span class="p">(</span><span class="nb">str</span><span class="w"> </span><span class="p">(</span><span class="nf">java.time.Instant/now</span><span class="p">)))]</span><span class="w">

    </span><span class="c1">;; 3. (swap!): "troca" o conteúdo da caixa 'todos-db',</span><span class="w">
    </span><span class="c1">;;    aplicando 'assoc' ao mapa antigo para criar um novo.</span><span class="w">
    </span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">todos-db</span><span class="w"> </span><span class="nb">assoc</span><span class="w"> </span><span class="n">id</span><span class="w"> </span><span class="n">new-todo</span><span class="p">)</span><span class="w">

    </span><span class="c1">;; 4. Incrementa o contador na caixa 'next-id'.</span><span class="w">
    </span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">next-id</span><span class="w"> </span><span class="nb">inc</span><span class="p">)</span><span class="w">

    </span><span class="c1">;; 5. Retorna o 'todo' recém-criado.</span><span class="w">
    </span><span class="n">new-todo</span><span class="p">))</span><span class="w">
</span></code></pre></div></div> <h3 id="o-que-fizemos-5">O que fizemos?</h3> <ul> <li><strong><code class="language-plaintext highlighter-rouge">todos-db</code> e <code class="language-plaintext highlighter-rouge">next-id</code></strong>: dois <code class="language-plaintext highlighter-rouge">atom</code>s (caixas) para guardar nosso “banco” e nosso contador de IDs.</li> <li><strong><code class="language-plaintext highlighter-rouge">get-all-todos</code></strong>: uma função que <em>lê</em> o atom (<code class="language-plaintext highlighter-rouge">@</code>).</li> <li><strong><code class="language-plaintext highlighter-rouge">create-todo</code></strong>: uma função que <em>escreve</em> no atom (<code class="language-plaintext highlighter-rouge">swap!</code>), adicionando um todo e incrementando o ID.</li> </ul> <p>Agora temos a lógica do banco. Mas, como bons engenheiros, não vamos <em>assumir</em> que funciona. Vamos <em>provar</em>.</p> <h3 id="passo-23-teste-no-repl">Passo 2.3: Teste no REPL</h3> <p><strong>O que é o REPL?</strong> REPL significa <strong>Read-Eval-Print-Loop</strong> (Ler-Avaliar-Imprimir-Repetir). É um terminal interativo do Clojure: você digita uma expressão, o Clojure a executa e imprime o resultado. É a ferramenta número um do desenvolvimento em Clojure.</p> <p><strong>Ação 1: Inicie o REPL</strong></p> <p>Na raiz do projeto:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>clj
</code></pre></div></div> <p>Você verá um prompt <code class="language-plaintext highlighter-rouge">user=&gt;</code>. Agora você está “dentro” do Clojure.</p> <p><strong>Ação 2: Carregue seu namespace</strong></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="nf">require</span><span class="w"> </span><span class="o">'</span><span class="p">[</span><span class="n">todo.backend.db</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">db</span><span class="p">])</span><span class="w">
</span></code></pre></div></div> <p>O REPL deve responder <code class="language-plaintext highlighter-rouge">nil</code>. Isso é <strong>bom</strong>: significa que o Clojure encontrou e leu seu <code class="language-plaintext highlighter-rouge">db.clj</code> sem erros. Agora as funções públicas estão disponíveis com o prefixo <code class="language-plaintext highlighter-rouge">db/</code>.</p> <p><strong>Ação 3: Teste as funções!</strong></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">;; 1. Veja se o banco está vazio</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="nf">db/get-all-todos</span><span class="p">)</span><span class="w">
</span><span class="p">[]</span><span class="w">

</span><span class="c1">;; 2. Crie um 'todo'</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="nf">db/create-todo</span><span class="w"> </span><span class="p">{</span><span class="no">:title</span><span class="w"> </span><span class="s">"Testar o REPL"</span><span class="p">})</span><span class="w">
</span><span class="p">{</span><span class="no">:title</span><span class="w"> </span><span class="s">"Testar o REPL"</span><span class="n">,</span><span class="w"> </span><span class="no">:id</span><span class="w"> </span><span class="mi">1</span><span class="n">,</span><span class="w"> </span><span class="no">:completed</span><span class="w"> </span><span class="n">false,</span><span class="w"> </span><span class="no">:created-at</span><span class="w"> </span><span class="s">"2026-..."</span><span class="p">}</span><span class="w">

</span><span class="c1">;; 3. Veja se foi salvo!</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="nf">db/get-all-todos</span><span class="p">)</span><span class="w">
</span><span class="p">[{</span><span class="no">:title</span><span class="w"> </span><span class="s">"Testar o REPL"</span><span class="n">,</span><span class="w"> </span><span class="no">:id</span><span class="w"> </span><span class="mi">1</span><span class="n">,</span><span class="w"> </span><span class="no">:completed</span><span class="w"> </span><span class="n">false,</span><span class="w"> </span><span class="no">:created-at</span><span class="w"> </span><span class="s">"..."</span><span class="p">}]</span><span class="w">

</span><span class="c1">;; 4. Crie mais um</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="nf">db/create-todo</span><span class="w"> </span><span class="p">{</span><span class="no">:title</span><span class="w"> </span><span class="s">"Aprender Clojure"</span><span class="w"> </span><span class="no">:description</span><span class="w"> </span><span class="s">"É divertido"</span><span class="p">})</span><span class="w">
</span><span class="p">{</span><span class="no">:title</span><span class="w"> </span><span class="s">"Aprender Clojure"</span><span class="n">,</span><span class="w"> </span><span class="no">:description</span><span class="w"> </span><span class="s">"É divertido"</span><span class="n">,</span><span class="w"> </span><span class="no">:id</span><span class="w"> </span><span class="mi">2</span><span class="n">,</span><span class="w"> </span><span class="n">...</span><span class="p">}</span><span class="w">

</span><span class="c1">;; 5. Veja a lista completa</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="nf">db/get-all-todos</span><span class="p">)</span><span class="w">
</span><span class="p">[{</span><span class="no">:title</span><span class="w"> </span><span class="s">"Testar o REPL"</span><span class="n">,</span><span class="w"> </span><span class="no">:id</span><span class="w"> </span><span class="mi">1</span><span class="n">,</span><span class="w"> </span><span class="n">...</span><span class="p">}</span><span class="w">
 </span><span class="p">{</span><span class="no">:title</span><span class="w"> </span><span class="s">"Aprender Clojure"</span><span class="n">,</span><span class="w"> </span><span class="no">:description</span><span class="w"> </span><span class="s">"É divertido"</span><span class="n">,</span><span class="w"> </span><span class="no">:id</span><span class="w"> </span><span class="mi">2</span><span class="n">,</span><span class="w"> </span><span class="n">...</span><span class="p">}]</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 4 (Opcional): olhe “dentro” das caixas</strong></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">;; O mapa completo de 'todos'</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="o">@</span><span class="n">db/todos-db</span><span class="w">
</span><span class="p">{</span><span class="mi">1</span><span class="w"> </span><span class="p">{</span><span class="no">:title</span><span class="w"> </span><span class="s">"Testar o REPL"</span><span class="n">,</span><span class="w"> </span><span class="n">...</span><span class="p">}</span><span class="n">,</span><span class="w"> </span><span class="mi">2</span><span class="w"> </span><span class="p">{</span><span class="no">:title</span><span class="w"> </span><span class="s">"Aprender Clojure"</span><span class="n">,</span><span class="w"> </span><span class="n">...</span><span class="p">}}</span><span class="w">

</span><span class="c1">;; O contador de ID (deve ser 3 agora)</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="o">@</span><span class="n">db/next-id</span><span class="w">
</span><span class="mi">3</span><span class="w">
</span></code></pre></div></div> <h3 id="o-que-fizemos-6">O que fizemos?</h3> <p>Validamos <strong>100%</strong> da lógica de banco sem tocar em servidor, navegador ou rota. Temos confiança total de que o <code class="language-plaintext highlighter-rouge">db.clj</code> funciona.</p> <p><strong>Para sair do REPL:</strong> pressione <code class="language-plaintext highlighter-rouge">Ctrl+D</code> (ou digite <code class="language-plaintext highlighter-rouge">(System/exit 0)</code>).</p> <aside> <p>⚠️ Não existe um comando <code class="language-plaintext highlighter-rouge">(exit)</code> no REPL do <code class="language-plaintext highlighter-rouge">clj</code> — se você tentar, verá um erro <code class="language-plaintext highlighter-rouge">Unable to resolve symbol: exit</code>. Use <code class="language-plaintext highlighter-rouge">Ctrl+D</code>.</p> </aside> <h3 id="passo-24-atualização-incremental-handlers">Passo 2.4: Atualização Incremental (Handlers)</h3> <p>Nosso <code class="language-plaintext highlighter-rouge">db.clj</code> está testado. Agora vamos “conectá-lo” ao servidor web em duas etapas: primeiro ensinamos o <code class="language-plaintext highlighter-rouge">handler.clj</code> a chamar o <code class="language-plaintext highlighter-rouge">db.clj</code>; depois ensinamos o <code class="language-plaintext highlighter-rouge">core.clj</code> (o roteador) a chamar os novos handlers.</p> <p><strong>Ação 1: Adicionar os <code class="language-plaintext highlighter-rouge">require</code>s</strong></p> <p>Abra <code class="language-plaintext highlighter-rouge">src/todo/backend/handler.clj</code>. <em>Substitua</em> a declaração do namespace:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.backend.handler</span><span class="w">
  </span><span class="s">"Este namespace define nossas 'funções de resposta' (Handlers)."</span><span class="p">)</span><span class="w">
</span></code></pre></div></div> <p>por esta:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.backend.handler</span><span class="w">
  </span><span class="s">"Este namespace define nossas 'funções de resposta' (Handlers)."</span><span class="w">
  </span><span class="p">(</span><span class="no">:require</span><span class="w"> </span><span class="p">[</span><span class="n">todo.backend.db</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">db</span><span class="p">]</span><span class="w">     </span><span class="c1">;; &lt;- ADICIONE ISTO</span><span class="w">
            </span><span class="p">[</span><span class="n">clojure.string</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="nb">str</span><span class="p">]))</span><span class="w">   </span><span class="c1">;; &lt;- E ISTO (para validação)</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 2: Adicionar os novos handlers</strong></p> <p>Agora, <em>abaixo</em> da função <code class="language-plaintext highlighter-rouge">hello-handler</code> existente, adicione estas duas funções. Note como elas são a “ponte” entre o HTTP e o nosso <code class="language-plaintext highlighter-rouge">db.clj</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">;; --- Handler para Listar Todos ---</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">list-todos-handler</span><span class="w">
  </span><span class="s">"Handler para a rota GET /api/todos."</span><span class="w">
  </span><span class="p">[</span><span class="n">_request</span><span class="p">]</span><span class="w">
  </span><span class="c1">;; Chama nossa função de banco e a coloca no 'body'</span><span class="w">
  </span><span class="p">{</span><span class="no">:status</span><span class="w"> </span><span class="mi">200</span><span class="w">
   </span><span class="no">:body</span><span class="w"> </span><span class="p">{</span><span class="no">:todos</span><span class="w"> </span><span class="p">(</span><span class="nf">db/get-all-todos</span><span class="p">)}})</span><span class="w">

</span><span class="c1">;; --- Handler para Criar um Todo ---</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">create-todo-handler</span><span class="w">
  </span><span class="s">"Handler para a rota POST /api/todos."</span><span class="w">
  </span><span class="p">[</span><span class="n">request</span><span class="p">]</span><span class="w">
  </span><span class="c1">;; :body é um mapa Clojure que o *middleware*</span><span class="w">
  </span><span class="c1">;; (que adicionaremos no próximo passo) vai criar para nós</span><span class="w">
  </span><span class="c1">;; a partir do JSON que o cliente enviar.</span><span class="w">
  </span><span class="p">(</span><span class="k">let</span><span class="w"> </span><span class="p">[</span><span class="n">todo-data</span><span class="w"> </span><span class="p">(</span><span class="no">:body</span><span class="w"> </span><span class="n">request</span><span class="p">)</span><span class="w">
        </span><span class="n">title</span><span class="w"> </span><span class="p">(</span><span class="no">:title</span><span class="w"> </span><span class="n">todo-data</span><span class="p">)]</span><span class="w">

    </span><span class="c1">;; É uma boa prática validar os dados que chegam.</span><span class="w">
    </span><span class="p">(</span><span class="k">if</span><span class="w"> </span><span class="p">(</span><span class="nb">and</span><span class="w"> </span><span class="n">title</span><span class="w"> </span><span class="p">(</span><span class="nb">not</span><span class="w"> </span><span class="p">(</span><span class="nf">str/blank?</span><span class="w"> </span><span class="n">title</span><span class="p">)))</span><span class="w">
      </span><span class="c1">;; Sucesso! Os dados são válidos.</span><span class="w">
      </span><span class="p">(</span><span class="k">let</span><span class="w"> </span><span class="p">[</span><span class="n">new-todo</span><span class="w"> </span><span class="p">(</span><span class="nf">db/create-todo</span><span class="w"> </span><span class="n">todo-data</span><span class="p">)]</span><span class="w">
        </span><span class="c1">;; :status 201 = "Created"</span><span class="w">
        </span><span class="p">{</span><span class="no">:status</span><span class="w"> </span><span class="mi">201</span><span class="w">
         </span><span class="no">:body</span><span class="w"> </span><span class="n">new-todo</span><span class="p">})</span><span class="w">

      </span><span class="c1">;; Erro de validação.</span><span class="w">
      </span><span class="p">{</span><span class="no">:status</span><span class="w"> </span><span class="mi">400</span><span class="w"> </span><span class="c1">;; "Bad Request"</span><span class="w">
       </span><span class="no">:body</span><span class="w"> </span><span class="p">{</span><span class="no">:error</span><span class="w"> </span><span class="s">"O 'título' (title) é obrigatório"</span><span class="p">}})))</span><span class="w">
</span></code></pre></div></div> <p><strong>Importante:</strong> o servidor ainda não sabe sobre essas funções. Se você rodar agora, <code class="language-plaintext highlighter-rouge">/api/todos</code> ainda dará <code class="language-plaintext highlighter-rouge">404</code>.</p> <h3 id="passo-25-atualizar-o-depsedn-json">Passo 2.5: Atualizar o <code class="language-plaintext highlighter-rouge">deps.edn</code> (JSON)</h3> <p>Nosso <code class="language-plaintext highlighter-rouge">create-todo-handler</code> precisa receber JSON, e o <code class="language-plaintext highlighter-rouge">list-todos-handler</code> quer <em>enviar</em> JSON. Quem faz essas conversões são os <strong>middlewares</strong> da biblioteca <code class="language-plaintext highlighter-rouge">ring-json</code>, que ainda não está na nossa “lista de compras”.</p> <p><strong>Ação:</strong> Abra o <code class="language-plaintext highlighter-rouge">deps.edn</code> e adicione a linha do <code class="language-plaintext highlighter-rouge">ring/ring-json</code> ao bloco <code class="language-plaintext highlighter-rouge">:deps</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="w"> </span><span class="no">:deps</span><span class="w">
 </span><span class="p">{</span><span class="n">org.clojure/clojure</span><span class="w"> </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"1.11.1"</span><span class="p">}</span><span class="w">

  </span><span class="c1">;; --- Dependências do Backend (API REST) ---</span><span class="w">
  </span><span class="n">ring/ring-core</span><span class="w">          </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"1.12.2"</span><span class="p">}</span><span class="w">
  </span><span class="n">ring/ring-jetty-adapter</span><span class="w"> </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"1.12.2"</span><span class="p">}</span><span class="w">

  </span><span class="n">ring/ring-json</span><span class="w">          </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"0.5.1"</span><span class="p">}</span><span class="w"> </span><span class="c1">;; &lt;- ADICIONE ESTA LINHA</span><span class="w">

  </span><span class="n">metosin/reitit-ring</span><span class="w">     </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"0.7.0"</span><span class="p">}}</span><span class="w">
</span></code></pre></div></div> <aside> <p>💡 <strong>Regra de ouro:</strong> sempre que o <code class="language-plaintext highlighter-rouge">deps.edn</code> mudar, o servidor precisa ser <strong>parado e reiniciado</strong> para baixar/carregar a nova dependência. Mudanças no <code class="language-plaintext highlighter-rouge">deps.edn</code> nunca são aplicadas “a quente”.</p> </aside> <h3 id="passo-26-atualizar-o-coreclj-rotas-e-middlewares">Passo 2.6: Atualizar o <code class="language-plaintext highlighter-rouge">core.clj</code> (Rotas e Middlewares)</h3> <p><strong>O que é Middleware?</strong> Pense em middlewares como “assistentes” que “envelopam” seu handler. Eles rodam <strong>antes</strong> e <strong>depois</strong> dele, para fazer tarefas comuns:</p> <ul> <li><code class="language-plaintext highlighter-rouge">Request (JSON)</code> → <strong>Middleware (converte JSON → mapa)</strong> → <code class="language-plaintext highlighter-rouge">Seu Handler</code></li> <li><code class="language-plaintext highlighter-rouge">Seu Handler</code> → <strong>Middleware (converte mapa → JSON)</strong> → <code class="language-plaintext highlighter-rouge">Response (JSON)</code></li> </ul> <p><strong>Ação 1: Adicionar os <code class="language-plaintext highlighter-rouge">require</code>s de middleware</strong></p> <p>No topo do <code class="language-plaintext highlighter-rouge">src/todo/backend/core.clj</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.backend.core</span><span class="w">
  </span><span class="p">(</span><span class="no">:require</span><span class="w"> </span><span class="p">[</span><span class="n">ring.adapter.jetty</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">jetty</span><span class="p">]</span><span class="w">
            </span><span class="p">[</span><span class="n">reitit.ring</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">ring</span><span class="p">]</span><span class="w">
            </span><span class="c1">;; --- ADICIONE ESTAS 3 LINHAS ---</span><span class="w">
            </span><span class="p">[</span><span class="n">ring.middleware.json</span><span class="w"> </span><span class="no">:refer</span><span class="w"> </span><span class="p">[</span><span class="n">wrap-json-response</span><span class="w"> </span><span class="n">wrap-json-body</span><span class="p">]]</span><span class="w">
            </span><span class="p">[</span><span class="n">ring.middleware.params</span><span class="w"> </span><span class="no">:refer</span><span class="w"> </span><span class="p">[</span><span class="n">wrap-params</span><span class="p">]]</span><span class="w">
            </span><span class="p">[</span><span class="n">ring.middleware.keyword-params</span><span class="w"> </span><span class="no">:refer</span><span class="w"> </span><span class="p">[</span><span class="n">wrap-keyword-params</span><span class="p">]]</span><span class="w">
            </span><span class="c1">;; --------------------------------</span><span class="w">
            </span><span class="p">[</span><span class="n">todo.backend.handler</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">handler</span><span class="p">])</span><span class="w">
  </span><span class="p">(</span><span class="no">:gen-class</span><span class="p">))</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 2: Atualizar as rotas (<code class="language-plaintext highlighter-rouge">app-routes</code>)</strong></p> <p>Vamos adicionar <code class="language-plaintext highlighter-rouge">GET /api/todos</code> e <code class="language-plaintext highlighter-rouge">POST /api/todos</code>, aninhando todas as rotas sob o prefixo comum <code class="language-plaintext highlighter-rouge">/api</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">def</span><span class="w"> </span><span class="n">app-routes</span><span class="w">
  </span><span class="p">(</span><span class="nf">ring/router</span><span class="w">
   </span><span class="c1">;; Aninhamos tudo sob "/api"</span><span class="w">
   </span><span class="p">[</span><span class="s">"/api"</span><span class="w">
    </span><span class="p">[</span><span class="s">"/hello"</span><span class="w"> </span><span class="p">{</span><span class="no">:get</span><span class="w"> </span><span class="p">{</span><span class="no">:handler</span><span class="w"> </span><span class="n">handler/hello-handler</span><span class="p">}}]</span><span class="w">

    </span><span class="p">[</span><span class="s">"/todos"</span><span class="w">
     </span><span class="p">{</span><span class="no">:get</span><span class="w">  </span><span class="p">{</span><span class="no">:handler</span><span class="w"> </span><span class="n">handler/list-todos-handler</span><span class="p">}</span><span class="w">      </span><span class="c1">;; GET  chama 'list'</span><span class="w">
      </span><span class="no">:post</span><span class="w"> </span><span class="p">{</span><span class="no">:handler</span><span class="w"> </span><span class="n">handler/create-todo-handler</span><span class="p">}}]]))</span><span class="w"> </span><span class="c1">;; POST chama 'create'</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 3: Substituir o <code class="language-plaintext highlighter-rouge">def app</code> (adicionar os middlewares)</strong></p> <p>Nosso <code class="language-plaintext highlighter-rouge">def app</code> atual é muito simples. Substitua-o por esta versão:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">def</span><span class="w"> </span><span class="n">app</span><span class="w">
  </span><span class="p">(</span><span class="nf">ring/ring-handler</span><span class="w">
   </span><span class="n">app-routes</span><span class="w">                     </span><span class="c1">;; Nossas rotas</span><span class="w">
   </span><span class="p">(</span><span class="nf">ring/create-default-handler</span><span class="p">)</span><span class="w">  </span><span class="c1">;; Handler padrão para 404 (Not Found)</span><span class="w">

   </span><span class="c1">;; --- A "Linha de Montagem" de Middlewares ---</span><span class="w">
   </span><span class="c1">;; A requisição percorre o vetor DE CIMA PARA BAIXO até chegar</span><span class="w">
   </span><span class="c1">;; ao handler; a resposta volta DE BAIXO PARA CIMA.</span><span class="w">
   </span><span class="p">{</span><span class="no">:middleware</span><span class="w"> </span><span class="p">[</span><span class="c1">;; 1. Converte a *resposta* (nosso mapa) em JSON</span><span class="w">
                 </span><span class="n">wrap-json-response</span><span class="w">

                 </span><span class="c1">;; 2. Converte o *corpo* da requisição (JSON)</span><span class="w">
                 </span><span class="c1">;;    em um mapa Clojure e o coloca em :body</span><span class="w">
                 </span><span class="p">[</span><span class="n">wrap-json-body</span><span class="w"> </span><span class="p">{</span><span class="no">:keywords?</span><span class="w"> </span><span class="n">true</span><span class="p">}]</span><span class="w">

                 </span><span class="c1">;; 3. Lê parâmetros de URL (ex: /user?id=1)</span><span class="w">
                 </span><span class="c1">;;    e os coloca em :query-params (chaves string)</span><span class="w">
                 </span><span class="n">wrap-params</span><span class="w">

                 </span><span class="c1">;; 4. Converte as chaves-string dos parâmetros</span><span class="w">
                 </span><span class="c1">;;    em keywords ("id" -&gt; :id).</span><span class="w">
                 </span><span class="c1">;;    Precisa vir DEPOIS do wrap-params!</span><span class="w">
                 </span><span class="n">wrap-keyword-params</span><span class="p">]}))</span><span class="w">
</span></code></pre></div></div> <p>As funções <code class="language-plaintext highlighter-rouge">start-server</code> e <code class="language-plaintext highlighter-rouge">-main</code> no final do arquivo ficam exatamente como estão.</p> <ul> <li> <p><strong>Vamos entender melhor o novo <code class="language-plaintext highlighter-rouge">(def app ...)</code>?</strong></p> <p>Pense no <code class="language-plaintext highlighter-rouge">:middleware</code> como uma <strong>Linha de Montagem</strong> (um <em>pipeline</em>):</p> <ul> <li>A <strong>Requisição</strong> entra no topo (item 1 do vetor) e desce até o seu handler.</li> <li>A <strong>Resposta</strong> sai do handler e sobe do fundo (item 4) até o topo.</li> </ul> <p>Vamos seguir uma requisição <code class="language-plaintext highlighter-rouge">POST /api/todos?debug=true</code> com o corpo <code class="language-plaintext highlighter-rouge">{"title": "Meu Todo"}</code>:</p> <p><strong>O Caminho da Requisição (Descendo ⬇️)</strong></p> <ol> <li><strong>⬇️ <code class="language-plaintext highlighter-rouge">wrap-json-response</code></strong> — só se importa com a <em>resposta</em>; na requisição, apenas passa adiante.</li> <li><strong>⬇️ <code class="language-plaintext highlighter-rouge">wrap-json-body</code></strong> — vê que o <code class="language-plaintext highlighter-rouge">Content-Type</code> é <code class="language-plaintext highlighter-rouge">application/json</code>, lê o corpo, converte a string JSON em um mapa Clojure e (graças a <code class="language-plaintext highlighter-rouge">{:keywords? true}</code>) converte as chaves em keywords. Adiciona à requisição: <code class="language-plaintext highlighter-rouge">{:body {:title "Meu Todo"}}</code>.</li> <li><strong>⬇️ <code class="language-plaintext highlighter-rouge">wrap-params</code></strong> — a “estação criadora”: olha a URL, vê <code class="language-plaintext highlighter-rouge">?debug=true</code> e adiciona <code class="language-plaintext highlighter-rouge">{:query-params {"debug" "true"}}</code> (chaves <strong>string</strong>).</li> <li><strong>⬇️ <code class="language-plaintext highlighter-rouge">wrap-keyword-params</code></strong> — a “estação conversora”: encontra os parâmetros criados no passo anterior e converte as chaves para keywords: <code class="language-plaintext highlighter-rouge">{:query-params {:debug "true"}}</code>.</li> <li><strong>⬇️ Handler</strong> — recebe a requisição “enriquecida”:</li> </ol> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="n">...</span><span class="w">
 </span><span class="no">:body</span><span class="w"> </span><span class="p">{</span><span class="no">:title</span><span class="w"> </span><span class="s">"Meu Todo"</span><span class="p">}</span><span class="w">
 </span><span class="no">:query-params</span><span class="w"> </span><span class="p">{</span><span class="no">:debug</span><span class="w"> </span><span class="s">"true"</span><span class="p">}}</span><span class="w">
</span></code></pre></div> </div> <p><strong>O Caminho da Resposta (Subindo ⬆️)</strong></p> <ol> <li><strong>⬆️ Handler</strong> retorna um mapa Clojure puro: <code class="language-plaintext highlighter-rouge">{:status 201 :body {:id 1, ...}}</code>.</li> <li><strong>⬆️ <code class="language-plaintext highlighter-rouge">wrap-keyword-params</code></strong> e <strong>⬆️ <code class="language-plaintext highlighter-rouge">wrap-params</code></strong> — não se importam com respostas; passam adiante.</li> <li><strong>⬆️ <code class="language-plaintext highlighter-rouge">wrap-json-body</code></strong> — só se importa com o <em>corpo da requisição</em>; passa adiante.</li> <li><strong>⬆️ <code class="language-plaintext highlighter-rouge">wrap-json-response</code></strong> — vê que o <code class="language-plaintext highlighter-rouge">:body</code> é um mapa Clojure, converte-o em uma <em>string JSON</em> e adiciona o header <code class="language-plaintext highlighter-rouge">Content-Type: application/json</code>.</li> </ol> <p>É por isso que a <strong>ordem é crucial</strong>: o <code class="language-plaintext highlighter-rouge">wrap-params</code> precisa <strong>criar</strong> os parâmetros <em>antes</em> que o <code class="language-plaintext highlighter-rouge">wrap-keyword-params</code> possa convertê-los — por isso, no vetor, <code class="language-plaintext highlighter-rouge">wrap-params</code> vem <strong>antes</strong> de <code class="language-plaintext highlighter-rouge">wrap-keyword-params</code> (a requisição percorre o vetor de cima para baixo).</p> </li> </ul> <h3 id="passo-27-teste-api-completa-com-curl">Passo 2.7: Teste (API Completa com <code class="language-plaintext highlighter-rouge">curl</code>)</h3> <p><strong>Ação 1: Reinicie o servidor</strong></p> <p>Pare o servidor (<code class="language-plaintext highlighter-rouge">Ctrl+C</code>, se estiver rodando) e inicie de novo, para carregar a nova dependência e o novo código:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>clj <span class="nt">-M</span>:run
</code></pre></div></div> <p>O <code class="language-plaintext highlighter-rouge">clj</code> vai baixar o <code class="language-plaintext highlighter-rouge">ring/ring-json</code> e então: <code class="language-plaintext highlighter-rouge">Servidor iniciado na porta 3000</code>. Deixe rodando.</p> <p><strong>Ação 2: Teste em um novo terminal</strong></p> <p><strong>Teste 1 — Listar todos (deve estar vazio):</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:3000/api/todos
</code></pre></div></div> <p><strong>Resultado Esperado:</strong> uma string JSON com um array vazio. Isso prova que <code class="language-plaintext highlighter-rouge">list-todos-handler</code> e <code class="language-plaintext highlighter-rouge">wrap-json-response</code> funcionam:</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w"> </span><span class="nl">"todos"</span><span class="p">:</span><span class="w"> </span><span class="p">[]</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p><strong>Teste 2 — Criar um todo:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-X</span> POST http://localhost:3000/api/todos <span class="se">\</span>
     <span class="nt">-H</span> <span class="s2">"Content-Type: application/json"</span> <span class="se">\</span>
     <span class="nt">-d</span> <span class="s1">'{"title": "Testar a API", "description": "com curl"}'</span>
</code></pre></div></div> <p><strong>Resultado Esperado</strong> (o ID pode variar):</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Testar a API"</span><span class="p">,</span><span class="w"> </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"com curl"</span><span class="p">,</span><span class="w"> </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w"> </span><span class="nl">"completed"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w"> </span><span class="nl">"created-at"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p><strong>Teste 3 — Listar novamente:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl http://localhost:3000/api/todos
</code></pre></div></div> <p><strong>Resultado Esperado</strong> (o todo criado agora aparece):</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w"> </span><span class="nl">"todos"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="w"> </span><span class="nl">"title"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Testar a API"</span><span class="p">,</span><span class="w"> </span><span class="nl">"description"</span><span class="p">:</span><span class="w"> </span><span class="s2">"com curl"</span><span class="p">,</span><span class="w"> </span><span class="nl">"id"</span><span class="p">:</span><span class="w"> </span><span class="mi">1</span><span class="p">,</span><span class="w"> </span><span class="nl">"completed"</span><span class="p">:</span><span class="w"> </span><span class="kc">false</span><span class="p">,</span><span class="w"> </span><span class="nl">"created-at"</span><span class="p">:</span><span class="w"> </span><span class="s2">"..."</span><span class="w"> </span><span class="p">}]</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p><strong>Teste 4 — Validação (título vazio):</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-X</span> POST http://localhost:3000/api/todos <span class="se">\</span>
     <span class="nt">-H</span> <span class="s2">"Content-Type: application/json"</span> <span class="se">\</span>
     <span class="nt">-d</span> <span class="s1">'{"title": ""}'</span>
</code></pre></div></div> <p><strong>Resultado Esperado:</strong></p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w"> </span><span class="nl">"error"</span><span class="p">:</span><span class="w"> </span><span class="s2">"O 'título' (title) é obrigatório"</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <aside> <p>💡 <strong>Prefere uma interface gráfica?</strong> Ferramentas como <strong>Insomnia</strong> ou <strong>Postman</strong> fazem o mesmo que o <code class="language-plaintext highlighter-rouge">curl</code>: crie uma requisição, escolha o método (GET/POST), digite a URL e, no caso do POST, selecione <em>Body → JSON</em> e cole o corpo. O resultado deve ser idêntico.</p> </aside> <aside> <p>⚠️ <strong>Erro comum no Windows:</strong> o <code class="language-plaintext highlighter-rouge">curl</code> do PowerShell trata aspas de forma diferente. Se o POST falhar com erro de JSON, use o <em>Prompt de Comando</em> (cmd), o Git Bash, ou o Insomnia/Postman.</p> </aside> <h3 id="o-que-fizemos-7">O que fizemos?</h3> <p>Se você viu esses resultados, <strong>parabéns!</strong> 🥳 Você construiu uma API REST funcional em Clojure, que:</p> <ol> <li>Recebe e entende JSON (<code class="language-plaintext highlighter-rouge">POST</code>);</li> <li>Usa o <code class="language-plaintext highlighter-rouge">atom</code> para salvar dados na memória;</li> <li>Retorna dados como JSON (<code class="language-plaintext highlighter-rouge">GET</code>);</li> <li>Valida entradas e responde com códigos HTTP corretos (<code class="language-plaintext highlighter-rouge">201</code>, <code class="language-plaintext highlighter-rouge">400</code>).</li> </ol> <h3 id="passo-28-git-checkpoint-api-funcional">Passo 2.8: Git Checkpoint (API Funcional)</h3> <p><strong>Ação 1:</strong> Pare o servidor (<code class="language-plaintext highlighter-rouge">Ctrl+C</code>) e verifique:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
</code></pre></div></div> <p>Ele deve listar <code class="language-plaintext highlighter-rouge">db.clj</code> (novo), e <code class="language-plaintext highlighter-rouge">deps.edn</code>, <code class="language-plaintext highlighter-rouge">core.clj</code>, <code class="language-plaintext highlighter-rouge">handler.clj</code> (modificados).</p> <p><strong>Ação 2:</strong> Prepare e salve:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"feat: implementa API REST de 'todos' com banco em memória"</span>
</code></pre></div></div> <aside> <p>💡 Se você usa o <strong>VS Code</strong>, essa operação também pode ser feita pela aba <em>Source Control</em> da interface — inclusive a publicação no GitHub (“Publish to GitHub”), que criará o repositório remoto para você. Lembre-se: o repositório da entrega deve ser <strong>público</strong>.</p> </aside> <hr/> <p><strong>Fim da Fase 2!</strong> 🏁</p> <p>Nosso backend tem um histórico limpo: setup → Hello World → API REST funcional. Ele está pronto e esperando por requisições. Agora, vamos construir algo para <em>usá-lo</em>.</p> <hr/> <h1 id="fase-3-introdução-ao-frontend-reagent-e-shadow-cljs">Fase 3: Introdução ao Frontend (Reagent e Shadow-CLJS)</h1> <p><strong>Objetivo:</strong> Aprender os fundamentos do ClojureScript e do Reagent. Vamos construir uma UI interativa que gerencia seu <em>próprio</em> estado (na memória do navegador), <strong>sem</strong> se comunicar com a API ainda.</p> <aside> <p>💡 <strong>Nesta fase, o servidor de backend (porta 3000) não é necessário.</strong> Pode deixá-lo desligado. Vamos trabalhar apenas com o frontend (porta 8000).</p> </aside> <h3 id="passo-31-setup-do-ambiente-nodejs-npm">Passo 3.1: Setup do Ambiente Node.js (<code class="language-plaintext highlighter-rouge">npm</code>)</h3> <p>O <code class="language-plaintext highlighter-rouge">shadow-cljs</code> (nosso compilador de frontend) é uma ferramenta “híbrida”: ele é uma biblioteca Clojure (que entrará no <code class="language-plaintext highlighter-rouge">deps.edn</code>), mas também é uma ferramenta de linha de comando que roda em <strong>Node.js</strong>. Além disso, o React será instalado via <code class="language-plaintext highlighter-rouge">npm</code>.</p> <p><strong>Ação:</strong> Na raiz do projeto (<code class="language-plaintext highlighter-rouge">todo-app/</code>), execute estes dois comandos:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 1. Cria o package.json (o "deps.edn" do mundo Node.js)</span>
npm init <span class="nt">-y</span>

<span class="c"># 2. Instala as ferramentas, com versões FIXADAS</span>
npm <span class="nb">install </span>shadow-cljs@2.28.23 react@19.2.0 react-dom@19.2.0
</code></pre></div></div> <p><strong>O que esses comandos fazem?</strong></p> <ol> <li><strong><code class="language-plaintext highlighter-rouge">npm init -y</code></strong> cria o arquivo <code class="language-plaintext highlighter-rouge">package.json</code>, que rastreia as dependências de JavaScript do projeto (o <code class="language-plaintext highlighter-rouge">-y</code> aceita todas as respostas padrão).</li> <li><strong><code class="language-plaintext highlighter-rouge">npm install ...</code></strong> baixa as ferramentas para a pasta <code class="language-plaintext highlighter-rouge">node_modules/</code> (que nosso <code class="language-plaintext highlighter-rouge">.gitignore</code> da Fase 0 já está, corretamente, ignorando) e as registra no <code class="language-plaintext highlighter-rouge">package.json</code>.</li> </ol> <aside> <p>⚠️ <strong>Por que fixamos as versões?</strong> O <code class="language-plaintext highlighter-rouge">shadow-cljs</code> existe em <strong>dois lugares</strong>: como pacote <code class="language-plaintext highlighter-rouge">npm</code> (a linha de comando) e como biblioteca Maven no <code class="language-plaintext highlighter-rouge">deps.edn</code> (o compilador em si). <strong>As duas versões precisam ser idênticas.</strong> Se você rodar apenas <code class="language-plaintext highlighter-rouge">npm install shadow-cljs</code> (sem versão), o npm instalará a versão mais recente (3.x), que não vai bater com a que colocaremos no <code class="language-plaintext highlighter-rouge">deps.edn</code> — e você verá avisos de <em>“version mismatch”</em> e erros estranhos de compilação. Fixar <code class="language-plaintext highlighter-rouge">2.28.23</code> nos dois lugares elimina esse problema pela raiz.</p> </aside> <h3 id="passo-32-configurar-as-bibliotecas-do-frontend-depsedn">Passo 3.2: Configurar as Bibliotecas do Frontend (<code class="language-plaintext highlighter-rouge">deps.edn</code>)</h3> <p><strong>O que é o Reagent?</strong> O Reagent é uma biblioteca que nos permite usar o <strong>React</strong> (a popular biblioteca de UI) de uma forma muito limpa e “Clojure-nativa”. Escrevemos vetores simples (chamados <strong>Hiccup</strong>), e o Reagent os transforma em componentes React super rápidos.</p> <p><strong>Ação:</strong> Abra o <code class="language-plaintext highlighter-rouge">deps.edn</code> e adicione as duas linhas do frontend ao bloco <code class="language-plaintext highlighter-rouge">:deps</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="no">:paths</span><span class="w"> </span><span class="p">[</span><span class="s">"src"</span><span class="w"> </span><span class="s">"resources"</span><span class="p">]</span><span class="w">

 </span><span class="no">:deps</span><span class="w">
 </span><span class="p">{</span><span class="n">org.clojure/clojure</span><span class="w"> </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"1.11.1"</span><span class="p">}</span><span class="w">

  </span><span class="c1">;; --- Dependências do Backend (API REST) ---</span><span class="w">
  </span><span class="n">ring/ring-core</span><span class="w">          </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"1.12.2"</span><span class="p">}</span><span class="w">
  </span><span class="n">ring/ring-jetty-adapter</span><span class="w"> </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"1.12.2"</span><span class="p">}</span><span class="w">
  </span><span class="n">ring/ring-json</span><span class="w">          </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"0.5.1"</span><span class="p">}</span><span class="w">
  </span><span class="n">metosin/reitit-ring</span><span class="w">     </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"0.7.0"</span><span class="p">}</span><span class="w">

  </span><span class="c1">;; --- Dependências do Frontend ---</span><span class="w">
  </span><span class="c1">;; ADICIONE ESTAS DUAS LINHAS:</span><span class="w">
  </span><span class="c1">;; (a versão do shadow-cljs é a MESMA que instalamos via npm!)</span><span class="w">
  </span><span class="n">thheller/shadow-cljs</span><span class="w">    </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"2.28.23"</span><span class="p">}</span><span class="w">
  </span><span class="n">reagent/reagent</span><span class="w">         </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"2.0.0"</span><span class="p">}}</span><span class="w">

 </span><span class="no">:aliases</span><span class="w">
 </span><span class="p">{</span><span class="no">:run</span><span class="w">
  </span><span class="p">{</span><span class="no">:main-opts</span><span class="w"> </span><span class="p">[</span><span class="s">"-m"</span><span class="w"> </span><span class="s">"todo.backend.core"</span><span class="p">]}}}</span><span class="w">
</span></code></pre></div></div> <h3 id="o-que-fizemos-8">O que fizemos?</h3> <p>Nossa “lista de compras” Clojure agora inclui:</p> <ol> <li><strong><code class="language-plaintext highlighter-rouge">thheller/shadow-cljs</code></strong> <code class="language-plaintext highlighter-rouge">2.28.23</code>: o compilador — na mesma versão do pacote npm.</li> <li><strong><code class="language-plaintext highlighter-rouge">reagent/reagent</code></strong> <code class="language-plaintext highlighter-rouge">2.0.0</code>: a biblioteca de UI, na versão moderna que funciona com React 18/19.</li> </ol> <h3 id="passo-33-configurar-o-compilador-shadow-cljsedn">Passo 3.3: Configurar o Compilador (<code class="language-plaintext highlighter-rouge">shadow-cljs.edn</code>)</h3> <p>Temos as bibliotecas, mas precisamos de um arquivo para dizer ao <code class="language-plaintext highlighter-rouge">shadow-cljs</code> <em>como</em> compilar nosso frontend. Este arquivo é o “cérebro” do shadow-cljs.</p> <p><strong>Ação:</strong> Crie um arquivo <strong>novo</strong> chamado <code class="language-plaintext highlighter-rouge">shadow-cljs.edn</code> na <strong>raiz</strong> do projeto (ao lado do <code class="language-plaintext highlighter-rouge">deps.edn</code>) e cole:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="no">:deps</span><span class="w"> </span><span class="n">true</span><span class="w"> </span><span class="c1">;; 1. Informa ao shadow-cljs para usar o deps.edn</span><span class="w">

 </span><span class="no">:builds</span><span class="w">
 </span><span class="p">{</span><span class="no">:app</span><span class="w"> </span><span class="c1">;; Damos ao nosso "build" (processo) o nome de ":app"</span><span class="w">
  </span><span class="p">{</span><span class="c1">;; :browser = o destino do nosso código é um navegador web</span><span class="w">
   </span><span class="no">:target</span><span class="w"> </span><span class="no">:browser</span><span class="w">

   </span><span class="c1">;; Onde o JavaScript compilado deve ser salvo</span><span class="w">
   </span><span class="no">:output-dir</span><span class="w"> </span><span class="s">"resources/public/js"</span><span class="w">

   </span><span class="c1">;; O caminho que o navegador usará para acessar esses arquivos</span><span class="w">
   </span><span class="no">:asset-path</span><span class="w"> </span><span class="s">"/js"</span><span class="w">

   </span><span class="c1">;; O "ponto de entrada" do nosso código.</span><span class="w">
   </span><span class="c1">;; Diz ao shadow-cljs para compilar o namespace 'todo.frontend.core'</span><span class="w">
   </span><span class="c1">;; e, quando a página carregar, chamar a função 'init'.</span><span class="w">
   </span><span class="no">:modules</span><span class="w"> </span><span class="p">{</span><span class="no">:main</span><span class="w"> </span><span class="p">{</span><span class="no">:init-fn</span><span class="w"> </span><span class="n">todo.frontend.core/init</span><span class="p">}}</span><span class="w">

   </span><span class="c1">;; Configurações do servidor de desenvolvimento</span><span class="w">
   </span><span class="no">:devtools</span><span class="w">
   </span><span class="p">{</span><span class="c1">;; O servidor serve os arquivos estáticos (como o index.html)</span><span class="w">
    </span><span class="c1">;; desta pasta:</span><span class="w">
    </span><span class="no">:http-root</span><span class="w"> </span><span class="s">"resources/public"</span><span class="w">

    </span><span class="c1">;; O servidor roda na porta 8000 (diferente da API, na 3000)</span><span class="w">
    </span><span class="no">:http-port</span><span class="w"> </span><span class="mi">8000</span><span class="p">}}}}</span><span class="w">
</span></code></pre></div></div> <h3 id="o-que-fizemos-9">O que fizemos?</h3> <ol> <li><strong><code class="language-plaintext highlighter-rouge">:deps true</code></strong>: o shadow-cljs vai usar as dependências do nosso <code class="language-plaintext highlighter-rouge">deps.edn</code> (por isso as versões precisam bater!).</li> <li><strong><code class="language-plaintext highlighter-rouge">:output-dir</code></strong>: o JavaScript final irá para <code class="language-plaintext highlighter-rouge">resources/public/js</code> (pasta que o <code class="language-plaintext highlighter-rouge">.gitignore</code> já ignora).</li> <li><strong><code class="language-plaintext highlighter-rouge">:modules</code></strong>: o “ponto de partida” é a função <code class="language-plaintext highlighter-rouge">init</code> no namespace <code class="language-plaintext highlighter-rouge">todo.frontend.core</code> (que ainda não existe — criaremos já já).</li> <li><strong><code class="language-plaintext highlighter-rouge">:devtools</code></strong>: o servidor de desenvolvimento roda na <strong>porta 8000</strong> e serve os arquivos de <code class="language-plaintext highlighter-rouge">resources/public</code>.</li> </ol> <h3 id="passo-34-criar-o-html-base">Passo 3.4: Criar o HTML Base</h3> <p>O shadow-cljs precisa de um <code class="language-plaintext highlighter-rouge">index.html</code> para servir ao navegador. Este arquivo é a “concha” onde nossa aplicação ClojureScript será carregada.</p> <p><strong>Ação 1:</strong> Crie a pasta que o servidor de desenvolvimento usa:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> resources/public
</code></pre></div></div> <p><strong>Ação 2:</strong> Crie o arquivo <code class="language-plaintext highlighter-rouge">resources/public/index.html</code> e cole:</p> <div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">&lt;!DOCTYPE html&gt;</span>
<span class="nt">&lt;html&gt;</span>
  <span class="nt">&lt;head&gt;</span>
    <span class="nt">&lt;meta</span> <span class="na">charset=</span><span class="s">"UTF-8"</span> <span class="nt">/&gt;</span>
    <span class="nt">&lt;meta</span> <span class="na">name=</span><span class="s">"viewport"</span> <span class="na">content=</span><span class="s">"width=device-width, initial-scale=1"</span> <span class="nt">/&gt;</span>
    <span class="nt">&lt;title&gt;</span>Todo App (CLJS)<span class="nt">&lt;/title&gt;</span>
  <span class="nt">&lt;/head&gt;</span>
  <span class="nt">&lt;body&gt;</span>
    <span class="nt">&lt;div</span> <span class="na">id=</span><span class="s">"app"</span><span class="nt">&gt;</span>Carregando...<span class="nt">&lt;/div&gt;</span>

    <span class="nt">&lt;script </span><span class="na">src=</span><span class="s">"/js/main.js"</span><span class="nt">&gt;&lt;/script&gt;</span>
  <span class="nt">&lt;/body&gt;</span>
<span class="nt">&lt;/html&gt;</span>
</code></pre></div></div> <h3 id="o-que-fizemos-10">O que fizemos?</h3> <ol> <li><strong><code class="language-plaintext highlighter-rouge">&lt;div id="app"&gt;</code></strong>: o “ponto de montagem” <strong>crucial</strong>. Quando nossa aplicação iniciar, ela vai procurar este <code class="language-plaintext highlighter-rouge">div</code> e injetar toda a interface dentro dele.</li> <li><strong><code class="language-plaintext highlighter-rouge">&lt;script src="/js/main.js"&gt;</code></strong>: carrega o JavaScript compilado que o shadow-cljs vai gerar (o nome <code class="language-plaintext highlighter-rouge">main.js</code> vem do módulo <code class="language-plaintext highlighter-rouge">:main</code> do <code class="language-plaintext highlighter-rouge">shadow-cljs.edn</code>).</li> </ol> <h3 id="passo-35-olá-reagent-ui-estática">Passo 3.5: “Olá, Reagent!” (UI Estática)</h3> <p><strong>Objetivo:</strong> provar que o compilador consegue ler nosso arquivo <code class="language-plaintext highlighter-rouge">.cljs</code>, transformá-lo em JavaScript e injetá-lo no <code class="language-plaintext highlighter-rouge">index.html</code>. Sem estado, sem botões — apenas um “Olá”.</p> <p><strong>Ação 1:</strong> Crie a pasta do frontend:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">mkdir</span> <span class="nt">-p</span> src/todo/frontend
</code></pre></div></div> <p><strong>Ação 2:</strong> Crie o arquivo <code class="language-plaintext highlighter-rouge">src/todo/frontend/core.cljs</code> e cole:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.frontend.core</span><span class="w">
  </span><span class="c1">;; Requeremos o "coração" do Reagent (para 'r/atom' e 'r/as-element')</span><span class="w">
  </span><span class="p">(</span><span class="no">:require</span><span class="w"> </span><span class="p">[</span><span class="n">reagent.core</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">r</span><span class="p">]</span><span class="w">
            </span><span class="c1">;; E o DOM do Reagent (para 'create-root', API do React 18+)</span><span class="w">
            </span><span class="p">[</span><span class="n">reagent.dom.client</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">rdom</span><span class="p">]))</span><span class="w">

</span><span class="c1">;; --- 1. O Componente ---</span><span class="w">
</span><span class="c1">;; Um componente em Reagent é apenas uma função</span><span class="w">
</span><span class="c1">;; que retorna "Hiccup" (HTML escrito como vetores CLJS).</span><span class="w">
</span><span class="c1">;;</span><span class="w">
</span><span class="c1">;; [:h1 "Olá"] -&gt; &lt;h1&gt;Olá&lt;/h1&gt;</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">hello-world</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:div</span><span class="w">
   </span><span class="p">[</span><span class="no">:h1</span><span class="w"> </span><span class="s">"Olá, Alunos!"</span><span class="p">]</span><span class="w">
   </span><span class="p">[</span><span class="no">:p</span><span class="w"> </span><span class="s">"Nossa aplicação ClojureScript está funcionando."</span><span class="p">]])</span><span class="w">

</span><span class="c1">;; --- 2. A Inicialização (React 18+) ---</span><span class="w">
</span><span class="c1">;; Esta é a função que o shadow-cljs.edn chama.</span><span class="w">
</span><span class="c1">;; Ela "monta" nosso componente [hello-world]</span><span class="w">
</span><span class="c1">;; no &lt;div id="app"&gt; do nosso index.html.</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="o">^</span><span class="no">:export</span><span class="w"> </span><span class="n">init</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">(</span><span class="nb">println</span><span class="w"> </span><span class="s">"Frontend inicializado..."</span><span class="p">)</span><span class="w">
  </span><span class="p">(</span><span class="k">let</span><span class="w"> </span><span class="p">[</span><span class="nb">root</span><span class="w"> </span><span class="p">(</span><span class="nf">rdom/create-root</span><span class="w"> </span><span class="p">(</span><span class="nf">js/document.getElementById</span><span class="w"> </span><span class="s">"app"</span><span class="p">))]</span><span class="w">
    </span><span class="p">(</span><span class="nf">.render</span><span class="w"> </span><span class="nb">root</span><span class="w"> </span><span class="p">(</span><span class="nf">r/as-element</span><span class="w"> </span><span class="p">[</span><span class="n">hello-world</span><span class="p">]))))</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 3: Compile e rode</strong></p> <p>No terminal (pode ser o mesmo de antes, já que o backend está desligado):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npx shadow-cljs watch app
</code></pre></div></div> <ul> <li>Na <strong>primeira vez</strong>, o shadow-cljs vai baixar várias dependências — pode demorar alguns minutos. Espere até ver algo como <code class="language-plaintext highlighter-rouge">Build completed</code>.</li> <li>O comando <code class="language-plaintext highlighter-rouge">watch</code> fica <strong>observando</strong> seus arquivos: cada vez que você salvar um <code class="language-plaintext highlighter-rouge">.cljs</code>, ele recompila automaticamente (<em>hot-reload</em>). Deixe este terminal rodando — ele será o nosso <strong>Terminal 2 (Frontend)</strong> daqui em diante.</li> </ul> <p><strong>Ação 4: Abra o navegador em</strong> <code class="language-plaintext highlighter-rouge">http://localhost:8000</code></p> <p><strong>Resultado Esperado:</strong> a página não deve mais mostrar “Carregando…”. Em vez disso:</p> <blockquote> <p><strong>Olá, Alunos!</strong> Nossa aplicação ClojureScript está funcionando.</p> </blockquote> <h3 id="se-algo-deu-errado-1">Se algo deu errado…</h3> <table> <thead> <tr> <th>Sintoma</th> <th>Causa provável</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">shadow-cljs - dependency version mismatch</code></td> <td>A versão do shadow-cljs no <code class="language-plaintext highlighter-rouge">npm</code> é diferente da do <code class="language-plaintext highlighter-rouge">deps.edn</code>. Confira o Passo 3.1/3.2: ambas devem ser <code class="language-plaintext highlighter-rouge">2.28.23</code>.</td> </tr> <tr> <td>Página continua em “Carregando…”</td> <td>O build falhou. Olhe o terminal do <code class="language-plaintext highlighter-rouge">watch</code>: haverá um erro de compilação apontando arquivo e linha. Console do navegador (F12) também ajuda.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">port 8000 already in use</code></td> <td>Outro processo está usando a porta. Pare-o ou mude o <code class="language-plaintext highlighter-rouge">:http-port</code> no <code class="language-plaintext highlighter-rouge">shadow-cljs.edn</code>.</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">Cannot find module 'react'</code></td> <td>O <code class="language-plaintext highlighter-rouge">npm install</code> do Passo 3.1 não foi executado (ou foi executado em outra pasta).</td> </tr> </tbody> </table> <h3 id="passo-36-o-cérebro-reativo-o-ratom">Passo 3.6: O “Cérebro” Reativo (o <code class="language-plaintext highlighter-rouge">r/atom</code>)</h3> <p><strong>A ligação: <code class="language-plaintext highlighter-rouge">atom</code> (Backend) vs. <code class="language-plaintext highlighter-rouge">r/atom</code> (Frontend)</strong></p> <p>Na <strong>Fase 2</strong>, aprendemos o <code class="language-plaintext highlighter-rouge">atom</code> do Clojure:</p> <ul> <li><strong>O problema:</strong> a imutabilidade — não podíamos “mudar” um mapa.</li> <li><strong>A solução:</strong> uma “caixa” segura onde <em>trocamos</em> (<code class="language-plaintext highlighter-rouge">swap!</code>) o valor antigo por um novo.</li> </ul> <p>No <strong>frontend</strong>, temos um problema parecido: como a UI (o HTML) vai “saber” quando um valor muda? A solução do Reagent é a sua própria versão do atom: <code class="language-plaintext highlighter-rouge">reagent.core/atom</code> (que apelidamos de <code class="language-plaintext highlighter-rouge">r/atom</code>).</p> <p><strong>Qual é a diferença?</strong></p> <ol> <li><strong>Sintaxe:</strong> nenhuma! Você usa <code class="language-plaintext highlighter-rouge">@</code> para ler, <code class="language-plaintext highlighter-rouge">swap!</code> para atualizar e <code class="language-plaintext highlighter-rouge">reset!</code> para substituir — exatamente como aprendeu.</li> <li><strong>A “mágica”:</strong> o <code class="language-plaintext highlighter-rouge">r/atom</code> é uma caixa que <strong>“toca um sino”</strong>. Qualquer componente que “lê” um <code class="language-plaintext highlighter-rouge">r/atom</code> (usando <code class="language-plaintext highlighter-rouge">@</code>) está “ouvindo” esse sino. Quando você usa <code class="language-plaintext highlighter-rouge">swap!</code> ou <code class="language-plaintext highlighter-rouge">reset!</code>, o sino toca, e o Reagent redesenha automaticamente <em>apenas</em> os componentes que estavam ouvindo.</li> </ol> <p>Vamos provar isso com um contador.</p> <p><strong>Ação 1:</strong> <em>Substitua</em> todo o conteúdo do <code class="language-plaintext highlighter-rouge">src/todo/frontend/core.cljs</code> por:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.frontend.core</span><span class="w">
  </span><span class="p">(</span><span class="no">:require</span><span class="w"> </span><span class="p">[</span><span class="n">reagent.core</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">r</span><span class="p">]</span><span class="w">
            </span><span class="p">[</span><span class="n">reagent.dom.client</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">rdom</span><span class="p">]))</span><span class="w">

</span><span class="c1">;; --- 1. O "Cérebro" (o r/atom) ---</span><span class="w">
</span><span class="c1">;;</span><span class="w">
</span><span class="c1">;; (defonce) garante que o valor (o 0) NÃO seja resetado</span><span class="w">
</span><span class="c1">;; toda vez que o "hot-reload" do shadow-cljs recompilar.</span><span class="w">
</span><span class="p">(</span><span class="nf">defonce</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="p">(</span><span class="nf">r/atom</span><span class="w"> </span><span class="mi">0</span><span class="p">))</span><span class="w">

</span><span class="c1">;; --- 2. O Componente ---</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">counter-app</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:div</span><span class="w">
   </span><span class="p">[</span><span class="no">:h1</span><span class="w"> </span><span class="s">"Entendendo o 'r/atom'"</span><span class="p">]</span><span class="w">

   </span><span class="c1">;; (Leitura): "lemos" o valor dentro da caixa usando '@'</span><span class="w">
   </span><span class="p">[</span><span class="no">:p</span><span class="w"> </span><span class="s">"O valor atual do contador é: "</span><span class="w"> </span><span class="o">@</span><span class="n">app-state</span><span class="p">]</span><span class="w">

   </span><span class="p">[</span><span class="no">:button</span><span class="w">
    </span><span class="c1">;; (Escrita): no clique, usamos 'swap!' para "trocar"</span><span class="w">
    </span><span class="c1">;; o valor na caixa, aplicando a função 'inc'.</span><span class="w">
    </span><span class="p">{</span><span class="no">:on-click</span><span class="w"> </span><span class="o">#</span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="nb">inc</span><span class="p">)}</span><span class="w">
    </span><span class="s">"Clique para Incrementar"</span><span class="p">]</span><span class="w">

   </span><span class="p">[</span><span class="no">:button</span><span class="w">
    </span><span class="p">{</span><span class="no">:on-click</span><span class="w"> </span><span class="o">#</span><span class="p">(</span><span class="nf">reset!</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="mi">0</span><span class="p">)}</span><span class="w">
    </span><span class="s">"Resetar"</span><span class="p">]])</span><span class="w">

</span><span class="c1">;; --- 3. A Inicialização ---</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="o">^</span><span class="no">:export</span><span class="w"> </span><span class="n">init</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">(</span><span class="nb">println</span><span class="w"> </span><span class="s">"Frontend 'Contador' inicializado..."</span><span class="p">)</span><span class="w">
  </span><span class="p">(</span><span class="k">let</span><span class="w"> </span><span class="p">[</span><span class="nb">root</span><span class="w"> </span><span class="p">(</span><span class="nf">rdom/create-root</span><span class="w"> </span><span class="p">(</span><span class="nf">js/document.getElementById</span><span class="w"> </span><span class="s">"app"</span><span class="p">))]</span><span class="w">
    </span><span class="p">(</span><span class="nf">.render</span><span class="w"> </span><span class="nb">root</span><span class="w"> </span><span class="p">(</span><span class="nf">r/as-element</span><span class="w"> </span><span class="p">[</span><span class="n">counter-app</span><span class="p">]))))</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 2 (Teste):</strong></p> <ol> <li>Salve o arquivo. O shadow-cljs (Terminal 2) recompila sozinho.</li> <li>Vá ao navegador em <code class="language-plaintext highlighter-rouge">http://localhost:8000</code>.</li> <li><strong>Teste a mágica:</strong> clique nos botões. O número muda instantaneamente, sem recarregar a página.</li> </ol> <p><strong>A lição:</strong> você acabou de aprender o “coração” do Reagent: <code class="language-plaintext highlighter-rouge">(defonce app-state (r/atom ...))</code> (o cérebro), <code class="language-plaintext highlighter-rouge">@app-state</code> (leitura) e <code class="language-plaintext highlighter-rouge">swap!</code>/<code class="language-plaintext highlighter-rouge">reset!</code> (escrita).</p> <h3 id="passo-37-a-ui-estática-do-todo-com-dados-falsos">Passo 3.7: A UI Estática do Todo (com dados falsos)</h3> <p><strong>Objetivo:</strong> agora que sabemos <em>como</em> o Reagent funciona, vamos construir o <em>visual</em> (estático) do Todo App. Ele não vai funcionar ainda — apenas parecer correto.</p> <p><strong>Ação:</strong> <em>Substitua</em> o conteúdo do <code class="language-plaintext highlighter-rouge">core.cljs</code> (o contador) por este, agora separado em componentes:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.frontend.core</span><span class="w">
  </span><span class="p">(</span><span class="no">:require</span><span class="w"> </span><span class="p">[</span><span class="n">reagent.core</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">r</span><span class="p">]</span><span class="w">
            </span><span class="p">[</span><span class="n">reagent.dom.client</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">rdom</span><span class="p">]))</span><span class="w">

</span><span class="c1">;; --- 1. Nossos Componentes (Estáticos) ---</span><span class="w">

</span><span class="c1">;; O Formulário (não faz nada ainda)</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">todo-form</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:div.todo-input</span><span class="w">
   </span><span class="p">[</span><span class="no">:input</span><span class="w"> </span><span class="p">{</span><span class="no">:type</span><span class="w"> </span><span class="s">"text"</span><span class="w"> </span><span class="no">:placeholder</span><span class="w"> </span><span class="s">"O que precisa ser feito?"</span><span class="p">}]</span><span class="w">
   </span><span class="p">[</span><span class="no">:button</span><span class="w"> </span><span class="s">"Adicionar (Desligado)"</span><span class="p">]])</span><span class="w">

</span><span class="c1">;; A Lista (recebe uma lista de "todos" como argumento)</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">todo-list</span><span class="w"> </span><span class="p">[</span><span class="n">todos</span><span class="p">]</span><span class="w"> </span><span class="c1">;; "todos" é um argumento!</span><span class="w">
  </span><span class="p">[</span><span class="no">:ul.todo-list</span><span class="w">
   </span><span class="p">(</span><span class="k">for</span><span class="w"> </span><span class="p">[</span><span class="n">todo</span><span class="w"> </span><span class="n">todos</span><span class="p">]</span><span class="w">
     </span><span class="o">^</span><span class="p">{</span><span class="no">:key</span><span class="w"> </span><span class="p">(</span><span class="no">:id</span><span class="w"> </span><span class="n">todo</span><span class="p">)}</span><span class="w">
     </span><span class="p">[</span><span class="no">:li.todo-item</span><span class="w">
      </span><span class="p">(</span><span class="no">:title</span><span class="w"> </span><span class="n">todo</span><span class="p">)])])</span><span class="w">

</span><span class="c1">;; O App Principal (que monta tudo)</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">app</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:div.todo-app</span><span class="w">
   </span><span class="p">[</span><span class="no">:h1</span><span class="w"> </span><span class="s">"Todo App (Estático)"</span><span class="p">]</span><span class="w">
   </span><span class="p">[</span><span class="no">:p</span><span class="w"> </span><span class="s">"Isto é apenas o visual. Nada funciona ainda."</span><span class="p">]</span><span class="w">

   </span><span class="c1">;; 1. Renderiza o formulário</span><span class="w">
   </span><span class="p">[</span><span class="n">todo-form</span><span class="p">]</span><span class="w">

   </span><span class="c1">;; 2. Renderiza a lista, passando "dados falsos"</span><span class="w">
   </span><span class="p">[</span><span class="n">todo-list</span><span class="w"> </span><span class="p">[{</span><span class="no">:id</span><span class="w"> </span><span class="mi">1</span><span class="w"> </span><span class="no">:title</span><span class="w"> </span><span class="s">"Meu primeiro item (falso)"</span><span class="p">}</span><span class="w">
               </span><span class="p">{</span><span class="no">:id</span><span class="w"> </span><span class="mi">2</span><span class="w"> </span><span class="no">:title</span><span class="w"> </span><span class="s">"Meu segundo item (falso)"</span><span class="p">}]]])</span><span class="w">

</span><span class="c1">;; --- 2. A Inicialização ---</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="o">^</span><span class="no">:export</span><span class="w"> </span><span class="n">init</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">(</span><span class="nb">println</span><span class="w"> </span><span class="s">"Frontend 'Todo Estático' inicializado..."</span><span class="p">)</span><span class="w">
  </span><span class="p">(</span><span class="k">let</span><span class="w"> </span><span class="p">[</span><span class="nb">root</span><span class="w"> </span><span class="p">(</span><span class="nf">rdom/create-root</span><span class="w"> </span><span class="p">(</span><span class="nf">js/document.getElementById</span><span class="w"> </span><span class="s">"app"</span><span class="p">))]</span><span class="w">
    </span><span class="p">(</span><span class="nf">.render</span><span class="w"> </span><span class="nb">root</span><span class="w"> </span><span class="p">(</span><span class="nf">r/as-element</span><span class="w"> </span><span class="p">[</span><span class="n">app</span><span class="p">]))))</span><span class="w">
</span></code></pre></div></div> <aside> <p>💡 <strong>O que é o <code class="language-plaintext highlighter-rouge">^{:key ...}</code>?</strong> Quando renderizamos uma lista com <code class="language-plaintext highlighter-rouge">for</code>, o React precisa de uma “identidade” para cada item, para saber o que mudou entre uma renderização e outra. O metadado <code class="language-plaintext highlighter-rouge">^{:key (:id todo)}</code> fornece essa identidade. Sem ele, o console mostra o aviso <em>“Every element in a seq should have a unique :key”</em>. <strong>Guarde esse detalhe: ele vai reaparecer na Fase 5.</strong></p> </aside> <p><strong>Teste:</strong> salve e veja no navegador. Você deve ver a UI completa, com os dois todos falsos listados. O campo e o botão estão lá, mas clicar não faz nada.</p> <h3 id="passo-38-conectando-a-ui-ao-ratom-tornando-interativo">Passo 3.8: Conectando a UI ao <code class="language-plaintext highlighter-rouge">r/atom</code> (Tornando Interativo)</h3> <p>Agora vamos fazer o formulário funcionar de verdade — mas ainda <strong>100% local</strong>, sem API.</p> <p><strong>Ação 1: Adicionar o <code class="language-plaintext highlighter-rouge">clojure.string</code></strong></p> <p>No <code class="language-plaintext highlighter-rouge">ns</code> do <code class="language-plaintext highlighter-rouge">core.cljs</code>, precisamos do <code class="language-plaintext highlighter-rouge">clojure.string</code> para checar se o input está em branco. <em>Mude isto:</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.frontend.core</span><span class="w">
  </span><span class="p">(</span><span class="no">:require</span><span class="w"> </span><span class="p">[</span><span class="n">reagent.core</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">r</span><span class="p">]</span><span class="w">
            </span><span class="p">[</span><span class="n">reagent.dom.client</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">rdom</span><span class="p">]))</span><span class="w">
</span></code></pre></div></div> <p><em>Para isto:</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.frontend.core</span><span class="w">
  </span><span class="p">(</span><span class="no">:require</span><span class="w"> </span><span class="p">[</span><span class="n">reagent.core</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">r</span><span class="p">]</span><span class="w">
            </span><span class="p">[</span><span class="n">reagent.dom.client</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">rdom</span><span class="p">]</span><span class="w">
            </span><span class="p">[</span><span class="n">clojure.string</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="nb">str</span><span class="p">]))</span><span class="w"> </span><span class="c1">;; &lt;- ADICIONE ESTA LINHA</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 2: Adicionar o “Cérebro” (<code class="language-plaintext highlighter-rouge">app-state</code>) e a lógica local</strong></p> <p>Logo abaixo do <code class="language-plaintext highlighter-rouge">ns</code>, <strong>adicione</strong> este bloco. Este é o <code class="language-plaintext highlighter-rouge">r/atom</code> central e a função local que o modifica:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">;; --- O "Cérebro" Reativo ---</span><span class="w">
</span><span class="p">(</span><span class="nf">defonce</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="p">(</span><span class="nf">r/atom</span><span class="w"> </span><span class="p">{</span><span class="no">:next-id</span><span class="w"> </span><span class="mi">1</span><span class="w">
                            </span><span class="no">:input-text</span><span class="w"> </span><span class="s">""</span><span class="w">
                            </span><span class="no">:todos</span><span class="w"> </span><span class="p">[]}))</span><span class="w">

</span><span class="c1">;; --- A Lógica de Negócios (Local) ---</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">adicionar-todo-local</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">app-state</span><span class="w">
         </span><span class="p">(</span><span class="k">fn</span><span class="w"> </span><span class="p">[</span><span class="n">estado-atual</span><span class="p">]</span><span class="w">
           </span><span class="p">(</span><span class="k">let</span><span class="w"> </span><span class="p">[</span><span class="n">novo-titulo</span><span class="w"> </span><span class="p">(</span><span class="no">:input-text</span><span class="w"> </span><span class="n">estado-atual</span><span class="p">)</span><span class="w">
                 </span><span class="n">novo-id</span><span class="w"> </span><span class="p">(</span><span class="no">:next-id</span><span class="w"> </span><span class="n">estado-atual</span><span class="p">)]</span><span class="w">
             </span><span class="p">(</span><span class="k">if</span><span class="w"> </span><span class="p">(</span><span class="nf">str/blank?</span><span class="w"> </span><span class="n">novo-titulo</span><span class="p">)</span><span class="w">
               </span><span class="n">estado-atual</span><span class="w"> </span><span class="c1">;; Não faz nada se vazio</span><span class="w">
               </span><span class="c1">;; Retorna um NOVO estado</span><span class="w">
               </span><span class="p">{</span><span class="no">:next-id</span><span class="w"> </span><span class="p">(</span><span class="nb">inc</span><span class="w"> </span><span class="n">novo-id</span><span class="p">)</span><span class="w">
                </span><span class="no">:input-text</span><span class="w"> </span><span class="s">""</span><span class="w"> </span><span class="c1">;; Limpa o input</span><span class="w">
                </span><span class="no">:todos</span><span class="w"> </span><span class="p">(</span><span class="nb">conj</span><span class="w"> </span><span class="p">(</span><span class="no">:todos</span><span class="w"> </span><span class="n">estado-atual</span><span class="p">)</span><span class="w">
                             </span><span class="p">{</span><span class="no">:id</span><span class="w"> </span><span class="n">novo-id</span><span class="w">
                              </span><span class="no">:title</span><span class="w"> </span><span class="n">novo-titulo</span><span class="p">})})))))</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 3: Modificar o <code class="language-plaintext highlighter-rouge">todo-form</code></strong></p> <p>Encontre a função <code class="language-plaintext highlighter-rouge">todo-form</code> e conecte-a ao <code class="language-plaintext highlighter-rouge">app-state</code>. <em>Mude esta versão (estática):</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">todo-form</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:div.todo-input</span><span class="w">
   </span><span class="p">[</span><span class="no">:input</span><span class="w"> </span><span class="p">{</span><span class="no">:type</span><span class="w"> </span><span class="s">"text"</span><span class="w"> </span><span class="no">:placeholder</span><span class="w"> </span><span class="s">"O que precisa ser feito?"</span><span class="p">}]</span><span class="w">
   </span><span class="p">[</span><span class="no">:button</span><span class="w"> </span><span class="s">"Adicionar (Desligado)"</span><span class="p">]])</span><span class="w">
</span></code></pre></div></div> <p><em>Para esta versão (conectada):</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">todo-form</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:div.todo-input</span><span class="w">
   </span><span class="p">[</span><span class="no">:input</span><span class="w">
    </span><span class="p">{</span><span class="no">:type</span><span class="w"> </span><span class="s">"text"</span><span class="w">
     </span><span class="no">:placeholder</span><span class="w"> </span><span class="s">"O que precisa ser feito?"</span><span class="w">
     </span><span class="c1">;; (Leitura): o valor do input vem do app-state</span><span class="w">
     </span><span class="no">:value</span><span class="w"> </span><span class="p">(</span><span class="no">:input-text</span><span class="w"> </span><span class="o">@</span><span class="n">app-state</span><span class="p">)</span><span class="w">
     </span><span class="c1">;; (Escrita): o on-change atualiza o app-state a cada tecla</span><span class="w">
     </span><span class="no">:on-change</span><span class="w"> </span><span class="o">#</span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="nb">assoc</span><span class="w"> </span><span class="no">:input-text</span><span class="w"> </span><span class="p">(</span><span class="nb">-&gt;</span><span class="w"> </span><span class="n">%</span><span class="w"> </span><span class="n">.-target</span><span class="w"> </span><span class="n">.-value</span><span class="p">))}]</span><span class="w">

   </span><span class="p">[</span><span class="no">:button</span><span class="w">
    </span><span class="c1">;; (Ação): o on-click chama nossa lógica local</span><span class="w">
    </span><span class="p">{</span><span class="no">:on-click</span><span class="w"> </span><span class="n">adicionar-todo-local</span><span class="p">}</span><span class="w">
    </span><span class="s">"Adicionar (Local)"</span><span class="p">]])</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 4: Modificar o <code class="language-plaintext highlighter-rouge">todo-list</code></strong></p> <p>Vamos remover o argumento <code class="language-plaintext highlighter-rouge">[todos]</code> e fazê-la ler diretamente do <code class="language-plaintext highlighter-rouge">app-state</code>. <em>Mude esta versão:</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">todo-list</span><span class="w"> </span><span class="p">[</span><span class="n">todos</span><span class="p">]</span><span class="w"> </span><span class="c1">;; &lt;-- Recebe "todos" como argumento</span><span class="w">
  </span><span class="p">[</span><span class="no">:ul.todo-list</span><span class="w">
   </span><span class="p">(</span><span class="k">for</span><span class="w"> </span><span class="p">[</span><span class="n">todo</span><span class="w"> </span><span class="n">todos</span><span class="p">]</span><span class="w">
     </span><span class="o">^</span><span class="p">{</span><span class="no">:key</span><span class="w"> </span><span class="p">(</span><span class="no">:id</span><span class="w"> </span><span class="n">todo</span><span class="p">)}</span><span class="w">
     </span><span class="p">[</span><span class="no">:li.todo-item</span><span class="w">
      </span><span class="p">(</span><span class="no">:title</span><span class="w"> </span><span class="n">todo</span><span class="p">)])])</span><span class="w">
</span></code></pre></div></div> <p><em>Para esta versão:</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">todo-list</span><span class="w"> </span><span class="p">[]</span><span class="w"> </span><span class="c1">;; &lt;-- Argumento "todos" REMOVIDO</span><span class="w">
  </span><span class="p">[</span><span class="no">:ul.todo-list</span><span class="w">
   </span><span class="c1">;; (Leitura): o 'for' agora observa o @app-state</span><span class="w">
   </span><span class="p">(</span><span class="k">for</span><span class="w"> </span><span class="p">[</span><span class="n">todo</span><span class="w"> </span><span class="p">(</span><span class="no">:todos</span><span class="w"> </span><span class="o">@</span><span class="n">app-state</span><span class="p">)]</span><span class="w">
     </span><span class="o">^</span><span class="p">{</span><span class="no">:key</span><span class="w"> </span><span class="p">(</span><span class="no">:id</span><span class="w"> </span><span class="n">todo</span><span class="p">)}</span><span class="w">
     </span><span class="p">[</span><span class="no">:li.todo-item</span><span class="w">
      </span><span class="p">(</span><span class="no">:title</span><span class="w"> </span><span class="n">todo</span><span class="p">)])])</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 5: Modificar o <code class="language-plaintext highlighter-rouge">app</code> (remover os dados falsos)</strong></p> <p><em>Mude esta versão:</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">app</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:div.todo-app</span><span class="w">
   </span><span class="p">[</span><span class="no">:h1</span><span class="w"> </span><span class="s">"Todo App (Estático)"</span><span class="p">]</span><span class="w">
   </span><span class="p">[</span><span class="no">:p</span><span class="w"> </span><span class="s">"Isto é apenas o visual. Nada funciona ainda."</span><span class="p">]</span><span class="w">

   </span><span class="p">[</span><span class="n">todo-form</span><span class="p">]</span><span class="w">

   </span><span class="p">[</span><span class="n">todo-list</span><span class="w"> </span><span class="p">[{</span><span class="no">:id</span><span class="w"> </span><span class="mi">1</span><span class="w"> </span><span class="no">:title</span><span class="w"> </span><span class="s">"Meu primeiro item (falso)"</span><span class="p">}</span><span class="w">
               </span><span class="p">{</span><span class="no">:id</span><span class="w"> </span><span class="mi">2</span><span class="w"> </span><span class="no">:title</span><span class="w"> </span><span class="s">"Meu segundo item (falso)"</span><span class="p">}]]])</span><span class="w">
</span></code></pre></div></div> <p><em>Para esta versão (limpa e interativa):</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">app</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:div.todo-app</span><span class="w">
   </span><span class="p">[</span><span class="no">:h1</span><span class="w"> </span><span class="s">"Todo App (Somente Frontend)"</span><span class="p">]</span><span class="w">
   </span><span class="p">[</span><span class="no">:p</span><span class="w"> </span><span class="s">"Isto é 100% local. Recarregue (F5) para ver os dados sumirem."</span><span class="p">]</span><span class="w">

   </span><span class="c1">;; Os componentes agora se viram sozinhos!</span><span class="w">
   </span><span class="p">[</span><span class="n">todo-form</span><span class="p">]</span><span class="w">
   </span><span class="p">[</span><span class="n">todo-list</span><span class="p">]])</span><span class="w">
</span></code></pre></div></div> <h3 id="teste-a-lição">Teste (A “Lição”)</h3> <ol> <li>Salve o <code class="language-plaintext highlighter-rouge">core.cljs</code>. O shadow-cljs recompila.</li> <li>Vá ao navegador em <code class="language-plaintext highlighter-rouge">http://localhost:8000</code>.</li> </ol> <p><strong>Teste a interatividade:</strong></p> <ol> <li>A lista deve estar vazia.</li> <li>Digite “Meu primeiro todo” e clique em “Adicionar (Local)”.</li> <li><strong>Mágica:</strong> o todo aparece na lista <em>instantaneamente</em>, e o input é limpo.</li> </ol> <p><strong>O “Aha!”:</strong> agora, <strong>recarregue a página (F5)</strong>.</p> <p><em>Todos os todos que você adicionou desapareceram.</em></p> <p>Isso prova que o estado vivia apenas na memória do navegador. Agora temos uma <strong>motivação</strong> clara para a próxima fase: como fazer essa lista persistir? A resposta é a nossa API do backend.</p> <h3 id="passo-39-git-checkpoint">Passo 3.9: Git Checkpoint</h3> <p><strong>Por que fazemos isso?</strong> Acabamos de construir uma “mini-aplicação” de frontend completa: <code class="language-plaintext highlighter-rouge">npm</code>, <code class="language-plaintext highlighter-rouge">shadow-cljs</code>, <code class="language-plaintext highlighter-rouge">react</code> e a reatividade do Reagent. É o ponto de salvamento perfeito antes de misturar a complexidade da API.</p> <p><strong>Ação 1:</strong> Pare o <code class="language-plaintext highlighter-rouge">shadow-cljs</code> (<code class="language-plaintext highlighter-rouge">Ctrl+C</code> no Terminal 2) e verifique:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
</code></pre></div></div> <p>Você deve ver os novos arquivos e modificações: <code class="language-plaintext highlighter-rouge">package.json</code>, <code class="language-plaintext highlighter-rouge">package-lock.json</code>, <code class="language-plaintext highlighter-rouge">shadow-cljs.edn</code>, <code class="language-plaintext highlighter-rouge">resources/public/index.html</code>, <code class="language-plaintext highlighter-rouge">src/todo/frontend/core.cljs</code> e o <code class="language-plaintext highlighter-rouge">deps.edn</code> modificado. (Repare que <code class="language-plaintext highlighter-rouge">node_modules/</code> e <code class="language-plaintext highlighter-rouge">resources/public/js/</code> <strong>não aparecem</strong> — obrigado, <code class="language-plaintext highlighter-rouge">.gitignore</code>!)</p> <p><strong>Ação 2:</strong> Prepare e salve — este é <strong>um único commit</strong>, com exatamente esta mensagem (ela é um dos marcos avaliados):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"feat: implementa UI do frontend com estado local (sem API)"</span>
</code></pre></div></div> <hr/> <p><strong>Fim da Fase 3!</strong> 🏁</p> <p>Temos um backend funcional (Fase 2) e um frontend funcional (Fase 3), em commits separados. Eles ainda não se “conhecem”.</p> <hr/> <h1 id="fase-4-conectando-o-frontend-ao-backend">Fase 4: Conectando o Frontend ao Backend</h1> <p><strong>Objetivo:</strong> Substituir o estado local “de mentira” do frontend por chamadas reais à nossa API — e, no caminho, encontrar (de propósito!) e corrigir o famoso erro de <strong>CORS</strong>.</p> <h3 id="passo-41-adicionar-o-coreasync-ao-depsedn">Passo 4.1: Adicionar o <code class="language-plaintext highlighter-rouge">core.async</code> ao <code class="language-plaintext highlighter-rouge">deps.edn</code></h3> <p>Para conversar com a API, o frontend fará requisições <strong>assíncronas</strong> (<code class="language-plaintext highlighter-rouge">fetch</code>). Em ClojureScript, a forma idiomática de lidar com assincronia é a biblioteca <code class="language-plaintext highlighter-rouge">core.async</code> (com os blocos <code class="language-plaintext highlighter-rouge">go</code> e o operador <code class="language-plaintext highlighter-rouge">&lt;p!</code>, que “espera” uma Promise do JavaScript).</p> <p><strong>Ação:</strong> Abra o <code class="language-plaintext highlighter-rouge">deps.edn</code> e adicione o <code class="language-plaintext highlighter-rouge">core.async</code> ao bloco <code class="language-plaintext highlighter-rouge">:deps</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="w">  </span><span class="c1">;; --- Dependências do Frontend ---</span><span class="w">
  </span><span class="n">thheller/shadow-cljs</span><span class="w">    </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"2.28.23"</span><span class="p">}</span><span class="w">
  </span><span class="n">reagent/reagent</span><span class="w">         </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"2.0.0"</span><span class="p">}</span><span class="w">
  </span><span class="n">org.clojure/core.async</span><span class="w">  </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"1.6.681"</span><span class="p">}</span><span class="w"> </span><span class="c1">;; &lt;- ADICIONE ESTA LINHA</span><span class="w">
</span></code></pre></div></div> <aside> <p>⚠️ <strong>Lembre-se da regra de ouro:</strong> mudou o <code class="language-plaintext highlighter-rouge">deps.edn</code>, <strong>reinicie</strong> o que estiver rodando. Se o <code class="language-plaintext highlighter-rouge">npx shadow-cljs watch app</code> estiver ativo, pare-o (<code class="language-plaintext highlighter-rouge">Ctrl+C</code>) e suba de novo — só assim ele enxerga a nova dependência.</p> </aside> <h3 id="passo-42-o-primeiro-fetch--e-o-erro-cors-intencional">Passo 4.2: O Primeiro <code class="language-plaintext highlighter-rouge">fetch</code> — e o Erro CORS (Intencional)</h3> <p><strong>Objetivo:</strong> vamos tentar buscar a lista de todos do backend. Vamos <em>esperar</em> que isso falhe, para ver <em>por que</em> o CORS existe.</p> <p><strong>Ação 1: Adicionar os <code class="language-plaintext highlighter-rouge">require</code>s de assincronia</strong></p> <p>No <code class="language-plaintext highlighter-rouge">ns</code> do <code class="language-plaintext highlighter-rouge">src/todo/frontend/core.cljs</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.frontend.core</span><span class="w">
  </span><span class="p">(</span><span class="no">:require</span><span class="w"> </span><span class="p">[</span><span class="n">reagent.core</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">r</span><span class="p">]</span><span class="w">
            </span><span class="p">[</span><span class="n">reagent.dom.client</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">rdom</span><span class="p">]</span><span class="w">
            </span><span class="p">[</span><span class="n">clojure.string</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="nb">str</span><span class="p">]</span><span class="w">
            </span><span class="c1">;; --- ADICIONE ESTAS DUAS LINHAS ---</span><span class="w">
            </span><span class="p">[</span><span class="n">cljs.core.async</span><span class="w"> </span><span class="no">:refer</span><span class="w"> </span><span class="p">[</span><span class="n">go</span><span class="p">]]</span><span class="w">
            </span><span class="p">[</span><span class="n">cljs.core.async.interop</span><span class="w"> </span><span class="no">:refer-macros</span><span class="w"> </span><span class="p">[</span><span class="n">&lt;p!</span><span class="p">]]))</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 2: Adicionar as funções da API</strong></p> <p>Abaixo do <code class="language-plaintext highlighter-rouge">defonce app-state</code>, adicione o <code class="language-plaintext highlighter-rouge">api-url</code> e as funções <code class="language-plaintext highlighter-rouge">fetch-json</code> e <code class="language-plaintext highlighter-rouge">get-todos</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">;; --- A URL base da nossa API (o backend, porta 3000) ---</span><span class="w">
</span><span class="p">(</span><span class="k">def</span><span class="w"> </span><span class="n">api-url</span><span class="w"> </span><span class="s">"http://localhost:3000/api"</span><span class="p">)</span><span class="w">

</span><span class="c1">;; Função auxiliar: faz o fetch e converte a resposta JSON</span><span class="w">
</span><span class="c1">;; em dados Clojure (mapas com keywords).</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">fetch-json</span><span class="w"> </span><span class="p">[</span><span class="n">url</span><span class="w"> </span><span class="n">options</span><span class="p">]</span><span class="w">
  </span><span class="p">(</span><span class="nb">-&gt;</span><span class="w"> </span><span class="p">(</span><span class="nf">js/fetch</span><span class="w"> </span><span class="n">url</span><span class="w"> </span><span class="p">(</span><span class="nf">clj-&gt;js</span><span class="w"> </span><span class="n">options</span><span class="p">))</span><span class="w">
      </span><span class="p">(</span><span class="nf">.then</span><span class="w"> </span><span class="p">(</span><span class="k">fn</span><span class="w"> </span><span class="p">[</span><span class="n">response</span><span class="p">]</span><span class="w">
               </span><span class="p">(</span><span class="nb">when-not</span><span class="w"> </span><span class="p">(</span><span class="nf">.-ok</span><span class="w"> </span><span class="n">response</span><span class="p">)</span><span class="w">
                 </span><span class="p">(</span><span class="nf">throw</span><span class="w"> </span><span class="p">(</span><span class="nf">js/Error.</span><span class="w"> </span><span class="p">(</span><span class="nb">str</span><span class="w"> </span><span class="s">"HTTP error: "</span><span class="w"> </span><span class="p">(</span><span class="nf">.-status</span><span class="w"> </span><span class="n">response</span><span class="p">)))))</span><span class="w">
               </span><span class="p">(</span><span class="nf">.json</span><span class="w"> </span><span class="n">response</span><span class="p">)))</span><span class="w">
      </span><span class="c1">;; Converte o objeto JS em dados Clojure com chaves keyword</span><span class="w">
      </span><span class="p">(</span><span class="nf">.then</span><span class="w"> </span><span class="o">#</span><span class="p">(</span><span class="nf">js-&gt;clj</span><span class="w"> </span><span class="n">%</span><span class="w"> </span><span class="no">:keywordize-keys</span><span class="w"> </span><span class="n">true</span><span class="p">))))</span><span class="w">

</span><span class="c1">;; Busca todos os "todos" da API</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">get-todos</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="nb">assoc</span><span class="w"> </span><span class="no">:loading</span><span class="w"> </span><span class="n">true</span><span class="w"> </span><span class="no">:error</span><span class="w"> </span><span class="n">nil</span><span class="p">)</span><span class="w">
  </span><span class="p">(</span><span class="nf">go</span><span class="w">
    </span><span class="p">(</span><span class="nf">try</span><span class="w">
      </span><span class="p">(</span><span class="k">let</span><span class="w"> </span><span class="p">[</span><span class="n">response</span><span class="w"> </span><span class="p">(</span><span class="nf">&lt;p!</span><span class="w"> </span><span class="p">(</span><span class="nf">fetch-json</span><span class="w"> </span><span class="p">(</span><span class="nb">str</span><span class="w"> </span><span class="n">api-url</span><span class="w"> </span><span class="s">"/todos"</span><span class="p">)</span><span class="w"> </span><span class="p">{</span><span class="no">:method</span><span class="w"> </span><span class="s">"GET"</span><span class="p">}))]</span><span class="w">
        </span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="nb">assoc</span><span class="w"> </span><span class="no">:todos</span><span class="w"> </span><span class="p">(</span><span class="no">:todos</span><span class="w"> </span><span class="n">response</span><span class="p">)</span><span class="w"> </span><span class="no">:loading</span><span class="w"> </span><span class="n">false</span><span class="p">))</span><span class="w">
      </span><span class="p">(</span><span class="nf">catch</span><span class="w"> </span><span class="n">js/Error</span><span class="w"> </span><span class="n">e</span><span class="w">
        </span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="nb">assoc</span><span class="w"> </span><span class="no">:error</span><span class="w"> </span><span class="p">(</span><span class="nf">.-message</span><span class="w"> </span><span class="n">e</span><span class="p">)</span><span class="w"> </span><span class="no">:loading</span><span class="w"> </span><span class="n">false</span><span class="p">)))))</span><span class="w">
</span></code></pre></div></div> <aside> <p>💡 <strong>Como ler o <code class="language-plaintext highlighter-rouge">go</code> + <code class="language-plaintext highlighter-rouge">&lt;p!</code>:</strong> o bloco <code class="language-plaintext highlighter-rouge">(go ...)</code> cria um “processo” assíncrono. Dentro dele, <code class="language-plaintext highlighter-rouge">(&lt;p! promise)</code> significa “pause aqui até a Promise resolver e me dê o valor”. É o equivalente do <code class="language-plaintext highlighter-rouge">async/await</code> do JavaScript, em ClojureScript.</p> </aside> <p><strong>Ação 3: Chamar <code class="language-plaintext highlighter-rouge">get-todos</code> na inicialização</strong></p> <p>Encontre a função <code class="language-plaintext highlighter-rouge">init</code> no final do arquivo e adicione a chamada:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="o">^</span><span class="no">:export</span><span class="w"> </span><span class="n">init</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">(</span><span class="nb">println</span><span class="w"> </span><span class="s">"Frontend inicializado..."</span><span class="p">)</span><span class="w">
  </span><span class="p">(</span><span class="k">let</span><span class="w"> </span><span class="p">[</span><span class="nb">root</span><span class="w"> </span><span class="p">(</span><span class="nf">rdom/create-root</span><span class="w"> </span><span class="p">(</span><span class="nf">js/document.getElementById</span><span class="w"> </span><span class="s">"app"</span><span class="p">))]</span><span class="w">
    </span><span class="p">(</span><span class="nf">.render</span><span class="w"> </span><span class="nb">root</span><span class="w"> </span><span class="p">(</span><span class="nf">r/as-element</span><span class="w"> </span><span class="p">[</span><span class="n">app</span><span class="p">])))</span><span class="w">

  </span><span class="c1">;; --- ADICIONE ESTA LINHA ---</span><span class="w">
  </span><span class="c1">;; Ao carregar a página, busca os todos da API</span><span class="w">
  </span><span class="p">(</span><span class="nf">get-todos</span><span class="p">))</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 4: Teste (A Falha Intencional)</strong></p> <p>Agora, sim, vamos rodar <strong>ambos</strong> os servidores:</p> <ol> <li> <p><strong>Terminal 1 (Backend):</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>clj <span class="nt">-M</span>:run
</code></pre></div> </div> <p>(Deve dizer <code class="language-plaintext highlighter-rouge">Servidor iniciado na porta 3000</code>. Este servidor ainda <em>não</em> sabe nada sobre CORS.)</p> </li> <li> <p><strong>Terminal 2 (Frontend):</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npx shadow-cljs watch app
</code></pre></div> </div> <p>(Espere o <code class="language-plaintext highlighter-rouge">Build completed</code>.)</p> </li> <li> <p><strong>Navegador:</strong> abra <code class="language-plaintext highlighter-rouge">http://localhost:8000</code>.</p> </li> </ol> <p><strong>Resultado Esperado:</strong> a página carrega, mas a lista de todos <strong>não</strong> aparece.</p> <p><strong>A “Lição” (O “Aha!”):</strong></p> <ol> <li>Abra o <strong>Console do Desenvolvedor</strong> (<code class="language-plaintext highlighter-rouge">F12</code>, aba <em>Console</em>).</li> <li>Você verá um <strong>erro vermelho</strong> bem claro:</li> </ol> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Access to fetch at 'http://localhost:3000/api/todos' from origin
'http://localhost:8000' has been blocked by CORS policy...
</code></pre></div></div> <h3 id="o-que-aprendemos">O que aprendemos</h3> <p>Você acabou de descobrir o <strong>CORS</strong> (<em>Cross-Origin Resource Sharing</em>). O navegador, por segurança, impediu que o <code class="language-plaintext highlighter-rouge">localhost:8000</code> (o frontend) fizesse uma requisição para o <code class="language-plaintext highlighter-rouge">localhost:3000</code> (o backend), porque eles são “origens” diferentes (portas diferentes contam como origens diferentes!).</p> <p>Importante: <strong>quem bloqueia é o navegador</strong>, não o servidor. É por isso que o <code class="language-plaintext highlighter-rouge">curl</code> funcionava — ele não é um navegador e não aplica a política de CORS. A correção, porém, é feita <strong>no servidor</strong>: é ele quem precisa declarar “eu confio na origem <code class="language-plaintext highlighter-rouge">localhost:8000</code>”.</p> <h3 id="passo-43-corrigir-o-cors-no-backend">Passo 4.3: Corrigir o CORS (no Backend)</h3> <p><strong>Ação 1: Adicionar a dependência</strong></p> <p>Abra o <code class="language-plaintext highlighter-rouge">deps.edn</code> e adicione o <code class="language-plaintext highlighter-rouge">ring-cors</code> ao bloco <code class="language-plaintext highlighter-rouge">:deps</code> (junto às dependências do backend):</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="w">  </span><span class="c1">;; --- Dependências do Backend (API REST) ---</span><span class="w">
  </span><span class="n">ring/ring-core</span><span class="w">          </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"1.12.2"</span><span class="p">}</span><span class="w">
  </span><span class="n">ring/ring-jetty-adapter</span><span class="w"> </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"1.12.2"</span><span class="p">}</span><span class="w">
  </span><span class="n">ring/ring-json</span><span class="w">          </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"0.5.1"</span><span class="p">}</span><span class="w">
  </span><span class="n">metosin/reitit-ring</span><span class="w">     </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"0.7.0"</span><span class="p">}</span><span class="w">
  </span><span class="n">ring-cors/ring-cors</span><span class="w">     </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"0.1.13"</span><span class="p">}</span><span class="w"> </span><span class="c1">;; &lt;- ADICIONE ESTA LINHA</span><span class="w">
</span></code></pre></div></div> <aside> <p>⚠️ Atenção ao nome: é <code class="language-plaintext highlighter-rouge">ring-cors/ring-cors</code> (grupo e artefato iguais). Escrever <code class="language-plaintext highlighter-rouge">ring/ring-cors</code> fará o download falhar.</p> </aside> <p><strong>Ação 2: Adicionar o middleware ao <code class="language-plaintext highlighter-rouge">core.clj</code></strong></p> <p>Abra <code class="language-plaintext highlighter-rouge">src/todo/backend/core.clj</code>:</p> <ol> <li> <p><strong>Requeira</strong> o <code class="language-plaintext highlighter-rouge">wrap-cors</code> no <code class="language-plaintext highlighter-rouge">ns</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.backend.core</span><span class="w">
  </span><span class="p">(</span><span class="no">:require</span><span class="w"> </span><span class="p">[</span><span class="n">ring.adapter.jetty</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">jetty</span><span class="p">]</span><span class="w">
            </span><span class="p">[</span><span class="n">reitit.ring</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">ring</span><span class="p">]</span><span class="w">
            </span><span class="p">[</span><span class="n">ring.middleware.json</span><span class="w"> </span><span class="no">:refer</span><span class="w"> </span><span class="p">[</span><span class="n">wrap-json-response</span><span class="w"> </span><span class="n">wrap-json-body</span><span class="p">]]</span><span class="w">
            </span><span class="p">[</span><span class="n">ring.middleware.params</span><span class="w"> </span><span class="no">:refer</span><span class="w"> </span><span class="p">[</span><span class="n">wrap-params</span><span class="p">]]</span><span class="w">
            </span><span class="p">[</span><span class="n">ring.middleware.keyword-params</span><span class="w"> </span><span class="no">:refer</span><span class="w"> </span><span class="p">[</span><span class="n">wrap-keyword-params</span><span class="p">]]</span><span class="w">
            </span><span class="c1">;; --- ADICIONE ESTA LINHA ---</span><span class="w">
            </span><span class="p">[</span><span class="n">ring.middleware.cors</span><span class="w"> </span><span class="no">:refer</span><span class="w"> </span><span class="p">[</span><span class="n">wrap-cors</span><span class="p">]]</span><span class="w">
            </span><span class="p">[</span><span class="n">todo.backend.handler</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">handler</span><span class="p">])</span><span class="w">
  </span><span class="p">(</span><span class="no">:gen-class</span><span class="p">))</span><span class="w">
</span></code></pre></div> </div> </li> <li> <p><strong>Adicione o <code class="language-plaintext highlighter-rouge">wrap-cors</code> como o PRIMEIRO middleware</strong> da “linha de montagem” no <code class="language-plaintext highlighter-rouge">(def app ...)</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">def</span><span class="w"> </span><span class="n">app</span><span class="w">
  </span><span class="p">(</span><span class="nf">ring/ring-handler</span><span class="w">
   </span><span class="n">app-routes</span><span class="w">
   </span><span class="p">(</span><span class="nf">ring/create-default-handler</span><span class="p">)</span><span class="w">
   </span><span class="p">{</span><span class="no">:middleware</span><span class="w"> </span><span class="p">[</span><span class="c1">;; --- ADICIONE ESTE VETOR (o primeiro da lista!) ---</span><span class="w">
                 </span><span class="p">[</span><span class="n">wrap-cors</span><span class="w">
                  </span><span class="c1">;; Em quais origens o backend confia:</span><span class="w">
                  </span><span class="no">:access-control-allow-origin</span><span class="w"> </span><span class="p">[</span><span class="o">#</span><span class="s">"http://localhost:8000"</span><span class="p">]</span><span class="w">
                  </span><span class="c1">;; Quais métodos HTTP são permitidos:</span><span class="w">
                  </span><span class="no">:access-control-allow-methods</span><span class="w"> </span><span class="p">[</span><span class="no">:get</span><span class="w"> </span><span class="no">:post</span><span class="w"> </span><span class="no">:put</span><span class="w"> </span><span class="no">:delete</span><span class="p">]</span><span class="w">
                  </span><span class="c1">;; Quais headers o frontend pode enviar:</span><span class="w">
                  </span><span class="c1">;; (necessário para o POST com Content-Type: application/json)</span><span class="w">
                  </span><span class="no">:access-control-allow-headers</span><span class="w"> </span><span class="p">[</span><span class="s">"Content-Type"</span><span class="p">]]</span><span class="w">

                 </span><span class="c1">;; O resto dos middlewares (como antes)...</span><span class="w">
                 </span><span class="n">wrap-json-response</span><span class="w">
                 </span><span class="p">[</span><span class="n">wrap-json-body</span><span class="w"> </span><span class="p">{</span><span class="no">:keywords?</span><span class="w"> </span><span class="n">true</span><span class="p">}]</span><span class="w">
                 </span><span class="n">wrap-params</span><span class="w">
                 </span><span class="n">wrap-keyword-params</span><span class="p">]}))</span><span class="w">
</span></code></pre></div> </div> </li> </ol> <aside> <p>💡 <strong>Por que o <code class="language-plaintext highlighter-rouge">wrap-cors</code> precisa ser o primeiro?</strong> Além das requisições normais, o navegador envia uma requisição extra de “sondagem” chamada <strong>preflight</strong>: um <code class="language-plaintext highlighter-rouge">OPTIONS</code> perguntando <em>“posso fazer um POST com o header Content-Type para você?”</em>. Nós não temos nenhuma rota <code class="language-plaintext highlighter-rouge">OPTIONS</code> — quem responde a essa pergunta é o próprio <code class="language-plaintext highlighter-rouge">wrap-cors</code>. Sendo o primeiro (o mais “externo”) da linha de montagem, ele intercepta o preflight <strong>antes</strong> de o roteador procurar (e não achar) uma rota.</p> <p>E é por isso que declaramos <code class="language-plaintext highlighter-rouge">:access-control-allow-headers ["Content-Type"]</code>: um <code class="language-plaintext highlighter-rouge">GET</code> simples não dispara preflight, mas o nosso <code class="language-plaintext highlighter-rouge">POST</code> com JSON dispara — e o navegador só o libera se o servidor disser explicitamente que aceita esse header. Sem essa linha, você cairia na situação mais confusa possível: <em>o GET funciona, mas o POST continua bloqueado por CORS</em>.</p> </aside> <p><strong>Ação 3: Teste (A Correção)</strong></p> <ol> <li><strong>Terminal 1 (Backend):</strong> <strong>pare</strong> (<code class="language-plaintext highlighter-rouge">Ctrl+C</code>) e <strong>reinicie</strong> (<code class="language-plaintext highlighter-rouge">clj -M:run</code>) — crucial para baixar o <code class="language-plaintext highlighter-rouge">ring-cors</code>.</li> <li><strong>Terminal 2 (Frontend):</strong> pode deixar rodando.</li> <li><strong>Navegador:</strong> volte a <code class="language-plaintext highlighter-rouge">http://localhost:8000</code> e recarregue (F5).</li> </ol> <p><strong>Resultado Esperado:</strong> o erro de CORS no console (F12) desapareceu! A página ainda estará vazia (não há todos no banco em memória), mas sem erros. Seu frontend agora pode “falar” com o backend.</p> <h3 id="passo-44-conectar-a-criação-post">Passo 4.4: Conectar a Criação (<code class="language-plaintext highlighter-rouge">POST</code>)</h3> <p>O <code class="language-plaintext highlighter-rouge">GET</code> funciona. Agora vamos fazer o botão “Adicionar” parar de usar a função local (<code class="language-plaintext highlighter-rouge">adicionar-todo-local</code>) e usar a API de verdade.</p> <p><strong>Ação 1: Adicionar a função <code class="language-plaintext highlighter-rouge">create-todo</code></strong></p> <p>No <code class="language-plaintext highlighter-rouge">src/todo/frontend/core.cljs</code>, logo <strong>abaixo</strong> da função <code class="language-plaintext highlighter-rouge">get-todos</code>, adicione:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">;; --- Cria um "todo" via API (POST) ---</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">create-todo</span><span class="w"> </span><span class="p">[</span><span class="n">todo-data</span><span class="p">]</span><span class="w">
  </span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="nb">assoc</span><span class="w"> </span><span class="no">:loading</span><span class="w"> </span><span class="n">true</span><span class="w"> </span><span class="no">:error</span><span class="w"> </span><span class="n">nil</span><span class="p">)</span><span class="w">
  </span><span class="p">(</span><span class="nf">go</span><span class="w">
    </span><span class="p">(</span><span class="nf">try</span><span class="w">
      </span><span class="p">(</span><span class="nf">&lt;p!</span><span class="w"> </span><span class="p">(</span><span class="nf">fetch-json</span><span class="w"> </span><span class="p">(</span><span class="nb">str</span><span class="w"> </span><span class="n">api-url</span><span class="w"> </span><span class="s">"/todos"</span><span class="p">)</span><span class="w">
                       </span><span class="p">{</span><span class="no">:method</span><span class="w"> </span><span class="s">"POST"</span><span class="w">
                        </span><span class="no">:headers</span><span class="w"> </span><span class="p">{</span><span class="s">"Content-Type"</span><span class="w"> </span><span class="s">"application/json"</span><span class="p">}</span><span class="w">
                        </span><span class="c1">;; Converte o mapa Clojure em uma string JSON</span><span class="w">
                        </span><span class="no">:body</span><span class="w"> </span><span class="p">(</span><span class="nf">js/JSON.stringify</span><span class="w"> </span><span class="p">(</span><span class="nf">clj-&gt;js</span><span class="w"> </span><span class="n">todo-data</span><span class="p">))}))</span><span class="w">

      </span><span class="c1">;; Se o POST funcionou, recarregamos a lista</span><span class="w">
      </span><span class="p">(</span><span class="nf">get-todos</span><span class="p">)</span><span class="w">
      </span><span class="p">(</span><span class="nf">catch</span><span class="w"> </span><span class="n">js/Error</span><span class="w"> </span><span class="n">e</span><span class="w">
        </span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="nb">assoc</span><span class="w"> </span><span class="no">:error</span><span class="w"> </span><span class="p">(</span><span class="nf">.-message</span><span class="w"> </span><span class="n">e</span><span class="p">)</span><span class="w"> </span><span class="no">:loading</span><span class="w"> </span><span class="n">false</span><span class="p">)))))</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 2: Modificar o <code class="language-plaintext highlighter-rouge">todo-form</code></strong></p> <p>Agora temos duas funções de “adicionar”: a antiga <code class="language-plaintext highlighter-rouge">adicionar-todo-local</code> (de mentira) e a nova <code class="language-plaintext highlighter-rouge">create-todo</code> (da API). Vamos ligar o formulário na nova.</p> <p><em>Mude esta versão:</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">todo-form</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:div.todo-input</span><span class="w">
   </span><span class="p">[</span><span class="no">:input</span><span class="w">
    </span><span class="p">{</span><span class="no">:type</span><span class="w"> </span><span class="s">"text"</span><span class="w">
     </span><span class="no">:placeholder</span><span class="w"> </span><span class="s">"O que precisa ser feito?"</span><span class="w">
     </span><span class="no">:value</span><span class="w"> </span><span class="p">(</span><span class="no">:input-text</span><span class="w"> </span><span class="o">@</span><span class="n">app-state</span><span class="p">)</span><span class="w">
     </span><span class="no">:on-change</span><span class="w"> </span><span class="o">#</span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="nb">assoc</span><span class="w"> </span><span class="no">:input-text</span><span class="w"> </span><span class="p">(</span><span class="nb">-&gt;</span><span class="w"> </span><span class="n">%</span><span class="w"> </span><span class="n">.-target</span><span class="w"> </span><span class="n">.-value</span><span class="p">))}]</span><span class="w">

   </span><span class="p">[</span><span class="no">:button</span><span class="w">
    </span><span class="p">{</span><span class="no">:on-click</span><span class="w"> </span><span class="n">adicionar-todo-local</span><span class="p">}</span><span class="w"> </span><span class="c1">;; &lt;-- MUDE AQUI</span><span class="w">
    </span><span class="s">"Adicionar (Local)"</span><span class="p">]])</span><span class="w">           </span><span class="c1">;; &lt;-- E AQUI</span><span class="w">
</span></code></pre></div></div> <p><em>Para esta versão:</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">todo-form</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:div.todo-input</span><span class="w">
   </span><span class="p">[</span><span class="no">:input</span><span class="w">
    </span><span class="p">{</span><span class="no">:type</span><span class="w"> </span><span class="s">"text"</span><span class="w">
     </span><span class="no">:placeholder</span><span class="w"> </span><span class="s">"O que precisa ser feito?"</span><span class="w">
     </span><span class="no">:value</span><span class="w"> </span><span class="p">(</span><span class="no">:input-text</span><span class="w"> </span><span class="o">@</span><span class="n">app-state</span><span class="p">)</span><span class="w">
     </span><span class="no">:on-change</span><span class="w"> </span><span class="o">#</span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="nb">assoc</span><span class="w"> </span><span class="no">:input-text</span><span class="w"> </span><span class="p">(</span><span class="nb">-&gt;</span><span class="w"> </span><span class="n">%</span><span class="w"> </span><span class="n">.-target</span><span class="w"> </span><span class="n">.-value</span><span class="p">))}]</span><span class="w">

   </span><span class="p">[</span><span class="no">:button</span><span class="w">
    </span><span class="p">{</span><span class="no">:on-click</span><span class="w"> </span><span class="p">(</span><span class="k">fn</span><span class="w"> </span><span class="p">[]</span><span class="w">
                 </span><span class="p">(</span><span class="nf">create-todo</span><span class="w"> </span><span class="p">{</span><span class="no">:title</span><span class="w"> </span><span class="p">(</span><span class="no">:input-text</span><span class="w"> </span><span class="o">@</span><span class="n">app-state</span><span class="p">)})</span><span class="w">
                 </span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="nb">assoc</span><span class="w"> </span><span class="no">:input-text</span><span class="w"> </span><span class="s">""</span><span class="p">))}</span><span class="w"> </span><span class="c1">;; Limpa o input</span><span class="w">
    </span><span class="s">"Adicionar"</span><span class="p">]])</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 3: Limpar o código</strong></p> <p>Duas pequenas faxinas para o código não “mentir”:</p> <ol> <li><strong>Delete</strong> a função <code class="language-plaintext highlighter-rouge">adicionar-todo-local</code> — ela não é mais usada. (Como consequência, a chave <code class="language-plaintext highlighter-rouge">:next-id</code> do <code class="language-plaintext highlighter-rouge">app-state</code> também ficou sem uso; pode removê-la do <code class="language-plaintext highlighter-rouge">defonce</code>, deixando <code class="language-plaintext highlighter-rouge">{:input-text "" :todos []}</code>.)</li> <li>No componente <code class="language-plaintext highlighter-rouge">app</code>, <strong>atualize o texto</strong>, que ainda dizia que os dados somem no F5:</li> </ol> <p><em>Mude:</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="w">   </span><span class="p">[</span><span class="no">:h1</span><span class="w"> </span><span class="s">"Todo App (Somente Frontend)"</span><span class="p">]</span><span class="w">
   </span><span class="p">[</span><span class="no">:p</span><span class="w"> </span><span class="s">"Isto é 100% local. Recarregue (F5) para ver os dados sumirem."</span><span class="p">]</span><span class="w">
</span></code></pre></div></div> <p><em>Para:</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="w">   </span><span class="p">[</span><span class="no">:h1</span><span class="w"> </span><span class="s">"Todo App"</span><span class="p">]</span><span class="w">
   </span><span class="p">[</span><span class="no">:p</span><span class="w"> </span><span class="s">"Conectado à API. Os dados sobrevivem ao F5!"</span><span class="p">]</span><span class="w">
</span></code></pre></div></div> <aside> <p>💡 Depois de deletar o <code class="language-plaintext highlighter-rouge">defonce</code> antigo com <code class="language-plaintext highlighter-rouge">:next-id</code>, o <em>hot-reload</em> pode manter o valor antigo na memória (é justamente o que o <code class="language-plaintext highlighter-rouge">defonce</code> faz!). Se algo parecer estranho, um simples <strong>F5</strong> recarrega tudo do zero.</p> </aside> <h3 id="passo-45-teste-final-full-stack">Passo 4.5: Teste Final (Full-Stack)</h3> <p><strong>Ação 1:</strong> confira que os dois servidores estão de pé:</p> <ol> <li><strong>Terminal 1:</strong> <code class="language-plaintext highlighter-rouge">clj -M:run</code> → <code class="language-plaintext highlighter-rouge">Servidor iniciado na porta 3000</code> (com a correção do CORS).</li> <li><strong>Terminal 2:</strong> <code class="language-plaintext highlighter-rouge">npx shadow-cljs watch app</code> → <code class="language-plaintext highlighter-rouge">Build completed</code>.</li> </ol> <p><strong>Ação 2: Teste no navegador</strong></p> <ol> <li>Abra a URL do <strong>frontend</strong>: <code class="language-plaintext highlighter-rouge">http://localhost:8000</code>.</li> <li>Confira o console (F12): <strong>sem erros de CORS</strong>. Lista vazia.</li> <li>Digite um todo (ex: “Testar a API completa”) e clique em “Adicionar”.</li> </ol> <p><strong>O que acontece agora (a mágica):</strong></p> <ul> <li>O <code class="language-plaintext highlighter-rouge">core.cljs</code> chama <code class="language-plaintext highlighter-rouge">create-todo</code> (o <code class="language-plaintext highlighter-rouge">POST</code> para a porta 3000);</li> <li>O navegador dispara antes um <strong>preflight</strong> <code class="language-plaintext highlighter-rouge">OPTIONS</code>, que o <code class="language-plaintext highlighter-rouge">wrap-cors</code> responde;</li> <li>O backend cria o todo no <code class="language-plaintext highlighter-rouge">atom</code>;</li> <li>O <code class="language-plaintext highlighter-rouge">create-todo</code> chama <code class="language-plaintext highlighter-rouge">get-todos</code> (o <code class="language-plaintext highlighter-rouge">GET</code>);</li> <li>O <code class="language-plaintext highlighter-rouge">@app-state</code> é atualizado com a nova lista;</li> <li>O Reagent redesenha a UI → <strong>seu todo aparece na lista!</strong></li> </ul> <p><strong>Ação 3: O “Aha!” (a persistência da API)</strong></p> <p>Faça o teste que falhou na Fase 3: <strong>recarregue a página (F5)</strong>.</p> <ul> <li>O <code class="language-plaintext highlighter-rouge">init</code> roda de novo e chama <code class="language-plaintext highlighter-rouge">get-todos</code>;</li> <li>O backend (que <strong>não</strong> foi reiniciado) ainda tem o todo no seu <code class="language-plaintext highlighter-rouge">atom</code> e o devolve;</li> <li><strong>Resultado:</strong> o todo <strong>continua lá!</strong></li> </ul> <p>A aplicação agora é <em>full-stack</em>: a UI está desacoplada dos dados, e os dados sobrevivem ao recarregamento da página (por enquanto, na memória do backend).</p> <h3 id="passo-46-git-checkpoint">Passo 4.6: Git Checkpoint</h3> <p><strong>Ação 1:</strong> pare os servidores e verifique:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
</code></pre></div></div> <p>Você deve ver: <code class="language-plaintext highlighter-rouge">deps.edn</code> (core.async + ring-cors), <code class="language-plaintext highlighter-rouge">src/todo/backend/core.clj</code> (wrap-cors) e <code class="language-plaintext highlighter-rouge">src/todo/frontend/core.cljs</code> (funções de API + limpeza).</p> <p><strong>Ação 2:</strong> prepare e salve:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"feat: conecta frontend com API do backend (CORS corrigido)"</span>
</code></pre></div></div> <hr/> <p><strong>Fim da Fase 4!</strong> 🏁</p> <p>Temos uma aplicação full-stack funcional, com o histórico salvo no Git.</p> <p>Mas ela ainda tem uma grande limitação: se você parar e reiniciar o servidor de <strong>backend</strong> (<code class="language-plaintext highlighter-rouge">clj -M:run</code>), os todos desaparecem — eles vivem em um <code class="language-plaintext highlighter-rouge">atom</code>, que é memória volátil. Faça o teste, se quiser: pare o backend, suba de novo, dê F5… lista vazia. 😢</p> <p>A próxima fase resolve isso trocando o <code class="language-plaintext highlighter-rouge">db.clj</code> por um banco de dados <strong>real</strong>.</p> <hr/> <h1 id="fase-5-persistência-real-banco-de-dados">Fase 5: Persistência Real (Banco de Dados)</h1> <p><strong>Objetivo:</strong> Substituir nosso <code class="language-plaintext highlighter-rouge">db.clj</code> “de mentira” (o <code class="language-plaintext highlighter-rouge">atom</code>) por um <code class="language-plaintext highlighter-rouge">db.clj</code> “de verdade”, que usa um banco de dados <strong>SQLite</strong>.</p> <p><strong>Por que SQLite?</strong> É o banco mais simples de usar: não requer um servidor separado (como o PostgreSQL) e armazena o banco inteiro em um único arquivo (<code class="language-plaintext highlighter-rouge">prod.db</code>) na pasta do projeto.</p> <p><strong>Nossa “Cirurgia”:</strong> A parte mais legal desta fase é o quão pouco código vamos mudar. Graças à forma como estruturamos o aplicativo, <strong>só precisamos mudar o <code class="language-plaintext highlighter-rouge">db.clj</code></strong>.</p> <p>Os arquivos <code class="language-plaintext highlighter-rouge">handler.clj</code> e <code class="language-plaintext highlighter-rouge">core.clj</code> não se importam com <em>como</em> os dados são armazenados; eles apenas chamam funções como <code class="language-plaintext highlighter-rouge">(db/get-all-todos)</code>. Vamos reescrever o <em>interior</em> dessas funções para falar SQL em vez de mexer em um <code class="language-plaintext highlighter-rouge">atom</code>.</p> <h3 id="passo-51-adicionar-as-dependências-do-banco">Passo 5.1: Adicionar as Dependências do Banco</h3> <ul> <li><strong><code class="language-plaintext highlighter-rouge">next.jdbc</code></strong>: a biblioteca moderna de Clojure para “falar” com bancos SQL.</li> <li><strong><code class="language-plaintext highlighter-rouge">org.xerial/sqlite-jdbc</code></strong>: o “driver” que permite ao <code class="language-plaintext highlighter-rouge">next.jdbc</code> se comunicar especificamente com o SQLite.</li> </ul> <p><strong>Ação:</strong> Abra o <code class="language-plaintext highlighter-rouge">deps.edn</code> e adicione estas duas linhas ao bloco <code class="language-plaintext highlighter-rouge">:deps</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="w">  </span><span class="c1">;; --- Banco de Dados ---</span><span class="w">
  </span><span class="c1">;; ADICIONE ESTAS DUAS LINHAS:</span><span class="w">
  </span><span class="n">seancorfield/next.jdbc</span><span class="w">  </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"1.2.659"</span><span class="p">}</span><span class="w">
  </span><span class="n">org.xerial/sqlite-jdbc</span><span class="w">  </span><span class="p">{</span><span class="no">:mvn/version</span><span class="w"> </span><span class="s">"3.45.3.0"</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <h3 id="passo-52-entendendo-o-nextjdbc-laboratório-no-repl">Passo 5.2: Entendendo o <code class="language-plaintext highlighter-rouge">next.jdbc</code> (Laboratório no REPL)</h3> <p><strong>Objetivo:</strong> aprender como o <code class="language-plaintext highlighter-rouge">next.jdbc</code> funciona <strong>em isolamento</strong>, antes de integrá-lo. Vamos criar um banco, inserir e ler dados, tudo a partir do REPL — sem tocar em nenhum arquivo do projeto.</p> <p><strong>Ação 1: Inicie um REPL novo</strong></p> <ol> <li><strong>Pare</strong> todos os servidores.</li> <li> <p>Na raiz do projeto:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>clj
</code></pre></div> </div> <p>(O <code class="language-plaintext highlighter-rouge">clj</code> lerá o <code class="language-plaintext highlighter-rouge">deps.edn</code> e baixará as novas bibliotecas na primeira vez.)</p> </li> </ol> <p><strong>Ação 2: Carregue a biblioteca e defina a conexão</strong></p> <p>Digite no prompt <code class="language-plaintext highlighter-rouge">user=&gt;</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="nf">require</span><span class="w"> </span><span class="o">'</span><span class="p">[</span><span class="n">next.jdbc</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">jdbc</span><span class="p">])</span><span class="w">
</span><span class="n">nil</span><span class="w">

</span><span class="c1">;; A "especificação" do banco: um simples mapa Clojure</span><span class="w">
</span><span class="c1">;; que diz ao next.jdbc como se conectar.</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="k">def</span><span class="w"> </span><span class="n">db-spec</span><span class="w"> </span><span class="p">{</span><span class="no">:dbtype</span><span class="w"> </span><span class="s">"sqlite"</span><span class="w">    </span><span class="c1">;; o tipo de banco</span><span class="w">
                     </span><span class="no">:dbname</span><span class="w"> </span><span class="s">"lab.db"</span><span class="p">})</span><span class="w">  </span><span class="c1">;; o arquivo onde tudo será salvo</span><span class="w">
</span><span class="o">#</span><span class="ss">'user/db-spec</span><span class="w">
</span></code></pre></div></div> <p>(Estamos usando <code class="language-plaintext highlighter-rouge">lab.db</code> como “banco de laboratório” descartável — o banco de verdade da aplicação, <code class="language-plaintext highlighter-rouge">prod.db</code>, será criado no próximo passo.)</p> <p><strong>Ação 3: Execute SQL!</strong> A principal função é <code class="language-plaintext highlighter-rouge">jdbc/execute!</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">;; --- 1. CRIAR UMA TABELA (CREATE) ---</span><span class="w">
</span><span class="c1">;; (execute! recebe um vetor cujo primeiro item é a string SQL)</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="nf">jdbc/execute!</span><span class="w"> </span><span class="n">db-spec</span><span class="w"> </span><span class="p">[</span><span class="s">"
  CREATE TABLE IF NOT EXISTS todos (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    title TEXT,
    completed BOOLEAN
  )
"</span><span class="p">])</span><span class="w">
</span><span class="p">[</span><span class="o">#</span><span class="no">:next.jdbc</span><span class="p">{</span><span class="no">:update-count</span><span class="w"> </span><span class="mi">0</span><span class="p">}]</span><span class="w">

</span><span class="c1">;; --- 2. INSERIR DADOS (INSERT) ---</span><span class="w">
</span><span class="c1">;; (Passamos os valores como '?' para prevenir "SQL Injection".</span><span class="w">
</span><span class="c1">;;  O next.jdbc cuida da substituição com segurança.)</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="nf">jdbc/execute!</span><span class="w"> </span><span class="n">db-spec</span><span class="w">
         </span><span class="p">[</span><span class="s">"INSERT INTO todos (title, completed) VALUES (?, ?)"</span><span class="w">
          </span><span class="s">"Aprender next.jdbc"</span><span class="w"> </span><span class="n">false</span><span class="p">])</span><span class="w">
</span><span class="p">[</span><span class="o">#</span><span class="no">:next.jdbc</span><span class="p">{</span><span class="no">:update-count</span><span class="w"> </span><span class="mi">1</span><span class="p">}]</span><span class="w">

</span><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="nf">jdbc/execute!</span><span class="w"> </span><span class="n">db-spec</span><span class="w">
         </span><span class="p">[</span><span class="s">"INSERT INTO todos (title, completed) VALUES (?, ?)"</span><span class="w">
          </span><span class="s">"Integrar com a API"</span><span class="w"> </span><span class="n">false</span><span class="p">])</span><span class="w">
</span><span class="p">[</span><span class="o">#</span><span class="no">:next.jdbc</span><span class="p">{</span><span class="no">:update-count</span><span class="w"> </span><span class="mi">1</span><span class="p">}]</span><span class="w">

</span><span class="c1">;; --- 3. LER OS DADOS (SELECT) ---</span><span class="w">
</span><span class="c1">;; ESTE É O MOMENTO "AHA!":</span><span class="w">
</span><span class="c1">;; o next.jdbc executa o SQL e retorna... DADOS CLOJURE!</span><span class="w">
</span><span class="c1">;; (Um vetor de mapas — exatamente o que nossa API precisa.)</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="nf">jdbc/execute!</span><span class="w"> </span><span class="n">db-spec</span><span class="w"> </span><span class="p">[</span><span class="s">"SELECT * FROM todos"</span><span class="p">])</span><span class="w">
</span><span class="p">[{</span><span class="no">:todos/id</span><span class="w"> </span><span class="mi">1</span><span class="n">,</span><span class="w"> </span><span class="no">:todos/title</span><span class="w"> </span><span class="s">"Aprender next.jdbc"</span><span class="n">,</span><span class="w"> </span><span class="no">:todos/completed</span><span class="w"> </span><span class="mi">0</span><span class="p">}</span><span class="w">
 </span><span class="p">{</span><span class="no">:todos/id</span><span class="w"> </span><span class="mi">2</span><span class="n">,</span><span class="w"> </span><span class="no">:todos/title</span><span class="w"> </span><span class="s">"Integrar com a API"</span><span class="n">,</span><span class="w"> </span><span class="no">:todos/completed</span><span class="w"> </span><span class="mi">0</span><span class="p">}]</span><span class="w">

</span><span class="c1">;; --- 4. LER UM ITEM (SELECT com WHERE) ---</span><span class="w">
</span><span class="n">user=&gt;</span><span class="w"> </span><span class="p">(</span><span class="nf">jdbc/execute!</span><span class="w"> </span><span class="n">db-spec</span><span class="w"> </span><span class="p">[</span><span class="s">"SELECT * FROM todos WHERE id = ?"</span><span class="w"> </span><span class="mi">2</span><span class="p">])</span><span class="w">
</span><span class="p">[{</span><span class="no">:todos/id</span><span class="w"> </span><span class="mi">2</span><span class="n">,</span><span class="w"> </span><span class="no">:todos/title</span><span class="w"> </span><span class="s">"Integrar com a API"</span><span class="n">,</span><span class="w"> </span><span class="no">:todos/completed</span><span class="w"> </span><span class="mi">0</span><span class="p">}]</span><span class="w">
</span></code></pre></div></div> <p><strong>Pare e observe duas coisas no resultado do SELECT:</strong></p> <ol> <li><strong>As chaves vêm “qualificadas”</strong>: não é <code class="language-plaintext highlighter-rouge">:id</code>, é <strong><code class="language-plaintext highlighter-rouge">:todos/id</code></strong> — o <code class="language-plaintext highlighter-rouge">next.jdbc</code> prefixa cada coluna com o nome da tabela. Guarde isso: <strong>vai causar um bug no nosso frontend daqui a pouco</strong> (e vamos corrigi-lo de propósito, no Passo 5.5).</li> <li><strong>Booleanos viram números</strong>: o SQLite armazena <code class="language-plaintext highlighter-rouge">true</code>/<code class="language-plaintext highlighter-rouge">false</code> como <code class="language-plaintext highlighter-rouge">1</code>/<code class="language-plaintext highlighter-rouge">0</code>. Isso também reaparecerá na Fase 6.</li> </ol> <p>Saia do REPL com <code class="language-plaintext highlighter-rouge">Ctrl+D</code>. Olhe a pasta do projeto: há um novo arquivo <code class="language-plaintext highlighter-rouge">lab.db</code>. Nosso banco é real! Como foi só um laboratório, pode apagá-lo:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">rm </span>lab.db
</code></pre></div></div> <ul> <li> <p><strong>A Lição: <code class="language-plaintext highlighter-rouge">next.jdbc</code> vs. o resto (o “porquê”)</strong></p> <p><strong>1. <code class="language-plaintext highlighter-rouge">next.jdbc</code> vs. ORMs (Django, GORM, SQLAlchemy):</strong></p> <ul> <li><strong>ORMs (mundo Objeto):</strong> tentam <strong>esconder</strong> o SQL. Você lida com objetos e métodos (<code class="language-plaintext highlighter-rouge">user.save()</code>), e o ORM <em>adivinha</em> o SQL. Fácil no começo, difícil de otimizar depois.</li> <li><strong><code class="language-plaintext highlighter-rouge">next.jdbc</code> (mundo Funcional/Dados):</strong> <strong>abraça</strong> o SQL. Você escreve o SQL exato que quer, e a biblioteca é só uma <strong>função</strong>: <code class="language-plaintext highlighter-rouge">(função ["SQL..." dados]) → dados-clojure</code>. Transparente e focado em <strong>transformação de dados</strong>.</li> </ul> <p><strong>2. <code class="language-plaintext highlighter-rouge">next.jdbc</code> vs. Java JDBC (o “antes e depois”):</strong></p> <p>Para um simples <code class="language-plaintext highlighter-rouge">SELECT *</code> em Java JDBC puro, você escreveria toda esta “cerimônia”:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Connection</span> <span class="n">conn</span> <span class="o">=</span> <span class="kc">null</span><span class="o">;</span>
<span class="nc">PreparedStatement</span> <span class="n">ps</span> <span class="o">=</span> <span class="kc">null</span><span class="o">;</span>
<span class="nc">ResultSet</span> <span class="n">rs</span> <span class="o">=</span> <span class="kc">null</span><span class="o">;</span>
<span class="nc">List</span><span class="o">&lt;</span><span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">Object</span><span class="o">&gt;&gt;</span> <span class="n">results</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>

<span class="k">try</span> <span class="o">{</span>
    <span class="n">conn</span> <span class="o">=</span> <span class="nc">DriverManager</span><span class="o">.</span><span class="na">getConnection</span><span class="o">(</span><span class="s">"jdbc:sqlite:prod.db"</span><span class="o">);</span>
    <span class="n">ps</span> <span class="o">=</span> <span class="n">conn</span><span class="o">.</span><span class="na">prepareStatement</span><span class="o">(</span><span class="s">"SELECT * FROM todos"</span><span class="o">);</span>
    <span class="n">rs</span> <span class="o">=</span> <span class="n">ps</span><span class="o">.</span><span class="na">executeQuery</span><span class="o">();</span>
    <span class="k">while</span> <span class="o">(</span><span class="n">rs</span><span class="o">.</span><span class="na">next</span><span class="o">())</span> <span class="o">{</span>
        <span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">Object</span><span class="o">&gt;</span> <span class="n">row</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">HashMap</span><span class="o">&lt;&gt;();</span>
        <span class="n">row</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="s">"id"</span><span class="o">,</span> <span class="n">rs</span><span class="o">.</span><span class="na">getInt</span><span class="o">(</span><span class="s">"id"</span><span class="o">));</span>
        <span class="n">row</span><span class="o">.</span><span class="na">put</span><span class="o">(</span><span class="s">"title"</span><span class="o">,</span> <span class="n">rs</span><span class="o">.</span><span class="na">getString</span><span class="o">(</span><span class="s">"title"</span><span class="o">));</span>
        <span class="n">results</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="n">row</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span> <span class="k">catch</span> <span class="o">(</span><span class="nc">SQLException</span> <span class="n">e</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">e</span><span class="o">.</span><span class="na">printStackTrace</span><span class="o">();</span>
<span class="o">}</span> <span class="k">finally</span> <span class="o">{</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">rs</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span> <span class="n">rs</span><span class="o">.</span><span class="na">close</span><span class="o">();</span> <span class="o">}</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">ps</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span> <span class="n">ps</span><span class="o">.</span><span class="na">close</span><span class="o">();</span> <span class="o">}</span>
    <span class="k">if</span> <span class="o">(</span><span class="n">conn</span> <span class="o">!=</span> <span class="kc">null</span><span class="o">)</span> <span class="o">{</span> <span class="n">conn</span><span class="o">.</span><span class="na">close</span><span class="o">();</span> <span class="o">}</span>
<span class="o">}</span>
<span class="k">return</span> <span class="n">results</span><span class="o">;</span>
</code></pre></div> </div> <p>Com <code class="language-plaintext highlighter-rouge">next.jdbc</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">jdbc/execute!</span><span class="w"> </span><span class="n">db-spec</span><span class="w"> </span><span class="p">[</span><span class="s">"SELECT * FROM todos"</span><span class="p">])</span><span class="w">
</span><span class="c1">;;=&gt; [{:todos/id 1, :todos/title "Aprender next.jdbc", ...}]</span><span class="w">
</span></code></pre></div> </div> <p>O <code class="language-plaintext highlighter-rouge">next.jdbc</code> cuida de <strong>toda</strong> a cerimônia: abrir a conexão, criar o <em>statement</em>, executar, iterar o <code class="language-plaintext highlighter-rouge">ResultSet</code>, construir os mapas e fechar tudo com segurança.</p> </li> </ul> <h3 id="passo-53-o-novo-dbclj-com-sql">Passo 5.3: O Novo <code class="language-plaintext highlighter-rouge">db.clj</code> (com SQL)</h3> <p>Agora vamos usar o que aprendemos para <strong>substituir permanentemente</strong> o <code class="language-plaintext highlighter-rouge">db.clj</code> antigo. O objetivo: trocar o “motor” do carro sem que o motorista (o <code class="language-plaintext highlighter-rouge">handler.clj</code>) perceba.</p> <p><strong>Ação:</strong> <strong>Apague todo o conteúdo</strong> de <code class="language-plaintext highlighter-rouge">src/todo/backend/db.clj</code> e substitua por:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.backend.db</span><span class="w">
  </span><span class="s">"Este namespace gerencia os dados dos 'todos'
   usando um banco de dados persistente SQLite."</span><span class="w">
  </span><span class="p">(</span><span class="no">:require</span><span class="w"> </span><span class="p">[</span><span class="n">next.jdbc</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">jdbc</span><span class="p">]))</span><span class="w">

</span><span class="c1">;; --- 1. A Configuração (Especificação do Banco) ---</span><span class="w">
</span><span class="c1">;; O banco será salvo em um arquivo chamado "prod.db",</span><span class="w">
</span><span class="c1">;; criado automaticamente na raiz do projeto.</span><span class="w">
</span><span class="p">(</span><span class="k">def</span><span class="w"> </span><span class="n">db-spec</span><span class="w"> </span><span class="p">{</span><span class="no">:dbtype</span><span class="w"> </span><span class="s">"sqlite"</span><span class="w">
              </span><span class="no">:dbname</span><span class="w"> </span><span class="s">"prod.db"</span><span class="p">})</span><span class="w">

</span><span class="c1">;; --- 2. Função de Inicialização ---</span><span class="w">
</span><span class="c1">;; Cria a tabela "todos" se ela ainda não existir.</span><span class="w">
</span><span class="c1">;; Vamos chamá-la no 'core.clj' quando o servidor iniciar.</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">initialize-database!</span><span class="w">
  </span><span class="s">"Cria a tabela 'todos' no banco de dados se ela não existir."</span><span class="w">
  </span><span class="p">[]</span><span class="w">
  </span><span class="p">(</span><span class="nf">jdbc/execute!</span><span class="w"> </span><span class="n">db-spec</span><span class="w"> </span><span class="p">[</span><span class="s">"
    CREATE TABLE IF NOT EXISTS todos (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      title TEXT,
      description TEXT,
      completed BOOLEAN DEFAULT 0,
      created_at TEXT
    )
  "</span><span class="p">]))</span><span class="w">

</span><span class="c1">;; --- 3. As Novas Funções (substituindo os 'atoms') ---</span><span class="w">

</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">get-all-todos</span><span class="w">
  </span><span class="s">"Retorna uma lista com todos os 'todos' no banco."</span><span class="w">
  </span><span class="p">[]</span><span class="w">
  </span><span class="c1">;; O 'execute!' roda o SQL e já retorna mapas Clojure!</span><span class="w">
  </span><span class="p">(</span><span class="nf">jdbc/execute!</span><span class="w"> </span><span class="n">db-spec</span><span class="w"> </span><span class="p">[</span><span class="s">"SELECT * FROM todos ORDER BY created_at DESC"</span><span class="p">]))</span><span class="w">

</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">create-todo</span><span class="w">
  </span><span class="s">"Cria um novo 'todo', salva no banco e o retorna."</span><span class="w">
  </span><span class="p">[</span><span class="n">todo-data</span><span class="p">]</span><span class="w"> </span><span class="c1">;; ex: {:title "Minha tarefa", :description "..."}</span><span class="w">
  </span><span class="c1">;; A cláusula 'RETURNING *' pede ao SQLite para devolver</span><span class="w">
  </span><span class="c1">;; a linha recém-inserida (com o id gerado, o completed, etc.).</span><span class="w">
  </span><span class="c1">;; 'execute-one!' retorna esse único resultado como um mapa.</span><span class="w">
  </span><span class="p">(</span><span class="nf">jdbc/execute-one!</span><span class="w"> </span><span class="n">db-spec</span><span class="w"> </span><span class="p">[</span><span class="s">"
    INSERT INTO todos (title, description, completed, created_at)
    VALUES (?, ?, ?, ?)
    RETURNING *"</span><span class="w">
    </span><span class="p">(</span><span class="no">:title</span><span class="w"> </span><span class="n">todo-data</span><span class="p">)</span><span class="w">
    </span><span class="p">(</span><span class="no">:description</span><span class="w"> </span><span class="n">todo-data</span><span class="p">)</span><span class="w">
    </span><span class="mi">0</span><span class="w"> </span><span class="c1">;; completed = falso (lembre-se: SQLite usa 0/1)</span><span class="w">
    </span><span class="p">(</span><span class="nb">str</span><span class="w"> </span><span class="p">(</span><span class="nf">java.time.Instant/now</span><span class="p">))]))</span><span class="w">
</span></code></pre></div></div> <h3 id="o-que-fizemos-11">O que fizemos?</h3> <ol> <li><strong>Removemos os <code class="language-plaintext highlighter-rouge">atom</code>s</strong>: <code class="language-plaintext highlighter-rouge">todos-db</code> e <code class="language-plaintext highlighter-rouge">next-id</code> desapareceram.</li> <li><strong><code class="language-plaintext highlighter-rouge">(:require [next.jdbc :as jdbc])</code> dentro do <code class="language-plaintext highlighter-rouge">ns</code></strong>: exatamente o padrão que aprendemos na Fase 1 — os <code class="language-plaintext highlighter-rouge">require</code>s de um arquivo sempre vivem na declaração do namespace.</li> <li><strong><code class="language-plaintext highlighter-rouge">initialize-database!</code></strong>: o <code class="language-plaintext highlighter-rouge">CREATE TABLE IF NOT EXISTS</code> configura o banco na primeira execução e não faz nada nas seguintes.</li> <li><strong><code class="language-plaintext highlighter-rouge">get-all-todos</code></strong>: substituímos <code class="language-plaintext highlighter-rouge">(vals @todos-db)</code> por um <code class="language-plaintext highlighter-rouge">SELECT</code>.</li> <li><strong><code class="language-plaintext highlighter-rouge">create-todo</code></strong>: substituímos o <code class="language-plaintext highlighter-rouge">swap!</code> por um <code class="language-plaintext highlighter-rouge">INSERT ... RETURNING *</code>.</li> </ol> <aside> <p>💡 <strong>O que é o <code class="language-plaintext highlighter-rouge">RETURNING *</code>?</strong> É uma cláusula SQL (suportada pelo SQLite desde a versão 3.35) que faz o comando <code class="language-plaintext highlighter-rouge">INSERT</code>/<code class="language-plaintext highlighter-rouge">UPDATE</code>/<code class="language-plaintext highlighter-rouge">DELETE</code> <strong>devolver as linhas afetadas</strong>, como se fosse um <code class="language-plaintext highlighter-rouge">SELECT</code>. Sem ela, um <code class="language-plaintext highlighter-rouge">INSERT</code> retornaria apenas <code class="language-plaintext highlighter-rouge">{:next.jdbc/update-count 1}</code> (“1 linha afetada”) — e nosso handler responderia o objeto errado ao cliente. Com ela, <code class="language-plaintext highlighter-rouge">execute-one!</code> nos entrega o todo completo, com o <code class="language-plaintext highlighter-rouge">id</code> que o banco acabou de gerar. Usaremos o mesmo truque no <code class="language-plaintext highlighter-rouge">UPDATE</code> e no <code class="language-plaintext highlighter-rouge">DELETE</code> da Fase 6.</p> </aside> <p><strong>A “Mágica”:</strong> os <em>nomes</em> das funções (<code class="language-plaintext highlighter-rouge">get-all-todos</code>, <code class="language-plaintext highlighter-rouge">create-todo</code>) e o formato do que elas retornam (lista de mapas / mapa) são <strong>os mesmos</strong> de antes. Por isso o <code class="language-plaintext highlighter-rouge">handler.clj</code> <strong>não precisa de nenhuma modificação</strong> — ele nem vai “saber” que trocamos um <code class="language-plaintext highlighter-rouge">atom</code> por SQL.</p> <h3 id="passo-54-a-modificação-no-coreclj">Passo 5.4: A Modificação no <code class="language-plaintext highlighter-rouge">core.clj</code></h3> <p>Nosso novo <code class="language-plaintext highlighter-rouge">db.clj</code> tem a função <code class="language-plaintext highlighter-rouge">initialize-database!</code>, mas ela nunca é chamada. Precisamos executá-la <strong>uma vez</strong>, quando o servidor de backend iniciar.</p> <p><strong>Ação 1: O <code class="language-plaintext highlighter-rouge">require</code></strong></p> <p>No topo do <code class="language-plaintext highlighter-rouge">src/todo/backend/core.clj</code>, adicione o <code class="language-plaintext highlighter-rouge">todo.backend.db</code> à lista de <code class="language-plaintext highlighter-rouge">require</code>s (atenção aos parênteses — o <code class="language-plaintext highlighter-rouge">(:gen-class)</code> continua <em>dentro</em> do <code class="language-plaintext highlighter-rouge">ns</code>, como sempre esteve):</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="nf">ns</span><span class="w"> </span><span class="n">todo.backend.core</span><span class="w">
  </span><span class="p">(</span><span class="no">:require</span><span class="w"> </span><span class="p">[</span><span class="n">ring.adapter.jetty</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">jetty</span><span class="p">]</span><span class="w">
            </span><span class="p">[</span><span class="n">reitit.ring</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">ring</span><span class="p">]</span><span class="w">
            </span><span class="p">[</span><span class="n">ring.middleware.json</span><span class="w"> </span><span class="no">:refer</span><span class="w"> </span><span class="p">[</span><span class="n">wrap-json-response</span><span class="w"> </span><span class="n">wrap-json-body</span><span class="p">]]</span><span class="w">
            </span><span class="p">[</span><span class="n">ring.middleware.params</span><span class="w"> </span><span class="no">:refer</span><span class="w"> </span><span class="p">[</span><span class="n">wrap-params</span><span class="p">]]</span><span class="w">
            </span><span class="p">[</span><span class="n">ring.middleware.keyword-params</span><span class="w"> </span><span class="no">:refer</span><span class="w"> </span><span class="p">[</span><span class="n">wrap-keyword-params</span><span class="p">]]</span><span class="w">
            </span><span class="p">[</span><span class="n">ring.middleware.cors</span><span class="w"> </span><span class="no">:refer</span><span class="w"> </span><span class="p">[</span><span class="n">wrap-cors</span><span class="p">]]</span><span class="w">
            </span><span class="p">[</span><span class="n">todo.backend.handler</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">handler</span><span class="p">]</span><span class="w">
            </span><span class="c1">;; --- ADICIONE ESTA LINHA ---</span><span class="w">
            </span><span class="p">[</span><span class="n">todo.backend.db</span><span class="w"> </span><span class="no">:as</span><span class="w"> </span><span class="n">db</span><span class="p">])</span><span class="w">
  </span><span class="p">(</span><span class="no">:gen-class</span><span class="p">))</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 2: Chamar a inicialização no <code class="language-plaintext highlighter-rouge">-main</code></strong></p> <p>Encontre a função <code class="language-plaintext highlighter-rouge">-main</code> no final do arquivo. <em>Mude esta versão:</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">-main</span><span class="w"> </span><span class="p">[</span><span class="o">&amp;</span><span class="w"> </span><span class="n">args</span><span class="p">]</span><span class="w">
  </span><span class="p">(</span><span class="k">let</span><span class="w"> </span><span class="p">[</span><span class="n">port</span><span class="w"> </span><span class="p">(</span><span class="nf">Integer/parseInt</span><span class="w"> </span><span class="p">(</span><span class="nb">or</span><span class="w"> </span><span class="p">(</span><span class="nb">first</span><span class="w"> </span><span class="n">args</span><span class="p">)</span><span class="w"> </span><span class="s">"3000"</span><span class="p">))]</span><span class="w">
    </span><span class="p">(</span><span class="nf">start-server</span><span class="w"> </span><span class="n">port</span><span class="p">)))</span><span class="w">
</span></code></pre></div></div> <p><em>Para esta:</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">-main</span><span class="w"> </span><span class="p">[</span><span class="o">&amp;</span><span class="w"> </span><span class="n">args</span><span class="p">]</span><span class="w">
  </span><span class="p">(</span><span class="k">let</span><span class="w"> </span><span class="p">[</span><span class="n">port</span><span class="w"> </span><span class="p">(</span><span class="nf">Integer/parseInt</span><span class="w"> </span><span class="p">(</span><span class="nb">or</span><span class="w"> </span><span class="p">(</span><span class="nb">first</span><span class="w"> </span><span class="n">args</span><span class="p">)</span><span class="w"> </span><span class="s">"3000"</span><span class="p">))]</span><span class="w">
    </span><span class="c1">;; --- ADICIONE ESTA LINHA ---</span><span class="w">
    </span><span class="p">(</span><span class="nf">db/initialize-database!</span><span class="p">)</span><span class="w"> </span><span class="c1">;; Garante que a tabela exista</span><span class="w">
    </span><span class="c1">;; ---------------------------</span><span class="w">
    </span><span class="p">(</span><span class="nf">start-server</span><span class="w"> </span><span class="n">port</span><span class="p">)))</span><span class="w">
</span></code></pre></div></div> <p>Na primeira execução, isso criará o <code class="language-plaintext highlighter-rouge">prod.db</code> e a tabela <code class="language-plaintext highlighter-rouge">todos</code>. Nas seguintes, o <code class="language-plaintext highlighter-rouge">CREATE TABLE IF NOT EXISTS</code> simplesmente não fará nada — exatamente o que queremos.</p> <h3 id="passo-55-a-correção-de-acoplamento-frontend">Passo 5.5: A Correção de Acoplamento (Frontend)</h3> <p><strong>Objetivo:</strong> antes de testar, precisamos corrigir uma incompatibilidade de “dialeto” entre o backend e o frontend — aquela que você viu nascer no laboratório do REPL.</p> <p><strong>A Lição (o “porquê”):</strong></p> <ol> <li><strong>Nosso backend (<code class="language-plaintext highlighter-rouge">next.jdbc</code>)</strong> agora retorna <em>keywords qualificadas</em> (“namespaced”): <code class="language-plaintext highlighter-rouge">:todos/id</code>, <code class="language-plaintext highlighter-rouge">:todos/title</code>, <code class="language-plaintext highlighter-rouge">:todos/completed</code>.</li> <li><strong>Nosso frontend</strong> espera <em>keywords simples</em>: <code class="language-plaintext highlighter-rouge">(:id todo)</code> e <code class="language-plaintext highlighter-rouge">(:title todo)</code>.</li> </ol> <p>Se não corrigirmos, o frontend tentará ler <code class="language-plaintext highlighter-rouge">(:id todo)</code> de um mapa <code class="language-plaintext highlighter-rouge">{:todos/id 1, ...}</code>, receberá <code class="language-plaintext highlighter-rouge">nil</code>, e a UI quebrará: a lista mostrará <em>bullet points</em> sem texto, e o console exibirá avisos de <code class="language-plaintext highlighter-rouge">:key</code>.</p> <p><strong>A solução (deste tutorial):</strong> corrigir o <strong>frontend</strong> para entender o “dialeto” do backend.</p> <p><strong>A consequência (o “acoplamento”):</strong> é a solução mais rápida, mas cria <strong>acoplamento</strong>: o frontend agora “sabe” que os dados vêm de uma tabela chamada <code class="language-plaintext highlighter-rouge">todos</code>. Se um dia a tabela for renomeada para <code class="language-plaintext highlighter-rouge">tasks</code>, o backend continuaria funcionando, mas o frontend quebraria. <em>(A solução “desacoplada” seria configurar o <code class="language-plaintext highlighter-rouge">next.jdbc</code> no backend para retornar keywords simples — fica como exercício para os curiosos: procure por <code class="language-plaintext highlighter-rouge">next.jdbc.result-set/as-unqualified-maps</code>.)</em></p> <p><strong>Ação:</strong> Abra <code class="language-plaintext highlighter-rouge">src/todo/frontend/core.cljs</code> e encontre o componente <code class="language-plaintext highlighter-rouge">todo-list</code>.</p> <aside> <p>⚠️ <strong>Atenção: são DOIS lugares para corrigir, não um!</strong> O erro mais comum deste passo é corrigir o <code class="language-plaintext highlighter-rouge">(:title ...)</code> (porque o texto sumido é visível) e <strong>esquecer o <code class="language-plaintext highlighter-rouge">^{:key ...}</code></strong> (porque o aviso fica escondido no console). Se a key ficar errada, o app até “funciona”, mas o React perde a identidade dos itens da lista — e isso causará bugs visuais reais na Fase 6 (checkboxes marcando o item errado!). Corrija os dois.</p> </aside> <p><em>Mude esta versão:</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">todo-list</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:ul.todo-list</span><span class="w">
   </span><span class="p">(</span><span class="k">for</span><span class="w"> </span><span class="p">[</span><span class="n">todo</span><span class="w"> </span><span class="p">(</span><span class="no">:todos</span><span class="w"> </span><span class="o">@</span><span class="n">app-state</span><span class="p">)]</span><span class="w">
     </span><span class="o">^</span><span class="p">{</span><span class="no">:key</span><span class="w"> </span><span class="p">(</span><span class="no">:id</span><span class="w"> </span><span class="n">todo</span><span class="p">)}</span><span class="w">        </span><span class="c1">;; &lt;-- Bug 1 aqui</span><span class="w">
     </span><span class="p">[</span><span class="no">:li.todo-item</span><span class="w">
      </span><span class="p">(</span><span class="no">:title</span><span class="w"> </span><span class="n">todo</span><span class="p">)])])</span><span class="w">        </span><span class="c1">;; &lt;-- Bug 2 aqui</span><span class="w">
</span></code></pre></div></div> <p><em>Para esta versão (corrigida nos dois lugares):</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">todo-list</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:ul.todo-list</span><span class="w">
   </span><span class="p">(</span><span class="k">for</span><span class="w"> </span><span class="p">[</span><span class="n">todo</span><span class="w"> </span><span class="p">(</span><span class="no">:todos</span><span class="w"> </span><span class="o">@</span><span class="n">app-state</span><span class="p">)]</span><span class="w">
     </span><span class="o">^</span><span class="p">{</span><span class="no">:key</span><span class="w"> </span><span class="p">(</span><span class="no">:todos/id</span><span class="w"> </span><span class="n">todo</span><span class="p">)}</span><span class="w">  </span><span class="c1">;; &lt;-- CORRIGIDO (1/2)</span><span class="w">
     </span><span class="p">[</span><span class="no">:li.todo-item</span><span class="w">
      </span><span class="p">(</span><span class="no">:todos/title</span><span class="w"> </span><span class="n">todo</span><span class="p">)])])</span><span class="w">  </span><span class="c1">;; &lt;-- CORRIGIDO (2/2)</span><span class="w">
</span></code></pre></div></div> <p>Salve o arquivo.</p> <h3 id="passo-56-o-teste-final-a-persistência-real">Passo 5.6: O Teste Final (A Persistência Real)</h3> <p><strong>Ação 1: Limpe qualquer banco antigo (recomendado)</strong></p> <p>Para garantir que estamos começando do zero:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">rm</span> <span class="nt">-f</span> prod.db
</code></pre></div></div> <p><strong>Ação 2: Inicie os dois servidores</strong></p> <ol> <li> <p><strong>Terminal 1 (Backend):</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>clj <span class="nt">-M</span>:run
</code></pre></div> </div> <p>(O <code class="language-plaintext highlighter-rouge">prod.db</code> e a tabela <code class="language-plaintext highlighter-rouge">todos</code> serão criados. <code class="language-plaintext highlighter-rouge">Servidor iniciado na porta 3000</code>.)</p> </li> <li> <p><strong>Terminal 2 (Frontend):</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npx shadow-cljs watch app
</code></pre></div> </div> </li> </ol> <p><strong>Ação 3: Teste a aplicação</strong></p> <ol> <li>Abra <code class="language-plaintext highlighter-rouge">http://localhost:8000</code>.</li> <li>A lista deve estar vazia — <strong>e sem erros no console (F12)</strong>.</li> <li>Adicione um todo (ex: “Testar o banco SQLite”).</li> <li>Ele deve aparecer na lista, <strong>com o texto visível</strong>.</li> </ol> <p><strong>Ação 4: O “Aha!” Final (o teste de persistência)</strong></p> <p>Agora, o teste que <strong>falhava</strong> no fim da Fase 4:</p> <ol> <li>Vá ao <strong>Terminal 1 (Backend)</strong> e <strong>pare o servidor</strong> (<code class="language-plaintext highlighter-rouge">Ctrl+C</code>).</li> <li><strong>Reinicie-o</strong>: <code class="language-plaintext highlighter-rouge">clj -M:run</code>.</li> <li>No navegador, <strong>recarregue a página (F5)</strong>.</li> </ol> <p><strong>Resultado Esperado:</strong> o todo <strong>continua lá!</strong> O backend reiniciado leu o arquivo <code class="language-plaintext highlighter-rouge">prod.db</code> do disco e devolveu os dados. Persistência real. 🎉</p> <h3 id="passo-57-git-checkpoint">Passo 5.7: Git Checkpoint</h3> <p><strong>Ação 1:</strong> pare os servidores e verifique:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git status
</code></pre></div></div> <p>Você deve ver modificados: <code class="language-plaintext highlighter-rouge">deps.edn</code>, <code class="language-plaintext highlighter-rouge">src/todo/backend/core.clj</code>, <code class="language-plaintext highlighter-rouge">src/todo/backend/db.clj</code> e <code class="language-plaintext highlighter-rouge">src/todo/frontend/core.cljs</code>.</p> <p>Note que o <code class="language-plaintext highlighter-rouge">prod.db</code> <strong>não aparece</strong> na lista — o <code class="language-plaintext highlighter-rouge">.gitignore</code> que escrevemos lá na Fase 0 (regra <code class="language-plaintext highlighter-rouge">*.db</code>) já está fazendo o trabalho dele. O arquivo de banco é <em>resultado</em> da aplicação, não código-fonte, e jamais deve ir para o GitHub.</p> <p><strong>Ação 2:</strong> prepare e salve:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"refactor(db): substitui banco em memória por persistência SQLite"</span>
</code></pre></div></div> <p>(Usamos <code class="language-plaintext highlighter-rouge">refactor:</code> porque mudamos a <em>implementação</em> do banco sem mudar o comportamento externo da API.)</p> <hr/> <p><strong>Fim da Fase 5!</strong> 🏁</p> <p>Você tem uma aplicação full-stack completa, moderna e <strong>persistente</strong>. Mas nosso Todo List ainda só <em>cria</em> e <em>lista</em>. Na próxima (e última) fase de código, completaremos o CRUD: marcar como feito (<strong>U</strong>pdate) e deletar (<strong>D</strong>elete) — com um visual decente.</p> <hr/> <h1 id="-fase-6-crud-completo--marcar-como-feito-deletar-e-o-visual-final">🏆 Fase 6: CRUD Completo — “Marcar como Feito”, “Deletar” e o Visual Final</h1> <p>Você tem uma aplicação <strong>C</strong>reate/<strong>R</strong>ead funcional e persistente. Agora vamos completar o CRUD:</p> <ul> <li>o <strong>U</strong> (Update): marcar um todo como feito/não feito (<em>toggle</em>);</li> <li>o <strong>D</strong> (Delete): remover um todo.</li> </ul> <p>Você verá que o padrão é <em>exatamente</em> o mesmo que usamos até aqui, repetido duas vezes: <strong>função no <code class="language-plaintext highlighter-rouge">db.clj</code> → handler no <code class="language-plaintext highlighter-rouge">handler.clj</code> → rota no <code class="language-plaintext highlighter-rouge">core.clj</code> → função de API e evento no <code class="language-plaintext highlighter-rouge">core.cljs</code></strong>.</p> <hr/> <h2 id="️-parte-1-o-toggle-update-no-backend">⚙️ Parte 1: O Toggle (Update) no Backend</h2> <h3 id="passo-61-a-lógica-no-dbclj">Passo 6.1: A Lógica no <code class="language-plaintext highlighter-rouge">db.clj</code></h3> <p>Precisamos de uma função que receba um <code class="language-plaintext highlighter-rouge">id</code> e “vire” o valor de <code class="language-plaintext highlighter-rouge">completed</code> no banco.</p> <p><strong>Ação:</strong> Abra <code class="language-plaintext highlighter-rouge">src/todo/backend/db.clj</code> e adicione no final:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">toggle-todo!</span><span class="w">
  </span><span class="s">"Alterna o status 'completed' de um todo no banco."</span><span class="w">
  </span><span class="p">[</span><span class="n">id</span><span class="p">]</span><span class="w">
  </span><span class="c1">;; (1 - completed) é um truque SQL para inverter: 0 -&gt; 1 e 1 -&gt; 0.</span><span class="w">
  </span><span class="c1">;; RETURNING * devolve a linha já atualizada (ou nada, se o id não existir).</span><span class="w">
  </span><span class="p">(</span><span class="nf">jdbc/execute-one!</span><span class="w"> </span><span class="n">db-spec</span><span class="w"> </span><span class="p">[</span><span class="s">"
    UPDATE todos
    SET completed = (1 - completed)
    WHERE id = ?
    RETURNING *"</span><span class="w">
    </span><span class="n">id</span><span class="p">]))</span><span class="w">
</span></code></pre></div></div> <aside> <p>💡 O <code class="language-plaintext highlighter-rouge">!</code> no final de <code class="language-plaintext highlighter-rouge">toggle-todo!</code> é uma convenção Clojure: sinaliza que a função tem <em>efeitos colaterais</em> (ela muda o mundo — neste caso, o banco). Já vínhamos usando isso no <code class="language-plaintext highlighter-rouge">initialize-database!</code> e no <code class="language-plaintext highlighter-rouge">swap!</code>.</p> </aside> <h3 id="passo-62-o-handler-no-handlerclj">Passo 6.2: O Handler no <code class="language-plaintext highlighter-rouge">handler.clj</code></h3> <p><strong>Ação:</strong> Abra <code class="language-plaintext highlighter-rouge">src/todo/backend/handler.clj</code> e adicione no final:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">;; --- Handler para Alternar (toggle) um Todo ---</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">toggle-todo-handler</span><span class="w">
  </span><span class="s">"Handler para a rota POST /api/todos/:id/toggle."</span><span class="w">
  </span><span class="p">[</span><span class="n">request</span><span class="p">]</span><span class="w">
  </span><span class="c1">;; O Reitit coloca os parâmetros da URL (o :id de "/todos/:id/toggle")</span><span class="w">
  </span><span class="c1">;; em :path-params, sempre como STRINGS. Convertemos para inteiro.</span><span class="w">
  </span><span class="p">(</span><span class="k">let</span><span class="w"> </span><span class="p">[</span><span class="n">id</span><span class="w"> </span><span class="p">(</span><span class="nf">Integer/parseInt</span><span class="w"> </span><span class="p">(</span><span class="nf">get-in</span><span class="w"> </span><span class="n">request</span><span class="w"> </span><span class="p">[</span><span class="no">:path-params</span><span class="w"> </span><span class="no">:id</span><span class="p">]))]</span><span class="w">
    </span><span class="c1">;; execute-one! retorna nil se nenhuma linha foi afetada</span><span class="w">
    </span><span class="c1">;; (id inexistente) -&gt; respondemos 404.</span><span class="w">
    </span><span class="p">(</span><span class="nb">if-let</span><span class="w"> </span><span class="p">[</span><span class="n">updated-todo</span><span class="w"> </span><span class="p">(</span><span class="nf">db/toggle-todo!</span><span class="w"> </span><span class="n">id</span><span class="p">)]</span><span class="w">
      </span><span class="p">{</span><span class="no">:status</span><span class="w"> </span><span class="mi">200</span><span class="w"> </span><span class="no">:body</span><span class="w"> </span><span class="n">updated-todo</span><span class="p">}</span><span class="w">
      </span><span class="p">{</span><span class="no">:status</span><span class="w"> </span><span class="mi">404</span><span class="w"> </span><span class="no">:body</span><span class="w"> </span><span class="p">{</span><span class="no">:error</span><span class="w"> </span><span class="s">"Todo não encontrado"</span><span class="p">}})))</span><span class="w">
</span></code></pre></div></div> <h3 id="passo-63-a-rota-no-coreclj">Passo 6.3: A Rota no <code class="language-plaintext highlighter-rouge">core.clj</code></h3> <p><strong>Ação:</strong> Abra <code class="language-plaintext highlighter-rouge">src/todo/backend/core.clj</code> e adicione a nova rota. Para não haver dúvida de <strong>onde</strong> ela entra, aqui está o <code class="language-plaintext highlighter-rouge">app-routes</code> <strong>completo</strong> como deve ficar:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">def</span><span class="w"> </span><span class="n">app-routes</span><span class="w">
  </span><span class="p">(</span><span class="nf">ring/router</span><span class="w">
   </span><span class="p">[</span><span class="s">"/api"</span><span class="w">
    </span><span class="p">[</span><span class="s">"/hello"</span><span class="w"> </span><span class="p">{</span><span class="no">:get</span><span class="w"> </span><span class="p">{</span><span class="no">:handler</span><span class="w"> </span><span class="n">handler/hello-handler</span><span class="p">}}]</span><span class="w">

    </span><span class="p">[</span><span class="s">"/todos"</span><span class="w">
     </span><span class="p">{</span><span class="no">:get</span><span class="w">  </span><span class="p">{</span><span class="no">:handler</span><span class="w"> </span><span class="n">handler/list-todos-handler</span><span class="p">}</span><span class="w">
      </span><span class="no">:post</span><span class="w"> </span><span class="p">{</span><span class="no">:handler</span><span class="w"> </span><span class="n">handler/create-todo-handler</span><span class="p">}}]</span><span class="w">

    </span><span class="c1">;; --- NOVA ROTA ---</span><span class="w">
    </span><span class="p">[</span><span class="s">"/todos/:id/toggle"</span><span class="w">
     </span><span class="p">{</span><span class="no">:post</span><span class="w"> </span><span class="p">{</span><span class="no">:handler</span><span class="w"> </span><span class="n">handler/toggle-todo-handler</span><span class="p">}}]]))</span><span class="w">
</span></code></pre></div></div> <p><strong>✅ Backend do toggle concluído!</strong> Pare e <strong>reinicie o servidor de backend</strong> (<code class="language-plaintext highlighter-rouge">clj -M:run</code>).</p> <p><strong>Teste rápido com <code class="language-plaintext highlighter-rouge">curl</code></strong> (supondo que exista um todo de id 1):</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-X</span> POST http://localhost:3000/api/todos/1/toggle
</code></pre></div></div> <p><strong>Resultado Esperado:</strong> o todo com <code class="language-plaintext highlighter-rouge">"completed":1</code>. Rode de novo e ele volta para <code class="language-plaintext highlighter-rouge">0</code>. E um id inexistente:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-X</span> POST http://localhost:3000/api/todos/999/toggle
</code></pre></div></div> <p>deve responder <code class="language-plaintext highlighter-rouge">{"error":"Todo não encontrado"}</code>.</p> <hr/> <h2 id="️-parte-2-o-toggle-update-no-frontend">⚡️ Parte 2: O Toggle (Update) no Frontend</h2> <h3 id="passo-64-a-função-de-api-no-corecljs">Passo 6.4: A Função de API no <code class="language-plaintext highlighter-rouge">core.cljs</code></h3> <p><strong>Ação:</strong> Abra <code class="language-plaintext highlighter-rouge">src/todo/frontend/core.cljs</code> e adicione esta função junto às outras (<code class="language-plaintext highlighter-rouge">get-todos</code>, <code class="language-plaintext highlighter-rouge">create-todo</code>):</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">toggle-todo</span><span class="w">
  </span><span class="s">"Chama a API para alternar o status de um todo."</span><span class="w">
  </span><span class="p">[</span><span class="n">id</span><span class="p">]</span><span class="w">
  </span><span class="p">(</span><span class="nf">go</span><span class="w">
    </span><span class="p">(</span><span class="nf">try</span><span class="w">
      </span><span class="p">(</span><span class="nf">&lt;p!</span><span class="w"> </span><span class="p">(</span><span class="nf">fetch-json</span><span class="w"> </span><span class="p">(</span><span class="nb">str</span><span class="w"> </span><span class="n">api-url</span><span class="w"> </span><span class="s">"/todos/"</span><span class="w"> </span><span class="n">id</span><span class="w"> </span><span class="s">"/toggle"</span><span class="p">)</span><span class="w">
                       </span><span class="p">{</span><span class="no">:method</span><span class="w"> </span><span class="s">"POST"</span><span class="p">}))</span><span class="w">
      </span><span class="c1">;; Se funcionou, recarrega a lista inteira</span><span class="w">
      </span><span class="p">(</span><span class="nf">get-todos</span><span class="p">)</span><span class="w">
      </span><span class="p">(</span><span class="nf">catch</span><span class="w"> </span><span class="n">js/Error</span><span class="w"> </span><span class="n">e</span><span class="w">
        </span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="nb">assoc</span><span class="w"> </span><span class="no">:error</span><span class="w"> </span><span class="p">(</span><span class="nf">.-message</span><span class="w"> </span><span class="n">e</span><span class="p">)</span><span class="w"> </span><span class="no">:loading</span><span class="w"> </span><span class="n">false</span><span class="p">)))))</span><span class="w">
</span></code></pre></div></div> <h3 id="passo-65-o-checkbox-no-todo-list-e-a-conversão-01">Passo 6.5: O Checkbox no <code class="language-plaintext highlighter-rouge">todo-list</code> (e a Conversão 0/1)</h3> <p><strong>Atenção: por que <code class="language-plaintext highlighter-rouge">(not= 0 ...)</code> é necessário?</strong></p> <p>O SQLite armazena <code class="language-plaintext highlighter-rouge">true</code> como o número <strong>1</strong> e <code class="language-plaintext highlighter-rouge">false</code> como <strong>0</strong>. Mas o atributo <code class="language-plaintext highlighter-rouge">checked</code> de um checkbox HTML só aceita booleanos de verdade (<code class="language-plaintext highlighter-rouge">true</code>/<code class="language-plaintext highlighter-rouge">false</code>).</p> <ul> <li>Se usarmos apenas <code class="language-plaintext highlighter-rouge">:checked (:todos/completed todo)</code>, o valor seria <code class="language-plaintext highlighter-rouge">1</code> ou <code class="language-plaintext highlighter-rouge">0</code>.</li> <li>A expressão <code class="language-plaintext highlighter-rouge">(not= 0 (:todos/completed todo))</code> <strong>converte explicitamente</strong>: <code class="language-plaintext highlighter-rouge">1</code> → <code class="language-plaintext highlighter-rouge">true</code> e <code class="language-plaintext highlighter-rouge">0</code> → <code class="language-plaintext highlighter-rouge">false</code>.</li> </ul> <p><strong>Ação:</strong> Modifique a função <code class="language-plaintext highlighter-rouge">todo-list</code> do <code class="language-plaintext highlighter-rouge">core.cljs</code>. <em>Mude esta versão:</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">todo-list</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:ul.todo-list</span><span class="w">
   </span><span class="p">(</span><span class="k">for</span><span class="w"> </span><span class="p">[</span><span class="n">todo</span><span class="w"> </span><span class="p">(</span><span class="no">:todos</span><span class="w"> </span><span class="o">@</span><span class="n">app-state</span><span class="p">)]</span><span class="w">
     </span><span class="o">^</span><span class="p">{</span><span class="no">:key</span><span class="w"> </span><span class="p">(</span><span class="no">:todos/id</span><span class="w"> </span><span class="n">todo</span><span class="p">)}</span><span class="w">
     </span><span class="p">[</span><span class="no">:li.todo-item</span><span class="w">
      </span><span class="p">(</span><span class="no">:todos/title</span><span class="w"> </span><span class="n">todo</span><span class="p">)])])</span><span class="w">
</span></code></pre></div></div> <p><em>Para esta versão:</em></p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">todo-list</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:ul.todo-list</span><span class="w">
   </span><span class="p">(</span><span class="k">for</span><span class="w"> </span><span class="p">[</span><span class="n">todo</span><span class="w"> </span><span class="p">(</span><span class="no">:todos</span><span class="w"> </span><span class="o">@</span><span class="n">app-state</span><span class="p">)]</span><span class="w">
     </span><span class="o">^</span><span class="p">{</span><span class="no">:key</span><span class="w"> </span><span class="p">(</span><span class="no">:todos/id</span><span class="w"> </span><span class="n">todo</span><span class="p">)}</span><span class="w">

     </span><span class="c1">;; 1. Classe CSS 'completed' quando o status for 1</span><span class="w">
     </span><span class="p">[</span><span class="no">:li.todo-item</span><span class="w"> </span><span class="p">{</span><span class="no">:class</span><span class="w"> </span><span class="p">(</span><span class="nb">when</span><span class="w"> </span><span class="p">(</span><span class="nb">=</span><span class="w"> </span><span class="mi">1</span><span class="w"> </span><span class="p">(</span><span class="no">:todos/completed</span><span class="w"> </span><span class="n">todo</span><span class="p">))</span><span class="w"> </span><span class="s">"completed"</span><span class="p">)}</span><span class="w">

      </span><span class="c1">;; 2. O Checkbox</span><span class="w">
      </span><span class="p">[</span><span class="no">:input.todo-checkbox</span><span class="w">
       </span><span class="p">{</span><span class="no">:type</span><span class="w"> </span><span class="s">"checkbox"</span><span class="w">
        </span><span class="c1">;; 3. A CONVERSÃO: 0/1 -&gt; booleano de verdade</span><span class="w">
        </span><span class="no">:checked</span><span class="w"> </span><span class="p">(</span><span class="nb">not=</span><span class="w"> </span><span class="mi">0</span><span class="w"> </span><span class="p">(</span><span class="no">:todos/completed</span><span class="w"> </span><span class="n">todo</span><span class="p">))</span><span class="w">
        </span><span class="c1">;; 4. O evento chama nossa nova função de API</span><span class="w">
        </span><span class="no">:on-change</span><span class="w"> </span><span class="o">#</span><span class="p">(</span><span class="nf">toggle-todo</span><span class="w"> </span><span class="p">(</span><span class="no">:todos/id</span><span class="w"> </span><span class="n">todo</span><span class="p">))}]</span><span class="w">

      </span><span class="c1">;; 5. O título (agora dentro de um span, para o CSS)</span><span class="w">
      </span><span class="p">[</span><span class="no">:span.todo-title</span><span class="w"> </span><span class="p">(</span><span class="no">:todos/title</span><span class="w"> </span><span class="n">todo</span><span class="p">)]])])</span><span class="w">
</span></code></pre></div></div> <p><strong>Teste intermediário:</strong></p> <ol> <li>Com os dois servidores rodando, vá a <code class="language-plaintext highlighter-rouge">http://localhost:8000</code>.</li> <li>Adicione um todo e <strong>clique no checkbox</strong>. Ele deve marcar.</li> <li>Recarregue (F5): o item <strong>continua marcado</strong> — o <code class="language-plaintext highlighter-rouge">UPDATE</code> persistiu! 🎉</li> </ol> <hr/> <h2 id="️-parte-3-o-delete--backend">🗑️ Parte 3: O Delete — Backend</h2> <p>O padrão se repete pela última vez. Você já sabe a receita: <code class="language-plaintext highlighter-rouge">db</code> → <code class="language-plaintext highlighter-rouge">handler</code> → <code class="language-plaintext highlighter-rouge">rota</code> → <code class="language-plaintext highlighter-rouge">frontend</code>.</p> <h3 id="passo-66-a-lógica-no-dbclj">Passo 6.6: A Lógica no <code class="language-plaintext highlighter-rouge">db.clj</code></h3> <p><strong>Ação:</strong> Adicione ao final de <code class="language-plaintext highlighter-rouge">src/todo/backend/db.clj</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">delete-todo!</span><span class="w">
  </span><span class="s">"Remove um todo do banco. Retorna o todo removido, ou nil se não existir."</span><span class="w">
  </span><span class="p">[</span><span class="n">id</span><span class="p">]</span><span class="w">
  </span><span class="c1">;; RETURNING * aqui nos devolve a linha que acabou de ser apagada.</span><span class="w">
  </span><span class="c1">;; É perfeito para o handler saber se o id existia (nil = não existia).</span><span class="w">
  </span><span class="p">(</span><span class="nf">jdbc/execute-one!</span><span class="w"> </span><span class="n">db-spec</span><span class="w"> </span><span class="p">[</span><span class="s">"
    DELETE FROM todos
    WHERE id = ?
    RETURNING *"</span><span class="w">
    </span><span class="n">id</span><span class="p">]))</span><span class="w">
</span></code></pre></div></div> <h3 id="passo-67-o-handler-no-handlerclj">Passo 6.7: O Handler no <code class="language-plaintext highlighter-rouge">handler.clj</code></h3> <p><strong>Ação:</strong> Adicione ao final de <code class="language-plaintext highlighter-rouge">src/todo/backend/handler.clj</code>:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">;; --- Handler para Deletar um Todo ---</span><span class="w">
</span><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">delete-todo-handler</span><span class="w">
  </span><span class="s">"Handler para a rota DELETE /api/todos/:id."</span><span class="w">
  </span><span class="p">[</span><span class="n">request</span><span class="p">]</span><span class="w">
  </span><span class="p">(</span><span class="k">let</span><span class="w"> </span><span class="p">[</span><span class="n">id</span><span class="w"> </span><span class="p">(</span><span class="nf">Integer/parseInt</span><span class="w"> </span><span class="p">(</span><span class="nf">get-in</span><span class="w"> </span><span class="n">request</span><span class="w"> </span><span class="p">[</span><span class="no">:path-params</span><span class="w"> </span><span class="no">:id</span><span class="p">]))]</span><span class="w">
    </span><span class="p">(</span><span class="nb">if-let</span><span class="w"> </span><span class="p">[</span><span class="n">deleted-todo</span><span class="w"> </span><span class="p">(</span><span class="nf">db/delete-todo!</span><span class="w"> </span><span class="n">id</span><span class="p">)]</span><span class="w">
      </span><span class="p">{</span><span class="no">:status</span><span class="w"> </span><span class="mi">200</span><span class="w"> </span><span class="no">:body</span><span class="w"> </span><span class="n">deleted-todo</span><span class="p">}</span><span class="w">
      </span><span class="p">{</span><span class="no">:status</span><span class="w"> </span><span class="mi">404</span><span class="w"> </span><span class="no">:body</span><span class="w"> </span><span class="p">{</span><span class="no">:error</span><span class="w"> </span><span class="s">"Todo não encontrado"</span><span class="p">}})))</span><span class="w">
</span></code></pre></div></div> <p>(Compare com o <code class="language-plaintext highlighter-rouge">toggle-todo-handler</code>: é o mesmo esqueleto. Isso não é preguiça — é <strong>arquitetura</strong>. Padrões repetíveis são fáceis de escrever, ler e testar.)</p> <h3 id="passo-68-a-rota-no-coreclj">Passo 6.8: A Rota no <code class="language-plaintext highlighter-rouge">core.clj</code></h3> <p><strong>Ação:</strong> O <code class="language-plaintext highlighter-rouge">app-routes</code> <strong>completo e final</strong> fica assim:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">def</span><span class="w"> </span><span class="n">app-routes</span><span class="w">
  </span><span class="p">(</span><span class="nf">ring/router</span><span class="w">
   </span><span class="p">[</span><span class="s">"/api"</span><span class="w">
    </span><span class="p">[</span><span class="s">"/hello"</span><span class="w"> </span><span class="p">{</span><span class="no">:get</span><span class="w"> </span><span class="p">{</span><span class="no">:handler</span><span class="w"> </span><span class="n">handler/hello-handler</span><span class="p">}}]</span><span class="w">

    </span><span class="p">[</span><span class="s">"/todos"</span><span class="w">
     </span><span class="p">{</span><span class="no">:get</span><span class="w">  </span><span class="p">{</span><span class="no">:handler</span><span class="w"> </span><span class="n">handler/list-todos-handler</span><span class="p">}</span><span class="w">
      </span><span class="no">:post</span><span class="w"> </span><span class="p">{</span><span class="no">:handler</span><span class="w"> </span><span class="n">handler/create-todo-handler</span><span class="p">}}]</span><span class="w">

    </span><span class="p">[</span><span class="s">"/todos/:id/toggle"</span><span class="w">
     </span><span class="p">{</span><span class="no">:post</span><span class="w"> </span><span class="p">{</span><span class="no">:handler</span><span class="w"> </span><span class="n">handler/toggle-todo-handler</span><span class="p">}}]</span><span class="w">

    </span><span class="c1">;; --- NOVA ROTA ---</span><span class="w">
    </span><span class="p">[</span><span class="s">"/todos/:id"</span><span class="w">
     </span><span class="p">{</span><span class="no">:delete</span><span class="w"> </span><span class="p">{</span><span class="no">:handler</span><span class="w"> </span><span class="n">handler/delete-todo-handler</span><span class="p">}}]]))</span><span class="w">
</span></code></pre></div></div> <aside> <p>💡 Repare que usamos o <strong>método HTTP</strong> <code class="language-plaintext highlighter-rouge">DELETE</code> na URL “canônica” do recurso (<code class="language-plaintext highlighter-rouge">/todos/:id</code>), em vez de inventar uma URL como <code class="language-plaintext highlighter-rouge">/todos/:id/delete</code>. Essa é a convenção REST: a URL identifica <em>o recurso</em>, e o método diz <em>o que fazer</em> com ele. (E lembre-se: lá na Fase 4, nosso <code class="language-plaintext highlighter-rouge">wrap-cors</code> já incluiu <code class="language-plaintext highlighter-rouge">:delete</code> nos métodos permitidos — por isso o navegador vai deixar essa chamada passar.)</p> </aside> <p><strong>✅ Backend completo!</strong> Pare e <strong>reinicie</strong> o backend (<code class="language-plaintext highlighter-rouge">clj -M:run</code>), e teste com <code class="language-plaintext highlighter-rouge">curl</code>:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>curl <span class="nt">-X</span> DELETE http://localhost:3000/api/todos/1
</code></pre></div></div> <p><strong>Resultado Esperado:</strong> o JSON do todo removido. Chame de novo com o mesmo id: <code class="language-plaintext highlighter-rouge">{"error":"Todo não encontrado"}</code> — ele já foi.</p> <hr/> <h2 id="️-parte-4-o-delete--frontend">🗑️ Parte 4: O Delete — Frontend</h2> <h3 id="passo-69-a-função-de-api-e-o-botão">Passo 6.9: A Função de API e o Botão</h3> <p><strong>Ação 1:</strong> No <code class="language-plaintext highlighter-rouge">src/todo/frontend/core.cljs</code>, adicione junto às outras funções de API:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">delete-todo</span><span class="w">
  </span><span class="s">"Chama a API para remover um todo."</span><span class="w">
  </span><span class="p">[</span><span class="n">id</span><span class="p">]</span><span class="w">
  </span><span class="p">(</span><span class="nf">go</span><span class="w">
    </span><span class="p">(</span><span class="nf">try</span><span class="w">
      </span><span class="p">(</span><span class="nf">&lt;p!</span><span class="w"> </span><span class="p">(</span><span class="nf">fetch-json</span><span class="w"> </span><span class="p">(</span><span class="nb">str</span><span class="w"> </span><span class="n">api-url</span><span class="w"> </span><span class="s">"/todos/"</span><span class="w"> </span><span class="n">id</span><span class="p">)</span><span class="w">
                       </span><span class="p">{</span><span class="no">:method</span><span class="w"> </span><span class="s">"DELETE"</span><span class="p">}))</span><span class="w">
      </span><span class="c1">;; Se funcionou, recarrega a lista</span><span class="w">
      </span><span class="p">(</span><span class="nf">get-todos</span><span class="p">)</span><span class="w">
      </span><span class="p">(</span><span class="nf">catch</span><span class="w"> </span><span class="n">js/Error</span><span class="w"> </span><span class="n">e</span><span class="w">
        </span><span class="p">(</span><span class="nf">swap!</span><span class="w"> </span><span class="n">app-state</span><span class="w"> </span><span class="nb">assoc</span><span class="w"> </span><span class="no">:error</span><span class="w"> </span><span class="p">(</span><span class="nf">.-message</span><span class="w"> </span><span class="n">e</span><span class="p">)</span><span class="w"> </span><span class="no">:loading</span><span class="w"> </span><span class="n">false</span><span class="p">)))))</span><span class="w">
</span></code></pre></div></div> <p><strong>Ação 2:</strong> Adicione o botão de deletar ao <code class="language-plaintext highlighter-rouge">todo-list</code>. A versão <strong>final</strong> do componente:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">(</span><span class="k">defn</span><span class="w"> </span><span class="n">todo-list</span><span class="w"> </span><span class="p">[]</span><span class="w">
  </span><span class="p">[</span><span class="no">:ul.todo-list</span><span class="w">
   </span><span class="p">(</span><span class="k">for</span><span class="w"> </span><span class="p">[</span><span class="n">todo</span><span class="w"> </span><span class="p">(</span><span class="no">:todos</span><span class="w"> </span><span class="o">@</span><span class="n">app-state</span><span class="p">)]</span><span class="w">
     </span><span class="o">^</span><span class="p">{</span><span class="no">:key</span><span class="w"> </span><span class="p">(</span><span class="no">:todos/id</span><span class="w"> </span><span class="n">todo</span><span class="p">)}</span><span class="w">
     </span><span class="p">[</span><span class="no">:li.todo-item</span><span class="w"> </span><span class="p">{</span><span class="no">:class</span><span class="w"> </span><span class="p">(</span><span class="nb">when</span><span class="w"> </span><span class="p">(</span><span class="nb">=</span><span class="w"> </span><span class="mi">1</span><span class="w"> </span><span class="p">(</span><span class="no">:todos/completed</span><span class="w"> </span><span class="n">todo</span><span class="p">))</span><span class="w"> </span><span class="s">"completed"</span><span class="p">)}</span><span class="w">

      </span><span class="p">[</span><span class="no">:input.todo-checkbox</span><span class="w">
       </span><span class="p">{</span><span class="no">:type</span><span class="w"> </span><span class="s">"checkbox"</span><span class="w">
        </span><span class="no">:checked</span><span class="w"> </span><span class="p">(</span><span class="nb">not=</span><span class="w"> </span><span class="mi">0</span><span class="w"> </span><span class="p">(</span><span class="no">:todos/completed</span><span class="w"> </span><span class="n">todo</span><span class="p">))</span><span class="w">
        </span><span class="no">:on-change</span><span class="w"> </span><span class="o">#</span><span class="p">(</span><span class="nf">toggle-todo</span><span class="w"> </span><span class="p">(</span><span class="no">:todos/id</span><span class="w"> </span><span class="n">todo</span><span class="p">))}]</span><span class="w">

      </span><span class="p">[</span><span class="no">:span.todo-title</span><span class="w"> </span><span class="p">(</span><span class="no">:todos/title</span><span class="w"> </span><span class="n">todo</span><span class="p">)]</span><span class="w">

      </span><span class="c1">;; --- O BOTÃO DE DELETAR (agora de verdade!) ---</span><span class="w">
      </span><span class="p">[</span><span class="no">:button.delete-btn</span><span class="w">
       </span><span class="p">{</span><span class="no">:on-click</span><span class="w"> </span><span class="o">#</span><span class="p">(</span><span class="nf">delete-todo</span><span class="w"> </span><span class="p">(</span><span class="no">:todos/id</span><span class="w"> </span><span class="n">todo</span><span class="p">))}</span><span class="w">
       </span><span class="s">"X"</span><span class="p">]])])</span><span class="w">
</span></code></pre></div></div> <p><strong>Teste intermediário:</strong> adicione alguns todos e clique no “X” de um deles. Ele some da lista. Recarregue (F5): continua fora — o <code class="language-plaintext highlighter-rouge">DELETE</code> persistiu.</p> <aside> <p>💡 <strong>Extensão opcional:</strong> quer pedir confirmação antes de apagar? Troque o <code class="language-plaintext highlighter-rouge">:on-click</code> por:</p> <div class="language-clojure highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="no">:on-click</span><span class="w"> </span><span class="o">#</span><span class="p">(</span><span class="nb">when</span><span class="w"> </span><span class="p">(</span><span class="nf">js/confirm</span><span class="w"> </span><span class="s">"Remover esta tarefa?"</span><span class="p">)</span><span class="w">
              </span><span class="p">(</span><span class="nf">delete-todo</span><span class="w"> </span><span class="p">(</span><span class="no">:todos/id</span><span class="w"> </span><span class="n">todo</span><span class="p">)))}</span><span class="w">
</span></code></pre></div> </div> </aside> <hr/> <h2 id="-parte-5-o-visual-final-css">🎨 Parte 5: O Visual Final (CSS)</h2> <p>Nossa aplicação funciona, mas está com cara de 1996. Vamos dar um banho de loja.</p> <p><strong>Ação:</strong> Abra <code class="language-plaintext highlighter-rouge">resources/public/index.html</code> e adicione o bloco <code class="language-plaintext highlighter-rouge">&lt;style&gt;</code> dentro do <code class="language-plaintext highlighter-rouge">&lt;head&gt;</code> (depois do <code class="language-plaintext highlighter-rouge">&lt;title&gt;</code>):</p> <div class="language-html highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="cp">&lt;!DOCTYPE html&gt;</span>
<span class="nt">&lt;html&gt;</span>
  <span class="nt">&lt;head&gt;</span>
    <span class="nt">&lt;meta</span> <span class="na">charset=</span><span class="s">"UTF-8"</span> <span class="nt">/&gt;</span>
    <span class="nt">&lt;meta</span> <span class="na">name=</span><span class="s">"viewport"</span> <span class="na">content=</span><span class="s">"width=device-width, initial-scale=1"</span> <span class="nt">/&gt;</span>
    <span class="nt">&lt;title&gt;</span>Todo App (CLJS)<span class="nt">&lt;/title&gt;</span>
    <span class="nt">&lt;style&gt;</span>
      <span class="nt">body</span> <span class="p">{</span>
        <span class="nl">font-family</span><span class="p">:</span> <span class="n">Arial</span><span class="p">,</span> <span class="nb">sans-serif</span><span class="p">;</span>
        <span class="nl">background-color</span><span class="p">:</span> <span class="nx">#f5f5f5</span><span class="p">;</span>
        <span class="nl">display</span><span class="p">:</span> <span class="nb">flex</span><span class="p">;</span>
        <span class="nl">justify-content</span><span class="p">:</span> <span class="nb">center</span><span class="p">;</span>
        <span class="nl">padding</span><span class="p">:</span> <span class="m">50px</span> <span class="m">20px</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="nc">.todo-app</span> <span class="p">{</span>
        <span class="nl">width</span><span class="p">:</span> <span class="m">100%</span><span class="p">;</span>
        <span class="nl">max-width</span><span class="p">:</span> <span class="m">600px</span><span class="p">;</span>
        <span class="nl">background</span><span class="p">:</span> <span class="nx">white</span><span class="p">;</span>
        <span class="nl">padding</span><span class="p">:</span> <span class="m">25px</span><span class="p">;</span>
        <span class="nl">border-radius</span><span class="p">:</span> <span class="m">8px</span><span class="p">;</span>
        <span class="nl">box-shadow</span><span class="p">:</span> <span class="m">0</span> <span class="m">4px</span> <span class="m">12px</span> <span class="nf">rgba</span><span class="p">(</span><span class="m">0</span><span class="p">,</span> <span class="m">0</span><span class="p">,</span> <span class="m">0</span><span class="p">,</span> <span class="m">0.1</span><span class="p">);</span>
      <span class="p">}</span>
      <span class="nt">h1</span> <span class="p">{</span>
        <span class="nl">color</span><span class="p">:</span> <span class="nx">#333</span><span class="p">;</span>
        <span class="nl">text-align</span><span class="p">:</span> <span class="nb">center</span><span class="p">;</span>
        <span class="nl">margin-bottom</span><span class="p">:</span> <span class="m">25px</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="nc">.todo-input</span> <span class="p">{</span>
        <span class="nl">display</span><span class="p">:</span> <span class="nb">flex</span><span class="p">;</span>
        <span class="nl">gap</span><span class="p">:</span> <span class="m">10px</span><span class="p">;</span>
        <span class="nl">margin-bottom</span><span class="p">:</span> <span class="m">20px</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="nc">.todo-input</span> <span class="nt">input</span><span class="o">[</span><span class="nt">type</span><span class="o">=</span><span class="s1">"text"</span><span class="o">]</span> <span class="p">{</span>
        <span class="nl">flex</span><span class="p">:</span> <span class="m">1</span><span class="p">;</span>
        <span class="nl">padding</span><span class="p">:</span> <span class="m">10px</span><span class="p">;</span>
        <span class="nl">border</span><span class="p">:</span> <span class="m">1px</span> <span class="nb">solid</span> <span class="nx">#ddd</span><span class="p">;</span>
        <span class="nl">border-radius</span><span class="p">:</span> <span class="m">4px</span><span class="p">;</span>
        <span class="nl">font-size</span><span class="p">:</span> <span class="m">16px</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="nc">.todo-list</span> <span class="p">{</span>
        <span class="nl">list-style</span><span class="p">:</span> <span class="nb">none</span><span class="p">;</span>
        <span class="nl">padding</span><span class="p">:</span> <span class="m">0</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="nc">.todo-item</span> <span class="p">{</span>
        <span class="nl">display</span><span class="p">:</span> <span class="nb">flex</span><span class="p">;</span>
        <span class="nl">align-items</span><span class="p">:</span> <span class="nb">center</span><span class="p">;</span>
        <span class="nl">padding</span><span class="p">:</span> <span class="m">12px</span><span class="p">;</span>
        <span class="nl">margin-bottom</span><span class="p">:</span> <span class="m">8px</span><span class="p">;</span>
        <span class="nl">background-color</span><span class="p">:</span> <span class="nx">#f8f9fa</span><span class="p">;</span>
        <span class="nl">border-radius</span><span class="p">:</span> <span class="m">4px</span><span class="p">;</span>
        <span class="nl">transition</span><span class="p">:</span> <span class="n">background-color</span> <span class="m">0.3s</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="c">/* O título ocupa todo o espaço entre o checkbox e o botão X */</span>
      <span class="nc">.todo-title</span> <span class="p">{</span>
        <span class="nl">flex</span><span class="p">:</span> <span class="m">1</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="c">/* O botão "Adicionar" */</span>
      <span class="nt">button</span> <span class="p">{</span>
        <span class="nl">padding</span><span class="p">:</span> <span class="m">10px</span> <span class="m">15px</span><span class="p">;</span>
        <span class="nl">background-color</span><span class="p">:</span> <span class="nx">#007bff</span><span class="p">;</span>
        <span class="nl">color</span><span class="p">:</span> <span class="nx">white</span><span class="p">;</span>
        <span class="nl">border</span><span class="p">:</span> <span class="nb">none</span><span class="p">;</span>
        <span class="nl">border-radius</span><span class="p">:</span> <span class="m">4px</span><span class="p">;</span>
        <span class="nl">cursor</span><span class="p">:</span> <span class="nb">pointer</span><span class="p">;</span>
        <span class="nl">font-size</span><span class="p">:</span> <span class="m">16px</span><span class="p">;</span>
        <span class="nl">transition</span><span class="p">:</span> <span class="n">background-color</span> <span class="m">0.2s</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="nt">button</span><span class="nd">:hover</span> <span class="p">{</span>
        <span class="nl">background-color</span><span class="p">:</span> <span class="nx">#0056b3</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="c">/* O botão de deletar (X) */</span>
      <span class="nc">.delete-btn</span> <span class="p">{</span>
        <span class="nl">background-color</span><span class="p">:</span> <span class="nx">#dc3545</span><span class="p">;</span>
        <span class="nl">font-size</span><span class="p">:</span> <span class="m">12px</span><span class="p">;</span>
        <span class="nl">padding</span><span class="p">:</span> <span class="m">5px</span> <span class="m">10px</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="nc">.delete-btn</span><span class="nd">:hover</span> <span class="p">{</span>
        <span class="nl">background-color</span><span class="p">:</span> <span class="nx">#c82333</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="c">/* Item concluído */</span>
      <span class="nc">.completed</span> <span class="p">{</span>
        <span class="nl">opacity</span><span class="p">:</span> <span class="m">0.6</span><span class="p">;</span>
        <span class="nl">background-color</span><span class="p">:</span> <span class="nx">#f1f1f1</span><span class="p">;</span>
        <span class="nl">text-decoration</span><span class="p">:</span> <span class="nb">line-through</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="nc">.todo-checkbox</span> <span class="p">{</span>
        <span class="nl">margin-right</span><span class="p">:</span> <span class="m">10px</span><span class="p">;</span>
        <span class="nl">cursor</span><span class="p">:</span> <span class="nb">pointer</span><span class="p">;</span>
      <span class="p">}</span>
    <span class="nt">&lt;/style&gt;</span>
  <span class="nt">&lt;/head&gt;</span>
  <span class="nt">&lt;body&gt;</span>
    <span class="nt">&lt;div</span> <span class="na">id=</span><span class="s">"app"</span><span class="nt">&gt;</span>Carregando...<span class="nt">&lt;/div&gt;</span>

    <span class="nt">&lt;script </span><span class="na">src=</span><span class="s">"/js/main.js"</span><span class="nt">&gt;&lt;/script&gt;</span>
  <span class="nt">&lt;/body&gt;</span>
<span class="nt">&lt;/html&gt;</span>
</code></pre></div></div> <aside> <p>💡 Mudou o <code class="language-plaintext highlighter-rouge">index.html</code> e não viu diferença? O servidor de desenvolvimento às vezes mantém o HTML em cache — um <strong>F5</strong> (ou Ctrl+F5) resolve.</p> </aside> <hr/> <h2 id="-teste-final-o-crud-completo">🎉 Teste Final (o CRUD completo)</h2> <p>Com os dois servidores rodando, em <code class="language-plaintext highlighter-rouge">http://localhost:8000</code>:</p> <ol> <li><strong>Create:</strong> adicione dois ou três todos. ✅</li> <li><strong>Read:</strong> recarregue (F5) — todos continuam lá. ✅</li> <li><strong>Update:</strong> clique no checkbox de um deles — ele fica riscado e opaco. F5 — continua marcado. ✅</li> <li><strong>Delete:</strong> clique no “X” de outro — ele some. F5 — continua fora. ✅</li> <li><strong>A prova final:</strong> pare o backend (<code class="language-plaintext highlighter-rouge">Ctrl+C</code>), reinicie (<code class="language-plaintext highlighter-rouge">clj -M:run</code>), F5 no navegador — <strong>tudo exatamente como você deixou.</strong> ✅</li> </ol> <p>Se os cinco passaram, você tem um CRUD full-stack completo e persistente.</p> <h2 id="passo-610-git-checkpoint">Passo 6.10: Git Checkpoint</h2> <p><strong>Ação:</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add <span class="nb">.</span>
git commit <span class="nt">-m</span> <span class="s2">"feat(crud): implementa funcionalidades de toggle e delete"</span>
</code></pre></div></div> <hr/> <p><strong>Fim da Fase 6!</strong> 🏁</p> <p>Você finalizou o desenvolvimento de uma aplicação full-stack completa, moderna e robusta:</p> <ul> <li><strong>Backend:</strong> Clojure, Ring e Reitit expondo uma API REST completa (CRUD).</li> <li><strong>Banco de Dados:</strong> persistência real com SQLite e <code class="language-plaintext highlighter-rouge">next.jdbc</code>.</li> <li><strong>Frontend:</strong> ClojureScript e Reagent (com o padrão moderno do React) para uma UI reativa.</li> <li><strong>Prática profissional:</strong> <code class="language-plaintext highlighter-rouge">git</code> incremental, <code class="language-plaintext highlighter-rouge">npm</code>, depuração de CORS (incluindo <em>preflight</em>), dados assíncronos (<code class="language-plaintext highlighter-rouge">go</code>/<code class="language-plaintext highlighter-rouge">&lt;p!</code>) e o uso correto de <code class="language-plaintext highlighter-rouge">atom</code> vs. <code class="language-plaintext highlighter-rouge">r/atom</code>.</li> </ul> <p>Falta só uma coisa para o projeto estar pronto para o mundo (e para a entrega): a <strong>documentação</strong>.</p> <hr/> <h1 id="fase-7-readme-e-entrega">Fase 7: README e Entrega</h1> <p><strong>Objetivo:</strong> Escrever um <code class="language-plaintext highlighter-rouge">README.md</code> claro — a “porta de entrada” de qualquer repositório — e fazer o checklist final antes da entrega.</p> <p><strong>Por que fazemos isso?</strong> Um projeto sem README é um projeto que ninguém consegue rodar. No mundo real, o README é o primeiro (e às vezes o único) arquivo que um colega, recrutador ou avaliador vai ler. Na avaliação desta disciplina, ele vale nota — e o critério é simples: <em>um colega conseguiria clonar e rodar seu projeto lendo apenas o README?</em></p> <h3 id="passo-71-criar-o-readmemd">Passo 7.1: Criar o <code class="language-plaintext highlighter-rouge">README.md</code></h3> <p><strong>Ação:</strong> Crie o arquivo <code class="language-plaintext highlighter-rouge">README.md</code> na <strong>raiz</strong> do projeto e adapte o modelo abaixo (troque o nome, o link do repositório e o que mais quiser personalizar):</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gh"># Todo List — Clojure/ClojureScript</span>

<span class="gs">**Aluno(a):**</span> Seu Nome Completo Aqui

<span class="gs">**Tutorial original:**</span> <span class="p">[</span><span class="nv">Tutorial Clojure/ClojureScript: Construindo uma Aplicação
Persistente e Reativa</span><span class="p">](</span><span class="sx">https://profsergiocosta.notion.site/Tutorial-Clojure-ClojureScript-Construindo-uma-Aplica-o-Persistente-e-Reativa-2a5cce975093807aa9f0f0cb0cf69645</span><span class="p">)</span>

<span class="gu">## Descrição</span>

Aplicação full-stack de lista de tarefas (Todo List), construída de forma
incremental para estudar a arquitetura de aplicações funcionais modernas:
<span class="p">
-</span> <span class="gs">**Backend:**</span> Clojure, com <span class="p">[</span><span class="nv">Ring</span><span class="p">](</span><span class="sx">https://github.com/ring-clojure/ring</span><span class="p">)</span>
  (servidor Jetty) e <span class="p">[</span><span class="nv">Reitit</span><span class="p">](</span><span class="sx">https://github.com/metosin/reitit</span><span class="p">)</span> <span class="p">(</span><span class="sx">roteamento</span><span class="p">)</span>,
  expondo uma API REST com CRUD completo.
<span class="p">-</span> <span class="gs">**Banco de dados:**</span> SQLite, acessado via
  <span class="p">[</span><span class="nv">next.jdbc</span><span class="p">](</span><span class="sx">https://github.com/seancorfield/next-jdbc</span><span class="p">)</span>, com persistência
  real em disco (<span class="sb">`prod.db`</span>).
<span class="p">-</span> <span class="gs">**Frontend:**</span> ClojureScript com
  <span class="p">[</span><span class="nv">Reagent</span><span class="p">](</span><span class="sx">https://github.com/reagent-project/reagent</span><span class="p">)</span> <span class="p">(</span><span class="sx">React</span><span class="p">)</span> e
  <span class="p">[</span><span class="nv">Shadow-CLJS</span><span class="p">](</span><span class="sx">https://github.com/thheller/shadow-cljs</span><span class="p">)</span>, consumindo a API
  via <span class="sb">`fetch`</span>.

<span class="gu">## Pré-requisitos</span>

| Ferramenta                                                        | Versão mínima |
| ----------------------------------------------------------------- | ------------- |
| Java (JDK)                                                        | 11+           |
| <span class="p">[</span><span class="nv">Clojure CLI</span><span class="p">](</span><span class="sx">https://clojure.org/guides/install_clojure</span><span class="p">)</span> <span class="p">(</span><span class="sx">`clj`</span><span class="p">)</span> | 1.11+         |
| Node.js (<span class="sb">`node`</span> / <span class="sb">`npm`</span>)                                          | 18+           |

<span class="gu">## Como Rodar</span>
<span class="p">
1.</span> <span class="gs">**Clone o repositório e instale as dependências do frontend:**</span>

   <span class="p">```</span><span class="nl">bash
</span>   git clone https://github.com/SEU-USUARIO/SEU-REPO.git
   <span class="nb">cd </span>SEU-REPO
   npm <span class="nb">install</span>
   <span class="p">```</span>
<span class="p">
2.</span> <span class="gs">**Terminal 1 — Backend (API na porta 3000):**</span>

   <span class="p">```</span><span class="nl">bash
</span>   clj <span class="nt">-M</span>:run
   <span class="p">```</span>

   Na primeira execução, as dependências Clojure serão baixadas e o banco
   <span class="sb">`prod.db`</span> será criado automaticamente.
<span class="p">
3.</span> <span class="gs">**Terminal 2 — Frontend (porta 8000):**</span>

   <span class="p">```</span><span class="nl">bash
</span>   npx shadow-cljs watch app
   <span class="p">```</span>

   Aguarde a mensagem <span class="sb">`Build completed`</span>.
<span class="p">
4.</span> <span class="gs">**Abra o navegador em:**</span> <span class="p">[</span><span class="nv">http://localhost:8000</span><span class="p">](</span><span class="sx">http://localhost:8000</span><span class="p">)</span>

<span class="gu">## Endpoints da API</span>

| Método   | Rota                    | Descrição                            |
| -------- | ----------------------- | ------------------------------------ |
| <span class="sb">`GET`</span>    | <span class="sb">`/api/todos`</span>            | Lista todas as tarefas               |
| <span class="sb">`POST`</span>   | <span class="sb">`/api/todos`</span>            | Cria uma tarefa (<span class="sb">`{"title": "..."}`</span>) |
| <span class="sb">`POST`</span>   | <span class="sb">`/api/todos/:id/toggle`</span> | Alterna o status feito/não feito     |
| <span class="sb">`DELETE`</span> | <span class="sb">`/api/todos/:id`</span>        | Remove uma tarefa                    |
</code></pre></div></div> <aside> <p>💡 O modelo acima usa blocos de código aninhados dentro de uma lista — se o seu editor “quebrar” a renderização, simplifique: o importante é que as informações (nome, link, descrição, pré-requisitos e os comandos dos dois terminais) estejam lá e corretas.</p> </aside> <h3 id="passo-72-o-teste-do-colega">Passo 7.2: O Teste do “Colega”</h3> <p><strong>Ação:</strong> Faça de conta que você é outra pessoa. Siga o <strong>seu próprio README</strong>, literalmente, do zero:</p> <ol> <li>Clone seu repositório em uma <strong>pasta nova</strong> (ex: <code class="language-plaintext highlighter-rouge">/tmp/teste-todo</code>).</li> <li>Execute exatamente os comandos do README, na ordem.</li> <li>A aplicação subiu? O CRUD funciona? Os dados persistem após reiniciar o backend?</li> </ol> <p>Se algo falhou, é o README (ou o repositório) que precisa de ajuste — melhor descobrir agora do que o avaliador descobrir depois. Um problema clássico revelado por esse teste: esquecer o passo <code class="language-plaintext highlighter-rouge">npm install</code> (a pasta <code class="language-plaintext highlighter-rouge">node_modules/</code> não vai para o Git!).</p> <h3 id="passo-73-commit-final">Passo 7.3: Commit Final</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git add README.md
git commit <span class="nt">-m</span> <span class="s2">"docs: adiciona README com instruções de execução"</span>
</code></pre></div></div> <h3 id="passo-74-checklist-de-entrega">Passo 7.4: Checklist de Entrega</h3> <p>Antes de enviar, confira o histórico:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git log <span class="nt">--oneline</span>
</code></pre></div></div> <p>Você deve ver (de cima para baixo, do mais novo para o mais antigo):</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>docs: adiciona README com instruções de execução
feat(crud): implementa funcionalidades de toggle e delete
refactor(db): substitui banco em memória por persistência SQLite
feat: conecta frontend com API do backend (CORS corrigido)
feat: implementa UI do frontend com estado local (sem API)
feat: implementa API REST de 'todos' com banco em memória
feat: implementa servidor 'Hello World' com Jetty e Reitit
feat: setup inicial do projeto com .gitignore
</code></pre></div></div> <p>(Commits extras no meio não são problema — o que importa é que os <strong>marcos</strong> estejam lá e na ordem certa.)</p> <p><strong>Checklist final:</strong></p> <ul class="task-list"> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>O repositório no GitHub é <strong>público</strong>?</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>O <code class="language-plaintext highlighter-rouge">git push</code> foi feito? (O que está só na sua máquina não conta!)</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>O README tem: nome completo, link do tutorial, descrição e instruções de execução?</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><code class="language-plaintext highlighter-rouge">prod.db</code> e <code class="language-plaintext highlighter-rouge">node_modules/</code> <strong>não</strong> estão no repositório (confira na página do GitHub)?</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>O CRUD completo funciona e os dados persistem após reiniciar o backend?</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><strong>SIGAA:</strong> link público do repositório na caixa de <strong>Comentários</strong> + o <strong>ZIP</strong> (GitHub → botão <em>Code</em> → <em>Download ZIP</em>) no <strong>Anexo</strong>.</li> </ul> <hr/> <h2 id="-tutorial-concluído">🚀 Tutorial Concluído!</h2> <p><strong>Parabéns!</strong> 🥳 Você construiu, passo a passo, uma aplicação web full-stack, moderna e persistente, depurando e corrigindo erros do mundo real ao longo do caminho — CORS, formatos de dados, keywords qualificadas, versões de ferramentas.</p> <p>Mais importante do que o Todo List em si é o <strong>padrão arquitetural</strong> que você agora domina: estado imutável em caixas (<code class="language-plaintext highlighter-rouge">atom</code>/<code class="language-plaintext highlighter-rouge">r/atom</code>), funções puras como handlers, dados fluindo como mapas do banco ao navegador, e um histórico Git que conta a história do projeto.</p> <p><strong>Quer ir além?</strong> Algumas ideias de extensão (opcionais):</p> <ul> <li>Um campo de <strong>edição</strong> do título (o “U” completo do CRUD);</li> <li>Filtros “Todas / Ativas / Concluídas” no frontend;</li> <li>Exibir o <code class="language-plaintext highlighter-rouge">:error</code> e o <code class="language-plaintext highlighter-rouge">:loading</code> do <code class="language-plaintext highlighter-rouge">app-state</code> na interface;</li> <li>Trocar o SQLite por PostgreSQL (só muda o <code class="language-plaintext highlighter-rouge">db-spec</code> e o driver!);</li> <li>Refazer o mesmo problema em <a href="/blog/tutorial-elixir-phoenix-liveview-todo-list/">Elixir/Phoenix LiveView</a>, no tutorial complementar da série.</li> </ul>]]></content><author><name></name></author><category term="tutorial"/><category term="clojure"/><category term="clojurescript"/><category term="reagent"/><category term="shadow-cljs"/><category term="ring"/><category term="reitit"/><category term="sqlite"/><category term="functional-programming"/><summary type="html"><![CDATA[Um guia prático e completo de arquitetura funcional e reativa, cobrindo desde o backend Ring/Reitit até o frontend Reagent com SQLite.]]></summary></entry><entry><title type="html">Two Ways to Simulate the Same World: Raster and Vector Backends in DisSModel</title><link href="https://lambdageo.github.io//blog/raster-vs-vector-dissmodel/" rel="alternate" type="text/html" title="Two Ways to Simulate the Same World: Raster and Vector Backends in DisSModel"/><published>2026-03-10T00:00:00+00:00</published><updated>2026-03-10T00:00:00+00:00</updated><id>https://lambdageo.github.io//blog/dissmodel-dual-backend</id><content type="html" xml:base="https://lambdageo.github.io//blog/raster-vs-vector-dissmodel/"><![CDATA[<p>I want to share something that has been quietly coming together over the past few weeks in our research group. It’s not a breakthrough, nothing dramatic — just one of those moments where you look at the code you’ve been building and think: <em>yes, this is working the way I hoped it would.</em></p> <p>We’ve been developing <a href="https://github.com/lambdageo/dissmodel">DisSModel</a>, a Python framework for spatial discrete simulation models. The core idea is simple: researchers should be able to describe <em>what</em> their model does without fighting the infrastructure of <em>how</em> it runs. We chose <a href="https://www.salabim.org/">salabim</a> as the discrete event simulation engine underneath. That decision is looking better and better as the framework grows.</p> <p>But what I really want to talk about in this post is something more specific: what happens when you implement the same ecological model twice — once on top of NumPy arrays, once on top of GeoDataFrames — and both versions run correctly, look similar, and each has a clear reason to exist.</p> <hr/> <h2 id="the-problem-we-were-solving">The Problem We Were Solving</h2> <p>Our group works on coastal dynamics models. Specifically, we’ve been implementing the BR-MANGUE model (Bezerra 2014), which simulates mangrove migration and flood propagation under IPCC RCP8.5 sea-level rise scenarios. The original model was written in TerraME/Lua. We’ve been translating it to Python.</p> <p>The domain is a 94,704-cell grid at 100×100m resolution, running 88 time steps from 2012 to 2100. At that scale, iterating cell by cell in Python is too slow to be useful. But we also wanted to keep a readable, geometry-aware version for comparison and validation.</p> <p>That tension — between performance and legibility — is what led us to the dual backend design.</p> <hr/> <h2 id="salabim-as-the-execution-backbone">salabim as the Execution Backbone</h2> <p>Before getting into the two backends, I want to say a few words about the choice to use salabim.</p> <p>When we first considered it, the question was: do we need a full discrete event simulation engine, or should we just write a loop? Salabim felt like overkill for cellular automata. But the more we used it, the more we appreciated what it gives us for free.</p> <p>Every model in DisSModel is a salabim <code class="language-plaintext highlighter-rouge">Component</code>. The environment controls the clock. Models register themselves and execute at every tick. This means:</p> <ul> <li>Multiple models sharing the same environment and clock with zero coordination code</li> <li><code class="language-plaintext highlighter-rouge">env.now()</code> giving the current simulation time to any model at any point</li> <li>The execution order is deterministic and follows registration order</li> <li>Adding visualization models (like <code class="language-plaintext highlighter-rouge">RasterMap</code> or <code class="language-plaintext highlighter-rouge">Map</code>) is just registering another component — they observe passively without touching the simulation logic</li> </ul> <p>That last point matters more than it sounds. In the BR-MANGUE simulation, we have:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">env</span> <span class="o">=</span> <span class="nc">Environment</span><span class="p">(</span><span class="n">start_time</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">end_time</span><span class="o">=</span><span class="mi">88</span><span class="p">)</span>

<span class="nc">FloodRasterModel</span><span class="p">(</span><span class="n">backend</span><span class="o">=</span><span class="n">backend</span><span class="p">,</span> <span class="n">taxa_elevacao</span><span class="o">=</span><span class="mf">0.011</span><span class="p">)</span>
<span class="nc">MangroveModel</span><span class="p">(</span><span class="n">backend</span><span class="o">=</span><span class="n">backend</span><span class="p">)</span>
<span class="nc">RasterMap</span><span class="p">(</span><span class="n">backend</span><span class="o">=</span><span class="n">backend</span><span class="p">,</span> <span class="n">band</span><span class="o">=</span><span class="sh">"</span><span class="s">uso</span><span class="sh">"</span><span class="p">,</span> <span class="n">color_map</span><span class="o">=</span><span class="n">USO_COLORS</span><span class="p">,</span> <span class="n">labels</span><span class="o">=</span><span class="n">USO_LABELS</span><span class="p">)</span>
<span class="nc">RasterMap</span><span class="p">(</span><span class="n">backend</span><span class="o">=</span><span class="n">backend</span><span class="p">,</span> <span class="n">band</span><span class="o">=</span><span class="sh">"</span><span class="s">alt</span><span class="sh">"</span><span class="p">,</span> <span class="n">cmap</span><span class="o">=</span><span class="sh">"</span><span class="s">terrain</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>Four objects, each doing its own thing, all synchronized by salabim. No callbacks, no explicit step loops, no shared state management. The flood model runs first, then the mangrove model reads the updated state, then the maps render. Every step. This emerged naturally from salabim’s architecture.</p> <hr/> <h2 id="the-two-backends">The Two Backends</h2> <p>DisSModel 0.2.0 formalized a class hierarchy that had been implicit for a while:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Model  (salabim Component)
  ├── SpatialModel              → GeoDataFrame + libpysal neighborhood
  │     └── CellularAutomaton  → rule(idx) per cell (pull pattern)
  │
  └── RasterModel              → RasterBackend (NumPy 2D arrays)
        └── RasterCellularAutomaton → rule(arrays) → dict (vectorized)
</code></pre></div></div> <p>The two branches are independent. A model that inherits <code class="language-plaintext highlighter-rouge">SpatialModel</code> never touches NumPy. A model that inherits <code class="language-plaintext highlighter-rouge">RasterModel</code> never touches GeoDataFrame. But both live in the same salabim environment, share the same clock, and are registered the same way.</p> <hr/> <h2 id="a-simple-example-first-conways-game-of-life">A Simple Example First: Conway’s Game of Life</h2> <p>Before getting into the coastal models, it’s worth showing what this symmetry looks like for a simple cellular automaton.</p> <p>Conway’s Game of Life has three rules: a live cell with 2 or 3 live neighbors survives; a dead cell with exactly 3 live neighbors becomes alive; all others die or stay dead.</p> <p>Here it is in the vector version:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">GameOfLife</span><span class="p">(</span><span class="n">CellularAutomaton</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">rule</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">idx</span><span class="p">):</span>
        <span class="n">state</span>     <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">gdf</span><span class="p">.</span><span class="n">loc</span><span class="p">[</span><span class="n">idx</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">state_attr</span><span class="p">]</span>
        <span class="n">neighbors</span> <span class="o">=</span> <span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">neighbor_values</span><span class="p">(</span><span class="n">idx</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">state_attr</span><span class="p">)</span> <span class="o">==</span> <span class="mi">1</span><span class="p">).</span><span class="nf">sum</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">state</span> <span class="o">==</span> <span class="mi">1</span><span class="p">:</span>
            <span class="k">return</span> <span class="mi">1</span> <span class="k">if</span> <span class="n">neighbors</span> <span class="ow">in</span> <span class="p">(</span><span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">)</span> <span class="k">else</span> <span class="mi">0</span>
        <span class="k">return</span> <span class="mi">1</span> <span class="k">if</span> <span class="n">neighbors</span> <span class="o">==</span> <span class="mi">3</span> <span class="k">else</span> <span class="mi">0</span>
</code></pre></div></div> <p>And in the raster version:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">GameOfLife</span><span class="p">(</span><span class="n">RasterCellularAutomaton</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">rule</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">arrays</span><span class="p">):</span>
        <span class="n">state</span>     <span class="o">=</span> <span class="n">arrays</span><span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">state_attr</span><span class="p">]</span>
        <span class="n">neighbors</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">backend</span><span class="p">.</span><span class="nf">focal_sum_mask</span><span class="p">(</span><span class="n">state</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span>
        <span class="n">survive</span>   <span class="o">=</span> <span class="p">(</span><span class="n">state</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span> <span class="o">&amp;</span> <span class="n">np</span><span class="p">.</span><span class="nf">isin</span><span class="p">(</span><span class="n">neighbors</span><span class="p">,</span> <span class="p">[</span><span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">])</span>
        <span class="n">born</span>      <span class="o">=</span> <span class="p">(</span><span class="n">state</span> <span class="o">==</span> <span class="mi">0</span><span class="p">)</span> <span class="o">&amp;</span> <span class="p">(</span><span class="n">neighbors</span> <span class="o">==</span> <span class="mi">3</span><span class="p">)</span>
        <span class="k">return</span> <span class="p">{</span><span class="n">self</span><span class="p">.</span><span class="n">state_attr</span><span class="p">:</span> <span class="n">np</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">survive</span> <span class="o">|</span> <span class="n">born</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="mi">0</span><span class="p">).</span><span class="nf">astype</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="n">int8</span><span class="p">)}</span>
</code></pre></div></div> <p>The rules are identical. The only thing that changes is the contract of <code class="language-plaintext highlighter-rouge">rule()</code>: the vector version receives a cell index and returns a single value; the raster version receives a snapshot of all arrays and returns a dict of updated arrays.</p> <p>For a 20×20 grid this difference barely matters. Both versions run in milliseconds. But the pattern is the same one that scales to a million cells when the domain demands it.</p> <h3 id="benchmark--game-of-life-vector-vs-raster">Benchmark — Game of Life: Vector vs Raster</h3> <p><strong>Platform:</strong> Python 3.12 · NumPy · salabim · GeoPandas<br/> <strong>Steps:</strong> 10 per grid · Vector skipped above 10,000 cells</p> <table> <thead> <tr> <th style="text-align: right">Grid</th> <th style="text-align: right">Cells</th> <th style="text-align: right">Raster (ms/step)</th> <th style="text-align: right">Vector (ms/step)</th> <th style="text-align: right">Speedup</th> <th style="text-align: center">Identical</th> </tr> </thead> <tbody> <tr> <td style="text-align: right">10×10</td> <td style="text-align: right">100</td> <td style="text-align: right">0.15</td> <td style="text-align: right">30.11</td> <td style="text-align: right">206×</td> <td style="text-align: center">✅</td> </tr> <tr> <td style="text-align: right">50×50</td> <td style="text-align: right">2,500</td> <td style="text-align: right">0.20</td> <td style="text-align: right">647.22</td> <td style="text-align: right">3,164×</td> <td style="text-align: center">✅</td> </tr> <tr> <td style="text-align: right">100×100</td> <td style="text-align: right">10,000</td> <td style="text-align: right">0.60</td> <td style="text-align: right">2,715.16</td> <td style="text-align: right">4,491×</td> <td style="text-align: center">✅</td> </tr> <tr> <td style="text-align: right">200×200</td> <td style="text-align: right">40,000</td> <td style="text-align: right">1.11</td> <td style="text-align: right">—</td> <td style="text-align: right">—</td> <td style="text-align: center">—</td> </tr> <tr> <td style="text-align: right">500×500</td> <td style="text-align: right">250,000</td> <td style="text-align: right">15.36</td> <td style="text-align: right">—</td> <td style="text-align: right">—</td> <td style="text-align: center">—</td> </tr> <tr> <td style="text-align: right">1,000×1,000</td> <td style="text-align: right">1,000,000</td> <td style="text-align: right">25.85</td> <td style="text-align: right">—</td> <td style="text-align: right">—</td> <td style="text-align: center">—</td> </tr> </tbody> </table> <h4 id="key-takeaways">Key takeaways</h4> <ul> <li>At <strong>100×100</strong> (10,000 cells) the raster substrate is <strong>4,491× faster</strong> than the vector substrate.</li> <li>The raster substrate scales to <strong>1,000,000 cells in ~26 ms/step</strong> — well within the budget for the BR-MANGUE model (94,704 cells, 88 steps).</li> <li>Results were validated cell-by-cell: both substrates produce <strong>identical output</strong> for all comparable grid sizes.</li> </ul> <hr/> <h2 id="the-vector-side-legibility">The Vector Side: legibility</h2> <p>The vector backend uses GeoDataFrame + libpysal. Each cell is a polygon row. Neighborhoods are computed once via Queen or Rook weights and cached. The transition rule is a Python function called once per cell per step.</p> <p>Here is the flood model’s execute loop in the vector version:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">execute</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
    <span class="n">nivel_mar</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">env</span><span class="p">.</span><span class="nf">now</span><span class="p">()</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">taxa_elevacao</span>

    <span class="n">uso_past</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">gdf</span><span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">attr_uso</span><span class="p">].</span><span class="nf">copy</span><span class="p">()</span>
    <span class="n">alt_past</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">gdf</span><span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">attr_alt</span><span class="p">].</span><span class="nf">copy</span><span class="p">()</span>

    <span class="n">fontes</span> <span class="o">=</span> <span class="nf">set</span><span class="p">(</span>
        <span class="n">uso_past</span><span class="p">.</span><span class="n">index</span><span class="p">[</span><span class="n">uso_past</span><span class="p">.</span><span class="nf">isin</span><span class="p">(</span><span class="n">USOS_INUNDADOS</span><span class="p">)</span> <span class="o">&amp;</span> <span class="p">(</span><span class="n">alt_past</span> <span class="o">&gt;=</span> <span class="mi">0</span><span class="p">)]</span>
    <span class="p">)</span>

    <span class="c1"># altimetry diffusion from flooded sources
</span>    <span class="n">alt_nova</span> <span class="o">=</span> <span class="n">alt_past</span><span class="p">.</span><span class="nf">copy</span><span class="p">()</span>
    <span class="k">for</span> <span class="n">idx</span> <span class="ow">in</span> <span class="n">fontes</span><span class="p">:</span>
        <span class="n">alt_atual</span> <span class="o">=</span> <span class="n">alt_past</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span>
        <span class="n">vizinhos</span>  <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">neighs_id</span><span class="p">(</span><span class="n">idx</span><span class="p">)</span>
        <span class="n">viz_baixos</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">+</span> <span class="nf">sum</span><span class="p">(</span><span class="mi">1</span> <span class="k">for</span> <span class="n">n</span> <span class="ow">in</span> <span class="n">vizinhos</span> <span class="k">if</span> <span class="n">alt_past</span><span class="p">[</span><span class="n">n</span><span class="p">]</span> <span class="o">&lt;=</span> <span class="n">alt_atual</span><span class="p">)</span>
        <span class="n">fluxo</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">taxa_elevacao</span> <span class="o">/</span> <span class="n">viz_baixos</span>
        <span class="n">alt_nova</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">+=</span> <span class="n">fluxo</span>
        <span class="k">for</span> <span class="n">n</span> <span class="ow">in</span> <span class="n">vizinhos</span><span class="p">:</span>
            <span class="k">if</span> <span class="n">alt_past</span><span class="p">[</span><span class="n">n</span><span class="p">]</span> <span class="o">&lt;=</span> <span class="n">alt_atual</span><span class="p">:</span>
                <span class="n">alt_nova</span><span class="p">[</span><span class="n">n</span><span class="p">]</span> <span class="o">+=</span> <span class="n">fluxo</span>

    <span class="n">self</span><span class="p">.</span><span class="n">gdf</span><span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">attr_alt</span><span class="p">]</span> <span class="o">=</span> <span class="n">alt_nova</span>

    <span class="c1"># absolute flood threshold
</span>    <span class="n">uso_novo</span> <span class="o">=</span> <span class="n">uso_past</span><span class="p">.</span><span class="nf">copy</span><span class="p">()</span>
    <span class="k">for</span> <span class="n">idx</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">gdf</span><span class="p">.</span><span class="n">index</span><span class="p">:</span>
        <span class="n">uso_atual</span> <span class="o">=</span> <span class="n">uso_past</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span>
        <span class="k">if</span> <span class="n">uso_atual</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">REGRAS_INUNDACAO</span><span class="p">:</span>
            <span class="k">continue</span>
        <span class="k">if</span> <span class="n">alt_past</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">&gt;</span> <span class="n">nivel_mar</span><span class="p">:</span>
            <span class="k">continue</span>
        <span class="k">if</span> <span class="nf">any</span><span class="p">(</span><span class="n">n</span> <span class="ow">in</span> <span class="n">fontes</span> <span class="k">for</span> <span class="n">n</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="nf">neighs_id</span><span class="p">(</span><span class="n">idx</span><span class="p">)):</span>
            <span class="n">uso_novo</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">REGRAS_INUNDACAO</span><span class="p">[</span><span class="n">uso_atual</span><span class="p">]</span>

    <span class="n">self</span><span class="p">.</span><span class="n">gdf</span><span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">attr_uso</span><span class="p">]</span> <span class="o">=</span> <span class="n">uso_novo</span>
</code></pre></div></div> <p>This is readable. You can trace the logic. You can see exactly where <code class="language-plaintext highlighter-rouge">alt_past</code> is used instead of the updated <code class="language-plaintext highlighter-rouge">alt_nova</code> — which is the critical <code class="language-plaintext highlighter-rouge">.past</code> semantics from TerraME. A student can read this alongside the original Lua code and verify they match.</p> <p>The cost: at 94,704 cells, each step takes around 2 minutes. Fine for a 10×10 test grid. Unusable at real scale.</p> <h3 id="the-raster-side-performance">The Raster Side: performance</h3> <p>The raster backend replaces the cell loop with NumPy array operations. The same flood logic:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">execute</span><span class="p">(</span><span class="n">self</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="bp">None</span><span class="p">:</span>
    <span class="n">nivel_mar</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">env</span><span class="p">.</span><span class="nf">now</span><span class="p">()</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">taxa_elevacao</span>

    <span class="n">uso_past</span>  <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">backend</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">uso</span><span class="sh">"</span><span class="p">).</span><span class="nf">copy</span><span class="p">()</span>
    <span class="n">alt_past</span>  <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">backend</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">alt</span><span class="sh">"</span><span class="p">).</span><span class="nf">copy</span><span class="p">()</span>

    <span class="n">eh_inundado</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">isin</span><span class="p">(</span><span class="n">uso_past</span><span class="p">,</span> <span class="n">USOS_INUNDADOS</span><span class="p">)</span> <span class="o">&amp;</span> <span class="p">(</span><span class="n">alt_past</span> <span class="o">&gt;=</span> <span class="mi">0</span><span class="p">)</span>

    <span class="c1"># altimetry diffusion
</span>    <span class="n">alt_nova</span> <span class="o">=</span> <span class="n">alt_past</span><span class="p">.</span><span class="nf">copy</span><span class="p">()</span>
    <span class="k">for</span> <span class="n">dr</span><span class="p">,</span> <span class="n">dc</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">dirs</span><span class="p">:</span>
        <span class="n">viz_alt</span>   <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">shift</span><span class="p">(</span><span class="n">alt_past</span><span class="p">,</span> <span class="n">dr</span><span class="p">,</span> <span class="n">dc</span><span class="p">)</span>
        <span class="n">viz_baixo</span> <span class="o">=</span> <span class="p">(</span><span class="n">viz_alt</span> <span class="o">&lt;=</span> <span class="n">alt_past</span><span class="p">)</span> <span class="o">&amp;</span> <span class="n">eh_inundado</span>
        <span class="n">contagem</span>  <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">backend</span><span class="p">.</span><span class="nf">focal_sum_mask</span><span class="p">(</span><span class="n">viz_baixo</span><span class="p">)</span> <span class="o">+</span> <span class="mi">1</span>
        <span class="n">fluxo</span>     <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">eh_inundado</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">taxa_elevacao</span> <span class="o">/</span> <span class="n">contagem</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
        <span class="n">alt_nova</span> <span class="o">+=</span> <span class="n">fluxo</span>

    <span class="n">self</span><span class="p">.</span><span class="n">backend</span><span class="p">.</span><span class="n">arrays</span><span class="p">[</span><span class="sh">"</span><span class="s">alt</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">alt_nova</span>

    <span class="c1"># absolute flood threshold
</span>    <span class="n">tem_fonte</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">backend</span><span class="p">.</span><span class="nf">neighbor_contact</span><span class="p">(</span><span class="n">eh_inundado</span><span class="p">)</span>
    <span class="n">cond</span> <span class="o">=</span> <span class="p">(</span>
        <span class="n">np</span><span class="p">.</span><span class="nf">isin</span><span class="p">(</span><span class="n">uso_past</span><span class="p">,</span> <span class="nf">list</span><span class="p">(</span><span class="n">REGRAS_INUNDACAO</span><span class="p">.</span><span class="nf">keys</span><span class="p">()))</span>
        <span class="o">&amp;</span> <span class="p">(</span><span class="n">alt_past</span> <span class="o">&lt;=</span> <span class="n">nivel_mar</span><span class="p">)</span>
        <span class="o">&amp;</span> <span class="n">tem_fonte</span>
    <span class="p">)</span>
    <span class="n">uso_novo</span> <span class="o">=</span> <span class="n">uso_past</span><span class="p">.</span><span class="nf">copy</span><span class="p">()</span>
    <span class="k">for</span> <span class="n">uso_orig</span><span class="p">,</span> <span class="n">uso_dest</span> <span class="ow">in</span> <span class="n">REGRAS_INUNDACAO</span><span class="p">.</span><span class="nf">items</span><span class="p">():</span>
        <span class="n">uso_novo</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">cond</span> <span class="o">&amp;</span> <span class="p">(</span><span class="n">uso_past</span> <span class="o">==</span> <span class="n">uso_orig</span><span class="p">),</span> <span class="n">uso_dest</span><span class="p">,</span> <span class="n">uso_novo</span><span class="p">)</span>

    <span class="n">self</span><span class="p">.</span><span class="n">backend</span><span class="p">.</span><span class="n">arrays</span><span class="p">[</span><span class="sh">"</span><span class="s">uso</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">uso_novo</span>
</code></pre></div></div> <p>The structure is the same. The concepts are the same — <code class="language-plaintext highlighter-rouge">uso_past</code>, <code class="language-plaintext highlighter-rouge">alt_past</code>, the <code class="language-plaintext highlighter-rouge">.past</code> semantics, the source/neighbor pattern. But instead of iterating, we shift entire arrays and apply masks.</p> <p>At 94,704 cells: each step takes around 8ms. The same logic, roughly 15,000× faster.</p> <hr/> <h2 id="the-mangrove-model-three-processes-two-substrates">The Mangrove Model: Three Processes, Two Substrates</h2> <p>The mangrove model has three sub-processes per step: soil migration, use migration, and optional sediment accretion. The use migration step has a subtle but critical dependency: it reads from <code class="language-plaintext highlighter-rouge">solo_past</code> — the soil state <em>before</em> this step — not <code class="language-plaintext highlighter-rouge">solo_novo</code>, the soil state after migration. This is the TerraME <code class="language-plaintext highlighter-rouge">.past</code> semantics.</p> <p>In the vector version:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">solo_past</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">gdf</span><span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">attr_solo</span><span class="p">].</span><span class="nf">copy</span><span class="p">()</span>
<span class="c1"># ... compute solo_novo ...
</span>
<span class="c1"># use migration reads solo_past, not solo_novo
</span><span class="k">for</span> <span class="n">idx</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">gdf</span><span class="p">.</span><span class="n">index</span><span class="p">:</span>
    <span class="k">if</span> <span class="n">solo_past</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">SOLOS_MANGUE</span><span class="p">:</span>   <span class="c1"># ← solo_past
</span>        <span class="k">continue</span>
    <span class="bp">...</span>
    <span class="n">uso_novo</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">MANGUE_MIGRADO</span>
</code></pre></div></div> <p>In the raster version:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">solo_past</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">backend</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="sh">"</span><span class="s">solo</span><span class="sh">"</span><span class="p">).</span><span class="nf">copy</span><span class="p">()</span>   <span class="c1"># snapshot before step
# ... build solo_novo ...
</span>
<span class="c1"># same rule, same correctness requirement
</span><span class="n">cond</span> <span class="o">=</span> <span class="p">(</span>
    <span class="n">fonte_viz</span>
    <span class="o">&amp;</span> <span class="n">np</span><span class="p">.</span><span class="nf">isin</span><span class="p">(</span><span class="n">uso_past</span><span class="p">,</span>  <span class="n">self</span><span class="p">.</span><span class="n">USOS_ALVO</span><span class="p">)</span>
    <span class="o">&amp;</span> <span class="n">np</span><span class="p">.</span><span class="nf">isin</span><span class="p">(</span><span class="n">solo_past</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">SOLOS_MANGUE</span><span class="p">)</span>   <span class="c1"># ← solo_past
</span>    <span class="o">&amp;</span> <span class="p">(</span><span class="n">alt_past</span> <span class="o">&lt;=</span> <span class="n">zi</span><span class="p">)</span>
<span class="p">)</span>
</code></pre></div></div> <p>The same rule, the same correctness requirement, expressed in the natural idiom of each substrate. Having both versions forced us to make this dependency explicit — it was easy to miss in the Lua original.</p> <hr/> <h2 id="what-symmetry-looks-like-in-practice">What Symmetry Looks Like in Practice</h2> <p>Here is the full simulation setup in each mode:</p> <p><strong>Raster:</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">backend</span><span class="p">,</span> <span class="n">meta</span> <span class="o">=</span> <span class="nf">load_geotiff</span><span class="p">(</span><span class="sh">"</span><span class="s">flood_p000_0.000m.tif</span><span class="sh">"</span><span class="p">,</span> <span class="n">BAND_SPEC</span><span class="p">)</span>
<span class="n">env</span> <span class="o">=</span> <span class="nc">Environment</span><span class="p">(</span><span class="n">start_time</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">end_time</span><span class="o">=</span><span class="mi">88</span><span class="p">)</span>

<span class="nc">FloodRasterModel</span><span class="p">(</span><span class="n">backend</span><span class="o">=</span><span class="n">backend</span><span class="p">,</span> <span class="n">taxa_elevacao</span><span class="o">=</span><span class="mf">0.011</span><span class="p">)</span>
<span class="nc">MangroveModel</span><span class="p">(</span><span class="n">backend</span><span class="o">=</span><span class="n">backend</span><span class="p">)</span>
<span class="nc">RasterMap</span><span class="p">(</span><span class="n">backend</span><span class="o">=</span><span class="n">backend</span><span class="p">,</span> <span class="n">band</span><span class="o">=</span><span class="sh">"</span><span class="s">uso</span><span class="sh">"</span><span class="p">,</span> <span class="n">color_map</span><span class="o">=</span><span class="n">USO_COLORS</span><span class="p">,</span> <span class="n">labels</span><span class="o">=</span><span class="n">USO_LABELS</span><span class="p">)</span>

<span class="n">env</span><span class="p">.</span><span class="nf">run</span><span class="p">()</span>
</code></pre></div></div> <p><strong>Vector:</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">gdf</span> <span class="o">=</span> <span class="n">gpd</span><span class="p">.</span><span class="nf">read_file</span><span class="p">(</span><span class="sh">"</span><span class="s">flood_model.shp</span><span class="sh">"</span><span class="p">)</span>
<span class="n">env</span> <span class="o">=</span> <span class="nc">Environment</span><span class="p">(</span><span class="n">start_time</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">end_time</span><span class="o">=</span><span class="mi">88</span><span class="p">)</span>

<span class="nc">FloodVectorModel</span><span class="p">(</span><span class="n">gdf</span><span class="o">=</span><span class="n">gdf</span><span class="p">,</span> <span class="n">taxa_elevacao</span><span class="o">=</span><span class="mf">0.011</span><span class="p">)</span>
<span class="nc">MangroveModel</span><span class="p">(</span><span class="n">gdf</span><span class="o">=</span><span class="n">gdf</span><span class="p">)</span>
<span class="nc">Map</span><span class="p">(</span><span class="n">gdf</span><span class="o">=</span><span class="n">gdf</span><span class="p">,</span> <span class="n">plot_params</span><span class="o">=</span><span class="p">{</span><span class="sh">"</span><span class="s">column</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">uso</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">cmap</span><span class="sh">"</span><span class="p">:</span> <span class="n">USO_CMAP</span><span class="p">})</span>

<span class="n">env</span><span class="p">.</span><span class="nf">run</span><span class="p">()</span>
</code></pre></div></div> <p>The pattern is identical: create environment, register models, register visualization, run. The only difference is what you pass in — <code class="language-plaintext highlighter-rouge">backend</code> or <code class="language-plaintext highlighter-rouge">gdf</code> — and which classes you instantiate. Salabim handles the rest.</p> <p>This symmetry is not cosmetic. It means that if a student learns to write a vector model, they already understand 80% of how to write the raster version. The concepts transfer: snapshot semantics, source/neighbor patterns, separate models composing in the same environment.</p> <hr/> <h2 id="what-this-enables">What This Enables</h2> <p>Having both substrates now opens things we couldn’t do before:</p> <p><strong>Validation.</strong> We can run both versions on the same 60×60 synthetic grid and compare cell by cell. If they match, we trust the raster version. If they don’t, the vector version is easier to debug because you can inspect individual cells and their neighbors directly.</p> <p><strong>Teaching.</strong> Students can start with the vector version — it reads like pseudocode. When performance becomes a constraint, they migrate to the raster version. The class hierarchy guides the migration: change the parent class, change <code class="language-plaintext highlighter-rouge">rule(idx)</code> to <code class="language-plaintext highlighter-rouge">rule(arrays)</code>, done.</p> <p><strong>Scale.</strong> The raster version runs 1000×1000 grids interactively. We tested it. The vector version would take hours on the same grid. Having both isn’t redundant — they serve different phases of model development.</p> <hr/> <h2 id="the-python-ecosystem-is-the-right-bet">The Python Ecosystem Is the Right Bet</h2> <p>One thing I keep noticing as this framework grows: we don’t need to invent much. NumPy gives us the array operations. libpysal gives us the spatial weights. geopandas gives us the geometry. rasterio gives us the GeoTIFF I/O. salabim gives us the simulation clock and component lifecycle. matplotlib gives us the visualization.</p> <p>Our contribution is the glue and the abstraction layer. We’re not competing with any of these libraries — we’re making them easier to use together for spatial simulation.</p> <p>That feels like the right approach. Not building everything from scratch, but making the existing ecosystem accessible to researchers who want to focus on the model, not the infrastructure.</p> <hr/> <h2 id="whats-next">What’s Next</h2> <p>The framework is published as <code class="language-plaintext highlighter-rouge">dissmodel 0.2.0</code> on PyPI. The coastal dynamics models are in a separate repository at <a href="https://github.com/lambdageo/coastal-dynamics">github.com/LambdaGeo/coastal-dynamics</a>, demonstrating how a domain project builds on top of the framework.</p> <p>Still on the roadmap:</p> <ul> <li>A benchmark notebook comparing raster vs vector across grid sizes</li> <li>Validation of <code class="language-plaintext highlighter-rouge">MangroveVectorModel</code> against the raster version on identical initial conditions</li> <li>Reorganizing <code class="language-plaintext highlighter-rouge">dissmodel.geo</code> into clean <code class="language-plaintext highlighter-rouge">vector/</code> and <code class="language-plaintext highlighter-rouge">raster/</code> subpackages</li> <li>More test coverage, particularly integration tests validating substrate equivalence</li> </ul> <p>I’m genuinely happy with where this is going. Not because it’s finished — it isn’t — but because the architecture is holding up under real use. That’s usually the first sign that you made a reasonable decision.</p> <hr/> <p><em>Sergio Souza Costa is an Associate Professor of Computer Engineering at the Federal University of Maranhão (UFMA) and lead researcher of the <a href="https://lambdageo.github.io">LambdaGEO</a> group.</em></p>]]></content><author><name>Sergio Souza Costa</name></author><category term="news"/><category term="dissmodel"/><category term="geospatial"/><category term="numpy"/><category term="geopandas"/><category term="salabim"/><category term="mangrove"/><category term="coastal-dynamics"/><summary type="html"><![CDATA[I want to share something that has been quietly coming together over the past few weeks in our research group. It’s not a breakthrough, nothing dramatic — just one of those moments where you look at the code you’ve been building and think: yes, this is working the way I hoped it would.]]></summary></entry><entry><title type="html">Revitalizing LambdaGEO: From Student Projects to Production-Ready Software</title><link href="https://lambdageo.github.io//blog/2026/launching-lambdageo-site/" rel="alternate" type="text/html" title="Revitalizing LambdaGEO: From Student Projects to Production-Ready Software"/><published>2026-03-05T10:00:00+00:00</published><updated>2026-03-05T10:00:00+00:00</updated><id>https://lambdageo.github.io//blog/2026/launching-lambdageo-site</id><content type="html" xml:base="https://lambdageo.github.io//blog/2026/launching-lambdageo-site/"><![CDATA[<p>I am thrilled to announce the launch of the new official page for our research group: <a href="https://lambdageo.github.io/">lambdageo.github.io</a>.</p> <p>More than just a website, this launch represents a major milestone in our group’s workflow. For years, the <strong>LambdaGEO</strong> group (UFMA) has fostered high-potential projects developed by brilliant undergraduate students. However, we often faced a common academic challenge: once students graduate, great projects can lose momentum, lacking that final “polish” required for public distribution.</p> <h3 id="the-last-mile-challenge">The “Last Mile” Challenge</h3> <p>Many of our tools, like <strong>rdfmapper</strong> and <strong>QGISSPARQL</strong>, were functionally complete but needed a final push to reach the broader community. They required better documentation, standardized packaging, and official distribution channels.</p> <p>In the last two weeks, I decided to tackle this “last mile.” Using AI as a collaborative partner, I was able to streamline the organization and deployment process significantly. The AI didn’t just help with code; it assisted in:</p> <ul> <li><strong>Structuring Documentation:</strong> Mapping ideas from Portuguese into professional, natural-sounding English.</li> <li><strong>Packaging:</strong> Moving projects from local scripts to <strong>PyPI</strong> and the official <strong>QGIS Plugin Repository</strong>.</li> <li><strong>Academic Submission:</strong> Preparing manuscripts for the <strong>Journal of Open Source Software (JOSS)</strong>.</li> </ul> <h3 id="results">Results</h3> <p>In record time, we have successfully:</p> <ol> <li>Published <strong>rdfmapper-py</strong> on PyPI.</li> <li>Deployed <strong>Triple2Layer</strong> to the QGIS community.</li> <li>Submitted our findings for peer review at <strong>JOSS</strong>.</li> </ol> <p>This experience showed me that with the right tools, we can bridge the gap between “student research” and “production-ready open source software.” I invite you to explore our projects and stay tuned for more updates—including my own journey learning English along the way!</p> <hr/> <p><em>Stay curious, stay functional.</em></p>]]></content><author><name></name></author><category term="news"/><category term="open-source"/><category term="research-management"/><category term="AI"/><summary type="html"><![CDATA[How AI assisted in finishing, publishing, and documenting years of research group projects in record time.]]></summary></entry><entry><title type="html">Shipping a Python Library to PyPI: The Complete Checklist</title><link href="https://lambdageo.github.io//blog/2026/publishing-research-python-library/" rel="alternate" type="text/html" title="Shipping a Python Library to PyPI: The Complete Checklist"/><published>2026-03-01T03:00:00+00:00</published><updated>2026-03-01T03:00:00+00:00</updated><id>https://lambdageo.github.io//blog/2026/publishing-research-python-library</id><content type="html" xml:base="https://lambdageo.github.io//blog/2026/publishing-research-python-library/"><![CDATA[<p>A checklist-driven walkthrough for shipping a Python library — from cleaning up research code to automating releases on PyPI. Covers the decisions that actually matter and the gotchas that cost hours.</p> <hr/> <h2 id="1-code-quality">1. Code Quality</h2> <h3 id="type-annotations">Type Annotations</h3> <p>Annotate all public functions, methods, and class attributes. Add <code class="language-plaintext highlighter-rouge">from __future__ import annotations</code> at the top of every file for forward references.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Before
</span><span class="k">def</span> <span class="nf">process</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">idx</span><span class="p">):</span>
    <span class="bp">...</span>

<span class="c1"># After
</span><span class="kn">from</span> <span class="n">__future__</span> <span class="kn">import</span> <span class="n">annotations</span>
<span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">Any</span>

<span class="k">def</span> <span class="nf">process</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">idx</span><span class="p">:</span> <span class="n">Any</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">int</span><span class="p">:</span>
    <span class="bp">...</span>
</code></pre></div></div> <p>Run <code class="language-plaintext highlighter-rouge">mypy</code> in CI — catching type errors before users do is the cheapest bug fix:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mypy your_package <span class="nt">--ignore-missing-imports</span>
</code></pre></div></div> <h3 id="docstrings-numpy-style">Docstrings: NumPy style</h3> <p>NumPy style is the standard in scientific Python and is natively parsed by <code class="language-plaintext highlighter-rouge">mkdocstrings</code>. Write the docstring once, get API docs for free.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">compute</span><span class="p">(</span><span class="n">data</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">float</span><span class="p">],</span> <span class="n">scale</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">1.0</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">float</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">
    Compute a scaled result from input data.

    Parameters
    ----------
    data : list of float
        Input values to process.
    scale : float, optional
        Scaling factor, by default 1.0.

    Returns
    -------
    float
        Scaled result.

    Examples
    --------
</span><span class="gp">    &gt;&gt;&gt;</span> <span class="nf">compute</span><span class="p">([</span><span class="mf">1.0</span><span class="p">,</span> <span class="mf">2.0</span><span class="p">,</span> <span class="mf">3.0</span><span class="p">],</span> <span class="n">scale</span><span class="o">=</span><span class="mf">2.0</span><span class="p">)</span>
    <span class="mf">12.0</span>
    <span class="sh">"""</span>
</code></pre></div></div> <blockquote> <p>If your class uses <code class="language-plaintext highlighter-rouge">setup()</code> instead of <code class="language-plaintext highlighter-rouge">__init__()</code> for parameters, keep the <code class="language-plaintext highlighter-rouge">Parameters</code> section only in <code class="language-plaintext highlighter-rouge">setup()</code>. <code class="language-plaintext highlighter-rouge">mkdocstrings</code> will emit warnings if it can’t match parameters to the actual signature.</p> </blockquote> <h3 id="enums-over-integer-constants">Enums over Integer Constants</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Before — conflicts with tools that introspect __annotations__
</span><span class="k">class</span> <span class="nc">MyModel</span><span class="p">:</span>
    <span class="n">INACTIVE</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">ACTIVE</span> <span class="o">=</span> <span class="mi">1</span>

<span class="c1"># After
</span><span class="kn">from</span> <span class="n">enum</span> <span class="kn">import</span> <span class="n">IntEnum</span>

<span class="k">class</span> <span class="nc">MyState</span><span class="p">(</span><span class="n">IntEnum</span><span class="p">):</span>
    <span class="n">INACTIVE</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">ACTIVE</span> <span class="o">=</span> <span class="mi">1</span>
</code></pre></div></div> <h3 id="naming-conventions">Naming Conventions</h3> <p>All identifiers, docstrings, and comments in <strong>English</strong>. All names in <strong>snake_case</strong>. Non-negotiable for JOSS submission and PyPI discoverability.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Before
</span><span class="k">def</span> <span class="nf">setup</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">growthRate</span><span class="o">=</span><span class="mf">0.08</span><span class="p">,</span> <span class="n">maxPopulation</span><span class="o">=</span><span class="mi">1000</span><span class="p">):</span>

<span class="c1"># After
</span><span class="k">def</span> <span class="nf">setup</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">growth_rate</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.08</span><span class="p">,</span> <span class="n">max_population</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">1000</span><span class="p">):</span>
</code></pre></div></div> <h3 id="consistent-lifecycle-pattern">Consistent Lifecycle Pattern</h3> <p>If your library exposes a model or component API, define a fixed lifecycle and document it in every example. It is the #1 source of user errors.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>setup()       → configure parameters
initialize()  → set initial state
execute()     → run one step
</code></pre></div></div> <h3 id="performance-in-hot-loops">Performance in Hot Loops</h3> <p>Prefer NumPy array ops over per-element Pandas access inside simulation loops:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Slow — DataFrame overhead on every call
</span><span class="n">result</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">get_neighbors</span><span class="p">(</span><span class="n">idx</span><span class="p">).</span><span class="nf">fillna</span><span class="p">(</span><span class="mi">0</span><span class="p">).</span><span class="nf">sum</span><span class="p">()</span>

<span class="c1"># Fast — raw NumPy
</span><span class="n">result</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">neighbor_values</span><span class="p">(</span><span class="n">idx</span><span class="p">).</span><span class="nf">sum</span><span class="p">()</span>
</code></pre></div></div> <p>Document the tradeoff in the slower method’s docstring so users know when to use each.</p> <hr/> <h2 id="2-bug-prevention">2. Bug Prevention</h2> <h3 id="defensive-guards-for-edge-cases">Defensive Guards for Edge Cases</h3> <p>Bugs that surface only on non-square inputs or boundary conditions are the hardest to reproduce. Guard early:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">key</span> <span class="ow">in</span> <span class="n">collection</span><span class="p">:</span>
    <span class="n">collection</span><span class="p">[</span><span class="n">key</span><span class="p">]</span> <span class="o">=</span> <span class="n">value</span>
</code></pre></div></div> <p>Ensure consistent conventions — IDs, coordinate systems, key formats — across all modules. Mixed conventions only break on edge cases.</p> <h3 id="headless-compatibility">Headless Compatibility</h3> <p>Any library with a GUI backend will crash in CI, Docker, or headless servers unless you disable it by default:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="o">*</span><span class="n">args</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">):</span>
    <span class="n">kwargs</span><span class="p">.</span><span class="nf">setdefault</span><span class="p">(</span><span class="sh">"</span><span class="s">gui</span><span class="sh">"</span><span class="p">,</span> <span class="bp">False</span><span class="p">)</span>
    <span class="n">kwargs</span><span class="p">.</span><span class="nf">setdefault</span><span class="p">(</span><span class="sh">"</span><span class="s">animation</span><span class="sh">"</span><span class="p">,</span> <span class="bp">False</span><span class="p">)</span>
    <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">(</span><span class="o">*</span><span class="n">args</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">)</span>
</code></pre></div></div> <p>Users who want the GUI opt in explicitly. CI works out of the box.</p> <hr/> <h2 id="3-testing">3. Testing</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>tests/
    test_core.py          — expected behavior
    test_edge_cases.py    — non-square inputs, boundary conditions, silent failures
</code></pre></div></div> <p>Prioritize tests for things that fail silently — wrong output, not exceptions:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">test_nonsquare_input</span><span class="p">():</span>
    <span class="n">result</span> <span class="o">=</span> <span class="nf">process</span><span class="p">(</span><span class="n">rows</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span> <span class="n">cols</span><span class="o">=</span><span class="mi">5</span><span class="p">)</span>
    <span class="k">assert</span> <span class="n">result</span><span class="p">.</span><span class="n">shape</span> <span class="o">==</span> <span class="p">(</span><span class="mi">3</span><span class="p">,</span> <span class="mi">5</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">test_out_of_bounds_ignored</span><span class="p">():</span>
    <span class="sh">"""</span><span class="s">Should not raise KeyError or IndexError.</span><span class="sh">"""</span>
    <span class="nf">apply_pattern</span><span class="p">(</span><span class="n">data</span><span class="p">,</span> <span class="n">pattern</span><span class="o">=</span><span class="n">large_pattern</span><span class="p">)</span>
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pytest tests/
mypy your_package
</code></pre></div></div> <hr/> <h2 id="4-packaging">4. Packaging</h2> <h3 id="pyprojecttoml-is-the-single-source-of-truth"><code class="language-plaintext highlighter-rouge">pyproject.toml</code> is the single source of truth</h3> <p>If you have a <code class="language-plaintext highlighter-rouge">setup.py</code>, reduce it to a stub:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># setup.py — stub only
</span><span class="kn">from</span> <span class="n">setuptools</span> <span class="kn">import</span> <span class="n">setup</span>
<span class="nf">setup</span><span class="p">()</span>
</code></pre></div></div> <h3 id="minimal-pyprojecttoml">Minimal <code class="language-plaintext highlighter-rouge">pyproject.toml</code></h3> <div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">[</span><span class="n">build-system</span><span class="k">]</span>
<span class="n">requires</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="s">"setuptools&gt;=61.0"</span><span class="p">,</span> <span class="s">"wheel"</span><span class="p">]</span>
<span class="n">build-backend</span> <span class="o">=</span><span class="w"> </span><span class="s">"setuptools.build_meta"</span>

<span class="k">[</span><span class="n">project</span><span class="k">]</span>
<span class="n">name</span> <span class="o">=</span><span class="w"> </span><span class="s">"your-package"</span>
<span class="n">version</span> <span class="o">=</span><span class="w"> </span><span class="s">"0.1.0"</span>
<span class="n">description</span> <span class="o">=</span><span class="w"> </span><span class="s">"One-liner describing your library."</span>
<span class="n">readme</span> <span class="o">=</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="n">file</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"README.md"</span><span class="p">,</span><span class="w"> </span><span class="n">content-type</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"text/markdown"</span><span class="w"> </span><span class="p">}</span>
<span class="n">authors</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span>
    <span class="p">{</span><span class="w"> </span><span class="n">name</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"Your Name"</span><span class="p">,</span><span class="w"> </span><span class="n">email</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"you@example.com"</span><span class="w"> </span><span class="p">},</span>
<span class="p">]</span>
<span class="n">license</span> <span class="o">=</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="n">text</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">"MIT"</span><span class="w"> </span><span class="p">}</span>
<span class="n">requires-python</span> <span class="o">=</span><span class="w"> </span><span class="s">"&gt;=3.10"</span>
<span class="n">dependencies</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span>
    <span class="s">"numpy&gt;=1.25.0"</span><span class="p">,</span>
    <span class="s">"pandas&gt;=2.0.0"</span><span class="p">,</span>
<span class="p">]</span>

<span class="k">[</span><span class="n">project</span><span class="k">.</span><span class="n">optional-dependencies</span><span class="k">]</span>
<span class="n">dev</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="s">"mypy"</span><span class="p">,</span> <span class="s">"pytest"</span><span class="p">,</span> <span class="s">"mkdocs"</span><span class="p">,</span> <span class="s">"mkdocstrings[python]"</span><span class="p">,</span> <span class="s">"mkdocs-material"</span><span class="p">]</span>

<span class="k">[</span><span class="n">project</span><span class="k">.</span><span class="n">urls</span><span class="k">]</span>
<span class="n">Homepage</span> <span class="o">=</span><span class="w"> </span><span class="s">"https://github.com/yourorg/your-package"</span>
</code></pre></div></div> <p><strong>Rules:</strong></p> <ul> <li>Libraries use <strong><code class="language-plaintext highlighter-rouge">&gt;=</code></strong> (flexible). Applications use <strong><code class="language-plaintext highlighter-rouge">==</code></strong> (pinned). Never pin in a library.</li> <li>List <strong>all direct runtime dependencies</strong> explicitly — including transitive ones that may not always be installed.</li> <li>Dev tools go in <code class="language-plaintext highlighter-rouge">[project.optional-dependencies]</code>, not in <code class="language-plaintext highlighter-rouge">dependencies</code>.</li> </ul> <h3 id="verify-in-a-clean-virtualenv">Verify in a clean virtualenv</h3> <p>Missing transitive dependencies only appear in clean environments. Always test there before publishing:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> venv /tmp/test_pkg
<span class="nb">source</span> /tmp/test_pkg/bin/activate
pip <span class="nb">install</span> <span class="nt">-e</span> <span class="nb">.</span>
python <span class="nt">-c</span> <span class="s2">"import your_package; print('OK')"</span>
deactivate
</code></pre></div></div> <h3 id="requirementstxt--dev-only"><code class="language-plaintext highlighter-rouge">requirements.txt</code> — dev only</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code># pip install -e .        → installs runtime deps from pyproject.toml
# pip install -r requirements.txt → installs dev tools

mypy
pytest
mkdocs
mkdocstrings[python]
mkdocs-material
</code></pre></div></div> <hr/> <h2 id="5-examples">5. Examples</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>examples/
    cli/           — runnable scripts
    notebooks/     — Jupyter notebooks
    data/          — sample data
</code></pre></div></div> <p>Each example must be self-contained. Any required usage order must be documented with a comment — not just assumed.</p> <hr/> <h2 id="6-documentation">6. Documentation</h2> <h3 id="setup">Setup</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>mkdocs mkdocs-material mkdocstrings[python] mkdocs-jupyter
</code></pre></div></div> <h3 id="mkdocsyml"><code class="language-plaintext highlighter-rouge">mkdocs.yml</code></h3> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">site_name</span><span class="pi">:</span> <span class="s">Your Package</span>
<span class="na">plugins</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">search</span>
  <span class="pi">-</span> <span class="na">mkdocstrings</span><span class="pi">:</span>
      <span class="na">handlers</span><span class="pi">:</span>
        <span class="na">python</span><span class="pi">:</span>
          <span class="na">options</span><span class="pi">:</span>
            <span class="na">docstring_style</span><span class="pi">:</span> <span class="s">numpy</span>
            <span class="na">show_source</span><span class="pi">:</span> <span class="kc">true</span>
  <span class="pi">-</span> <span class="na">mkdocs-jupyter</span><span class="pi">:</span>
      <span class="na">include</span><span class="pi">:</span> <span class="pi">[</span><span class="s2">"</span><span class="s">*.ipynb"</span><span class="pi">]</span>

<span class="na">theme</span><span class="pi">:</span>
  <span class="na">name</span><span class="pi">:</span> <span class="s">material</span>
  <span class="na">language</span><span class="pi">:</span> <span class="s">en</span>

<span class="na">markdown_extensions</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">pymdownx.highlight</span>
  <span class="pi">-</span> <span class="s">pymdownx.superfences</span>
  <span class="pi">-</span> <span class="na">pymdownx.arithmatex</span><span class="pi">:</span>
      <span class="na">generic</span><span class="pi">:</span> <span class="kc">true</span>

<span class="na">extra_javascript</span><span class="pi">:</span>
  <span class="pi">-</span> <span class="s">https://unpkg.com/mathjax@3/es5/tex-mml-chtml.js</span>
</code></pre></div></div> <h3 id="api-reference-pages">API Reference Pages</h3> <p>One <code class="language-plaintext highlighter-rouge">.md</code> per module — <code class="language-plaintext highlighter-rouge">mkdocstrings</code> pulls docstrings automatically:</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gh"># MyClass</span>

::: your_package.module.MyClass
</code></pre></div></div> <h3 id="deploy">Deploy</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mkdocs gh-deploy
</code></pre></div></div> <p>Docs deployment is fully independent of PyPI. Update them at any time without creating a release.</p> <hr/> <h2 id="7-cicd-with-github-actions">7. CI/CD with GitHub Actions</h2> <h3 id="ciyml--every-push-to-main"><code class="language-plaintext highlighter-rouge">ci.yml</code> — every push to main</h3> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">CI</span>
<span class="na">on</span><span class="pi">:</span>
  <span class="na">push</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>
  <span class="na">pull_request</span><span class="pi">:</span>
    <span class="na">branches</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">main</span><span class="pi">]</span>
  <span class="na">workflow_dispatch</span><span class="pi">:</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">test</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v4</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/setup-python@v5</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">python-version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">3.12"</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Install</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">pip install -e ".[dev]"</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Test</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">pytest tests/</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Type check</span>
        <span class="na">run</span><span class="pi">:</span> <span class="s">mypy your_package --ignore-missing-imports</span>
</code></pre></div></div> <h3 id="publishyml--on-github-release"><code class="language-plaintext highlighter-rouge">publish.yml</code> — on GitHub Release</h3> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">name</span><span class="pi">:</span> <span class="s">Publish to PyPI</span>
<span class="na">on</span><span class="pi">:</span>
  <span class="na">release</span><span class="pi">:</span>
    <span class="na">types</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">published</span><span class="pi">]</span>
  <span class="na">workflow_dispatch</span><span class="pi">:</span>

<span class="na">permissions</span><span class="pi">:</span>
  <span class="na">contents</span><span class="pi">:</span> <span class="s">read</span>

<span class="na">jobs</span><span class="pi">:</span>
  <span class="na">build</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/checkout@v4</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/setup-python@v5</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">python-version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">3.x"</span>
      <span class="pi">-</span> <span class="na">name</span><span class="pi">:</span> <span class="s">Build</span>
        <span class="na">run</span><span class="pi">:</span> <span class="pi">|</span>
          <span class="s">pip install build</span>
          <span class="s">python -m build</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/upload-artifact@v4</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">name</span><span class="pi">:</span> <span class="s">release-dists</span>
          <span class="na">path</span><span class="pi">:</span> <span class="s">dist/</span>

  <span class="na">publish</span><span class="pi">:</span>
    <span class="na">runs-on</span><span class="pi">:</span> <span class="s">ubuntu-latest</span>
    <span class="na">needs</span><span class="pi">:</span> <span class="s">build</span>
    <span class="na">steps</span><span class="pi">:</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">actions/download-artifact@v4</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">name</span><span class="pi">:</span> <span class="s">release-dists</span>
          <span class="na">path</span><span class="pi">:</span> <span class="s">dist/</span>
      <span class="pi">-</span> <span class="na">uses</span><span class="pi">:</span> <span class="s">pypa/gh-action-pypi-publish@release/v1</span>
        <span class="na">with</span><span class="pi">:</span>
          <span class="na">password</span><span class="pi">:</span> <span class="s">$</span>
          <span class="na">packages-dir</span><span class="pi">:</span> <span class="s">dist/</span>
</code></pre></div></div> <blockquote> <p>⚠️ <strong>Critical gotcha:</strong> with token-based auth, do <strong>not</strong> add <code class="language-plaintext highlighter-rouge">id-token: write</code> to the publish job. It forces OIDC (Trusted Publishing), which silently ignores your token and causes authentication failures with no useful error message.</p> </blockquote> <h3 id="testing-workflows-without-a-release">Testing workflows without a Release</h3> <p><code class="language-plaintext highlighter-rouge">workflow_dispatch</code> lets you trigger any workflow manually from the GitHub UI on any branch — no release needed. Use it to validate the publish pipeline before going live.</p> <hr/> <h2 id="8-publication-process">8. Publication Process</h2> <h3 id="pre-flight-checklist">Pre-flight checklist</h3> <ul class="task-list"> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><code class="language-plaintext highlighter-rouge">pytest tests/</code> — all green</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/><code class="language-plaintext highlighter-rouge">mypy your_package</code> — no errors</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Version bumped in <code class="language-plaintext highlighter-rouge">pyproject.toml</code></li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Clean virtualenv install verified</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled"/>Validated on TestPyPI</li> </ul> <h3 id="build">Build</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">rm</span> <span class="nt">-rf</span> dist/
python <span class="nt">-m</span> build
<span class="nb">ls </span>dist/
<span class="c"># your-package-0.1.0-py3-none-any.whl</span>
<span class="c"># your-package-0.1.0.tar.gz</span>
</code></pre></div></div> <h3 id="validate-on-testpypi">Validate on TestPyPI</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>twine upload <span class="nt">--repository</span> testpypi dist/<span class="k">*</span>
<span class="c"># Username: __token__</span>
<span class="c"># Password: your TestPyPI token</span>

<span class="c"># Verify</span>
python <span class="nt">-m</span> venv /tmp/test_release
<span class="nb">source</span> /tmp/test_release/bin/activate
pip <span class="nb">install</span> <span class="se">\</span>
  <span class="nt">--index-url</span> https://test.pypi.org/simple/ <span class="se">\</span>
  <span class="nt">--extra-index-url</span> https://pypi.org/simple/ <span class="se">\</span>
  your-package
python <span class="nt">-c</span> <span class="s2">"import your_package; print('OK')"</span>
deactivate
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">--extra-index-url</code> is required: your dependencies are on real PyPI, not TestPyPI.</p> <h3 id="publish">Publish</h3> <p>Create a GitHub Release tagged <code class="language-plaintext highlighter-rouge">v0.1.0</code>. The <code class="language-plaintext highlighter-rouge">publish.yml</code> workflow triggers automatically.</p> <h3 id="verify-from-pypi">Verify from PyPI</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-m</span> venv /tmp/verify <span class="o">&amp;&amp;</span> <span class="nb">source</span> /tmp/verify/bin/activate
pip <span class="nb">install </span>your-package
python <span class="nt">-c</span> <span class="s2">"import your_package; print('OK')"</span>
deactivate
</code></pre></div></div> <hr/> <h2 id="9-post-release-fixes">9. Post-release Fixes</h2> <p>PyPI <strong>does not allow overwriting</strong> a published version. Your options:</p> <ul> <li><strong>Yank</strong> — the version stays indexed but <code class="language-plaintext highlighter-rouge">pip install</code> won’t pick it up by default. Use the PyPI web UI: Release history → Yank.</li> <li><strong>Patch release</strong> — bump to <code class="language-plaintext highlighter-rouge">0.1.1</code> and publish normally.</li> <li><strong>Re-run failed workflow</strong> — if the Actions workflow failed after a release was created, go to Actions → Re-run failed jobs. No new release needed.</li> </ul> <hr/> <h2 id="quick-reference">Quick Reference</h2> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Development</span>
pip <span class="nb">install</span> <span class="nt">-e</span> <span class="nb">.</span>
pip <span class="nb">install</span> <span class="nt">-r</span> requirements.txt
pytest tests/
mypy your_package

<span class="c"># Build and publish</span>
python <span class="nt">-m</span> build
twine upload <span class="nt">--repository</span> testpypi dist/<span class="k">*</span>   <span class="c"># TestPyPI</span>
twine upload dist/<span class="k">*</span>                          <span class="c"># PyPI</span>

<span class="c"># Docs</span>
mkdocs serve        <span class="c"># preview</span>
mkdocs gh-deploy    <span class="c"># deploy to GitHub Pages</span>

<span class="c"># Verify</span>
pip <span class="nb">install </span>your-package
pip <span class="nb">install</span> <span class="se">\</span>
  <span class="nt">--index-url</span> https://test.pypi.org/simple/ <span class="se">\</span>
  <span class="nt">--extra-index-url</span> https://pypi.org/simple/ <span class="se">\</span>
  your-package
</code></pre></div></div>]]></content><author><name></name></author><category term="tutorial"/><category term="python"/><category term="pypi"/><category term="packaging"/><category term="github-actions"/><category term="mkdocs"/><category term="mypy"/><category term="open-source"/><summary type="html"><![CDATA[Everything you need to take a Python library from research code to a production-grade PyPI package — packaging, testing, CI/CD, docs, and common pitfalls.]]></summary></entry></feed>